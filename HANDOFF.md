# 디어한의원 홈페이지 — 공용 인수인계

## 지금 상태 — 어느 PC의 어떤 AI든 여기부터 읽는다

**이 블록만 날짜순 기록이 아니다. 지금 사실을 적고, 바뀌면 아래에 새 항목을 더하는 대신 이 자리를 고쳐 쓴다.** 아래 날짜 항목들은 경위이고 여기가 현재다.

### 읽는 순서

0. **`git fetch` 먼저.** 뒤처져 있으면 합치고, **합친 뒤 이 파일을 다시 읽는다.** 받기 전의 낡은 사본으로 판단하면 이미 끝난 일을 남은 일로 안내하게 된다. 2026-09-19에 실제로 그랬다. 집 데스크톱이 19개 커밋 뒤처진 줄 모르고 낡은 인수인계를 읽어, 이미 끝난 Cloudflare 계정 통합을 "회사 PC에서 하시라"고 안내했다.
1. `CLAUDE.md` — 바뀌지 않는 원칙. 특히 `AI가 하지 않는 판단 — 탁상행정 금지`.
2. 이 블록.
3. 최근 날짜 항목 몇 개. **151개를 다 읽지 않는다.** 필요한 주제만 찾아 읽는다.
4. 디숭이를 건드린다면 `docs/DISOONGI_HANDOFF.md`.

### 채널 지도

**채널을 건드리기 전에 이 표를 본다.** 2026-09-18 점검에서 작업자가 채널을 여섯 개로 잘못 알고 진행해 분석을 한 번 버렸다.

| 채널 | 주소 | 상태 |
| --- | --- | --- |
| 홈페이지 | `dearhani.com` | 운영. GitHub Pages 배포 |
| 네이버 플레이스 | ID `1989406480` | 운영. 리뷰 1,638(방문자 181·블로그 1,457) |
| 구글 비즈니스 | CID `10101645763264428180` | 운영. 리뷰 7·평점 5.0·카테고리 1개 |
| 인스타그램 | `dearhani__` | 운영. 팔로워 약 3,600 |
| 티스토리 | `dearmydiet.tistory.com` | 운영. 다이어트 저널 |
| 네이버 블로그 ①  | `blog.naver.com/blanchard2552` | **미사용.** 명의 건은 정리가 끝났다. **이 계정에 대해 다시 묻지 않는다** |
| 네이버 블로그 ② | `blog.naver.com/thisisdear` | 대표원장 개인 블로그. **플레이스에 대표로 걸려 있고 홈페이지 `sameAs`에도 이 주소가 있다.** 최근 발행이 뜸하다 |
| 네이버 블로그 ③ | `blog.naver.com/dearhani220707` | 블로그 문서가 저품질 판정을 받은 이력이 있다. **계정 자체는 정상이며 플레이스·네이버 광고·서치어드바이저·세금계산서를 쓰는 주 계정이다.** 계정을 위험으로 보고하지 않는다 |
| 네이버 블로그 ④ | `blog.naver.com/lbc2588` | 가족 명의 계정을 박성호가 운영한다. **디어한의원 공식 채널이 아니라 별개 정체성이므로 `sameAs`나 푸터에 묶지 않는다** |

- 티스토리의 목적은 **다이어트 저널과 구글·다음 노출**이다. 카카오맵 상위노출 효과는 박성호가 실측으로 반증했으므로 **카카오맵을 근거로 무언가를 제안하지 않는다.**
- `niceenergy.tistory.com`과 「메디어바웃」은 외부인이 자발적으로 운영하는 것이며 디어한의원 자산이 아니다.
- 홈페이지 `sameAs`(네 언어판 홈)에 든 6개: 인스타그램 · 네이버 블로그 `thisisdear` · 티스토리 · 네이버 톡톡 · 네이버 플레이스 · 구글 지도.

### 칼럼 관리자와 대화 로그

- 주소는 **https://dear-column-admin.dearhani-ai.workers.dev** 이고 로그인 뒤 `/logs`가 디숭이 대화 로그 화면이다. 옛 주소는 지웠으므로 이 주소를 쓴다.
- **디어한의원의 Cloudflare 자산은 Towamk 계정(`96d973cc…`) 한 곳에 모여 있다.** 칼럼 관리자·디숭이 워커·D1이 같은 계정이다. 2026-09-18에 통합을 마쳤으므로 **계정이 갈려 있다는 전제로 무언가를 제안하지 않는다.**
- 로그인 계정은 박성호(`sho36036@gmail.com`)와 김민지 대표원장(`towamk@naver.com`) 둘이다. 초안은 `owner_email` 기준이라 서로의 초안은 보이지 않는 것이 정상이다.
- **발행용 GitHub 토큰은 2027-09-17에 만료된다.** 그 뒤에는 발행 버튼이 조용히 실패한다. 경위와 재발급 조건은 2026-09-18 `계정 통합 완료` 항목에 있다.

### 열려 있는 일

**남은 작업은 이 표가 전부다.** 날짜 항목의 `남은 작업` 줄은 그때의 기록이라 이미 끝난 것이 섞여 있다. 하나를 끝내면 이 줄을 지우고, 새로 생기면 여기에 더한다. 2026-09-20에 실물을 대조해 만들었다.

| 항목 | 지금 상태 | 성격 |
| --- | --- | --- |
| 번역 칼럼 | **0편.** `en/columns.html`이 한국어 칼럼 1편만 링크한다 | 작업. 전편 번역보다 BE DEER 다이어트 2~3편 검수 번역이 현실적이라고 정해뒀다 |
| `be-deer-case.html` 사례 상세 | 네 언어 모두 `준비 중` 안내 화면 | 작업. 원고가 준비되면 목록 링크를 그대로 두고 이 화면을 바꾼다 |
| `ja`·`zh-cn`의 `about`·`philosophy`·`columns`·법률 5종 | 오역 전수 대조를 아직 안 했다 | 작업. `services`·`care`·`director`에서 나온 유형(주어 붕괴, 조각난 문장, `你`/`您` 혼용)을 같은 기준으로 본다 |
| 주차 2시간 무료 표기 | 네이버 플레이스에는 있고 **홈페이지에는 한 글자도 없다** | 결정. 넣을지 정하지 않았다 |
| 다국어 번역문 확정 | 원어민 검수 전이다 | 확인. 대표원장이 영어·일본어·중국어 모두 가능하므로 확인을 받으면 확정 카피로 승격한다 |
| 디숭이 표준 화풍 | 수채풍·플랫 벡터풍이 갈린 채다 | 결정. 급하지 않다 |

### 지금 손대지 않는 것

흩어져 있던 것을 모았다. **각 항목은 검토를 거쳐 접은 것이며, 다시 제안하지 않는다.** 경위는 해당 날짜 항목에 있다.

| 항목 | 이유 |
| --- | --- |
| 메인 첫 화면 구성 | 대표원장 컨펌. 폐기안을 다시 올리지 않는다 |
| `be-deer.html` 후기 섹션과 관련 확정 카피 | 검토 후 유지 결정. 의료광고 위험으로 재분류하지 않는다 |
| 의료광고 심의번호 표기 | 자체 홈페이지는 심의 대상 매체가 아니다. 철회된 판단이다 |
| 칼럼 간 관련글 자동 배선 | 칼럼 31편에 이미 CTA가 있다. 점수가 낮다는 이유로 다시 꺼내지 않는다 |
| 구글맵스 주소 표기 | 구글 지오코더가 저장을 막는다. 층·호는 순위 요인이 아니다 |
| 네이버 플레이스 `0507` 안심번호 | 의도적으로 켠 설정이다. NAP 불일치로 보고하지 않는다 |
| 카카오톡 채널 개설 | 운영 계획이 없다 |
| `blanchard2552` 명의·복구 | 정리가 끝난 항목이다. 복구 가능 여부를 다시 확인하거나 묻지 않는다 |
| 공진단 칼럼의 진료 사례(CASE) 카드 | 현재 형태로 두기로 결정했다 |
| `robots.txt`에 AI 크롤러를 이름으로 나열 | `User-agent: *`가 이미 전부 허용이라 효과가 0이다. 이름 그룹을 만들면 그 봇이 `*` 그룹의 `Disallow: /preview/`를 무시한다 |
| 경쟁사 물량형 칼럼 따라가기 | 설명한의원 방식이며 우리 체급에 맞지 않는다 |

### 고정값

- `sharedCssVersion = 20260919-1` · `sharedMainVersion = 20260919-1`. **`css/style.css`나 `js/main.js`를 고치면 106개 HTML의 `?v=`와 `tools/publish-column.mjs`까지 함께 올린다.**
- 본문 폰트는 **Pretendard Variable 동적 서브셋**(`variable/pretendardvariable-dynamic-subset.css`)이다. 새 페이지를 만들 때 옛 `static/pretendard.css`를 붙여넣지 않는다.
- 칼럼 31편 · 사이트맵 93개. 칼럼을 늘리면 `tools/test-columns-serp.mjs`의 고정값도 올린다.
- 칼럼 31편 전부에 네이버 예약·전화·주소가 있다. 칼럼 하단을 건드리는 작업은 전후로 이 수를 대조한다.
- **검사의 기준은 `.github/workflows/deploy-pages.yml`이다.** 여기서 돌리는 9종을 변경 뒤 전부 통과시킨다: `test-column-publisher` `test-columns-serp` `test-seo-surfaces` `test-medical-editorial-trust` `test-naver-tracking` `test-dear-ai` `test-dear-ai-public` `test-dear-ai-dialogue` `test-dear-ai-columns` (앞서 `node tools/build-dear-ai-columns.mjs`도 돈다). 이 자리에 적힌 목록이 아니라 **워크플로 파일을 직접 보고 맞춘다.** 2026-09-19에 이 칸이 `6종`으로 낡아 있어 배포가 한 번 멈췄다.

---

## 2026-09-21 선잠·야간 각성 칼럼 발행

- `columns/frequent-night-awakening-light-sleep.html`을 수제 저널형 칼럼으로 발행했다. 분류 `Calm`, 저널 번호 31, 주소는 `https://dearhani.com/columns/frequent-night-awakening-light-sleep.html`이다.
- 대표 이미지는 사용자가 제공한 확장자 없는 1254×1254 PNG `디숭이수면`을 `assets/images/columns/frequent-night-awakening-light-sleep/cover.png`로 저장했다. 밤중에 잠에서 깬 디숭이 일러스트이며 Columns 카드와 OG 이미지, 최신 칼럼 카드에 함께 연결했다.
- CASE 1은 2개월 뒤 편안한 입면과 새벽 각성 주 1회 정도로의 감소, 스트레스가 심할 때 침 치료 뒤 다시 잠이 잘 이어진 경과를 담았다. CASE 2는 3개월 뒤 열감·홍조·발한이 거의 없어지고 수면과 새벽 손가락 부종·관절통이 함께 호전된 경과를 담았다.
- `columns.html`·`sitemap.xml`·`rss.xml`·`assets/data/latest-column.json`을 갱신했고 공개 칼럼 수 고정값을 31편으로 올렸다. 전용 CSS는 `css/frequent-night-awakening-light-sleep.css`다.

## 2026-09-20 원장 면허번호를 밝히고, 스키마를 화면에 맞췄다

- 경쟁사 `seolmyungclinic.com`(설명한의원, 8지점 체인) 점검에서 출발했다. 사이트맵 **5,932개 중 칼럼이 4,637편**이고 **138일간 하루 20편씩** 쌓였다. 지점 × 증상 × 지역 키워드 행렬을 자동 생성하는 방식이며, `/care/` 아래 같은 글의 지점별 복제본이 11~12개씩 있다. **물량은 우리가 따라갈 축이 아니다.** 구글의 대량 생산 콘텐츠 남용(scaled content abuse) 조항과 정면으로 부딪히는 형태이기도 하다.
- 저쪽에서 가져올 만한 것으로 두 가지를 꼽았으나 **확인해 보니 하나는 효과가 없고 하나는 이미 있었다.**
  - `robots.txt`의 AI 크롤러 명시 허용은 **효과가 0이다.** `User-agent: *`가 이미 전부 허용이고, 오히려 이름 그룹을 만들면 크롤러가 자기 그룹만 읽는 규칙 때문에 `Disallow: /preview/`가 풀린다. 위 표에 넣었다.
  - `reviewedBy`는 **칼럼 30편에 이미 있다.** 우리는 `@id`로 `director.html#kim-minji` 한 노드를 참조하고 저쪽은 글마다 Person을 복붙하므로, 구조는 우리 쪽이 낫다. 실제로 빠져 있던 것은 `hasCredential` 하나뿐이었다.
- **한의사 면허 제28063호를 공개했다.** `career.html` 자격 목록 첫 줄에 넣었고 **en·ja·zh-cn 세 판도 같이 넣었다.** 면허번호는 NAP과 같은 확정 사실이므로 네 판이 어긋나면 안 된다.
- `hasCredential`(보건복지부 발급 `license`)은 `career.html`의 Person 노드에만 적었다. `director.html`에는 넣지 않았다.

### 스키마는 그 페이지 화면에 보이는 사실만 적는다

- 이번에 기준을 세웠다. **마크업에만 있고 화면에 없는 사실은 그 페이지에 적지 않는다.**
- 그 기준으로 보니 `memberOf`(학회 5곳)가 반대로 붙어 있었다. **학회가 눈에 보이는 페이지는 `career.html`인데 마크업은 `director.html`에 있었다.** `career.html`로 옮기고 `director.html`에서는 덜어냈다. `honorificSuffix: 한의사`도 면허가 보이게 된 `career.html`에 적었다.
- `knowsAbout`은 칼럼 주제를 싣는 `director.html`에 그대로 두었다.
- **두 페이지의 Person은 `@id`가 같아 병합된다. 한쪽에 적으면 충분하므로 양쪽에 복붙하지 않는다.** 다음 사람이 "`director.html`에 학회가 없다"고 보고 다시 넣지 않도록 여기 적어 둔다.

### 노드 개수만 세면 `@id` 참조를 놓친다

- **이번 작업에서 같은 착각을 두 번 했다. 다음 사람도 똑같이 걸린다.**
- `services.html`의 `ItemList`는 원래부터 항목이 **네 개**였다. 1번이 `be-deer.html#service`로, **BE DEER의 Service 노드는 자기 페이지에 있고 `services.html`은 참조만 한다.** 그런데 `@type`이 `Service`인 노드 수만 세어 "세 개뿐이고 BE DEER가 빠졌다"고 잘못 보고했고, 그 잘못된 판단으로 세 언어판을 세 개짜리로 만들었다가 되돌렸다.
- 앞서 `reviewedBy`도 같은 착각이었다. "칼럼에 감수자 표기가 없다"고 보았으나 **30편 전부 `@id`로 `director.html#kim-minji`를 참조하고 있었다.**
- **점검 방법: 노드를 세지 말고 `@id`를 따라간다.** 다른 파일에 정의된 노드를 가리키는 참조는 그 파일을 열어야 보인다. 한 페이지의 JSON-LD만 읽고 "빠졌다"고 판단하지 않는다.
- 이 저장소는 **같은 내용을 두 번 적지 않고 `@id`로 참조하는 방식**을 일관되게 쓴다. 중복이 없는 것이 결함이 아니다.

### 남은 것

- `en`·`ja`·`zh-cn` 세 언어판에 JSON-LD를 채웠다. `career`·`director`(ProfilePage·Person), `about`(AboutPage), `philosophy`(MedicalWebPage), `care`(MedicalWebPage·ItemList·Service 5), `services`(CollectionPage·ItemList·Service 3 + BE DEER 참조). **18페이지 중 11페이지가 되었다.**
- 남은 7페이지는 `privacy`·`terms`·`non-covered`·`patient-rights`·`be-deer-case` 계열로, 유입에 의미가 없어 넣지 않았다.
- 언어판 Person은 **한국어판과 같은 `@id`(`director.html#kim-minji`)를 쓴다.** 언어마다 다른 `@id`를 주면 김민지·Minji Kim·キム・ミンジ·金敏智가 서로 다른 네 사람이 된다. `alternateName: 김민지`를 세 판 모두에 두었다.
- 구조화 데이터를 넣는 작업은 **본문 텍스트를 작업 전후로 대조해 같은지 확인하고 커밋한다.** 이번에 12페이지 전부 동일한 것을 확인했다.
- 배포 검사 `.mjs` 9종 전부 통과. CSS·JS를 건드리지 않아 캐시 버전은 올리지 않았다.

### 구글 비즈니스 프로필 — 카테고리 추가는 접었다

- 해외환자 유입을 위해 보조 카테고리를 늘리자고 제안했다가 **실물을 확인하고 철회했다.** 주 카테고리가 `한의원`으로 이미 정확하고, 이름에 `(DEAR KOREAN MEDICINE CLINIC)` 영문이 병기돼 있으며, 사진 79장·오너 포스트 최신·여성 소유 속성까지 관리되고 있다. **로컬 순위에서 무겁게 작용하는 것은 주 카테고리이고 그것이 이미 맞다.** 보조는 훨씬 약하며 관련 없는 것을 붙이면 오히려 흐려진다.
- 리뷰는 **구글 7개(평점 5.0)** 로 네이버(1,638개)와 차이가 크다. 다만 **리뷰를 구매하지 않는다.** 적발 시 리뷰가 통째로 삭제되고 프로필이 정지되어, 관리가 잘 된 프로필일수록 손해가 크다.
- 대신 좁은 수가 있다. 기존 리뷰 중 하나가 이미 **"외국인 환자도 편하게 진료받을 수 있는 한의원을 찾다가… 기본적인 의사소통은 영어로도 가능해"** 라고 적고 있으나 **한국어로 쓰여 있어 영어 검색에 걸리지 않는다.** 구글은 리뷰 본문과 그 언어를 검색 매칭에 쓴다. **영어 리뷰 몇 개가 한국어 리뷰 수백 개보다 이 목적에는 값이 크다.** 원장이 직접 영어로 진료한 환자에게만 부탁하는 좁은 작업이다.
- 홈페이지 쪽 해외환자 대응은 이미 갖춰져 있다. `international-appointment.html`이 **네이버 예약은 네이버 계정이 필요해 해외 방문객에게 맞지 않는다는 점을 밝히고 인스타그램 DM으로 유도한다.** `availableLanguage`(ko/en/ja/zh)도 네 언어판 `MedicalClinic` 노드에 이미 있다. **사이트가 병목이 아니다.**

---

## 2026-09-19 집 데스크톱에 묶여 있던 작업을 원격과 합쳤다

- 배경: 집 데스크톱의 저장소가 **원격보다 19개 커밋 뒤처진 채, 커밋되지 않은 변경 122개 파일을 들고 있었다.** 두 쪽이 같은 캐시 버전 줄을 각자 올려 그대로 당기면 105개 파일이 충돌하는 상태였다. **회사 PC·집 데스크톱을 오갈 때는 시작 전에 `git status`와 `git log --oneline master..origin/master`를 먼저 본다.**
- 처리 순서: 커밋되지 않은 작업을 `backup/20260919-local-work` 브랜치로 먼저 묶고, `master`를 `origin/master`에 맞춘 뒤, 기능 변경만 다시 얹었다. **캐시 버전 줄은 충돌을 푸는 대신 양쪽보다 새 값(`20260919-1`)으로 다시 깔았다.** 되돌린 원격 작업은 없다.
- 백업 브랜치 `backup/20260919-local-work`는 **배포 반영과 실물 확인을 마친 뒤 지웠다.** 로컬에만 있었고 원격에 올린 적이 없다. 찾지 않아도 된다.

### 함께 살린 작업 세 가지

- **팝업이 떠 있는 동안 디숭이를 감춘다.** 디숭이 위젯은 `z-index:9998`이고 팝업은 `1000`이라 **모바일에서 디숭이가 팝업 닫기 버튼을 통째로 덮어 팝업을 닫을 수 없었다.** `body.has-notice-popup`으로 감추고 마지막 팝업을 닫으면 돌아온다(`css/style.css`·`js/main.js`). 375px에서 닫기 버튼이 가려지지 않는 것과, 두 팝업을 다 닫으면 디숭이가 다시 보이는 것을 확인했다.
- **팝업 닫기 버튼을 44×44px로 키웠다.** 글자 크기는 그대로고 누를 수 있는 면적만 넓혔다.
- **본문 폰트를 Pretendard Variable 동적 서브셋으로 바꿨다.** 106개 HTML과 발행기의 링크, `css/style.css`·`css/dear-gongjindan.css`·`js/dear-ai-widget.js`의 `font-family`에 `"Pretendard Variable"`를 앞에 세웠다. 이름이 다른 폰트이므로 **링크만 바꾸고 `font-family`를 두면 글꼴이 적용되지 않는다.**
- 홈 팝업 프리로드 목록이 `data-popup-id`로 파일명을 짜맞추고 있어 **본문의 `data-src`와 어긋나면 엉뚱한 파일을 미리 받았다.** id와 파일명을 한 쌍으로 적게 고치고 `tools/test-home-loading.cjs`도 본문의 `data-src`를 읽도록 맞췄다.

### 함께 바로잡은 것

- 이 블록의 고정값이 `sharedMainVersion = 20260917-4`로 적혀 있었으나 실제 코드는 `20260918-2`였다. **고정값을 적는 자리와 코드가 어긋나면 다음 사람이 낡은 값을 그대로 박는다.** 이번에 둘 다 `20260919-1`로 맞췄다.
- 집 데스크톱에는 `playwright`가 없어 `tools/test-*.cjs` 4종이 실행되지 않는다. 배포 워크플로가 돌리는 `.mjs` 9종은 전부 통과했다.
- **첫 배포는 실패했다.** `tools/test-dear-ai.mjs`가 디숭이 위젯의 `font-family`를 옛 값으로 고정 검사하고 있었다. 폰트를 바꾸면 **위젯 검사까지 함께 본다.** 이 블록의 검사 목록이 `6종`으로 낡아 있어 빠뜨린 것이 원인이었고, 그래서 위에서 워크플로 파일을 기준으로 바꿨다.
- 생성물 `assets/data/dear-ai-columns.json`이 공진단 칼럼 설명 수정 뒤 다시 생성되지 않아 저장소 값이 낡아 있었다. **칼럼 본문이나 설명을 고치면 `node tools/build-dear-ai-columns.mjs`를 함께 돌린다.**

---

## 2026-09-18 검색엔진이 읽는 업체 정보를 한 곳으로 모았다

- 네 언어판 홈의 `sameAs`를 4개에서 6개로 정비했다. 네이버 지도를 **검색 주소에서 플레이스 주소로 바꾸고** 구글 지도를 더했다. 검색 주소는 업체를 특정하지 못해 엔티티가 묶이지 않는다.
- `international-appointment.html`이 `#clinic`을 **다른 이름·주소·sameAs로 다시 정의**하고 있었다. 참조만 남기는 `WebPage`로 바꿨다. 지우기 전에 그 노드에만 있던 값이 홈에 다 있는지 확인했고, **네이버 톡톡만 없어서 홈 `sameAs`로 옮겼다.**
- 배포 후 실측 완료. 홈·`/en/`에 구글 CID와 네이버 플레이스 주소가 들어갔고 옛 검색 주소는 0건이다.

### 식별자 (다시 찾기 번거롭다)

- **네이버 플레이스 ID `1989406480`** — 인스타 프로필의 `naver.me` 단축주소를 따라가야 나온다. 플레이스 주소는 `https://map.naver.com/p/entry/place/1989406480`.
- **구글 비즈니스 CID `10101645763264428180`** — 구글맵스 항목의 `data=` 파라미터에 있는 16진수 `0x8c30414d0634b894`를 10진수로 바꾼 값이다. 주소는 `https://maps.google.com/?cid=10101645763264428180`.

### 다시 올리지 않는다

- **구글맵스 주소 표기는 건드리지 않는다.** 상세주소가 `사임당로 143 / 310호 / 3층 309` 세 칸에 나뉘어 들어가 순서가 어긋나 보인다. 칸을 고쳐 저장하려 하면 **구글 한국 지오코더가 "주소를 찾을 수 없습니다"로 막는다.** 상세주소를 전부 비워도 막혔고 핀 조정 경로도 통하지 않았다. **층·호는 순위 요인이 아니고**, 우리 사이트와 이 업체를 묶는 일은 위의 CID가 이미 한다. 값이 없는 작업이다.
- **네이버 플레이스의 `0507-1433-1777`은 안심번호이며 의도적으로 켠 것이다.** 확정 NAP의 `02-3486-1777`과 다르지만 **NAP 불일치로 보고하지 않는다.** 네이버 예약 통계에 통화가 잡히게 하려는 설정이다.

### 확인해서 닫은 항목

- **도보 시간은 `강남역 5번 출구 도보 10분`으로 쓴다.** 네이버 플레이스 실측이 690m이고 플레이스도 10분 이내로 적고 있다. 광고 소재의 `도보 5분`은 거리에 맞지 않는다. 2026-09-17 항목에 미정으로 남아 있던 것을 닫는다.
- **주차 2시간 무료가 네이버 플레이스에 있는 것을 확인했다.** 홈페이지에는 여전히 없다. 넣을지는 정하지 않았다.
- 네이버 플레이스 주소 표기는 `서울 서초구 사임당로 143 3층 309호, 310호`로 **확정 NAP과 글자까지 같다.** 구글과 달리 손댈 것이 없다.

## 2026-09-18 칼럼 관련글 자동 배선은 검토 후 접었다

- 배경: 여섯 채널 점검에서 **칼럼 30편 중 19편이 다른 칼럼으로 가는 링크가 0개**(평균 0.7)로 나왔다. 항목별 점수에서 가장 낮아 여기부터 손대려 했다. **그 전제가 틀렸다.**
- **결정: 하지 않는다.** 만든 파일은 전부 지웠고 칼럼 HTML은 한 글자도 건드리지 않았다.

### 접은 이유

- **칼럼 30편 전부에 이미 예약·전화·주소가 있다.** 다 읽은 독자에게 다음 행동이 이미 있고 그것이 예약이다. 관련글 블록은 그 자리에 "다른 글 더 읽기"를 끼워 넣어 전환을 나눠 갖는다. 없어서 문제인 것이 아니라 있으면 좋을 수도 있는 것이었다.
- **칼럼은 고아가 아니다.** `columns.html` 목록이 30편 전부로 링크하고 `sitemap.xml`과 `rss.xml`에도 모두 들어 있다. 크롤러가 못 찾는 상태가 아니다.
- **자동 제안의 품질이 쓸 수준이 아니었다.** 단계(증상인식·원인이해·판단기준·결정실행)와 독자(성인여성체중·산후·수험생청소년·소아·공통) 태그를 30편에 달고 주제 유사도를 얹어 40개를 뽑았는데 쓸 만한 것이 8개였다. **낱말이 겹치면 주제가 겹친 것으로 오인한다.** `간수치` 하나로 우울증·비만·소아성장이 묶였고, `두통`이 나열됐다는 이유로 두통 칼럼이 수험생 보약으로 연결됐다.
- 독자 다음 질문·질환 변화·생활권·인구 욕망을 축으로 삼자는 방향은 옳았다. 다만 **생활권은 독자 동선이 아니라 검색 동선**이라 칼럼 하단이 아니라 지역 허브로 풀어야 하고, 나머지 축은 기계가 뽑을 수 없었다.

### 다시 올리지 않는다

- **점수표에서 내부링크가 가장 낮다는 이유만으로 이 작업을 다시 꺼내지 않는다.** 가장 낮은 점수가 가장 급한 문제라는 전제가 이번 실패의 원인이다.
- 다시 한다면 조건은 둘이다. 기계 제안이 아니라 **사람이 직접 지정**할 것, 그리고 **CTA와의 관계를 먼저 정할 것**.
- 이 작업 대신 잡은 우선순위는 구글맵스 주소 표기 정정, `sameAs` 엔티티 주소 교체, `#clinic` 식별자 충돌 해소다. 셋 다 효과가 분명하고 작업량이 작다.

### 남겨두는 사실 (다시 볼 때 필요하다)

- **`column-related` 규격은 이미 있다.** 마크업은 `<section class="column-related">` + `CONTINUE READING` + `함께 읽으면 좋은 글` + `.column-related__grid` 안의 `<a><small>분류</small><strong>제목</strong></a>`이고, **CSS는 `css/style.css`에 전역으로 들어 있다.** 새로 만들 필요가 없고 CSS를 건드리지 않으므로 캐시 버전 사슬도 올릴 필요가 없다.
- **이미 이 블록을 쓰는 칼럼이 9편 있다**: `dear-column-20260729-1907` `depression-functional-recovery` `gangnam-child-growth` `gangnam-depression-bdnf` `gangnam-obesity-fatty-liver` `seocho-diet-clinic` `seocho-diet-herbal-medicine` `seocho-functional-dyspepsia` `weight-inattentional-blindness`. 자동 삽입을 다시 시도한다면 **이 9편을 먼저 인식해 교체해야 한다.** 그러지 않으면 블록이 두 개가 된다.
- **`.dear-journal a{color:inherit}`이 `<main>` 전체에 걸린 칼럼이 셋 있다**(`gangnam-child-growth` `gangnam-depression-bdnf` `gangnam-obesity-fatty-liver`). 그 안에 링크 카드를 넣으면 만성두통 칼럼과 같은 사고가 난다. 해법은 이미 쓴 `:not([class])`로 좁히는 방식이다.
- 칼럼 하단 CTA·NAP 기준선: **30편 전부 네이버 예약·전화·주소를 갖고 있다.** 칼럼 하단을 건드리는 작업을 한다면 전후로 이 수를 대조한다.

## 2026-09-18 원고를 쓸 때의 사고 기준 — 박성호 문체를 흉내내지 말고 논리를 따를 것

- 배경: 공진단 칼럼 초안이 **논증 없이 정보만 나열한 AI 글**이었다. 박성호가 자신의 원고(트리니피부과 블로그 10편, 회문소 15화, 오늘의브랜딩 「병원의 네이버 의존성」)를 예문으로 주며 문체가 아니라 사고 흐름과 당위성을 따르라고 했다. 아래는 그 예문 86,976자에서 뽑은 기준이다. **다음 원고부터 이 순서로 쓴다.**

### 출발점 — 한 단어 안에 서로 다른 사건이 들어 있다고 의심한다

- `모공이 커졌다`는 피지·탄력·흉터가 겹친 말이고, `처졌다`는 피부·지방·유지인대가 겹친 말이며, `좋아졌다`는 색과 구조가 겹친 말이다. `네이버 의존성`은 발견·검증·행동이 겹쳐 있고, 회문소에서는 `죽었다`가 시신과 문장으로 갈린다.
- **이용자가 많다는 것과 구매가 일어난다는 것은 애초부터 같은 사건이 아니다.** 이 문장이 모든 글의 첫 동작이다. 통념을 그대로 옮긴 뒤 그 문장이 뭉뚱그린 것을 쪼개고, 쪼갠 것마다 판단이 갈린다는 것을 보인다.
- 결론은 지시가 아니라 순서와 구분이다. `치료법의 이름을 많이 외우는 것은 크게 중요하지 않습니다. 오히려 앞에서 살펴본 순서가 더 중요합니다.`

### 단정을 거두는 이유 — 겸손이 아니라 인식론이다

- 트리니 2편에 공리가 그대로 있다. **“보편은 서로 다른 사례에서 공통된 것을 남길 때 비로소 보편이 됩니다. 그러나 현실의 한 사람은 바로 그 과정에서 제외된 차이들까지 지닌 채 존재합니다. 기준과 개별이 완전히 포개지지 않는 것은 기준의 실패가 아니라, 기준이 성립하는 방식에 가깝습니다.”**
- 그래서 `~일 수 있습니다`가 444문장 중 51번 나온다. 조심스러운 말투를 흉내내면 흉내로 끝난다. **기준이 성립하는 방식을 정확히 서술하면 그 어미밖에 남지 않는다.**
- 자료를 쓸 때는 자료마다 말할 수 있는 것과 말할 수 없는 것을 갈라 적는다. 어느 자료 하나도 결론을 혼자 증명하지 못하고, 층을 겹쳤을 때 비로소 설명이 시작된다.

### 채우지 않는다

- 회문소의 사관 유겸은 알아낸 것을 계속 적지 않는다. `없는 것을 너무 빨리 이름 붙이면 나중에 찾은 흔적이 그 이름에 끌려갈 수 있다.`
- 문안꾼을 앞에 두고 하는 말이 기준 그 자체다. **`적히지 않은 것을 내가 채우기 시작하면, 그 문안꾼과 다를 것이 무엇이오.`**
- **확인한 것만 쓰고, 확인하지 못한 자리는 비운 채 넘긴다.** 채우는 판단은 박성호와 김민지 대표원장의 몫이다. 그럴듯함으로 메우는 순간 의료인이 읽는 글의 신뢰가 깨진다.

### 쓰지 않는 것 (이번에 실제로 저지른 것들이다)

- **반박을 상정한 해명조.** `이 구분은 말장난이 아닙니다`, `다툴 이유가 없습니다`처럼 누가 시비를 걸었다고 전제하는 문장을 쓰지 않는다. 사실만 놓는다.
- **`A가 아니라 B` 구문의 남용.** 한 편에서 일곱 번 썼다. 대조가 곧 논지인 자리에만 남기고 나머지는 평서문으로 푼다.
- **바깥에서 끌어온 비유.** `변수 두 개가 열려 있는 셈`처럼 글의 세계에 없는 비유를 만들지 않는다. 회문소의 `얼굴은 꾸밀 수 있어도 발바닥의 먼지는 늦게 지워지는 법`처럼 비유는 그 세계 안의 사물로만 만든다.
- **훈계.** `해야 합니다` 대신 `~하는 편이 낫습니다`, `~볼 필요가 생깁니다`로 쓴다. 예문 전체에 느낌표가 거의 없다.

### 어휘와 호흡

- **문체의 층이 둘이다. 칼럼은 트리니(설명문)의 층을 따르고 회문소(서사)의 층을 끌어오지 않는다.** (2026-09-21 바로잡음) 이전 판은 "접속어 1위는 `그러나`(74회), `그래서`를 연달아 쓰면 다른 사람의 글"이라고 적었으나, 다시 세어 보니 `그러나` 75회 가운데 72회가 회문소에서 나왔고 트리니에는 3회뿐이다. 트리니의 접속어는 `그런데` 8·`결국` 8·`그래서` 6·`다만` 6·`하지만` 5다. 앞 문장을 인정한 뒤 뒤집는 구조는 설명문에도 있으나 `그런데`·`다만`에 실린다. 설명문에서 `그래서`와 `결국`을 피할 이유는 없다.
- 상시 쓰는데 AI가 잘 놓치는 말: `곧`(29) `이어`(28) `그제야`(11) `비로소`(8) `오히려`(13) `정작`(4) `대개`(10) `도리어` `차라리` `애초`. 특히 `그제야`와 `비로소`는 인식이 도착하는 시점을 표시한다.
- **설명문은 짧게 끊지 않는다.** 트리니 원고의 문장 길이 중앙값은 61자이고 20자 이하는 4%뿐이다. 조건과 유보를 한 문장에 담는다. 반면 서사(회문소)는 중앙값 25자에 20자 이하가 37%다. 서사의 호흡을 설명문에 끌어오지 않는다.
- 전문용어는 `혈관확장(vasodilation; 혈관의 지름이 넓어져 해당 부위의 혈류가 증가하는 현상)` 형식으로 병기한다. 예문에서 34회 확인된 고정 형식이다.

### 2026-09-21 보강 — 예문과 초안을 나란히 재서 찾은 것

박성호가 예문 원본(트리니 11편 25,752자, 회문소 15화 63,496자)을 다시 주었다. 원본은 박성호 PC 바탕화면의 `트리니`·`회문소` 폴더에 있다. 같은 날 쓴 부모님 칼럼 초안이 **문체 규칙은 지켰는데도 AI 글로 읽혔고 의학적 근거가 없었다.** 초안과 트리니 칼럼형 6편(3·4·5·6·8·11편)을 같은 잣대로 재서 아래를 찾았다.

**근거가 쪼개기를 증명한다.** 쪼개기가 논리만으로 서면 수사가 된다. 모공 편은 성인 60명을 측정한 연구에서 피지와 탄력은 모공과 함께 움직였고 나이와 여드름 중증도는 그렇지 않았다는 결과를 놓은 뒤 "모공을 피지 하나의 문제로 설명하기에는 실제 얼굴에서 함께 움직이는 조건이 이미 하나가 아니었던 셈"이라고 닫는다. 쪼갠 축이 연구의 변수와 겹친다. 결과표는 한 줄씩 읽어 주고, 같은 문단에 "상관관계는 원인을 확정했다는 뜻이 아니라"를 붙인다. 흑자 편은 "대상자가 20명인 작은 연구여서 PIH 차이는 통계적으로 확정할 수준까지는 이르지 못했습니다"처럼 **한계를 결과와 같은 문장에** 싣는다. 디어 칼럼 중 지방간·우울 BDNF·성장 편(참고문헌 6~9편, 본문 약 14,000자)이 이 수준이다. **새 칼럼은 근거를 먼저 찾아 보인 뒤 쓴다.**

**도입은 독자의 장면과 독자가 내뱉는 한 줄이다.** "8월 말의 서래마을도 오후가 되면 아직은 여름입니다"에서 `모공이 또 커졌네.`로, "쿠션만 얇게 바르고 끝내고 싶은데"에서 `"이 정도 가지고 굳이 피부과까지 가야 하나."`로 간다. 모공 편은 끝에서 그 한 줄로 돌아온다. **진료실 경험을 주장하는 도입(`~라는 말을 자주 듣습니다`)을 쓰지 않는다.** 확인하지 않은 경험을 원장 입으로 지어내게 된다. 부모님 초안의 첫 문장이 그랬다.

**화자는 독자 옆에 선다.** `우리`가 트리니 칼럼형에서 1,000자당 0.92회, 초안에서 0회였다. "우리는 이런 변화를 대개 한 단어로 묶습니다", "아무래도 우리 마음이 그렇습니다."

**소제목은 독자의 질문이다.** "모공은 정말 '구멍 하나'의 문제일까요?", "얼굴이 작아지는 것과 좋아보이는 것. 둘은 같은 변화일까요?" `~까요?`가 0.66회였고 초안은 0회였다. 선언형·부정형 소제목(`~는 ~이 아니다`)을 쓰지 않는다.

**목록 대신 단계로 쪼갠다.** `첫째·둘째·셋째`는 트리니 전체에서 0회, 초안에서 3.24회였다. 여드름 흉터 편은 "같은 얼굴을 세 장 복사해 놓고" 한 장씩 색과 굴곡을 지우는 사고실험으로 색과 구조를 가른 뒤, 구조 안에서 다시 송곳·박스·롤링으로 내려간다. 쪼개는 과정 자체를 서술한다. "여기까지 오면 처음의 '여드름 흉터'라는 말이 꽤 많이 쪼개졌습니다."

**독자의 말을 따옴표로 붙잡는다.** `'처졌다'`, `'좋아짐'`, `'모공'`처럼 독자가 쓰는 낱말을 따옴표로 세워 두고 그 낱말을 해부한다. 따옴표가 4.34회, 초안은 1.08회였다.

**위트가 한두 군데 있다.** "점심을 먹고 돌아온 사이 갑자기 증축된 것은 아닐 것입니다", "원래 우리는 '앞머리 하나만 삐죽해도' 하루 종일 신경 쓰는 집단이기도 하죠", "괜스레요." 글 전체가 엄숙하면 훈계로 읽힌다.

**문제였던 대조는 부정문이 아니라 `~보다 ~쪽에 가깝다`였다.** 1,000자당 부정 대조(`아니라`·`아니며`·`아닙니다`)는 트리니 1.71, 초안 2.16으로 크게 다르지 않았다. 그러나 `~보다 … 쪽/가깝`은 0.13 대 1.62, `~기보다`는 0.13 대 1.62로 열두 배였다. 초안을 검사할 때는 이 두 형태를 센다. 부정문만 줄이면 헛짚는다.

**낱말 하나가 확인하지 않은 사실을 싣는다.** 회문소 14화에서 빈 관 앞의 나장이 "시신이 유실되었다고 적으면 되겠습니까"라고 묻자 유겸이 답한다. "유실되었다 쓰면, 이곳에 시신이 있었다는 뜻이 되오." 원고의 낱말마다 그 낱말이 전제하는 사실을 확인했는지 본다.

**맺음은 순서와 초대다.** "색의 문제인지, 구조의 문제인지. 구조의 문제라면 어떤 형태로 남아 있는지." 다음에 "직접 피부를 보면서 … 함께 정하겠다"로 닫는다. 판매 문장도, 면책 문장만으로 끝나는 결말도 아니다.

**도판 캡션은 그것이 무엇이고 무엇이 아닌지 적는다.** "실제 모낭의 깊이와 크기, 피지선의 형태와 비율을 그대로 재현한 해부학 도판은 아닙니다."

**트리니 7·9·10편의 짧은 줄바꿈은 네이버 블로그 형식이다.** 칼럼 본문에 옮기지 않는다. 칼럼은 3·4·5·6·8·11편의 긴 문단을 따른다.

## 2026-09-18 공진단 효능 칼럼 발행 — 저널형 칼럼은 발행기로 만들 수 없다

- `columns/gongjindan-effect.html`을 발행했다. 분류 `Restore`, 저널 번호 30, 본문 약 6,500자. 주소는 `https://dearhani.com/columns/gongjindan-effect.html`.
- 주제 선정 근거는 네이버 키워드 도구 실측이다. **공진단 46,290 · 공진단 효능 11,960 · 공진단 가격 7,830**(월간, PC+모바일)인데 기존 공진단 칼럼은 조제 과정과 청담 선물 두 편뿐이라 효능·가격을 정면으로 다루는 글이 없었다.
- **처음 잡았던 만성피로 각도는 폐기했다.** `만성피로한약`이 월 40회, `자고일어나도피곤`이 60회로 사실상 없는 검색어였다. 키워드 도구를 보기 전의 가설이 숫자로 무너진 경우다.
- **경옥고는 쓰지 않는다.** 검색량은 47,170으로 공진단과 맞먹지만 디어한의원에서 취급하지 않는다. 취급하지 않는 제품으로 유입을 받으면 문의가 와도 답할 수 없다. 같은 이유로 홍삼 비교 문항도 뺐고, 대신 `복용 중인 약이나 건강식품` 문진 질문으로 흡수했다.

### 발행기는 저널형 칼럼을 만들지 못한다

- `tools/publish-column.mjs`의 `validateHtmlBody()`가 **`<style>`·`<svg>`·`<script>`를 금지**한다. 전용 디자인과 도표를 쓰는 저널형 칼럼은 이 관문을 통과할 수 없다.
- 실제로 칼럼 30편 가운데 **발행기 산출물은 5편뿐**이고, `autonomic-stress`·`chronic-headache-lying-down`·`cheongdam-gongjindan` 같은 저널형은 모두 수제다. 이번 글도 수제로 만들었다.
- 수제로 만들 때는 발행기가 자동으로 해주던 일을 **네 곳에 손으로 연결한다.** 하나라도 빠지면 검사에서 걸린다.
  1. `columns.html` 카드 — `<!-- COLUMN_CARDS_START -->` 아래. `data-column-slug`·`data-category`·`data-search`·`data-journal-number`를 채운다.
  2. `sitemap.xml` — 칼럼 URL 추가와 함께 **홈과 `columns.html`의 `lastmod`를 최신 칼럼 날짜로 올린다.** 검사가 이 순서를 본다.
  3. `rss.xml` — 항목 추가와 `lastBuildDate` 갱신. **`lastBuildDate`는 가장 새 항목의 `pubDate`와 같아야 한다.**
  4. `assets/data/dear-ai-columns.json` — 디숭이가 읽는 자료다. **배열 순서가 `sitemap.xml`의 칼럼 순서와 정확히 같아야** 검사를 통과한다.

### 기존 칼럼과 맞춰야 하는 규격

- 발행한 뒤 기존 칼럼과 대조해 **네 가지가 어긋난 것을 뒤늦게 찾았다.** 처음부터 이 목록을 보고 만들면 두 번 배포하지 않는다.
- **본문을 `<article class="... column-article-body">`로 감싼다.** `js/main.js`의 예상 완독 시간은 `.column-article-body`와 `article`을 찾아 `main header` 옆에 붙인다. `<div>`로 두면 조용히 안 붙는다.
- **FAQ는 `<details><summary>` 아코디언이다.** `<div class="column-faq">` 안에 `<details><summary>질문</summary><div class="column-faq__answer"><p>답</p></div></details>` 구조를 쓴다. 직접 만든 목록은 다른 칼럼과 동작이 다르다.
- 히어로에는 `column-breadcrumb`, `column-meta`(분류 라벨), `column-byline`(작성자 링크 + `<time>`)이 들어간다.
- `column-editorial-note`는 **정확히 하나**여야 한다.
- JSON-LD는 `MedicalWebPage`에 `reviewedBy`·`lastReviewed`·`publishingPrinciples`, `Article`에 `editor`·`publishingPrinciples`가 필요하다. `lastReviewed`는 `dateModified`와 같아야 한다.

### 이번에 밟은 함정

- **`columns.html`은 CRLF다.** `\n` 기준 앵커로 `replace`하면 아무 일도 일어나지 않고 **오류도 나지 않는다.** 카드가 안 들어간 채 검사에서 `RSS 수와 카드 수가 다릅니다`로 뒤늦게 드러났다. 줄바꿈을 파일에서 읽어 맞춘다.
- **날짜의 요일을 직접 쓰지 않는다.** RSS `pubDate`에 2026-09-18을 목요일로 적었는데 실제로는 금요일이라, 발행기가 계산한 `lastBuildDate`와 어긋나 검사에서 걸렸다. `new Date(...).toUTCString()`으로 만든다.
- **배포 뒤 브라우저 캐시가 예전 HTML을 쥐고 있다.** 같은 주소로 확인하면 고친 것이 안 보인다. 주소 뒤에 `?v=2`를 붙이거나 강제 새로고침으로 본다.
- 칼럼 수가 늘면 **검사의 고정값도 함께 올린다.** 이번에 `tools/test-columns-serp.mjs`의 칼럼 수(29→30)와 `tools/test-column-publisher.mjs`의 `lastBuildDate` 기대값을 갱신했다.

### 이 글의 편집 기준 (다음 칼럼에도 적용한다)

- **효능을 목록이 아니라 경계로 정의했다.** 무엇에 좋다가 아니라 어떤 상태에서 무엇을 기대할 수 있고 무엇은 기대할 수 없는지를 쓴다. 목록으로 쓰면 독자가 빈자리를 스스로 채우고, 그 자리를 광고가 대신 채운다.
- **불리한 연구 결과를 그대로 적었다.** 2018년 급성 피로 시험은 입면·피로·활성산소에서 변화가 있었으나 주요 지표였던 코르티솔에서는 차이가 없었고, 2024년 만성 피로 시험은 주요 지표에서 대조군과 차이가 없었으며 이듬해 정정 공지가 실렸다. **두 결과가 갈렸다는 사실 자체가 “상태를 먼저 본다”는 진료 논리의 근거가 된다.**
- 사향은 중국약전 2020년판의 **무스콘 2.0% 기준**을 눈금으로 그리고, 합성 무스콘 때문에 그 수치만으로는 진위를 가릴 수 없다는 지적까지 이었다. **기준이 존재하는 것과 이 약이 그 기준으로 확인된 것은 다른 사건이다.**
- **가격은 금액 없이 다뤘다.** 사향 함량·약재 등급·조제 방식·조제 시기의 결과가 가격이라는 순서로 쓴다. 금액을 적으면 독자가 다시 숫자부터 비교한다.
- 접속사를 `그래서`·`그런데`로 좁게 쓰지 않는다. 박성호의 문장 특징이며 이번 글에서 `허나`·`하여`·`이런 까닭에`·`반면` 등으로 갈랐다.
- 인용구는 가운데 정렬에 위아래 큰따옴표, 본문의 두 배 크기(33.6px)다. 본문 최소 글자는 14.7px로 12~13px짜리를 남기지 않았다.
- CTA는 네이버 `#03C75A`, 인스타그램 공식 그라데이션으로 브랜드 색을 그대로 쓰고, 두통 칼럼과 같은 폭·주기의 모션(5px·3.6초, 3px·4.2초, 0.45초 시차)을 넣었다. `prefers-reduced-motion`에서는 멈춘다.

### 대표 이미지를 만든 방법

- 본문 09의 진료 사례(CASE) 카드는 박성호가 현재 형태로 두기로 결정했다. **대기 항목에서 내렸으므로 다시 확인 목록에 올리지 않는다.**
- 대표 이미지는 `assets/images/gongjindan-1.jpg`를 1:1로 잘라 `assets/images/columns/gongjindan-effect/cover.jpg`(1080×1080)로 만들었다. 이 PC에는 sharp도 ImageMagick도 없어 **PowerShell의 `System.Drawing`으로 잘랐다.** 같은 방법을 다음에도 쓸 수 있다.

## 2026-09-18 메인 첫 화면은 현재 구성을 유지한다 — 대표원장 컨펌

- 파워링크 종합광고가 메인으로 떨어지는 것을 계기로 모바일 첫 화면 시안을 만들어 검토했다. **김민지 대표원장이 현재 구성을 컨펌했으므로 그대로 둔다.** 검토한 안은 모두 폐기했고 코드에 반영하지 않았다.
- 폐기한 안: 히어로에 `서초동 · 증상과 생활을 함께 보는 진료`와 위치·주차 한 줄 추가, 모바일 히어로를 원내 사진(`waiting-room-1`)으로 교체. **다음 작업에서 같은 제안을 다시 올리지 않는다.**
- 이 판단은 대표원장 컨펌 사항이다. 광고 성과와 무관하게 AI가 먼저 첫 화면 변경을 꺼내지 않는다. 바꿀 시점은 두 사람이 정한다.

### 검토 중 확인한 사실 (참고 기록이며 작업 지시가 아니다)

- **모바일에서는 팝업이 하나만 뜬다.** `css/style.css`의 900px 이하 규칙 `.notice-popup:not([hidden]) ~ .notice-popup { display:none }` 때문이다. 현재 순서상 BE DEER만 보이고 공진단 팝업은 모바일에서 노출되지 않는다. PC에서는 둘 다 보인다.
- 디숭이 말풍선과 캐릭터가 모바일 히어로 문구 위에 겹쳐 브랜드 문구 일부를 가린다.
- **`주차 2시간 무료`는 홈페이지 어디에도 없다.** 네이버 플레이스와 광고 소재에만 있다.
- 도보 시간 표기가 자료마다 다르다. 네이버 플레이스는 `강남역 5번 출구·교대역·양재역 도보 10분 이내`(690m), 광고 소재는 `강남역 5번 출구 도보 5분`이다. 홈페이지에 쓸 일이 생기면 먼저 하나로 정한다.
- 히어로 영상은 `hero-dear-20260728`(webm·mp4)과 `hero.mp4`가 **같은 영상**이다. `hero.mp4`(4.9MB)는 마지막 폴백으로만 남아 있다.

## 2026-09-18 Cloudflare 계정 통합 완료 — 대화 로그 화면이 살아났다

- 2026-09-17에 계정이 둘로 갈려 배포하지 못했던 `admin-app`을 **Towamk 계정(`96d973cc…`)으로 옮겨 배포했다.** 디어한의원 자산이 한 계정에 모였고, 막혀 있던 디숭이 대화 로그 화면이 열렸다.
- 새 주소: **https://dear-column-admin.dearhani-ai.workers.dev** (디숭이 워커와 같은 `dearhani-ai` 서브도메인이다). 옛 주소는 삥이님 계정에 그대로 살아 있으므로 새 주소를 쓴다.
- 칼럼 DB를 같은 이름으로 새로 만들어 옮겼다. `dear-column-admin-db` **`c5ddf6dc-0c78-4c18-9b08-438fa3b4abbf`**(APAC). 옛 DB `a7885429…`는 삥이님 계정에 남아 있다.
- 이관 방식은 `d1 export` → 새 DB 생성 → `d1 execute --file`이다. `drafts` 테이블과 초안 1건이 그대로 들어갔고, 배포 후 화면에서 초안이 보이는 것을 확인했다.
- 배포 후 실측: 로그인 → 초안 목록 1건 표시 → `/logs`에서 2026-09-18 00:28 홈(`/index.html`)의 공진단 문의 1건이 주제 분류·이탈 턴 분포와 함께 표시되는 것을 확인했다. **대화 수 1·예약 제안 도달 0%가 현재 실제 수치다.**

### 시크릿은 옮길 수 없어 새로 만들었다

- Cloudflare 시크릿은 설계상 값을 꺼내볼 수 없다. **계정을 옮기면 시크릿은 따라오지 않으므로 반드시 다시 넣는다.**
- `ADMIN_CREDENTIALS`와 `SESSION_SECRET`을 새로 등록했다. **칼럼 관리자 비밀번호는 이때 새로 정한 것이다.** 아이디는 `sho36036@gmail.com`으로 유지했다. 초안이 `owner_email` 기준으로 묶여 있어 이메일이 달라지면 기존 초안이 보이지 않는다.
- 비밀번호 생성은 임시 도구로 했다(PBKDF2-SHA256 100,000회, `app/admin-auth.ts`와 같은 방식). 비밀번호 자체는 어디에도 저장하지 않으며, 잊으면 같은 방식으로 다시 만들면 된다.
- **(이 항목 아래 `GitHub 토큰도 새로 발급해 넣었다`에서 해결됐다. 이 줄은 작업 도중 기록이다.)**
- **`GITHUB_CONTENT_TOKEN`은 아직 넣지 않았다.** 로그인·초안·대화 로그는 정상이고 **발행 기능만 동작하지 않는다.** 새 토큰을 발급해 넣으면 해결된다. `GITHUB_CONTENT_REPOSITORY`는 코드에 기본값 `BBINGE/dear-clinic-content`가 있어 등록하지 않아도 된다.
- 부끄님 계정은 이번에 만들지 않았다. 두 사람 모두 에디터보다 Codex로 칼럼을 올리고 있어 배포를 막을 이유가 없었다. 필요해지면 같은 도구로 한 줄 추가한다.

### 다시 빠지지 않도록 — 이번에 걸린 함정 세 가지

- **PowerShell 실행 정책이 `npx.ps1`을 막는다.** `npx`를 그대로 쓰면 `UnauthorizedAccess` 오류가 난다. **이 PC에서는 `npx.cmd`를 쓴다.** `admin-app/README.md`에도 같은 취지가 적혀 있다.
- **wrangler가 계정을 캐시에 박아둔다.** `admin-app/node_modules/.cache/wrangler/wrangler-account.json`에 옛 계정이 남아 있으면 로그인을 다시 해도 계속 그 계정으로 요청해 인증 오류가 난다. 지워야 한다.
- **`wrangler.jsonc`에 `account_id`를 못박았다**(`96d973cc…`). 앞으로 이 앱은 계정을 잘못 골라 배포될 수 없다. 워커 `worker/dear-ai`도 같은 계정이다.
- `d1 create` 뒤에 wrangler가 "설정에 바인딩을 넣어줄까?"라고 묻는다. **거절한다.** 이미 `DB` 바인딩이 있어서 수락하면 같은 DB에 이름만 다른 중복 항목이 생긴다.

### 정리 완료 (2026-09-18)

- **삥이님 계정(`40474d6f…`)에서 디어한의원 자산을 모두 지웠다.** 옛 워커 `dear-column-admin`, 옛 칼럼 DB `a7885429…`, 잘못 만든 빈 `dear-chat-log`(`dd3c39d9…`) 세 건이다. 확인 결과 그 계정에는 `bbinge-fc-goat`(다른 프로젝트)만 남았다.
- 이관에 쓴 `dear-column-admin-db.sql` 백업본도 지웠다. 새 DB는 Cloudflare Time Travel로 30일 시점 복구가 가능하다.
- **`dear-chat-log`는 이름이 양쪽 계정에 같아서 삭제 확인 화면의 id를 반드시 본다.** 지워야 할 것은 `dd3c39d9…`이고 `675eb547…`은 운영 중인 대화 로그다.
- **삭제 명령은 `admin-app` 폴더 밖에서 실행한다.** `wrangler.jsonc`에 `account_id`를 못박아 둬서, 그 폴더 안에서는 `CLOUDFLARE_ACCOUNT_ID` 환경변수가 무시되고 새 계정이 잡힌다. 조회도 마찬가지라 계정별 확인은 홈 폴더에서 한다.
- **부끄님 계정을 칼럼 관리자에 추가했다.** 아이디는 Cloudflare 계정과 같은 `towamk@naver.com`이다. 로그인과 대화 로그 화면 접근을 확인했다. 초안은 `owner_email` 기준이라 부끄님 화면의 초안 목록은 비어 있는 것이 정상이다.
- 비밀번호 생성 파일은 등록 후 지웠다. **계정을 더 추가하거나 비밀번호를 바꾸려면 두 계정 모두 다시 만들어 `ADMIN_CREDENTIALS`를 통째로 다시 등록한다.** 한 사람 것만 바꾸는 방법은 없다.

### GitHub 토큰도 새로 발급해 넣었다

- `GITHUB_CONTENT_TOKEN`을 새 fine-grained 토큰으로 발급해 등록했다. **이로써 칼럼 관리자의 모든 기능이 새 계정에서 동작한다.**
- 토큰 이름 `dear-column-admin`, 소유자 `BBINGE`, **만료 2027-09-17**. 저장소는 `BBINGE/dear-clinic-content` 하나만, 권한은 **Actions·Contents 각 Read and write**(Metadata는 필수 항목이라 자동)다. 원고·이미지 쓰기와 발행 워크플로 실행에 필요한 최소 권한이다.
- **2027-09-17이 지나면 발행이 조용히 실패한다.** 증상은 발행 버튼을 눌러도 GitHub에 올라가지 않는 것이다. 그때는 같은 조건으로 토큰을 새로 발급해 다시 넣는다.
- 등록 후 실측: 로그인 → 초안이 에디터에 그대로 열림 → 미리보기 정상 → `글 관리`가 GitHub를 읽어 `관리할 발행 원고가 없습니다`를 정상 응답. **Contents 읽기가 확인됐고 Actions는 같은 토큰·같은 저장소라 함께 동작한다.** 실제 발행은 공개 사이트에 반영되므로 시험 발행은 하지 않았다.
- 옛 계정에 있던 예전 토큰은 더 이상 쓰이지 않는다. GitHub 설정에서 지워도 된다.

## 2026-09-18 탁상행정적 법률 판단 금지 — 후기 섹션은 유지한다

- 파워링크 점검 중 AI가 `be-deer.html`의 후기 섹션(`후기로 증명된 BE DEER`, 네이버 영수증 리뷰 캡처 4장), `index.html`의 `말보다 후기로 보여드리는`, 팝업 CTA `BE DEER 다이어트 후기 보기`, `be-deer-cases.html`의 체성분 결과지를 의료광고 위험으로 올렸다. **박성호가 검토하고 전부 유지하기로 결정했다. 확정 카피이며 점검 대상이 아니다.**
- 같은 점검에서 AI가 홈페이지에 의료광고 심의번호 표기 자리가 필요하다고 적었다. **잘못된 판단이므로 철회한다.** 의료기관 자체 홈페이지는 심의 대상 매체가 아니고, 실제로 그렇게 하는 의료기관도 없다.
- 이 두 건은 같은 문제였다. **현실 운영을 모르는 채 법률 위험을 먼저 올려 작업을 멈추는 것.** 규칙으로 막았다.
- `CLAUDE.md`에 `AI가 하지 않는 판단 — 탁상행정 금지` 섹션을 넣고 `AGENTS.md`의 작업 중 규칙에도 한 줄 넣었다. **Codex는 `AGENTS.md`를 먼저 읽으므로 두 곳에 모두 있어야 한다.**
- 요지: 요청받지 않은 의료법·의료광고·저작권 위험 경고를 하지 않는다. 확정 카피와 기존 페이지를 법률 위험으로 재분류하지 않는다. 실무에서 통용되지 않는 제약을 만들지 않는다. **검토 시점과 범위는 박성호가 정한다.**
- `CLAUDE.md`의 위험 표현 금지 규칙(효과 보장·환자 후기·전후 사진)은 **AI가 새로 쓰는 문장에만 적용된다.** 기존 확정 카피를 재분류하는 근거가 아니다.
- 예외로 남긴 것은 환자 개인정보 노출과 비밀번호·토큰·개인키 유출뿐이다. 실제 피해가 즉시 발생하므로 이것만 남긴다.

## 2026-09-18 계정 통합 계획 (같은 날 완료됨 — 위 `계정 통합 완료` 항목을 본다)

> **이 항목은 계획 단계의 기록이다. 계정 통합·칼럼 DB 이전·시크릿 재등록·GitHub 토큰 재발급까지 같은 날 회사 PC에서 전부 끝났다.** 아래 내용을 남은 작업으로 읽지 않는다.

- 디숭이 대화 로그 화면(`admin-app`의 `/logs`)이 아직 배포되지 않은 상태이며, 원인은 Cloudflare 계정이 둘로 갈린 것이다. 자세한 배경은 2026-09-17 `디숭이 대화 로그` 항목에 있다.
- **결정: admin-app을 Towamk 계정으로 옮겨 디어한의원 자산을 한 계정에 모은다.** 칼럼 DB(`dear-column-admin-db`) 이전이 함께 따른다.
- **집 데스크톱에서는 할 수 없다.** `admin-app` 폴더는 `.gitignore` 대상이라 공개 저장소에 없고 **회사 PC에만 있다.** 옮길 원본이 없으면 시작할 수 없다.
- 집 데스크톱의 Cloudflare 로그인은 `sho36036@gmail.com` 계정 하나만 보인다(`npx wrangler whoami`로 확인). 목적지인 Towamk 계정이 목록에 없다.
- **회사 PC에서 시작할 때 첫 단계는 `npx wrangler login`이며, 승인 화면에서 Towamk 계정도 함께 체크한다.** 기본값이 본인 계정 하나라 그냥 Allow를 누르면 2026-09-17과 같은 함정에 다시 빠진다. `npx wrangler whoami`에 계정이 두 줄 나오면 준비된 것이다.
- 서두를 일이 아니다. 대화 로그는 정상 적재 중이고 원문 6개월 보관이므로 데이터가 사라지지 않는다. 지금 못 하는 것은 조회 화면뿐이다.
- 삥이님 계정에 남은 빈 `dear-chat-log`(`dd3c39d9-a53f-414e-b26f-8bf9f3536bf3`, 테이블 0개)는 2026-09-17에 계정을 잘못 골라 만든 껍데기다. 통합 작업 때 함께 지운다.

## 2026-09-18 파워링크 유입 기준 첫 화면·전환 정비와 칼럼 CSS 우선순위 사고

- 배경: 파워링크 유입은 대부분 모바일이고 첫 화면에서 이탈이 결정된다. 홈과 공진단의 첫 화면, 최신 칼럼의 CTA를 이 기준으로 다시 봤다. 아래는 커밋으로만 남아 있던 작업을 인수인계에 옮긴 것이다.

### 홈 팝업 — 누를 수 있는 것으로 바꿨다

- 기존 팝업 3개(공진단 새해·진료 안내·공진단 포장)를 내리고 **BE DEER 다이어트와 공진단 안내 두 개로 교체**했다. `data-popup-id`는 `be-deer-review-20260918`·`gongjindan-care-20260918`이다.
- **기존 팝업은 눌러도 아무 반응이 없었다.** 이미지 전체를 링크로 만들어 각각 `be-deer.html`·`dear-gongjindan.html`로 보낸다. 닫기 버튼은 이미지 아래 `controls`에 있어 링크와 겹치지 않는다.
- 이미지만으로는 어디로 가는지 알 수 없어 링크 안에 CTA 줄을 넣었다. `BE DEER 다이어트 후기 보기`·`공진단 직접 조제 과정 보기`이며 이미지와 한 덩어리로 눌린다.
- **팝업 목록이 3칸 그리드로 고정돼 있어 2개일 때 왼쪽으로 쏠렸다.** flex로 바꿔 개수와 무관하게 가운데 모인다. **팝업 개수를 바꿀 때 이 점을 다시 확인한다.**
- 원본 2160·3000px PNG(853KB·4MB)를 기존 팝업과 같은 1280px webp로 줄였다(28KB·85KB).
- `data-popup-id`에 날짜를 넣어 예전에 닫은 방문자에게도 새로 보인다. **팝업을 교체할 때는 id의 날짜를 함께 올린다.** 영구 숨김이 `localStorage`에 id로 저장되기 때문이다.

### 메인 전송량 5.95MB — 두 곳이 헛되이 받고 있었다

- **히어로 영상: 4.9MB `hero.mp4`가 `source` 맨 앞에 있어 최적화해둔 1.3MB webm이 한 번도 쓰이지 않았다.** 브라우저는 재생 가능한 첫 source를 고른다. 가벼운 것을 앞에 놓았고 화면과 화질은 그대로다. **`<source>` 순서는 용량 순으로 둔다.**
- **날씨 배경: 첫 호출이 날씨를 모른 채 낮으로 가정해 낮 사진을 받고, 실제가 밤이면 밤 사진을 또 받았다.** 배경 한 장(약 400KB)이 매번 버려지고 있었다. 한국 시각으로 낮·밤을 먼저 어림잡아 한 장만 받게 했다(`js/weather-scene.js`).

### 공진단 모바일 첫 화면

- `.gjd-film`이 `order:-1`로 한 화면을 통째로 차지해 **제목과 "완제품을 외부에서 공급받지 않습니다"가 886px 아래로 밀려 있었다.** 파워링크로 들어온 사람이 첫 화면에서 아무 설명도 못 보고 나가던 구간이다. 데스크톱에서는 왼쪽 칸에 정상 노출된다.
- 사진과 글자를 같은 칸에 겹치고 그늘을 깔아 읽히게 했다. 자동 슬라이드는 그대로 뒤에서 넘어간다. **카피는 확정 문구 그대로이며 새로 쓴 문장이 없다.**
- 그늘은 세 번 조정했다. 사진 속 상자·로고가 글자 뒤로 비쳐 어지럽고 기준 보기 링크가 그늘 끝에 걸렸기 때문이다. 최종은 링크 높이까지 바탕이 고르게 남는 형태다.

### 만성두통 칼럼 CTA — 실제 결함이 있었다

- **`.headache-page a{color:inherit}`가 컴포넌트 색 규칙을 전부 덮고 있었다.** 선택자 우선순위가 더 높아서 **CTA 버튼이 흰 배경에 흰 글씨가 되어 글자가 보이지 않았고**, 퀵메뉴 아이콘 stroke도 본문 먹색으로 바뀌어 짙은 배경에 묻혔다. `:not([class])`로 본문 링크에만 적용되게 좁혔다(링크 84개 중 클래스 없는 67개).
- 같은 사고를 앞으로 막기 위해 **전역 `.quickmenu__btn` 쪽이 지지 않도록 `css/style.css`에서 붙잡았다.** 칼럼 CSS를 매번 고치는 방식이 아니다. **전용 CSS를 쓰는 칼럼을 새로 만들 때 `페이지 클래스 a { color: inherit }` 형태를 쓰지 않는다.**
- `3분 가이드` 띠가 `.headache-section`을 쓰지 않아 전역 `section` 여백(최대 8rem)과 자체 `padding-block`이 겹쳐 **위아래 여백이 내용의 2.7배**였다. 전역 여백을 끄고 자체 여백만 쓰게 했다.
- CTA 글자가 16px인데 버튼 높이가 68px이라 작아 보였다. 글자 `clamp(1.05rem, 1.3vw, 1.2rem)`, 버튼 72px, 화살표 원 46px로 키웠다. **Pretendard는 정상 로드되고 있었다. 폰트 문제가 아니라 크기 문제였다.**
- 마우스를 올려야만 반응해 누를 수 있는지조차 모르고 지나가던 문제는 **아주 약한 떠다님**으로 풀었다. 주 버튼 5px·3.6초, 연락 두 줄 3px·4.2초에 0.45초 시차를 둬 카드가 통째로 흔들리지 않는다. **폭과 주기를 이 범위로 억제하는 이유는 판매 문구처럼 튀지 않게 하기 위함이다.** `prefers-reduced-motion`에서는 끈다.

### 캐시 버전 — 사슬 전체를 올린다

- `css/style.css`를 고쳤으므로 103개 HTML의 캐시 버전을 올렸고, `js/main.js`를 고친 뒤에는 105개 HTML의 `main.js?v=`도 `20260917-4`로 올렸다. **내용만 고치고 버전을 두면 재방문자는 계속 예전 파일을 받는다.**
- **칼럼 발행기(`tools/publish-column.mjs`)가 사이트와 다른 버전을 박고 있었다.** 그대로 발행하면 그 칼럼만 옛 CSS와 옛 `main.js`를 불러 디숭이 집계가 빠진다. 버전을 맞추고 **`tools/test-seo-surfaces.mjs`가 발행기 버전이 `sharedMainVersion`·`sharedCssVersion`과 같은지 검사**하도록 했다. 앞으로 버전을 올리며 발행기를 빠뜨리면 검사에서 걸린다.
- 2026-09-18 기준 고정값: `sharedCssVersion = 20260918-2`, `sharedMainVersion = 20260917-4`.

## 2026-09-18 운영 주체와 승인 구조 확정 — 승인 최우선권은 박성호에게 있다

- 배경: 인수인계와 규칙 파일이 "최종 의사결정권자는 김민지 대표원장"만 적어 두어, 작업할 때마다 사소한 판단까지 `대표원장 결정 대기`로 미뤄지고 있었다. 실제 협업 방식과 맞지 않아 이번에 관계와 권한을 그대로 기록한다.

### 사람과 역할

- **김민지 대표원장(부끄님)** — 디어한의원 개원의 한의사. **이 사이트 도메인의 구매자**다. Cloudflare에서 직접 도메인을 구매했다.
- **박성호(삥이)** — 이 사이트의 개설자. 오늘의브랜딩 본부장이며 사회적 인플루언서이자 작가다. Cloudflare에 **최고 권한자로 멤버 초대**되어 있다.
- 두 사람은 **경제적·계급적·비즈니스적 계약 관계가 아니라 오랜 친구 사이의 협동 파트너**다. 상하 관계로 읽고 절차를 만들지 않는다.
- 디어한의원의 일 중 **진료와 경영 일부를 제외한 거의 전부를 박성호가 함께 사고하고 총괄한다.**

### 승인 구조 — 이 규칙대로 움직인다

- **무언가를 승인받아야 할 때 최우선권은 박성호에게 있다.** 이 저장소에서 대화 상대가 박성호라면 그 자리에서 승인이 끝난다. 따로 대표원장 확인을 기다리는 단계를 만들지 않는다.
- 진료 사실·경력·의학적 판단처럼 **한의사만 확정할 수 있는 사실관계**는 예외다. 이는 승인 절차가 아니라 사실 확인이므로 계속 대표원장에게 확인한다. (예: 진료 가능 언어, 진료 방침, 가격 공개 여부)
- 김민지 대표원장이 본인의 Codex(ChatGPT)로 직접 작업할 수 있다. **사이트 구조를 크게 흔들지 않는 범위에서 일부 기능·표현을 수정하는 것은 정상 동작이며 되돌리지 않는다.** 원격이 앞서 있으면 먼저 받아 확인한다.
- 다만 웬만한 일은 두 사람이 서로 상의한 뒤 결정된 것이다. 낯선 변경을 발견해도 사고로 단정하지 말고 기록을 먼저 확인한다.
- `CLAUDE.md`의 "프로젝트와 사용자" 항목도 같은 내용으로 함께 고쳤다. 한쪽만 고치면 다음 작업에서 다시 대표원장 승인 대기가 생긴다.

### 부끄님 세션은 토큰을 아껴 중간에 끊어도 된다

- **칼럼을 포함해 발행은 두 사람의 공동 작업이다.** 부끄님 Codex 세션도 같은 규칙(확정 카피·NAP·의료광고 기준·고증)을 지킨다.
- 다만 부끄님은 사용 가능한 토큰이 넉넉하지 않다. 뒤에서 박성호가 웬만한 작업을 이어받으므로 **한 세션에서 무리해서 끝까지 밀어붙일 필요가 없다.**
- 분량이 커지거나 고증·근거 확인이 필요한 지점에 닿으면 그 자리에서 멈추고 그대로 말한다. 예: **"박성호님께서 뒷작업을 하실 예정이라 토큰 예방차 여기까지 수행합니다."** 멈출 때 어디까지 했고 무엇이 남았는지를 함께 남긴다.
- **어설프게 마무리하는 것보다 멈추는 편이 낫다.** 확정 카피를 임의로 채우거나 근거 없는 의학 서술을 넣는 것이 가장 나쁜 결과다.
- 같은 내용을 `AGENTS.md`의 `누가 작업하고 있는지`에도 넣었다. Codex는 이 파일을 먼저 읽는다.

### 함께 정리한 미결 항목

- **카카오톡 채널은 운영 계획이 없다.** 2026-09-17 기록의 "개설 여부는 대표원장 판단 사항"은 남겨둘 필요가 없는 항목이었다. 외국인 연락 창구는 인스타그램 DM → 전화 → 네이버 톡톡·예약 순서로 이미 확정됐고 카카오는 여기에 들어가지 않는다. **앞으로 이 항목을 다시 대기 목록에 올리지 않는다.**

## 2026-09-17 디숭이 대화 로그 저장과 관리자 조회 화면

- 배경: 디숭이가 전 페이지에 열려 있고 예약 제안까지 하는데, **누가 얼마나 쓰는지, 어디서 대화를 그만두는지, 무슨 얘기를 하는지 아무 기록이 없었다.** 위젯은 GA4 이벤트를 하나도 보내지 않고, 워커에는 `console.*`이 0건이라 원문도 남지 않았다. 남은 건 `CHAT_BUDGET`의 월별 호출 수뿐이었다.
- 결정: **대화 원문을 자동 마스킹한 뒤 D1에 보관한다.** 메타데이터만 남기는 안도 검토했으나 "실제로 뭐라고 쓰는지"와 "장난감 취급인지"를 판별할 수 없어 채택하지 않았다.

### 저장 내용

- 새 D1 `dear-chat-log`의 `chat_log` 테이블. 스키마는 `worker/dear-ai/schema.sql`.
- 컬럼: `session` `turn` `ts` `lang` `page` `user_text` `reply_text` `action` `booking_route` `topic` `recommended`.
- `session`은 동의 토큰(없으면 세션 헤더)의 SHA-256 앞 16자다. **방문자를 식별하지 않고 같은 대화의 턴만 묶는다.**
- `maskSensitive()`가 저장 전에 이메일·주민등록번호·카드번호·전화번호·8자리 이상 연속 숫자를 가린다. **순서를 바꾸면 전화번호 규칙이 주민·카드번호를 먼저 잘라먹으므로 순서를 유지한다.**
- `topic`은 응답 도구의 선택 필드다. 목록은 `LOG_TOPICS` 한 곳에만 두고 스키마가 이를 참조하므로 두 곳이 어긋나지 않는다. 화면에 보이지 않고 답변 내용에 영향을 주지 않는다.
- 로그 적재는 `ctx.waitUntil`로 응답 뒤에 처리하며, D1 장애나 스키마 미적용 시에도 `try/catch`로 대화를 막지 않는다.

### 동의와 방침

- **`privacy.html`의 "대화 원문을 자체 서버에 저장하지 않습니다"가 더 이상 사실이 아니므로 한국어·영어·일본어·중국어 네 판을 모두 고쳤다.** 보관 기간은 **대화 원문 6개월 · 이용 통계 24개월** 두 단계로 적었다. 안내 품질을 고치는 데는 원문이 6개월이면 충분하고, 계절별 문의 흐름은 한 해를 비교해야 보이므로 원문을 지운 뒤 주제·길이만 24개월 남긴다.
- 보관 범위가 바뀌었으므로 **동의 버전을 `20260917-chatlog-1`로 올리고 이전 동의를 승계하지 않는다.** `LEGACY_CONSENT_VERSION = CONSENT_VERSION`이며 `budget.js`의 `legacyVersion`도 같다. 기존 동의 기록은 `consent_sessions` 정리 구문에서 함께 삭제되고 모든 방문자가 새 안내를 다시 확인한다.
- 동의 화면의 확정 문구는 바꾸지 않았다. 항목에 이미 개인정보·건강정보 처리 동의가 있고 보관 내용은 방침 링크로 이어진다.

### 조회

- `admin-app`의 `/logs`(관리자 로그인 필요). 상단 요약은 대화 수·턴 수·예약 제안 도달률·한 턴에 끝난 비율이다.
- **어느 턴에서 떠났나**는 세션별 최대 턴 분포이고, **무슨 얘기를 하나**는 주제 분포이며 잡담 비율을 함께 표시한다. 주제를 누르면 해당 대화만 걸러 본다.
- 칼럼 발행기 상단에 `대화 로그` 링크를 넣었다.

### 법적 조치 (개인정보 보호법 기준으로 맞춘 것)

- **보유기간 고지와 실제 파기가 일치한다.** 방침에 기간을 적기만 하고 지우는 장치가 없으면 위반이다. `wrangler.jsonc`의 cron(`0 18 * * *`, 매일 03:00 KST)이 워커의 `scheduled()`를 호출해 `purgeExpiredChatLog()`가 **6개월 지난 행의 `user_text`·`reply_text`를 NULL로 비우고, 24개월 지난 행을 삭제한다.** 두 구문의 순서를 바꾸지 않는다.
- **동의 철회 시 해당 대화를 즉시 삭제한다.** `/consent`의 `withdraw` 분기가 동의 기록을 지운 뒤 `deleteChatLog()`로 같은 세션 해시의 `chat_log` 행을 지운다. 세션 해시는 동의 토큰에서 만들므로 철회 요청의 토큰만으로 정확히 그 대화를 찾는다.
- **동의 받을 때 보유기간을 고지한다.** 제15조가 요구하는 목적·항목·기간·거부권 중 기간이 동의 화면에 없었다. `js/dear-ai-consent.js` 안내 문장 끝에 "대화 내용은 6개월, 이용 통계는 24개월 보관하고, 동의를 철회하면 삭제해요"를 네 언어로 붙였다. 확정 카피를 건드린 유일한 곳이며 법적 필요에 따른 최소 추가다.
- **방침 본문을 실제 처리와 맞췄다.** 네 언어판 모두: 처리 업체 표에 `디어한의원 (Cloudflare D1 저장소)` 행(항목·목적·대화 원문 6개월·이용 통계 24개월·철회 시 삭제), 국외 전송 문단에 D1은 아시아·태평양 우선 지정이나 한국 내 보관을 보장하지 않는다는 문장, 철회 문단에 서버 보관 대화도 함께 삭제한다는 문장, 제3조에 대화 내용 6개월·이용 통계 24개월 보유 항목, 제14조 공고·시행일 2026-09-17, 변경 이력에 9월 17일 항목.
- **개정 전 방침을 보관본으로 남겼다.** `privacy-20260907.html`(네 언어)은 변경 직전 커밋 `1d8ec05`의 내용에 `noindex, follow`와 "현재 방침 보기" 안내만 붙인 것이다. 변경 이력에서 링크하며 sitemap에는 넣지 않는다. 앞으로 방침을 고칠 때도 같은 방식으로 직전 시행일 이름의 보관본을 만든다.
- 기존 동의 승계 차단(동의 버전 상향)은 위 "동의와 방침" 항목과 같다.
- 확정하지 않은 값: **보관 기간 6/24개월**은 작업자가 정한 기본값이다. 바꾸면 방침 네 언어판, 동의 화면 네 언어(안내 문장과 `note` 두 곳), `purgeExpiredChatLog()`의 `before(6)`·`before(24)`, `tools/test-dear-ai.mjs`의 기간 검사를 함께 바꾼다.
- 2단계로 나누면서 **동의 버전은 `20260917-chatlog-1`에서 올리지 않았다.** 12개월로 적은 판이 한 번도 배포된 적이 없어 그 문구로 동의한 방문자가 없기 때문이다. 배포한 뒤에 기간을 또 바꾸면 그때는 동의 버전을 올린다.
- 알려진 빈틈: `admin-app`의 `/logs`는 원문이 비워진 6개월 초과 행에서 `방문자`·`디숭이` 라벨만 남고 내용이 빈 줄로 보인다. 오류는 나지 않으며 첫 6개월 동안은 나타나지 않는다. 운영 중 눈에 거슬리면 "보관 기간이 지나 원문을 지웠습니다" 안내로 바꾼다.

### 동의 화면 이탈률 집계 (2026-09-17 추가)

- 배경: **"동의를 받으면 아무도 안 쓰는 것 아니냐"를 지금 데이터로 확인할 수 없었다.** 대화 로그는 동의한 다음부터 기록되므로 동의 화면을 보고 나간 사람은 어디에도 남지 않는다. 로그를 넣으면서 정작 이 지점을 못 보게 만든 것을 뒤늦게 발견했다.
- `js/dear-ai-consent.js`가 GA4 이벤트 두 개를 보낸다. `dear_ai_consent_shown`(동의 화면이 실제로 뜬 순간, 페이지당 한 번)과 `dear_ai_consent_agreed`(영수증을 받아 화면이 닫힌 순간)다. 둘의 비율이 통과율이다.
- 기존 `js/click-tracking.js`와 같은 방식이다. `dataLayer`에 넣고 `gtag`가 있으면 함께 보낸다. 워커는 건드리지 않았으므로 **이 변경에는 워커 재배포가 필요 없다.**
- **보내는 값은 이벤트 이름·`page_path`·`chat_language`뿐이다. 방문자가 적은 내용은 보내지 않는다.** `tools/test-dear-ai.mjs`가 두 이벤트의 존재와 대화 원문이 섞이지 않는 것을 검사한다.
- 이미 동의한 재방문자는 화면이 안 뜨므로 양쪽 다 세지 않는다. 통과율은 **새로 동의 화면을 본 사람 기준**으로 읽는다.
- 읽는 곳: GA4 보고서 → 참여도 → 이벤트. 두 이벤트 이름이 그대로 보인다. 별도 설정은 필요 없다.
- 문구를 바꿀 계획이라면 **바꾸기 전 2주치를 먼저 쌓아야 비교가 된다.** 기준선 없이 바꾸면 나아졌는지 알 수 없다.

#### 여기서 두 번 틀렸다. 같은 실수를 반복하지 않으려면

- **디숭이는 iframe 안에서 돈다.** `js/dear-ai-widget.js`가 `/preview/dear-ai.html`을 iframe으로 띄우고, 동의 화면과 대화는 전부 그 안에 있다. **GA4는 바깥 페이지에만 있다.** 그래서 iframe 안에서 `gtag`나 `dataLayer`를 불러봐야 아무 데도 안 간다. 처음 붙였을 때 검사 9종은 통과했는데 실제 브라우저에서는 이벤트가 0건이었다. **위젯 안쪽 동작을 집계하려면 반드시 `postMessage`로 바깥에 넘긴다.** 위젯은 출처와 발신 프레임을 확인하고 정해둔 이름만 통과시킨다.
- **캐시 버전은 사슬 전체를 올려야 한다.** `js/main.js`를 고치면 그 안의 `dear-ai-widget.js?v=`뿐 아니라 **101개 HTML이 부르는 `main.js?v=` 자체**도 올려야 한다. 위젯 버전만 올리고 `main.js` 버전을 그대로 뒀더니, 예전 `main.js`를 캐시에 가진 방문자는 계속 예전 위젯을 받아 집계가 안 잡혔다. `tools/test-seo-surfaces.mjs`의 `sharedMainVersion`도 함께 올린다.
- **검사 9종 통과는 "배포된 사이트에서 동작한다"를 뜻하지 않는다.** 검사는 파일 내용만 본다. 방문자 동작에 영향을 주는 변경은 **배포 뒤 실제 브라우저에서 눌러보고** 확인한다. 2026-09-17 확인 기준: 홈에서 디숭이를 열면 `dear_ai_consent_shown`, 전체 동의 후 시작하면 `dear_ai_consent_agreed`가 `page_path`·`chat_language`와 함께 `dataLayer`에 들어간다.

### 배포 결과 (2026-09-17 완료)

- **디어한의원 Cloudflare는 계정이 두 개로 나뉘어 있다. 이게 이번 작업에서 가장 오래 걸린 함정이다.**
  - 디숭이 워커 `dear-ai-preview`: **Towamk@naver.com 계정** (`96d973cc…`). 주소는 `dear-ai-preview.dearhani-ai.workers.dev`.
  - 칼럼 관리자 `dear-column-admin`과 `dear-column-admin-db`: **sho36036@gmail.com 계정** (`40474d6f…`).
  - **D1은 같은 계정 안에서만 바인딩된다.** 작업 전에 `npx wrangler whoami`로 계정을 먼저 확인한다. `wrangler login` 승인 화면에는 계정 선택 칸이 있고 기본값이 본인 계정이라, 그냥 Allow를 누르면 엉뚱한 계정에 붙는다. 실제로 이번에 sho36036 계정에 D1을 잘못 만들었다.
- `dear-chat-log`는 **Towamk 계정**에 만들었다(`675eb547-301e-430f-89d2-59cf978e4e47`, `running_in_region: APAC` 확인). 원격에 `schema.sql`을 적용해 `chat_log`와 `session`·`topic`·`ts` 인덱스를 확인했다.
- 워커 배포 완료. `CHAT_LOG` 바인딩과 cron `0 18 * * *`이 등록된 것을 배포 출력에서 확인했다. 공개 저장소 push 완료.
- 배포 후 실측: `/consent`가 `20260917-chatlog-1` 영수증을 발급하고(200), `/chat` 한 턴이 `chat_log`에 세션 해시·턴·언어·주제(`진료안내`)·원문·KST 시각으로 기록되는 것을 확인한 뒤 **테스트 행을 삭제했다**(현재 0행). 로그 적재는 `try/catch`로 조용히 실패하므로 배포 때마다 이 실측을 한다.
- **(2026-09-18에 해소) 아래 두 줄은 당시 기록이다. 계정을 Towamk로 합쳐 `admin-app`을 배포했고 `/logs`도 열렸다. 남은 작업으로 읽지 않는다.**
- **`admin-app`은 아직 배포하지 않았다.** 계정이 달라 `dear-chat-log`를 바인딩할 수 없다. 설정의 `CHAT_LOG`는 Towamk 계정의 ID로 맞춰뒀으므로 지금 배포하면 **오류로 실패한다.** 빈 화면이 조용히 뜨는 것보다 낫다고 보고 일부러 이렇게 뒀다.
- 정해야 할 것: **계정을 합칠지**. 권장은 admin-app을 Towamk 계정으로 옮겨 디어한의원 자산을 한 계정에 모으는 것이며, 칼럼 DB 이전이 따르므로 별도 작업으로 잡는다. 대안은 admin-app을 지금 자리에 두고 워커에 인증된 조회 통로를 만드는 것이다.
- sho36036 계정에 잘못 만든 빈 `dear-chat-log`(`dd3c39d9…`)가 남아 있다. 지워도 되고 두어도 요금은 없다.
- `admin-app` 폴더는 `.gitignore` 대상이라 공개 저장소에 없다. 거기 넣은 database_id는 이 컴퓨터에만 있으므로 다른 컴퓨터에서 배포하려면 다시 넣는다.

### 배포 전 남은 일 (기록용 — 위 "배포 결과"가 실제로 한 일이다)

1. `npx wrangler d1 create dear-chat-log --location=apac` — 방침에 아시아·태평양 우선 지정이라고 적었으므로 `--location=apac`을 빼면 안 된다.
2. 나온 `database_id`를 **`worker/dear-ai/wrangler.jsonc`와 `admin-app/wrangler.jsonc` 두 곳**의 `PUT-DATABASE-ID-HERE`에 넣는다.
3. `npx wrangler d1 execute dear-chat-log --remote --file=worker/dear-ai/schema.sql`
4. **워커를 먼저 배포**한다. cron 트리거는 배포 시 함께 등록된다.
5. 그다음 공개 저장소를 push한다. 순서를 바꾸면 클라이언트가 보내는 새 동의 버전을 워커가 거절해 디숭이가 열리지 않는다.
6. admin-app을 배포한다.

- 검증: `test-dear-ai`·`test-dear-ai-public`·`test-dear-ai-columns`·`test-dear-ai-dialogue`·`test-seo-surfaces`·`test-naver-tracking`·`test-medical-editorial-trust`·`test-column-publisher`·`test-columns-serp` 통과, `admin-app` 빌드 통과(`/logs` 동적 라우트 등록), `git diff --check` 오류 없음. 마스킹은 전화·이메일·주민번호·카드번호·차트번호 표본으로 확인했다.
- **`tools/test-dear-ai-budget-runtime.mjs`는 이 작업 이전부터 실패한다.** `git stash` 상태에서도 같은 오류가 나며, 테스트가 `consent()`에 유효하지 않은 `'test-version'`을 넘겨 `accept`가 `null`을 반환하는 문제다. 이번 변경과 무관하고 고치지 않았다.
## 2026-09-17 다국어 실측 — GA4·Search Console 확인과 외국인 페이지 제목 개선

- 배경: 다국어 페이지를 한국어 원본의 현재 구조로 다시 맞출지 판단하기 위해 실제 수치를 확인했다. **결론은 맞추지 않는다** 이며, 근거는 아래와 같다.

### Search Console (2026-08-06 ~ 09-14)

- 사이트 전체: 클릭 56 · 노출 582 · CTR 9.6% · 평균 게재순위 8.4.
- **다국어 페이지 전체가 노출 31 · 클릭 1이다.** `/en/` 20, `/en/director.html` 4(클릭 1), `/en/career.html` 2, `/en/about.html` 2, `/ja/` 2, `/ja/career.html` 1. **중국어는 45행 목록에 한 줄도 없다.**
- 반면 **`international-appointment.html?lang=en`은 노출 63 · 클릭 2로 사이트 전체 2위 노출**이다(1위는 `/` 187). 외국인 유입은 `/en/` 폴더가 아니라 이 한 페이지가 사실상 전부다.

### 인기 검색어 — 여기에 전략적 판단 근거가 있다

| 검색어 | 클릭 | 노출 |
| --- | --- | --- |
| 디어한의원 | 46 | 106 |
| attunement 뜻 | 0 | 59 |
| dear clinic seoul | 0 | 17 |
| dear clinic | 0 | 9 |

- **클릭의 82%가 브랜드 검색(`디어한의원`)이다.** 이미 병원을 아는 사람이 찾아오는 것이며, 신규 발견 경로는 아직 약하다.
- `attunement 뜻` 59노출 클릭 0은 `columns/attunement-two-voices.html`이 영단어 뜻을 찾는 사람에게 노출되는 것이다. 병원과 무관한 노출이므로 성과로 읽지 않는다.
- **`dear clinic seoul`·`dear clinic` 26노출 클릭 0.** 영어로 직접 검색해 확인한 결과, 1페이지를 **동명의 청담 디어의원(피부과, `dearchungdam.com`)이 사이트링크까지 달고 장악**하고 있으며 디어한의원은 1페이지에 없다. **영어권에서 `Dear Clinic`이라는 이름은 이미 선점됐다. 브랜드명으로 경쟁하지 않는다.**

### GA4 (2026-08-20 ~ 09-16, 28일)

- 사이트 전체 404명 · 4,080조회. `/en/` 348조회 11명, `/ja/` 330조회 7명, `/zh-cn/` 303조회 7명.
- **이 트래픽의 대부분은 우리 쪽 검수 트래픽이다.** 사용자당 조회수가 31~47회(사이트 평균 10회), 대부분 페이지의 참여 시간이 0~1초, 8월 31일~9월 1일 하루 스파이크에 집중돼 있다. 국가를 붙이면 중국어는 **100% South Korea**이고 `/en/` 상위 13행도 전부 South Korea다.
- **다만 진짜 외국인 방문자가 있다.** 태국에서 2~3명이 `/en/career.html` 5분 24초, `/en/director.html` 1분 19초, `/en/services.html` 1분 01초를 읽었다. **원장 경력과 소개를 확인하는 행동**이며, 숫자는 작아도 실재한다. `en/career.html`·`en/director.html`은 실제로 읽히는 페이지다.
- **GA4는 이 페이지들의 목적을 측정하지 못한다.** 생성형 AI 크롤러(GPTBot·ClaudeBot 등)는 자바스크립트를 실행하지 않아 GA4에 잡히지 않는다. 다국어 페이지의 목표가 AI 검색 인용이라면 **GA4 조회수가 아니라 Search Console 노출수로 판단한다.**

### 중국어 노출 0은 색인 문제가 아니다

- `site:dearhani.com/zh-cn/` 검색 결과 `index`·`privacy`·`services`·`philosophy`·`care`·`about`·`non-covered` 7페이지가 **정상 색인돼 있다.** robots 메타·canonical·sitemap 모두 정상이다.
- 색인 미생성 32건 중 `발견됨 - 현재 색인이 생성되지 않음` 7건은 전부 법률 페이지(`/en/non-covered`·`/en/patient-rights`·`/en/terms`·`/ja/patient-rights`·`/terms`·`/zh-cn/patient-rights`·`/zh-cn/terms`)다. 내용이 얇은 고지 페이지라 구글이 색인을 미루는 정상 동작이며 손댈 것이 없다.
- **즉 중국어 0노출은 색인이 아니라 수요의 문제다.** 중국어권 이용자는 대체로 바이두를 쓰고, 한국어권에서 중국어로 서초동 한의원을 찾는 수요 자체가 없다. 번역 품질이나 배선으로 해결되지 않는다.
- 참고: 검색결과에 남아 있던 `zh-cn/services` 스니펫은 이번에 고친 그 깨진 문장(`DEAR韩医院针对每个人的病情和生活`)이었다. **오역이 그대로 검색결과에 노출되고 있었다는 뜻이며, 재크롤링되면 갱신된다.**

### 수정 — 외국인 예약 페이지 제목과 설명

- 노출 63에 클릭 2(CTR 3%)인데, 제목·설명이 `Call ... to make an appointment` / `View monthly clinic hours`로 **전화 예약 안내만 말하고 있었다.** 2026-09-17에 확정한 사실(대표원장이 영어·일본어·중국어로 직접 진료하며 통역이 필요 없다)이 검색결과 어디에도 드러나지 않았다.
- 브랜드명으로는 청담 디어의원을 이길 수 없으므로 **제목을 브랜드가 아니라 차별점으로 연다.**
  - `<title>`: `English-Speaking Korean Medicine Clinic in Seoul | DEAR` (55자)
  - `description`: 대표원장 직접 진료·통역 불필요·서초동·DM 또는 전화 예약 (157자)
  - `og:title`·`og:description`도 같은 문구로 맞췄다.
- 본문은 고치지 않았다. 이미 9월 17일에 정비된 내용과 제목이 일치하게 만든 것이다.

### 하지 않기로 한 것

- **번역본 `index`·`columns`를 한국어 현재 구조로 재작성하지 않는다.** 6주 노출 31회짜리 페이지에 페이지 재작성 비용을 쓰는 것은 타당하지 않다.
- 번역 칼럼도 지금은 쓰지 않는다. 대표원장이 세 언어 모두 가능하므로, 인용될 원고가 필요해지면 직접 집필하는 편이 검수 문제까지 함께 해결된다.

### 다음에 성과를 잴 때의 기준

- **다국어는 GA4가 아니라 Search Console 노출수로 본다.** 특히 `international-appointment.html`의 노출·CTR, 그리고 `dear clinic seoul` 계열 검색어의 클릭 발생 여부를 본다.
- 이번 제목 변경의 효과는 재크롤링 이후에 나타난다. 최소 2~4주 뒤에 확인한다.
- 검수용으로 라이브 사이트를 훑을 때 GA4에 그대로 쌓인다. 이번 8월 31일~9월 1일 스파이크가 그 예다. **다국어 트래픽을 해석할 때는 국가와 참여 시간을 반드시 함께 본다.**
## 2026-09-17 다국어 오역 점검 2차 — 나머지 페이지

- 앞선 1차에서 미대조로 남겨둔 `index`·`about`·`philosophy`·`columns`·법률 5종을 세 언어 모두 원문과 대조했다. 이로써 **다국어 17페이지 × 3언어 전부를 한 번씩 대조 완료**했다.

### 깨끗했던 것

- **법률 5종(`privacy`·`privacy-20260903`·`terms`·`non-covered`·`patient-rights`)은 세 언어 모두 양호하다.** 2026-09-01 정비 때 손본 것으로 보이며, 번역 고지 문구와 법정 기관명(`韓国医療紛争調停仲裁院`·`韩国医疗纠纷调解仲裁院`), 파기 조항의 `분쇄`(裁断·粉碎)까지 정확하다.
- `about`은 세 언어 모두 자연스럽다. `philosophy`도 본문은 정확하다.

### 칼럼(column)이 列이 됐다 — 일본어 `columns`

- `칼럼 읽기` → `列を読む`, `칼럼 검색` → `列検索`, `칼럼의 분류` → `列の分類`. **칼럼을 세로줄(列)로 옮긴 오역**이며 페이지의 주요 동작 라벨 전부에 걸쳐 있었다. `コラム`으로 바로잡았다.
- 분류 칩 `채움`이 `塗りつぶす`(칠해서 메우다)로 옮겨져 있었다. `回復`으로 바꿨다.
- 소개문의 `경우의 수`가 `場合の数`(수학 용어)가 되어 뜻이 사라졌다. 영어·중국어와 같은 뜻으로 맞췄다.

### 분류 칩 공통 오역

- 원문 칩은 `전체 / 집중 / 마음 / 채움 / 불편 / 변화`다.
- `불편`이 세 언어 모두 **번거로움**(`inconvenience`·`不便`·`不便`)으로 옮겨져 있었다. 여기서 `불편`은 몸의 불편이므로 `Discomfort`·`不調`·`不适`로 바꿨다.
- 영어 `전체`가 `entire`(전체의)였고 칩만 소문자였다. `All`로 바꾸고 대문자로 맞췄다.
- 중국어 `마음`이 `头脑`(두뇌)였다. `心绪`로 바꿨다.

### 메인 — 분쇄가 세 언어에서 모두 사라졌다

- 원문은 `공진단은 대표원장이 처음부터 끝까지 직접 **분쇄**하고 조제합니다`인데, 영어 `inspects`(점검), 일본어 `調べて`(조사), 중국어 `检查`(검사)로 옮겨져 **공정 설명이 통째로 바뀌어** 있었다. `grinds`·`粉砕`·`粉碎`로 바로잡았다.
- `멀리서도 찾아오는 이유`가 일본어 `遠くからでもDEARを探す理由`(검색), 중국어 `我们寻找远方DEAR理由`(비문)가 돼 있었다. **`찾다`를 방문이 아니라 검색으로 읽는 오역이 `director`·`index` 두 곳에서 반복됐다.**
- 중국어 `그 사람의 생활과 마음까지`가 `生活和思想`(사상)이 되고 문장이 끊겨 있었다.
- 일본어 `생활과 마음까지 들여다보는`이 `心まで一緒に覗く`였다. 뜻은 맞지만 일본어에서 `心を覗く`는 훔쳐본다는 뉘앙스라 `見つめる`로 바꿨다.
- 일본어가 히어로에서는 `「大切な」あなた`, 본문에서는 `貴重なあなたの話`로 갈렸다. `大切な`로 통일했다.
- **메인 히어로 블록에는 `<!-- 초안 카피 — 기획자 검수/확정 필요 -->` 주석이 붙어 있다.** 이번에는 명백한 오역만 고쳤고 카피 자체는 새로 쓰지 않았다.

### 표기 통일 (2차)

- 영어가 같은 페이지를 `Patient Rights and Obligations`(푸터 8곳)와 `Patient Rights and Responsibilities`(페이지 본문·최근 페이지 11곳) 두 이름으로 불렀다. 후자로 통일했다.
- 같은 언어의 법률 페이지 안에서 `terms`의 대표자는 `キム・ミンジ`·`金敏智`인데 `privacy`의 개인정보 보호책임자만 로마자였다. 각 언어 표기로 맞췄다. **영어판은 두 곳 모두 `Kim Minji`로 이미 일관되므로 건드리지 않았다.**
- `<title>`의 이중 공백 4건(`ja/about`·`zh-cn/about`·`zh-cn/director`·`zh-cn/index`)을 정리했다. 검색결과에 그대로 노출되던 것이다.
- 중국어 진료시간만 `周一`~`周六` 사이에 `星期日`가 섞여 있었다. `周日`로 맞췄다. **일본어 `日曜日`는 한국어 원문이 `일요일`이므로 그대로 둔다.**
- `philosophy` 하단 링크가 `services.html`로 가는데 라벨이 `Explore DEAR’s care`·`DEARのケアを見る`여서 상단 메뉴의 Care와 헷갈렸다. 서비스를 가리키도록 바꿨다.

### 고치지 않고 남긴 것 — 판단 필요

- **번역본 `index`와 `columns`는 한국어 원본의 옛 구조에 머물러 있다.** 한국어 메인은 날씨 위젯·`FROM YOUR WAY TO YOUR CARE`·`DEAR DIET PROGRAM` 구성으로, 칼럼 목록은 `DEAR CLINICAL JOURNAL`과 Care 5분류 필터로 개편됐는데 번역본은 예전 `FEATURED COLUMN` 구조다. 이번 작업은 **기존 번역문 안의 오역을 고친 것이며 구조를 맞추는 일은 별건**이다.
- 번역본 `philosophy`의 첫 CTA는 한국어(네이버 예약)와 달리 `director.html`로 간다. 라벨과 링크가 서로 맞으므로 의도된 현지화로 보고 두었다. 다만 **번역본 `philosophy`에는 예약 동선이 없다.**
- 중국어 메인 히어로의 브랜드 감성 카피에는 `你`를 그대로 두었다. 나머지 페이지는 전부 `您`다.

### 검증

- 회귀 검사 6종 통과, `git diff --check` 오류 없음, 한글 잔존 0건, 태그 균형 0건(`h2`·`h3`·`a` 포함).
- 20개 파일 50줄 변경이며 전 줄이 바뀐 파일은 없다.
- 로컬 서버에서 일본어 `columns`·`index`를 렌더 확인했다. 칼럼 라벨과 분류 칩, 공진단 문단이 모두 정상이다.

### 다국어 점검 상태 정리

- **17페이지 × 3언어 = 51개 파일을 모두 한국어 원문과 1회 대조 완료.** 이후 한국어 카피를 고칠 때는 해당 언어판도 같이 보거나, 최소한 `HANDOFF.md`에 어느 페이지가 어긋났는지 남긴다.
- 원어민 검수는 여전히 받지 않았다. 이번 두 차례 수정 모두 한국어 원문 대조 기준이며, 원문에 없는 문장은 만들지 않았다.
## 2026-09-17 다국어 오역 전수 점검과 수정

- 배경: 예전에 푸터 진료시간의 `목요일`이 영어에서 `Neck`으로 나갔던 것과 같은 유형의 오역이 남아 있는지 확인해 달라는 요청이었다. 원어민 검수를 받을 수 없는 상황이므로 원문 대조로 전수 점검했다.
- 51개 다국어 파일에서 본문·`alt`·`aria-label`·메타를 추출해 한국어 원문과 대조했다.

### 과거 오역은 재발하지 않았다

- 작업 폴더에 남아 있던 `다국어-오역-일괄수정.patch`(2026-07-27)의 수정 대상 15종(`共振団`·`贡金丹`·`Oriental Medicine`·`CEO`·`園内湯田`·비디어의 `视频`·`ビデオ` 등)을 전부 재검색해 **0건**을 확인했다. 패치 내용은 이미 반영돼 있었고 파일만 남아 있던 것이다.
- **품질이 두 층으로 갈린다.** 2026-09-17에 만든 `be-deer`·`be-deer-cases`·`dear-gongjindan`·`international-appointment`는 고유 문자열 전수 매핑 방식이라 깨끗하다. 문제는 **2026-07-27에 만든 기본 12페이지**이며, 이번에 고친 것은 대부분 여기서 나왔다.

### 뜻이 틀렸던 것 (중국어)

- `services`: `맞춤 다이어트`가 `定制饮食`(맞춤 **식단**)이 됐다. BE DEER가 진료 프로그램이 아니라 식단 상품으로 읽혔다. **목→neck과 같은 유형이다.**
- `services`: `목적에 맞는 방향`이 `适合我们目标的方向`(**우리**의 목표)으로 뒤집혔고 앞 절의 주어가 끊겨 비문이었다.
- `services`: `약재 선별부터 관리하는 공진단`이 `拱辰丹直接管理药材筛选`(공진단이 약재 선별을 관리한다)으로 주어·목적어가 뒤집혔다.
- `services`: 본문 중간에 `看看你的`(반말 명령)가 끼어들어 문장이 끊겼고, 같은 문단에서 `你`와 `您`가 섞였다.
- `care`: `DEAR一起关注什么`는 `一起`가 짝 없이 떠 있는 비문이며 한 페이지에 5번 반복됐다.
- `director`: **세 개의 병렬 문장 주어가 `您`·`你`·`我`로 제각각이었다.** 원문은 모두 원장 자신인데, 중국어에서는 방문자가 환자와 눈을 마주치지 않고 타이핑에 바빴던 것처럼 읽혔다.
- `director`: `喜欢的意思是珍贵，DEAR`는 뜻이 성립하지 않는 문장이었다.

### 뜻이 틀렸던 것 (일본어)

- `director`: `누구보다 소중한 당신이기를 바랍니다`가 `あなたが欲しい`(당신을 원한다)가 돼 있었다. 대표원장 소개 페이지의 첫 문장이다.
- `director`: `견문을 넓히고`가 `見学を広げて`(견학)로, `병원을 찾는 일`이 `病院を探すこと`(검색)으로 옮겨졌다. 둘 다 동음·다의 오독이다.
- `director`: `눈을 마주치다`→`目に直面する`, `마음 한켠`→`心のひとつ`, 인용문 `向き合わなかったらと思いました`는 문장이 되지 않았다.
- `care`: `증상의 양상`이 `症状の面`이 됐다(중국어도 `症状方面`으로 같은 오류). `수험생·직장인`(사람)이 `受験勉強・仕事`(행동)으로 바뀌었다.
- `services`: `함께 살피는 케어`가 `生かすケア`(살리는)로 옮겨졌다. `살피는`을 `살리는`으로 읽은 것이다.

### 조각난 기계번역 (영어)

- `services`의 FIND YOUR CARE 네 항목이 `weight and appetite,<br>If you want to…`처럼 소문자로 시작하고 문장 중간에 대문자 `If`가 들어간 상태였다.
- 하단 CTA 문단은 네 조각이 이어지지 않은 채였고, 환자 1인칭과 병원 화자가 섞여 있었다.
- `care`의 Calm 카드에서만 `불균형`이 빠져 있었다(일본어·중국어와 본문에는 있었다).

### 표기 통일

- 원장 성함: 비법률 페이지는 `Minji Kim`, 일본어 `キム・ミンジ`, 중국어 `金敏智`로 맞췄다. **법률 페이지(`privacy`·`terms`·`privacy-20260903`)의 `Kim Minji`는 한국어 NAP의 대표자 표기와 맞춘 법적 식별 표기이므로 세 언어 모두 그대로 둔다.**
- 경력 직함: `한방과장` = `Head of ... Department`, `진료원장` = `Clinical Director`로 각각 하나씩만 쓴다. 이전에는 같은 직함이 두 가지로 번역돼 있었다.
- `터한의원`이 일본어·중국어에 한글 그대로 남아 읽을 수 없었다. `テオ韓医院`·`Teo韩医院`으로 바꿨다.
- 구분자: 한국어 원문은 전부 `·`인데 영어·중국어가 절반만 쉼표로 바뀌어 있었다. 각 언어 안에서 하나로 통일했다.
- 중국어 `자율신경`이 한 페이지에서 `自主神经`·`植物神经` 두 가지였다. `自主神经`으로 통일했다.
- 퀵메뉴: 일본어 `電話連絡`은 같은 요소의 `aria-label`(`電話問い合わせ`)과도 어긋나 `電話問い合わせ`로, 중국어 `电话查询`(조회)은 `电话咨询`으로 바꿨다. 중국어 `Naver 预订`은 다른 곳에서 쓰는 `预约`으로 맞췄다.
- 일본어 `services`의 `<title>`에 공백이 두 칸 있었다.
- 메인 예약 카드 제목이 세 언어 모두 `온라인예약`인데 본문은 일정 확인 후 전화 안내였다. `Appointments`·`ご予約`·`预约`으로 바꿨다. `international-appointment`의 영어 `READY TO CALL?`도 본문이 DM 우선으로 바뀐 뒤 남아 있어 `READY TO BOOK?`으로 고쳤다.

### 작업 중 저지른 오판 — 반드시 기억할 것

- 한국어 `services.html`의 `CLINICAL CHOICE` 섹션이 **번역본 세 언어에 통째로 없다**고 판단해 정적 HTML로 넣었다. **틀렸다.** `js/main.js`가 `services-principle` 섹션을 `DEAR PRINCIPLE`이라는 다른 제목과 언어별 문구(`principleTitle`·`principleBody`·`principleSteps`)로 **런타임에 주입**하고 있었다. 브라우저로 확인하고서야 중복이 드러나 전부 되돌렸다.
- **디숭이 위젯·언어 전환 링크 때와 똑같은 유형의 오판이다.** HANDOFF에 이미 두 번 기록돼 있는데 또 반복했다. 기능·섹션의 유무는 HTML 문자열 검색으로 판단하지 말고 `js/main.js`의 주입 코드와 실제 렌더 결과를 함께 본다.
- `js/main.js`가 다국어 페이지에 주입하는 것: services 히어로 CTA 카드(`serviceCtas`), `services-principle` 섹션, 챕터별 `View details` 링크, care 히어로 시퀀스, director 하단 칼럼 섹션(`directorArticles`). **다국어 카피를 고칠 때는 이 표도 함께 본다.**
- 줄바꿈 검사를 `git show HEAD:` 결과와 비교했는데, `core.autocrlf=true`라 인덱스는 항상 LF다. 워킹트리(CRLF)와 비교하면 전 파일이 손상된 것처럼 나온다. **EOL 손상 여부는 `git diff --numstat`으로 전 줄이 바뀌었는지 보는 편이 정확하다.**

### 캐시 버전

- `js/main.js`의 원장 표기를 고쳤으므로 `main.js?v=20260907-5` → `20260917-3`으로 101개 페이지에서 일괄 상향하고 `tools/test-seo-surfaces.mjs`의 `sharedMainVersion`도 함께 갱신했다.
- `preview/` 두 파일은 원래 줄바꿈이 혼합(LF/CRLF)이다. 편집 시 강제로 CRLF를 넣지 말고 각 파일의 기존 줄바꿈을 유지한다.

### 검증

- 회귀 검사 6종 통과, `git diff --check` 오류 없음, 한글 잔존 0건(푸터 NAP·`터한의원` 제외), 태그 균형 0건.
- 104개 파일 233줄 변경이며 전 줄이 바뀐 파일은 없다(EOL 손상 없음).
- 로컬 서버(`.claude/launch.json`의 `dear-static`, 4173포트)에서 영어·일본어·중국어 화면과 375px 모바일을 확인했다. `services-principle` 중복이 사라졌고 수정한 문구가 모두 반영됐다.

### 정리한 잔여 파일

- `다국어-오역-일괄수정.patch`(2026-07-27)와 `assets/images/dear-favicon.ico`를 작업 폴더에서 치웠다. 패치는 이미 반영돼 적용도 되지 않는 상태였고, `.ico`는 어느 페이지도 참조하지 않는다(전 페이지가 `dear-favicon.png`를 쓴다). 둘 다 Git에 추적되지 않던 파일이다.

### 남은 작업

- **`about`·`philosophy`·`columns`·법률 5종의 일본어·중국어판은 아직 같은 수준으로 대조하지 않았다.** 이번에 `services`·`care`·`director`에서 나온 유형(주어 붕괴, 조각난 문장, `你`/`您` 혼용)이 남아 있을 가능성이 높다. 2026-07-27에 함께 만들어진 파일들이다.
- 번역문은 원어민 검수를 거치지 않았다. 이번 수정도 한국어 원문 대조 기준이며, 원문에 없는 문장은 새로 만들지 않았다.
- 중국어 메인 히어로의 브랜드 감성 카피에는 `你`를 그대로 두었다. 존대로 바꿀지는 판단이 필요하다.
## 2026-09-17 BE DEER 사례 페이지 영어·일본어·중국어판 발행

- `be-deer-cases.html`(체성분 기록 17건)과 `be-deer-case.html`(사례 상세, noindex)의 세 언어판을 만들었다. 이로써 다국어 BE DEER 페이지의 `다이어트 진료 사례 더 보기` CTA가 같은 언어의 사례 목록으로 이어진다. 이전에는 외국어판에서 이 버튼을 누르면 한국어 페이지로 떨어졌다.

### 사례 페이지는 HTML만 복제해서는 번역되지 않는다

- **카드 내용이 HTML이 아니라 자바스크립트에서 생성된다.** `js/be-deer-cases.js`가 17건의 측정값 배열을 갖고 카드 제목·지표 라벨·상세 링크를 만들고, `js/be-deer-case.js`가 상세 화면의 제목과 `document.title`을 만든다. HTML만 복제하면 외국어 페이지에서 카드가 한국어로 렌더링된다.
- 두 스크립트를 **언어 대응으로 고쳤다.** `js/main.js`의 `getDearPageLocale()`과 같은 방식으로 경로(`/en/`·`/ja/`·`/zh-cn/`)를 먼저 보고, 없으면 `documentElement.lang`으로 판별한다. 경로를 우선하는 이유도 동일하다. 브라우저 번역 확장이 `lang`을 바꿔도 흔들리지 않게 하기 위함이다.
- 측정 수치 배열과 카드 구조는 언어와 무관하게 하나만 유지한다. 언어별로 바뀌는 것은 연령대 표기, 성별, 지표 라벨(체중·체지방량·내장지방레벨), 단위 `레벨`, 상세 링크 문구뿐이다.
- `be-deer-case.js`는 사례별 제목을 한국어 문자열 배열로 갖고 있었는데, 연령대와 성별 코드 배열(`[30, "f"]`)로 바꾸고 문구는 언어 테이블에서 조립하도록 했다. 9번 사례가 남성으로 정확히 표시되는 것을 확인했다.

### 반복 문구는 규칙으로 생성한다

- 사례 페이지의 한글 문자열은 108개인데, 그중 68개가 `개인정보를 가린 NN번 사례 치료 시작/경과 확인 체성분 측정 결과지`와 `NN번 사례 ... 결과지 크게 보기` 형태의 반복이다. 17건 × 2시점 × 2종이다. 이런 패턴은 사전에 일일이 적지 않고 **번호와 시점을 조합해 생성**했다. 사례가 늘어도 같은 방식으로 대응된다.

### 배선

- `be-deer-cases.html`은 색인 대상이므로 네 언어 모두 hreflang 5개를 넣고 sitemap에 세 URL을 추가해 **91 URL**이 됐다.
- `be-deer-case.html`은 `noindex, nofollow`이므로 **hreflang을 넣지 않고 sitemap에도 올리지 않는다.** 생성기가 `robots` 메타를 보고 자동으로 판단한다. noindex 페이지에 hreflang을 다는 것은 이전에 `privacy-20260903`에서 정리한 것과 같은 종류의 죽은 배선이다.
- 다국어 `be-deer.html`의 사례 링크를 `../be-deer-cases.html`에서 같은 언어 폴더의 `be-deer-cases.html`로 바꿨다.
- `js/main.js`의 `localizedPages`에 `be-deer-cases.html`을 등록해 상단 언어 전환이 같은 페이지의 번역본으로 이동한다. 상세 페이지는 noindex이고 쿼리 파라미터로 동작하므로 등록하지 않는다.
- `be-deer-cases.js`·`be-deer-case.js` 캐시 버전을 `20260917-1`로 올렸다.

### 검증

- 여섯 페이지 모두 한글 잔존 0건(푸터 NAP 제외), 링크 깨짐 0건, hreflang 양방향 오류 0건.
- 브라우저에서 네 언어의 카드 렌더링을 확인했다. 카드 17건, 제목·지표 라벨·상세 버튼이 언어별로 올바르며 한국어 페이지의 기존 표시는 그대로다. 가로 넘침 0건.
- 회귀 검사 6종 통과, `git diff --check` 오류 없음.
- **주의:** Write 도구로 JS를 새로 쓰면 LF로 저장된다. 저장소는 CRLF이므로 작성 후 되돌려야 한다. 이번에도 Git 경고로 확인해 복구했다.

### 남은 작업

- `be-deer-case.html`은 네 언어 모두 여전히 "상세 진료 칼럼 연결 준비 중" 안내 화면이다. 사례별 원고가 준비되면 목록 링크를 바꾸지 않고 이 화면에 이어진다.
- 번역 칼럼은 여전히 0편이며 `en/columns.html`은 한국어 칼럼 1편만 링크한다.

## 2026-09-17 BE DEER 페이지 영어·일본어·중국어판 발행

- `en/be-deer.html`, `ja/be-deer.html`, `zh-cn/be-deer.html`을 새로 만들었다. 한국어 원본의 레이아웃·사진·모션·전용 CSS와 JS를 그대로 쓰고 본문만 번역했다. 본문 분량은 공진단의 약 2.7배인 12,372자다.
- 번역 대상은 본문 텍스트 노드와 `alt`·`aria-label`·`content` 속성에서 추출한 **고유 한글 문자열 211개**다. 세 언어 사전이 모두 211개로 완전히 일치하며 빈 번역과 한글 잔존은 0건이다.
- 푸터의 한국어 NAP(상호·대표자·사업자등록번호·주소)는 기존 관례대로 원문을 유지한다. 구조화 데이터의 `areaServed`(서초동·강남역·교대역·서울 서초구)도 다른 페이지와 같은 기준으로 한국어 표기를 남겼다.
- 번역본이 없는 링크는 한국어 루트로 보낸다. `be-deer-cases.html`, `medical-information-policy.html`, 칼럼 6편이 해당한다. `about`·`care`·`career`·`columns`·`director`·`services`와 법률 4종은 각 언어 폴더 안의 번역본으로 연결된다.
- 배선: 네 언어 모두 hreflang 5개, sitemap에 세 URL 추가해 **88 URL**, `js/main.js`의 `localizedPages`에 `be-deer.html` 등록, 푸터 언어 링크는 자기 언어를 뺀 나머지 셋.

### 번역 생성에서 확인한 함정

- **짧은 낱말을 사전에 단독으로 넣으면 긴 문장 안에서 터진다.** 치환은 문자열 길이 내림차순으로 하되, 짧은 키가 존재하려면 그것을 포함하는 모든 긴 문장이 사전에 함께 있어야 한다. 그래서 추측으로 사전을 쓰지 않고 **원본에서 고유 한글 문자열을 전부 추출한 뒤 전수 매핑**했다.
- **JSON-LD 안의 한글도 같은 방식으로 깨진다.** 실제로 `serviceType`이 `"Weight과 Daily rhythm을 함께 살피는 한의 비만 진료"`로 망가졌다. 스키마 안 문자열 27개를 따로 추출해 통째로 사전에 넣어 해결했다. **생성 후 검사에서 `<script>`를 제외하면 이 손상을 놓친다.**
- **마크업 형태를 추측하면 안 된다.** BE DEER의 h1은 `<em>`이 아니라 `<span class="hero__highlight">`를 쓰고, 매니페스토는 `<br>`이 아니라 `<p>` 네 개로 나뉘어 있다. 조각난 채 번역하면 어색해지므로 h1과 중간 CTA는 **HTML을 포함한 통 문자열로 매핑**하고, 매니페스토는 네 줄에 자연스럽게 의미를 분배했다.
- 원본을 복제해 가공하므로, hreflang 삽입 전에 기존 태그를 제거하는 단계가 반드시 필요하다.

### 반응형에서 찾은 실제 결함

- **일본어판에서 `.observe__grid`가 뷰포트를 넘쳤다.** 원인은 `grid-template-columns: 1fr 1fr`이다. `1fr`은 `minmax(auto, 1fr)`이라 최소 콘텐츠 폭에 밀리는데, 띄어쓰기가 없는 일본어에서 컬럼이 `428px + 479px`로 벌어졌다(한국어는 `422px 422px`). `minmax(0, 1fr)`로 고쳐 세 언어 모두 균등해졌다. `.observe-card__question`과 `.observe-card__body`에 `overflow-wrap: anywhere`도 함께 넣었다. **CJK 다국어판을 만들 때 `1fr` 그리드는 항상 `minmax(0, 1fr)`로 쓴다.**
- `be-deer.css` 캐시 버전을 `20260917-1`로 올렸다.
- `hero__orbit`은 `getBoundingClientRect`로는 뷰포트를 넘지만 `.hero__visual`과 `body`의 `overflow-x: hidden`이 잘라내므로 실제 가로 스크롤은 발생하지 않는다. **점검할 때는 요소 위치만 보지 말고 `window.scrollTo(9999,0)` 후 `scrollX`가 0인지로 실제 스크롤 여부를 확인한다.**

### 검증

- 세 언어 모두 한글 잔존 0건(푸터 NAP 제외), 링크·자산 43개 전부 정상, hreflang 각 5개, H1 1개, 중간 CTA 1개, FAQPage 6문항, JSON-LD 파싱 오류 0건.
- 390·768·885·1440에서 실제 가로 스크롤 0건. 회귀 검사 6종 통과, `git diff --check` 오류 없음.
- 번역문은 이번 작업에서 새로 작성한 것이며 원어민 검수를 거치지 않았다. 김민지 대표원장이 영어·일본어·중국어 모두 가능하므로 확인을 받으면 확정 카피로 승격한다.

### 남은 작업

- `be-deer-cases.html`과 `be-deer-case.html`의 다국어판은 없다. 다국어판에서 사례 링크는 한국어 페이지로 이동한다.  **→ 2026-09-17에 해소됐다. `en`·`ja`·`zh-cn` 세 언어판이 모두 있고 hreflang도 들어갔다. 남은 공백으로 읽지 않는다.**
- 번역 칼럼은 여전히 0편이며 `en/columns.html`은 한국어 칼럼 1편만 링크한다.

## 2026-09-17 비디어·공진단 반응형·CTA·구조화 데이터 점검

### 반응형 실측 — 오류 없음

- `be-deer.html`과 `dear-gongjindan.html`을 390·768·1440에서 실측했다. **두 페이지 모두 가로 넘침 0건**이다.
- 뷰포트를 넘어가는 요소로 잡히는 것들은 모두 의도된 구성이다. `be-deer`의 `.review-card`는 `overflow-x: auto`인 `.review-rail` 안의 가로 캐러셀이고, `.hero__orbit`은 `overflow: hidden`인 `section.voices` 안의 장식이다. 공진단의 `.gjd-chapters`도 가로 스크롤 목차다. **점검 스크립트는 이 컨테이너들을 제외해야 한다.** 제외하지 않으면 be-deer 390px에서 28건이 오류로 잡힌다.
- 닫힌 햄버거 메뉴(`.nav__menu`)는 화면 밖에 배치되므로 항상 오탐으로 잡힌다. `.nav`, `.quickmenu`, `.dear-ai`도 함께 제외한다.

### CTA — 실제 문제였고 고쳤다

- 두 페이지 모두 예약 CTA가 페이지 끝에만 있었다. 모바일 390px 기준 `be-deer`는 전체 16,456px 중 **93% 지점**, 공진단은 **90% 지점**에서야 예약 버튼이 처음 나타난다. 플로팅 퀵메뉴에 상시 예약이 있긴 하지만 본문 동선에는 중간 전환 지점이 없었다.
- `css/style.css`에 공용 `.midcta` 띠를 만들어 본문 중간에 넣었다. `be-deer`는 진료 흐름(`process`) 뒤 **47% 지점**, 공진단은 진료 판단(`gjd-clinical`) 뒤 **63% 지점**이다.
- 확정 팔레트의 `--green-deep` 배경에 흰색 알약형 버튼(172×56)과 전화 링크로 구성했다. 640px 이하에서 세로로 쌓이며 버튼이 가로 전체를 채운다. 네이버 예약과 전화에 `data-track-action`·`data-track-location`을 붙여 GA4 전환을 구분한다.
- 문구는 `be-deer` "지금의 체중이 아니라 지금의 몸부터 확인해 보세요.", 공진단 "공진단이 지금 필요한지부터 진찰로 확인해 드립니다."이며 세 언어로 번역했다.

### AEO — FAQPage 두 건 추가

- 공진단에는 화면에 FAQ 세 개가 이미 있었는데 구조화 데이터가 없었다. 화면 문구와 일치하는지 검사한 뒤 `FAQPage`를 생성했다.
- `be-deer`의 `함께 살펴보는 여섯 가지` 섹션은 **실제로 질문과 답변 구조**다. "왜 저녁만 되면 유독 식욕이 왕성해질까요?"처럼 여섯 개의 질문과 설명이 짝을 이룬다. 이를 `FAQPage` 문항 6개로 만들었다. 본문에서 직접 추출하고 물음표로 끝나는지 검사한 뒤 생성하므로 화면과 어긋날 수 없다.
- 현재 스키마 구성은 `be-deer`가 MedicalWebPage·Service·BreadcrumbList·FAQPage, 공진단이 MedicalWebPage·FAQPage다.

### 작업 중 만든 버그와 수정

- **언어판 hreflang이 10개로 중복됐다.** 한국어 원본에 hreflang 5개를 추가한 뒤 번역 생성기를 다시 돌리자, 원본에서 복사된 5개 위에 생성기가 5개를 더 넣었다. 생성기가 삽입 전에 기존 `rel="alternate" hreflang` 태그를 제거하도록 고쳤다. **원본을 복제해 가공하는 생성기는 삽입형 로직을 넣을 때 항상 기존 것을 먼저 제거해야 한다.**
- `en/dear-gongjindan.html`의 description이 170자로 길어 158자로 줄였다.
- 공통 CSS 캐시 버전을 `20260917-2`로 올리고 `tools/test-seo-surfaces.mjs`의 고정값도 함께 갱신했다.

### 검증

- 390·768·1440 전부 가로 넘침 0건, `.midcta`가 세 폭 모두 뷰포트 안에 들어옴.
- hreflang 양방향 오류 0건, 언어판 hreflang 각 5개, JSON-LD 파싱 오류 0건.
- 회귀 검사 6종 통과, `git diff --check` 오류 없음.

### 남은 작업

- **BE DEER 다국어판은 아직 없다.** 본문 12,372자로 공진단의 약 2.7배다. 번역 맵 방식은 공진단에서 검증됐으므로 같은 절차로 제작 가능하다.  **→ 2026-09-17에 해소됐다. `en`·`ja`·`zh-cn` 세 언어판이 모두 있고 hreflang도 들어갔다. 남은 공백으로 읽지 않는다.**
- `be-deer.html`에는 hreflang이 없다. 다국어판을 만들 때 네 언어 모두에 넣는다.

## 2026-09-17 공진단 페이지 영어·일본어·중국어판 발행

- 배경: 외국인 환자 유입 과제에서 주력 페이지의 다국어판이 없다는 점이 가장 큰 공백으로 확인됐다. 그중 `dear-gongjindan.html`을 먼저 세 언어로 제작했다.
- 새 파일은 `en/dear-gongjindan.html`, `ja/dear-gongjindan.html`, `zh-cn/dear-gongjindan.html`이다. 한국어 원본의 레이아웃·사진·모션·전용 CSS와 JS를 그대로 쓰고 본문만 번역했다. 새 디자인이나 새 섹션을 만들지 않았다.
- 확정 표기를 유지했다. 공진단은 일본어·중국어 모두 `拱辰丹`, 한의원은 `DEAR韓医院`·`DEAR韩医院`, 대표원장은 `キム・ミンジ`·`金敏智`다. 약재는 麝香·鹿茸·当帰(当归)·山茱萸로 옮겼다.
- 푸터의 한국어 NAP(상호·대표자·사업자등록번호·주소)는 기존 다국어 관례대로 한국어 원문을 유지한다. 번역 대상에서 의도적으로 제외했다.
- 배선을 함께 연결했다. 네 언어 모두에 hreflang 5개(ko·en·ja·zh-Hans·x-default)를 넣었고, 한국어 원본에도 새로 추가했다. sitemap에 세 URL을 `lastmod`와 함께 등록해 총 85개가 됐다. `js/main.js`의 `localizedPages`에 `dear-gongjindan.html`을 추가해 상단 언어 전환이 같은 페이지의 번역본으로 이동한다. 푸터 언어 링크는 각 페이지에서 자기 언어를 뺀 나머지 셋을 건다.

### 제작 방식과 재사용 가능한 도구

- 번역은 한국어 원문 문자열을 정확히 매칭해 치환하는 방식으로 했다. 생성 스크립트는 `scratchpad`에 두었고 저장소에 커밋하지 않았다.
- **치환 순서는 문자열 길이 내림차순이어야 한다.** 짧은 조각을 먼저 치환하면 긴 문장 안에서 잘못 터진다. 실제로 `멀리서도`를 단독 항목으로 두었다가 `이는 곧 멀리서도 디어한의원을 찾는...` 문장이 `이는 곧 Why people come to DEAR 디어한의원을...`로 깨졌다. **긴 문장을 통째로 매칭하고, 짧은 낱말 폴백 항목은 두지 않는다.**
- 헤딩 마크업은 파일마다 다르다. `<br>`만 쓰는 곳, `<span>`으로 감싼 곳, `<em>`이 들어간 곳이 섞여 있어 추정하지 말고 원문을 직접 확인해야 한다.
- 검수는 생성물에서 한글 토큰이 남았는지 세는 방식으로 했다. 최종적으로 세 파일 모두 `한국어`(푸터 언어 링크 라벨) 하나만 남았다.

### 검증

- 한글 잔존 토큰 각 1개(의도된 푸터 라벨), hreflang 양방향 오류 0건, 세 페이지의 링크·자산 93개 전부 정상 연결, sitemap 85 URL.
- 회귀 검사 6종 통과, `git diff --check` 오류 없음.
- 로컬 서버에서 세 언어 화면을 900x1300으로 확인했다. 히어로 사진 시퀀스, 상단 언어 전환, 퀵메뉴, 디숭이 위젯이 모두 정상 동작한다.

### 남은 작업

- **BE DEER 다국어판은 아직 없다.** 본문이 12,372자로 공진단의 약 2.7배이며, 체성분 전후 수치와 네이버 영수증 리뷰 섹션이 포함된다. 같은 방식으로 제작 가능하다.  **→ 2026-09-17에 해소됐다. `en`·`ja`·`zh-cn` 세 언어판이 모두 있고 hreflang도 들어갔다. 남은 공백으로 읽지 않는다.**
- 번역문은 기계번역이 아니라 이번 작업에서 새로 작성한 것이지만 원어민 검수를 거치지 않았다. 김민지 대표원장이 영어·일본어·중국어 모두 가능하므로 확인을 받으면 확정 카피로 승격한다.
- `en/columns.html`이 여전히 한국어 칼럼 1편만 링크한다. 번역 칼럼은 0편이다.

## 2026-09-17 다국어 검색 배선 점검과 정리

- 배경: 다국어 페이지의 구글 상위노출을 목표로 sitemap·hreflang·메타데이터·내부 링크를 전수 점검했다.

### 점검 결과 정상으로 확인한 항목

- hreflang 양방향 상호연결: 색인 대상 전 페이지 정상. 사이트는 `https://dearhani.com/en/` 같은 **디렉터리 형식**을 sitemap·canonical·hreflang에 일관되게 쓴다. 점검 스크립트가 `index.html` 형식으로 비교하면 전부 오류로 잡히므로 정규화가 필요하다.
- title·description: 누락 0건. **다국어 페이지는 `<meta content="..." name="description">`처럼 속성이 알파벳순으로 정렬돼 있다.** `name="description"`이 앞에 온다고 가정한 정규식으로 검사하면 24건이 누락으로 잘못 잡힌다.
- og:locale(`en_US`·`ja_JP`·`zh_CN`), og:url, canonical 형식, sitemap 유령 URL 0건, 색인 대상 고아 페이지 0건.

### 다시 확인한 잘못된 판단 — 언어 전환은 이미 있었다

- 2026-09-17 앞선 기록에서 "한국어 화면에 언어 전환 링크가 없다"고 적었으나 **사실이 아니다.** `js/main.js`가 상단 메뉴에 `KO / EN / 日本語 / 中文` 전환을 주입하며, `localizedPages` 목록에 있는 12개 페이지에서는 같은 페이지의 번역본(`/ja/privacy.html`)으로 정확히 연결한다. HTML만 검색하면 보이지 않는다. **디숭이 위젯 때와 같은 유형의 오판이므로, 기능 유무는 HTML 문자열이 아니라 공통 스크립트까지 확인한다.**
- 다만 이 링크는 자바스크립트 실행 후에만 생긴다. 구글은 JS를 렌더링하지만, GPTBot·ClaudeBot·PerplexityBot 같은 생성형 AI 크롤러는 대체로 JS를 실행하지 않는다. 생성형 검색 노출이 현재 과제이므로 **정적 HTML 링크를 함께 두는 것이 의미가 있다**고 판단해 푸터 링크를 추가했다.

### 수정 내용

- 다국어 URL 24개에 sitemap `lastmod`를 추가했다. 이전에는 36개 중 법률 페이지 12개에만 있었다. 날짜는 각 파일의 마지막 커밋일을 쓴다. 전체 82개 중 76개가 `lastmod`를 갖는다.
- 보관용 `privacy-20260903.html` 4개에서 hreflang 태그를 제거했다. `noindex, follow`인 페이지가 살아 있는 개인정보처리방침을 가리키고 역방향이 없어 상호연결이 성립하지 않던 죽은 배선이다. `noindex`는 그대로 유지한다.
- 한국어 45개 페이지(루트 16개 + 칼럼 29개) 푸터에 `.footer__lang-links`를 추가했다. **상단 JS 전환과 같은 규칙으로 페이지별 번역본에 연결한다.** `localizedPages`에 해당하면 `/en/<파일>`, 아니면 `/en/`이다. `js/main.js`의 목록과 어긋나지 않게 유지한다. `404.html`과 `international-appointment.html`은 공통 푸터가 없어 제외했고, `be-deer-case.html`과 `privacy-20260903.html`은 noindex라 제외했다.
- `css/style.css`에 `.footer__lang-links` 스타일을 추가하고, 공통 CSS 캐시 버전을 `20260904-1`에서 `20260917-1`로 87개 페이지에서 일괄 상향했다.
- `tools/test-seo-surfaces.mjs`의 `sharedCssVersion` 고정값도 함께 갱신했다. **이 검사는 전 페이지가 같은 CSS 버전을 쓰는지 확인하므로, 공통 CSS를 고칠 때는 페이지의 캐시 버전과 이 상수를 항상 같이 올린다.**
- `en/columns.html`(193자), `en/services.html`(174자)의 description을 각각 143자·152자로 줄였다. 구글 검색결과에서 잘리지 않도록 한 것이며 고유명사와 의미는 유지했다. 같은 문장을 쓰던 `og:description`도 함께 바뀐다.

### 검증

- hreflang 양방향 오류 0건, description 160자 초과 0건, 푸터 언어 링크 135개 전부 실제 파일로 연결, sitemap `<url>`/`</url>` 82쌍 일치, `lastmod` 형식 이상 0건.
- 회귀 검사 6종 통과, `git diff --check` 오류 없음, 편집 대상 파일의 CRLF 유지 확인.

### 남은 판단

- sitemap에 `xhtml:link` hreflang 주석은 없다. HTML `<link rel="alternate">`가 전 페이지에 정상이므로 필수는 아니며 이번에 추가하지 않았다.
- description 70자 미만이 49건이다. 대부분 법률·안내 페이지이고 내용상 짧은 것이 자연스러워 임의로 늘리지 않았다.
- **`sed -i`를 CRLF 파일에 쓰면 파일 전체가 LF로 바뀐다.** 이번 작업 전 단계에서 실제로 발생했다. HTML·CSS 편집은 Node로 읽고 쓰거나, 사용한 뒤 줄바꿈을 되돌린다.

## 2026-09-17 외국인 환자 진료 언어 안내와 연락 창구 정비

- 배경: ChatGPT 임시 채팅 실측에서 디어한의원이 영어 다이어트 질의에 4위로 언급되고도 최종 추천에서 제외됐다. 제외 사유로 모델이 명시한 것은 "영어 사이트는 쓸 만하지만 실제 진료를 영어로 받을 수 있다는 문장을 찾지 못했다"였다. 1위로 추천된 경희유안은 "원장이 직접 영어·일본어로 진료하며 통역에만 의존하지 않는다"를 명시하고 있었다. 번역 품질이 아니라 **진료 가능 언어를 문장으로 밝혔는지**가 갈랐다.

### 대표원장 확정 사항 (2026-09-17)

- **김민지 대표원장은 영어·일본어·중국어 모두 진료 대화가 가능하다.** 통역 동반이 필요하지 않다.
- **연락 창구 우선순위는 인스타그램 DM → 전화 → 네이버 톡톡·예약이다.** 전화는 접수 담당이 먼저 받을 확률이 높아 외국인에게는 DM이 더 확실하다는 판단이다.
- 네이버 톡톡·예약은 계정이 필요해 해외 방문자에게 맞지 않을 수 있으므로, 노출하되 "한국 앱 이용자용"이라는 단서를 함께 표시한다.
- **카카오톡 채널은 현재 존재하지 않는다.** 저장소 전체에 카카오 링크가 없어 이번 작업에서 넣지 않았다. 필요하면 채널 개설이 선행되어야 한다.

### 수정 내용

- `international-appointment.html`의 H1을 `Call us to make an appointment.`(전화 한정)에서 진료 가능 언어를 밝히는 문장으로 교체했다. 영어 `Consultations in English, Japanese and Chinese.`, 일본어 `英語・日本語・中国語で診療します。`, 중국어 `可用英语、日语、中文诊疗。`다. 검색·생성형 AI가 찾지 못하던 정보가 페이지의 첫 문장이 된다.
- 인트로 본문도 전화 전용 안내에서 대표원장 직접 진료·통역 불필요·DM 우선 안내로 바꿨다.
- `HOW TO REACH US` 섹션을 새로 추가했다. 인스타그램 DM(@dearhani__) → 전화 → 네이버 톡톡·예약 순서이며, 전화 항목에는 접수가 먼저 받고 영어 대응이 제한될 수 있으니 DM이 더 확실하다는 사실을 그대로 적었다. 세 언어 모두 `data-copy` 블록으로 전환된다.
- **의료광고 기준:** 이 창구들은 예약과 일반 문의를 위한 것이며 진찰과 치료 방침 판단은 내원 시 이루어진다는 문장을 세 언어 모두에 넣었다. 메신저를 비대면 진료 상담 창구로 읽히게 하지 않기 위한 장치이며 이후 작업에서도 유지한다.
- 이메일 예약을 받지 않는다는 최종 CTA 문구가 전화만 가리키던 부분을 DM 또는 전화로 바로잡았다. 새 우선순위와 모순되지 않게 하기 위한 최소 수정이다.
- `css/international-appointment.css`에 `.ways` 계열 스타일을 추가했다. 기존 `--green`·`--deep`·`--line`·`--muted` 변수와 번호 매김 방식을 그대로 쓰며 600px 이하에서 들여쓰기를 해제한다.
- 구조화 데이터에 `availableLanguage`(한국어·영어·일본어·중국어)를 추가했다. 대상은 `international-appointment.html`과 한국어·영어·일본어·중국어 `index.html`이다. 진료 가능 언어를 사람이 읽는 문장과 기계가 읽는 데이터 양쪽에 두기 위한 조치다. `international-appointment.html`에는 `sameAs`와 `founder` 연결도 함께 넣었다.

### 작업 중 확인한 주의점

- 한국어 `index.html`의 JSON-LD는 들여쓰기가 있는 형태로 유지해야 한다. 파싱 후 `JSON.stringify`로 다시 쓰면 한 줄로 압축되면서 `test-medical-editorial-trust`가 기대하는 `"publishingPrinciples": "..."` 문자열(콜론 뒤 공백 포함)과 어긋나 검사가 실패한다. 실제로 이번에 한 번 실패했고, 파일을 되돌린 뒤 문자열 삽입 방식으로 다시 작업해 통과시켰다. **이후 한국어 메인의 구조화 데이터를 편집할 때는 재직렬화하지 말고 해당 줄만 삽입·수정한다.**
- 검증: JSON-LD 파싱 오류 0건, 회귀 검사 6종 통과, `git diff --check` 오류 없음. 로컬 서버에서 영어·일본어·중국어 화면과 375px 모바일을 확인했고 언어 전환, 새 섹션 번호 매김, 디숭이 위젯 동작을 함께 확인했다.

### 남은 작업

- BE DEER·공진단 다국어 페이지 제작이 여전히 가장 큰 공백이다. ChatGPT가 영어 답변에서 `BE DEER weight-care program`을 언급했음에도 그 페이지의 영어판이 없다.  **→ 2026-09-17에 해소됐다. `en`·`ja`·`zh-cn` 세 언어판이 모두 있고 hreflang도 들어갔다. 남은 공백으로 읽지 않는다.**
- 번역 칼럼 0편 상태도 그대로다. 전편 번역보다 BE DEER 다이어트 2~3편의 검수 번역이 현실적이다.
- 카카오톡 채널은 운영 계획이 없다. 2026-09-18 기록으로 대기 항목에서 내렸다.

## 2026-09-17 다국어 페이지 배선 점검과 링크·엔티티 보정

- 배경: 외국인 환자 유입을 목표로 다국어 페이지의 실제 기능을 전수 점검했다. ChatGPT 임시 채팅(메모리 미사용)으로 실측한 결과, 한국어 질의 `서초동 다이어트 한의원`에서는 디어한의원이 1순위로 인용되지만, 영어 질의 `Korean medicine clinic near Gangnam for foreign patients`에서는 전혀 나오지 않았고, `Korean herbal weight loss clinic`에서는 4위로 언급되나 최종 추천에서 제외됐다. 제외 사유로 ChatGPT가 명시한 것은 "영어 사이트는 쓸 만하지만 실제 진료를 영어로 받을 수 있다는 문장을 찾지 못했다"였다. 경쟁 병원은 Medical Korea·강남구 의료관광센터 등록과 진료 가능 언어 명시를 갖추고 있었다.

### 점검 과정에서 정정한 잘못된 판단

- **디숭이 위젯은 다국어 페이지에 이미 실려 있다.** HTML에서 `dear-ai` 문자열만 찾아 "다국어 0건"으로 판단했으나 이는 오류였다. `js/main.js` 최상단이 `/js/dear-ai-widget.js`를 절대 경로로 주입하고, 다국어 39페이지 전부가 `main.js`를 로드한다. `dear-ai-widget.js`는 `document.documentElement.lang`을 읽어 언어를 정하며 각 페이지의 `lang` 속성은 `en`·`ja`·`zh-CN`으로 올바르다. 라이브에서도 `/en/`의 `main.js` 로드와 위젯 파일 200 응답을 확인했다. **추가 작업이 필요 없다.**
- **다국어 페이지는 고아 상태가 아니다.** 한국어 화면에 언어 전환 링크가 없는 것은 사실이나, hreflang이 39페이지 전부 정상이고 sitemap에 36개 다국어 URL이 모두 있어 검색엔진과 AI 크롤러는 정상적으로 도달한다. 실제로 ChatGPT가 "DEAR has a full English website"라고 확인했다. 한국어 내비게이션에 언어 선택 메뉴를 넣는 방안은 한국인 사용자에게 불필요한 UI이므로 폐기했다.
- **다국어 푸터에 남은 한글 NAP는 번역 누락이 아니다.** 2026-08-03 기록대로 외국인 환자의 현지 길찾기와 법적 식별을 위해 유지하는 설계다. 구조화 데이터의 `PostalAddress`도 전 언어에서 한국어 표기를 쓰고 있어 같은 관례를 따른다.

### 이번에 수정한 것

- 다국어 법률 페이지가 자기 언어 번역본을 두고 한국어 페이지로 보내던 링크 15건을 고쳤다. `en`·`ja`·`zh-cn` 각각의 `non-covered`·`patient-rights`·`privacy`·`privacy-20260903`·`terms`에서 `../<파일>` 링크를 같은 언어 폴더 안의 파일로 바꿨다. 번역본이 없는 대상(`international-appointment.html`, `columns/seocho-diet-clinic.html`, 공통 자산)은 그대로 두었다.
- `en`·`ja`·`zh-cn`의 `index.html` `MedicalClinic`에 한국어판에만 있던 엔티티 연결을 이식했다. `founder`·`employee`(대표원장 Person `@id`), `areaServed`, `availableService` 9건, `publishingPrinciples`이며, 기존 필드는 값까지 그대로 유지됨을 이전 커밋과 대조해 확인했다.
- `founder`·`employee`·`availableService`는 한국어 페이지에 정의된 `@id`를 그대로 참조한다. `@id`는 언어 중립 식별자이므로 언어판마다 별도 엔티티를 만들지 않고 하나로 통합하는 쪽이 맞다. 다국어 `director.html`에는 별도 Person `@id`가 없어 엔티티 분리 문제도 없다.
- `knowsAbout`은 이식하지 않았다. 언어별로 새 의료 용어 번역이 필요하며 이는 확정 카피 작성에 해당한다. 검수된 용어가 준비되면 추가한다.
- 검증: 다국어 JSON-LD 6블록 파싱 오류 0건, 잘못된 언어 링크 재검사 0건, hreflang 자기참조·x-default·canonical 일치 전수 통과, 회귀 검사 6종 통과, `git diff --check` 오류 없음.

### 다국어에 남은 실제 결함

- **주력 페이지의 다국어판이 없다.** `be-deer.html`, `be-deer-cases.html`, `be-deer-case.html`, `dear-gongjindan.html`, `international-appointment.html`, `medical-information-policy.html`, `404.html`이 세 언어 모두 없다. ChatGPT가 영어 답변에서 `BE DEER weight-care program`을 언급했음에도 그 페이지의 영어판이 존재하지 않는다. 외국인 유입의 핵심 주제가 다이어트인 만큼 우선순위가 가장 높다.
- **번역된 칼럼이 0편이다.** 한국어에서 1순위 인용을 만든 근거가 칼럼 29편인데 영어·일본어·중국어에는 인용될 본문이 없다. 전편 번역이 아니라 BE DEER 다이어트 2~3편의 검수 번역부터 시작하는 편이 현실적이다.
- **`en/columns.html`이 한국어 칼럼 1편만 링크한다.** 영어 사용자가 Columns를 눌러도 한국어 글로 이동한다.
- **진료 가능 언어를 명시한 문장이 없다.** ChatGPT가 추천에서 제외한 직접적 사유다. 실제 영어 진료 가능 범위와 통역 운영 방식은 김민지 대표원장만 확정할 수 있으므로 임의로 작성하지 않는다. 사실과 다르게 적으면 내원한 외국인 환자와 분쟁이 생긴다.
- 브라우저 언어에 따른 자동 리다이렉트는 도입하지 않기로 했다. GitHub Pages는 정적 호스팅이라 `Accept-Language`를 처리할 수 없어 클라이언트 스크립트로만 가능하고, Googlebot이 주로 미국 IP·영어 설정으로 크롤링하므로 한국어 루트 색인이 손상될 위험이 있다. 현재 한국어 검색 성과가 사이트의 최대 자산이므로 이를 흔드는 선택은 하지 않는다. 필요하다면 강제 이동이 아닌 해제 가능한 안내 배너 방식을 쓴다.

## 2026-09-17 진료 서비스·Care 구조화 데이터 엔티티 추가

- 배경: AEO/GEO 관점에서 사이트를 점검한 결과, `index.html`의 `MedicalClinic` 엔티티(areaServed·knowsAbout·sameAs·publishingPrinciples·founder 연결)와 `be-deer.html`의 `Service` 엔티티는 잘 갖춰져 있었으나, 나머지 진료 서비스와 Care 다섯 영역에는 각각의 엔티티가 없었다. "디어한의원이 무엇을 진료하는가"가 `knowsAbout` 문자열에만 걸려 있어 지역+진료 질의에서 붙잡을 엔티티가 없는 상태였다.
- `services.html`에 `Service` 3개를 추가했다. 디어 공진단(`#dear-gongjindan`), 원내탕전 체질한약(`#herbal-decoction`), 디어밸런스(`#deer-balance`)이며 각각 `provider`로 `#clinic`, `areaServed`로 서울 서초구·서초동·강남역·교대역을 갖는다. BE DEER는 기존 `be-deer.html#service`를 재사용하고 새로 만들지 않았다. 페이지 자체는 `CollectionPage` + `ItemList`(4건)로 묶었다.
- `care.html`에 `Service` 5개를 추가했다. FOCUS·CALM·RESTORE·RELIEF·SHAPE이며 `@id`는 기존 패널 앵커(`#care-panel-<키>`)를 그대로 쓴다. 기존 `MedicalWebPage`와 `author`(대표원장) 연결은 유지하고 `ItemList`(5건)를 `mainEntity`로 걸었다.
- `index.html`의 `MedicalClinic`에 `availableService` 9건을 추가해 한의원 엔티티와 위 서비스들을 직접 연결했다.
- 모든 `description`은 각 페이지에 이미 확정된 한국어 카피를 그대로 옮겼다. 구조화 데이터를 위해 새로 쓴 문장은 없다.
- **의도적으로 제외한 항목과 이유:**
  - `medicalSpecialty`: schema.org의 `MedicalSpecialty` 열거값에 한의학에 해당하는 항목이 없다. 맞지 않는 값을 넣는 것이 비워두는 것보다 나쁘므로 넣지 않는다.
  - `priceRange`: 검색결과에 가격대가 노출될 수 있어 대표원장 판단 사항이다.
  - Care의 "관련 상태"를 `MedicalCondition`으로 연결하는 구성: 스키마상 해당 진료가 그 질환을 치료한다는 의미가 되어 효과를 단정하는 표현이 된다. 의료광고 기준에 따라 넣지 않는다. 이후 작업에서도 같은 기준을 유지한다.
- 변경은 세 파일의 `<head>` 안 JSON-LD 한 줄씩이며 화면에 보이는 요소는 바뀌지 않는다. 변경 줄 위치가 `<body>` 앞임을 확인했다(index 105/215, services 18/22, care 18/22).
- 검증: JSON-LD 4개 블록 파싱 오류 0건, 회귀 검사 6종(`test-seo-surfaces`, `test-columns-serp`, `test-medical-editorial-trust`, `test-column-publisher`, `test-dear-ai-columns`, `test-naver-tracking`) 통과, `git diff --check` 오류 없음.

### 확인했으나 손대지 않기로 한 것

- `columns/dear-column-20260729-1907.html`과 `columns/gangnam-depression-bdnf.html`의 `<title>`이 둘 다 `강남역 한의원 우울증 진료`로 겹친다. 다만 본문(13,753자 / 44,466자)·H1·description은 서로 다른 글이므로 중복 콘텐츠가 아니며 검색 불이익 사유가 아니다. 같은 질의에서 두 글이 경쟁해 신호가 갈리는 정도의 영향이라 급한 작업이 아니다. 고칠 때는 화면에 보이는 확정 카피이므로 승인을 받고 바꾼다.
- `dear-column-20260729-1907`은 발행기 임시 이름이 그대로 URL이 된 경우다. 그러나 이미 색인·RSS·사이트맵에 올라간 공개 URL이고 GitHub Pages 정적 배포에서는 301 리다이렉트를 걸 수 없어, 슬러그를 바꾸면 기존 색인과 유입이 끊긴다. **URL은 바꾸지 않는다.**

### 남은 판단 — 실제 노출 측정

- 현재 사이트가 검색·생성형 AI 답변에서 실제로 얼마나 노출되는지는 확인되지 않았다. 이번 작업에서 시도한 경로는 모두 막혔다: 내장 웹검색은 미국 인덱스 전용이라 한국어 로컬 질의 판단에 쓸 수 없고(브랜드명 검색에 무관한 의원이 나옴), Perplexity는 익명 검색을 막고 로그인을 요구하며, Google은 봇 감지 페이지를 띄웠고, Bing은 `site:` 결과가 깨져서 반환됐으며, Naver는 도구의 안전 제한으로 접근할 수 없다.
- 따라서 "검색에 안 뜬다"는 아직 근거가 확인되지 않은 추정이다. 구현 점검 결과만으로 노출 상태를 단정하지 않는다.
- 실측 방법은 세 가지다. ① 박성호(삥이)의 실제 크롬 세션으로 ChatGPT·Gemini·Perplexity에 직접 질의해 인용 여부를 기록한다. ② Google Search Console과 Naver Search Advisor의 실제 노출·클릭·순위를 확인한다(속성 소유 계정 필요). ③ 직접 질의한 결과를 전달받아 분석한다.
- robots.txt는 `User-agent: * / Allow: /`이며 GPTBot·ClaudeBot·PerplexityBot·Google-Extended를 포함해 차단하는 크롤러가 없다. 전 페이지가 정적 HTML 서버 렌더라 JS 실행 없이 본문을 읽을 수 있다. 크롤링 측 장애 요인은 없는 상태다.

## 2026-09-16 우울 칼럼 대표 이미지를 디숭이로 교체

- `columns/depression-no-hope.html`의 대표·본문 이미지를 디숭이로 교체했다. 회색 담요를 두르고 무릎을 끌어안은 채 웅크려 앉은 모습이며, 표정은 울고 있는 쪽이 아니라 소진돼 비어 있는 쪽으로 잡았다. 칼럼이 다루는 것이 슬픔이 아니라 희망이 보이지 않는 상태이기 때문이다.
- 이전 이미지는 진한 초록 이불이 화면을 덮고 뿔만 내놓은 구성이었다. 은유는 좋았으나 캐릭터가 보이지 않아 디숭이로 읽히지 않았고, 채도 높은 초록이 무채색 배경 기준과도 어긋났다.
- 새 파일은 `assets/images/columns/depression-no-hope/thumbnail-disoongi.webp`(1254×1254, 71,780바이트)이며 생성 원본 PNG도 같은 폴더에 보관했다. 기존 `thumbnail-deer-v2` 파일들은 삭제하지 않았다. 본문 상단 hero인 `cover-v2.webp`는 다른 그림이므로 건드리지 않았다.
- 참조 5곳을 갱신했다. `og:image`, `twitter:image`, JSON-LD `image`, 본문 `figure`, `columns.html` 카드, 그리고 디숭이 AI 칼럼 자료다. 대체 텍스트는 목록용과 본문용 두 종류를 각각 새 그림에 맞춰 바꿨다.
- 회귀 검사 6종 통과. 로컬에서 카드와 본문 삽화의 정상 로드·대체 텍스트를 확인했다.
- **작화 주의점:** 디숭이는 정면 캐릭터라 옆모습이나 3/4 각도를 지시하면 얼굴이 무너진다. 실제로 `Seen from the side`로 요청한 시안에서 이목구비가 한쪽으로 몰리고 귀 하나가 머리에서 떨어지며 이마의 D 표식이 치우쳤다. 이번 채택본에도 얼굴이 약간 옆으로 돌아간 흔적이 남아 있으며, 사용자 판단으로 그대로 채택했다. 후속 작업에서는 정면·좌우 대칭을 명시하는 문단을 프롬프트에 항상 포함한다.
- 자세 변주 현황: 산후 다리 부종은 앉은 정면, 가을 불면은 누운 부감, 우울은 웅크린 자세다. 남은 칼럼도 자세·앵글·소품이 겹치지 않게 배분한다.

## 2026-09-16 가을 불면 칼럼 대표 이미지를 디숭이로 교체

- `columns/insomnia-without-sleeping-pills.html`의 대표·본문 이미지를 디숭이로 교체했다. 뿔 머리띠를 쓴 채 이불을 덮고 누워 눈을 감았지만 미간에 긴장이 남은 표정으로, 잠들지 못하는 상태를 나타낸다. 잠든 모습으로 보이면 칼럼 주제와 어긋나므로 표정의 긴장을 유지한다.
- 새 파일은 `assets/images/columns/autumn-insomnia-herbal-medicine/cover-disoongi.webp`(1254×1254, 179,098바이트)이며 생성 원본 PNG도 같은 폴더에 보관했다. 기존 `cover.png`는 삭제하지 않았다.
- 이 칼럼은 참조가 6곳이다. `og:image`, JSON-LD의 `url`과 `image`, 본문 `figure` 이미지, `columns.html` 카드, 디숭이 AI 칼럼 자료를 모두 갱신했다. 대체 텍스트는 목록용과 본문용 두 종류가 서로 다르므로 각각 새 그림에 맞춰 바꿨다. 이 칼럼에는 `twitter:image`가 없다.
- 이미지 폭·높이가 기존과 같은 1254라 레이아웃은 변하지 않는다. 로컬에서 목록 카드와 본문 삽화의 정상 로드·대체 텍스트를 확인했고 회귀 검사 6종이 통과했다.
- 배경 방침 정리: 앞선 시안에서 화면을 채우는 물감 번짐을 배경으로 쓰다가 모든 칼럼이 같은 색면 덩어리로 보이는 문제가 생겼다. 번짐은 부끄님 원본에 없는 장치이며 원본 배경은 종이 질감이 있는 평평한 색면이다. 이번 칼럼은 무채색 방 장면으로 갔다. 후속 교체에서 배경은 순백·평평한 면·무채색 방 중에서 고르고, 번짐을 기본값으로 쓰지 않는다. 변주는 자세·앵글·소품 하나로 만든다.
- 파일명 참고: 이 칼럼의 슬러그는 `insomnia-without-sleeping-pills`이고 이미지 폴더는 `autumn-insomnia-herbal-medicine`이라 서로 다르다. 기존 구조를 그대로 유지했다.

## 2026-09-16 산후 다리 부종 대표 이미지를 디숭이로 교체

- 구 사슴 캐릭터로 그려져 있던 `columns/postpartum-leg-swelling.html`의 대표 이미지를 디숭이로 교체했다. 뿔 머리띠 착용, 흰 티셔츠, 좌우 대칭 앞발, 수채 번짐 배경으로 원본 작화 규격에 맞췄다.
- 새 파일은 `assets/images/columns/postpartum-leg-swelling/cover.webp`(1254×1254, 104,818바이트)이며 생성 원본 PNG도 같은 폴더에 보관했다. 기존 `thumbnail.png`는 삭제하지 않았다.
- `og:image`, `twitter:image`, `columns.html` 카드 `src`, 두 곳의 대체 텍스트, 디숭이 AI 칼럼 자료의 썸네일 경로를 함께 갱신했다. 대체 텍스트는 새 그림에 맞춰 "뿔 머리띠를 쓴 디숭이가 흰 티셔츠를 입고 앉아 무거운 다리의 불편을 표현한 일러스트"로 바꿨다.
- 이미지 폭·높이가 기존과 같은 1254라 카드 레이아웃은 변하지 않는다. 로컬 브라우저에서 새 파일 정상 로드와 대체 텍스트 반영을 확인했고 회귀 검사 6종이 통과했다.
- 저장소에 WebP 변환 도구가 없어 브라우저 Canvas 내장 인코더(품질 0.90)로 변환했다. 새 의존성은 추가하지 않았다. 후속 교체도 같은 방법을 쓸 수 있다.
- **배경 기준 보정:** 바로 아래 기록의 "무채색톤"을 장면을 그린 뒤 채도만 낮추라는 뜻으로 읽으면 안 된다. 실제 칼럼 카드 관행은 배경을 거의 그리지 않고 단색 또는 수채 번짐만 두는 것이며, 방 전체를 그려 회색으로 칠한 시안은 사이트 결과 맞지 않아 반려됐다. 후속 교체는 이 기준을 따른다.

## 2026-09-16 디숭이 작화 규격 확정 — 이전 칼럼 사슴 캐릭터 통일 준비

- 배경: 디숭이 원작자는 김민지 대표원장(문서상 "부끄님")이며 아이패드 손그림이 원본이다. 이전 칼럼 이미지에는 디숭이가 아닌 별도의 갈색 사슴 캐릭터가 섞여 있어 디숭이로 통일하기로 했다. 이번 작업은 **규격 기록까지**이며 실제 이미지 교체·생성은 시작하지 않았다.
- 확정 작화 규격(대표원장 확인 사항):
  - **뿔 머리띠는 항상 착용한다.** `assets/images/disoongi-profile.png` 원본에서 머리띠를 옆에 벗어둔 것은 제작 편의였을 뿐 기본 상태가 아니다. 벗은 상태를 기본으로 재현하지 않는다.
  - **팔다리는 짧고 뭉툭하게 유지한다.** 다리가 길어지면 비율이 무너진다. 실제 반려 사례의 주된 사유가 비율이었다.
  - **체형은 `disoongi-profile.png`가 상한선이다.** 그보다 통통해지거나 몸통이 커지면 안 된다.
  - **배경은 계속 2D로 간다.** 실사풍·3D풍으로 바꾸지 않는다.
  - **배경은 기존 무채색톤으로 채도를 죽인다.** 침대·테이블·거울 같은 장면과 소품은 넣어도 되지만 주황 벽처럼 채도 높은 색면은 쓰지 않는다. 색은 디숭이 몸에 남긴다.
  - 디숭이 고정 요소를 유지한다: 크림빛 몸, 하트형 흰 얼굴 무늬, **이마의 D 표식**, 귀 끝 짙은 갈색, 갈색 외곽선.
  - **D 표식 상세:** 이마의 흰 무늬는 단순한 점이 아니다. 한쪽은 곧은 직선, 반대쪽은 둥글게 부푼 **D자 모양의 잎사귀 4장**이며, 서로 다른 각도로 이마 중앙 둘레에 흩어져 있다. 가지에서 잎이 떨어지는 디어한의원 로고 모티프와 같다. 생성 프롬프트에 `three white dots`처럼 쓰면 이 고증이 사라지므로 잎 모양과 장수, 직선-곡선 대비를 명시한다.
- 칼럼 이미지 중간 분류(실제 사용 중인 커버 16장 확인, 약 25장 미분류):
  - 디숭이 온모델: 만성두통·항암 통증(수채풍), 자율신경 스트레스·다이어트 한약 중단(플랫 벡터풍).
  - 구 사슴 캐릭터(교체 대상): 환절기 속건조, 아이 성장 한약, 공진단, 수험생 한약, 산후 다리 부종, 서초 산후 다이어트, 다이어트 한약 비용, 무주의 맹시. 진한 갈색 몸·머리에 직접 붙은 뿔·이마 무늬 없음·인간형 팔다리 비율로 구분된다.
  - 캐릭터 없음(교체 불필요): 어튜브먼트, 지도는 영토가 아니다, 우울 기능 회복.
  - 디숭이 온모델 안에서도 수채풍과 플랫 벡터풍이 갈린다. **표준 화풍은 아직 확정하지 않았다.**
- 교체 절차: 기존 파일을 덮어쓰지 않고 새 파일명으로 추가한 뒤 승인받고 HTML 본문·목록·공유 메타데이터의 대체 텍스트까지 함께 바꾼다.

## 2026-09-16 만성두통 칼럼 최종 편집·대표 이미지 교체

- `columns/chronic-headache-lying-down.html`에 검토 완료된 편집안을 반영했다. 문단과 소제목의 강제 줄바꿈을 줄이고, 섹션 여백과 글자 위계를 다른 수작업형 칼럼에 맞췄다.
- 기립성 두통 문구를 자세 변화만으로 원인을 확정하지 않는 설명으로 조정하고, 두통 일지를 네 단계 가로 카드와 한 줄 기록 예시로 재구성했다. 목 진찰 개념도는 움직임·압통·시간 연관성 체크 카드로 교체했다.
- 대표 이미지는 사슴뿔 머리띠를 쓰고 두통으로 시무룩하게 앉아 있는 디숭이 정사각형 이미지로 교체했다. Columns 목록과 공유 메타데이터의 대체 텍스트를 같은 내용으로 맞췄다.

## 2026-09-15 누우면 괜찮아지는 만성두통 칼럼 발행

- `columns/chronic-headache-lying-down.html`을 새 Relief 칼럼으로 제작했다. 누웠을 때 편해지는 흔한 근골격 부담과 기립성 두통을 구분하고, 긴장형두통·목 주변 압통·스트레스와 자율신경·소화 상태·두통 기록·즉시 평가가 필요한 위험 징후를 한 흐름으로 설명한다.
- ICHD-3, NICE CG150, VA/DoD 2023 진료지침과 PubMed 등 확인 가능한 원문을 근거로 연결했다. 자세만으로 원인을 확정하지 않도록 한계를 함께 적고, 김민지 대표원장의 집필·검토 정보와 편집 원칙을 표시했다.
- 전용 SVG 개념도와 반응형 편집 디자인을 적용하고, 공유·목록용 PNG 대표 이미지를 추가했다. FAQ 6개, MedicalWebPage·Article·BreadcrumbList·FAQPage 구조화 데이터를 화면 문구와 일치시켰다.
- `columns.html`, sitemap, RSS, 최신 칼럼 메뉴, 디어AI 칼럼 자료를 29편 기준으로 갱신하고 칼럼 발행기 회귀 기준도 새 최신 글에 맞췄다.

## 0. 2026-09-11 현재 작업 스냅샷 — 집/회사 Codex 공통 시작점

- **현재 기능 기준:** 최신 코드 변경은 `d32dda6`이며, 그 이후에는 이 현재 스냅샷을 추가·정정한 `HANDOFF.md`만 변경됐다. 작업 종료 시 로컬 `master`와 `origin/master`의 동기화를 확인했다. 이 아래의 날짜별 기록은 상세 작업 이력이며, 새 작업자는 먼저 이 스냅샷을 읽고 필요한 항목만 추적한다.
- **방금 완료한 범위 1:** `columns/autonomic-stress.html`의 깨진 공통 내비게이션·NAP·푸터를 복구하고, 하단 CTA를 칼럼 규격에 맞는 선택형 전환 UI로 재설계했다. 원고와 근거 문헌은 건드리지 않았다. 커밋은 `ddf373c`, `b9a8626`; Pages 실행 `34589334597` 성공.
- **방금 완료한 범위 2:** 전체 sitemap과 공개 칼럼을 대상으로 SEO/AEO/GEO 배선을 전수 점검했다. 현재 sitemap은 **81 URL**, 공개 칼럼은 **28편**이며, 81/81 운영 URL에서 200 응답·canonical 일치·sitemap 내부 noindex 없음이 확인됐다. 칼럼 외부 근거 링크 120개 중 확정 404/410은 0개였고, 14개는 DOI·CDC·BMJ·NEJM 측 접근 제한 때문에 차단/미확정으로 분리했다.
- **이번 보강:** 홈과 `columns.html`의 sitemap `lastmod`가 오래된 채 남던 배선을 고쳤다. `tools/publish-column.mjs`의 발행·삭제·`--refresh-index` 경로가 두 허브의 `lastmod`를 한국시간 실행일로 갱신하며, 회귀 검사가 이를 고정한다. 커밋 `d32dda6`; Pages 실행 `34590625920` 성공. 운영 sitemap에서도 두 허브가 `2026-09-11`로 확인됐다.
- **검증 상태:** `test-column-publisher`, `test-columns-serp`, `test-seo-surfaces`, `test-medical-editorial-trust`, `test-dear-ai-columns`, `test-naver-tracking`이 모두 통과했다. 기술 배선에서 과거 기준(77 URL·25칼럼) 대비 약화된 지점은 발견되지 않았다. 다만 이는 사이트 구현·운영 크롤 기준이며, 현재 Google Search Console/GA4의 실제 노출·클릭·순위는 이번 작업에서 인증 조회하지 않았다.
- **남은 판단:** 당장 추가 구현이 필요한 확정 결함은 없다. 다음 SEO 작업은 Search Console/GA4 최신 수치를 실제로 확인할 수 있을 때 노출·색인·검색어 변화를 구현 상태와 분리해 판단한다. Google이 요구하지 않는 별도 GEO 스키마나 `llms.txt`를 근거 없이 추가하지 않는다.
- **새 작업 시작 순서:** 저장소의 `AGENTS.md` 지침을 확인한 뒤 `CLAUDE.md` → `HANDOFF.md` → `COLLABORATOR_BRIEF.md`를 처음부터 끝까지 읽고, `git fetch origin` → `git status --short --branch` → `git log -10 --oneline`으로 현재 상태를 다시 확인한다. 기준보다 뒤처졌고 작업 트리가 안전할 때만 `git pull --ff-only origin master`를 사용한다.
- **보존 대상:** 로컬의 `assets/images/dear-favicon.ico`와 `다국어-오역-일괄수정.patch`는 사용자 소유 미추적 파일이다. 요청 없이 수정·삭제·스테이징·커밋하지 않는다. `git add .`를 사용하지 않는다.

## 2026-09-09 다이어트 한약 중단 칼럼 이미지·타이포그래피 정렬 보정

- `columns/diet-herbal-medicine-stop.html`의 정사각형 대표 이미지를 본문 축에 맞춘 760px 최대 폭으로 중앙 정렬하고, 화면 오른쪽에 떠 있던 캡션을 이미지 아래 중앙으로 옮겼다. 원본 이미지와 원고는 변경하지 않았다.
- 해당 칼럼의 Pretendard 가변 글꼴 실제 적용을 확인했다. 제목 34.4~48px/500, 주요 소제목 23~28px/700, 본문 16~16.5px, 요약 16~17px, 카드 제목 17px, 도표 제목 19px, 주석 12px로 역할별 위계를 명시했다.
- PC 1440px, 태블릿 768px, 모바일 390px에서 이미지·본문 중심축 일치, Pretendard 실제 로드, 제목>소제목>본문>주석 위계와 가로 넘침 0을 확인했다. 칼럼 발행기·Columns SERP·SEO 표면 검사도 통과했다.

## 2026-09-09 다이어트 한약 중단 후 식욕과 요요 칼럼 발행

- 사용자 승인 원고를 columns/diet-herbal-medicine-stop.html에 발행한다. 1개월·3개월 치료 목표, 입맛 변화, 소화·흡수·운반·이용/저장 개념도, 종료 확인표와 디어 진료 관점의 Q&A 6개를 포함한다. Wise 2016 문단은 사용자 요청으로 제외했다.
- 파란 티셔츠·양손 아령·사슴뿔 머리띠를 착용한 디숭이 정면 이미지를 대표·목록·공유 썸네일로 사용한다. 원본에서 WebP로 최적화했으며 기존 자산은 보존했다.
- Pretendard 가변 글꼴을 assets/fonts/PretendardVariable.woff2로 제공하고 글 전체에 적용한다. 기존 공통 칼럼 메뉴·목차·독서 시계·푸터를 유지한 수작업형 칼럼으로, 발행기로 본문을 덮어쓰지 않는다.
- Columns 최신 글·RSS·sitemap·최신 메뉴·디숭이 공개 칼럼 자료 연결. Article/MedicalWebPage/FAQPage와 작성·검토 정보를 본문과 동기화했다. 발행기/검색 테스트의 고정 최신 글·개수를 새 발행 상태로 갱신했다.
- 배포 전 필수 검사 9개 통과. 미리보기는 기존 로컬에 유지한다.


## 2026-09-07 디숭이의 공통 응대 입장을 병원 일원으로 통일

- 사용자 재확인: 디숭이는 저희 디어한의원의 AI 안내 직원이다. 저희라는 호칭만 붙이는 것이 아니라 가격·진료·원장 소개·칼럼·예약·운영 확인 전반을 저희 병원 입장에서 응대한다. 사람/의료진/실제 처리 완료를 가장하는 것은 아니다.
- Worker 역할과 공통 지침을 변경했다. 기존 '제가 확인할 수 있는 정보에 없어요'라는 출력 지침을 제거하고 개인별 판단·정확한 가격은 저희 대표원장님의 상담, 운영 확인은 저희 담당자의 확인으로 구분한다. 알 수 없는 정보를 미운영·비공개 정책으로 꾸미거나 실제 확인/전달을 했다고 말하지 않는다. 모든 언어에 공통 적용한다.
- 다이어트 가격에 일반 탕약 1팩 6,000원~을 대신 안내하지 않는다. 처방 구성과 기간에 따른 비용 및 저희 대표원장님 상담 안내로 답한다. 반복 문의에는 사용자가 승인한 '홈페이지에서 공개해드리는 비용 안내는 여기까지예요'라는 경계를 짧게 안내한다. 일반 탕약 자체의 단가를 명확히 묻는 경우에는 기존 고지 금액을 정확히 안내한다. 다른 상품도 동일하게 항목을 구분한다.
- 검사: AI 정적/공개 보호/응대 계약/칼럼 계약 4개와 Worker dry-run 통과. 실제 가상 질문 6개로 다이어트 첫/반복 가격, 원장 진료 소개, 주차 대행 운영 확인, 영어 소개, 일반 탕약 단가를 확인했다. 모두 200/continue. 시험 동의 철회 완료. 생성형 응답의 표본 검사이며 모든 발화의 품질 보장은 아니다.
- 운영 Worker c36c1dc8-30ff-485d-b1d4-cb7861346d1a 배포 및 실제 응답 확인. 이번에는 서버 지침만 변경했으며 화면/칼럼 카드/동의/모델/요청 한도는 변경하지 않았다.



## 2026-09-07 모든 주제의 칼럼 추천 흐름 개선

- 사용자 승인: 특정 다이어트 발화뿐 아니라 모든 주제에서 먼저 대화를 듣고, 칼럼 추천 시 원장님 글이라는 소개·기존 썸네일·읽기/대화 계속하기 선택지를 함께 보여준다.
- js/dear-ai.js/css/dear-ai.css의 추천을 예약용 큰 초록 버튼에서 흰색 미리보기 카드로 분리했다. 한국어·영어·일본어·중국어 고정 소개가 카드 앞에 항상 표시된다. 더 이야기하기는 사용자가 선택한 의사를 실제 메시지로 전송하고 앞 대화를 이어간다. 전송 중에는 중복 클릭을 막는다. 이미지 오류는 이미지만 숨기며 링크/제목/선택지는 유지한다.
- 공개 칼럼 자료 생성기는 기존 og:image에서 내부 이미지 경로와 버전 쿼리만 가져온다. 현재 26편 모두 썸네일 경로가 있다. 원본 이미지·원고·CMS 입력 형식은 변경하지 않았으며 이미지는 추천 카드 표시 시 lazy로 읽는다. 브라우저의 전체 칼럼 자료 선요청이나 자동 유료 호출은 없다.
- 서버 출력에 recommendation_intent(none/requested/contextual)를 추가했다. 첫 고민/단순 증상/소개/비용에는 관련 단어만으로 추천하지 않고, 명시적인 읽을거리 요청에는 1~2편을 안내한다. 충분한 후속 대화의 맥락 추천은 최대 1편이며 최근 6개 대화 항목에 추천 기록이 있으면 억제한다. 최근 공개 칼럼 ID만 검증 후 서버에 전달하며 새 대화 저장/분석 로그는 추가하지 않는다. 예약·긴급 행동에는 추천을 함께 붙이지 않는다. 추천 거절/대화 계속 선택 후 새로운 읽을거리 요청까지 추천을 멈추도록 공통 지침을 설정했다.
- 채팅 CSS/JS 캐시 20260907-7. 동의/모델/요청 한도는 유지한다. Worker 배포 34ef31f5-dd36-4bd5-abfc-f205989cdb4a.
- 로컬 검사: 공개 회귀 9개와 Worker dry-run 통과. 칼럼 서버 검사에 첫 발화 억제/최근 추천 억제/명시적 재요청/긴급·예약 배제/실제 썸네일 전달 추가. 칼럼 UI와 오류 복구 UI는 1440/768/390 및 네 언어 검사 통과. 운영 Pages 실행 34092757450(8905cc3) 성공과 HTML/JS/CSS/칼럼 자료 일치를 확인했다. 운영 사이트에서도 동일한 5개 화면·언어 UI 검사를 통과했다(AI/동의 모의 처리). 실제 모델 가상 질문 16회로 첫 고민/비용/수면 추천/추천 거절 뒤 연속 대화/영어 추천을 확인하며 비용 뒤 불필요한 읽기 권유와 카드 소개 중복을 보정했다. 최종 운영 브라우저에서 수면 칼럼 요청 1회로 실제 썸네일 로드·소개·선택지를 확인했다(16회에 포함). 시험 동의는 모두 철회했다. 생성형 응답의 표본 검증이며 모든 발화를 보장하지 않는다.



## 2026-09-07 같은 브라우저에서 동의 유지

- 사용자 승인: 동의는 한 번 기억하고 페이지 이동·새로고침·재방문 때 다시 묻지 않는다. 대화 기록까지 장기 보관하지는 않는다.
- 기존 원인은 동의 영수증이 iframe 메모리에만 있었고 서버도 30분 뒤 만료시키던 구조다. js/dear-ai-consent.js는 새 동의 번호·안내 버전·동의 시각을 localStorage의 dear-ai-consent에 보관하고 유효한 영수증이 있으면 show()도 다시 띄우지 않는다. 페이지 이동/새 대화/다른 탭/브라우저 재시작에 유지된다. 대화는 기존 dear-ai-chat sessionStorage와 14개/마지막 저장 후 30분 규칙을 유지한다.
- 새 안내 버전은 20260907-persistent-1이며 정해진 만료 시간이 없다. 기존 20260906-public-1 영수증을 자동으로 장기 연장하지 않는다. 배포 중 캐시된 구버전 화면은 기존 30분 동의로 계속 이용할 수 있다. 새 화면에서 한 번 새 안내에 동의한 뒤부터 지속된다.
- 동의 철회 시 브라우저 동의와 대화를 먼저 지우고 서버 기록 삭제를 요청한다. storage 이벤트로 다른 탭도 중단한다. 서버 철회 실패 시 기존처럼 재시도한다. 사이트 데이터 삭제/다른 브라우저/안내 버전 변경/서버에서 유효하지 않은 동의로 판단한 428 응답에는 다시 확인한다. 저장소 차단·비공개 모드에서는 재방문 시 기억하지 못할 수 있다.
- Worker는 과거 월에 발급한 영수증도 해당 월 Durable Object에서 조회한다. 새 영수증의 응답 expires는 null, 기존 SQL expires_at은 MAX_SAFE_INTEGER를 무기한 표식으로 사용한다. 이름·IP·대화는 동의 테이블에 저장하지 않는다. 월별 영수증 2000개 상한과 기존 채팅 요청 한도는 유지한다. 매일 정리 알람에서 만료/지원 종료된 안내 버전을 삭제하며 유효한 지속 영수증은 유지한다. 안내 버전 변경 시 index.js와 budget.js의 지원 버전 및 프런트 버전을 함께 갱신한다.
- 네 언어 개인정보처리방침의 동의 보관·철회 설명과 시행일을 2026-09-07로 갱신했다. 사이트 데이터 삭제가 서버 철회 요청을 보내지는 않는다는 동작도 명시했다. 동의 JS 캐시는 20260907-6, 그 외 화면/캐릭터/칼럼 해설 지침은 유지했다.
- 검사: 공개 회귀 9개와 Worker dry-run 통과. test-dear-ai-public.mjs에서 400일 경과·월 변경 후 동의 확인, 철회 후 428, 지원하지 않는 버전 정리 검사 추가. test-dear-ai-consent-ui.cjs에서 PC 1440/태블릿 768/모바일 390의 새로고침·31분 경과·새 대화·브라우저 상태 복원·탭 간 동의/철회·428·데이터 삭제·버전 변경을 검수했다. 기존 오류 복구와 4개 언어 칼럼 동행 UI 검사도 유지한다.
- Worker 배포: 0743b8f0-3ce5-4e0b-88c0-47594e94c9bd. 실제 서버에서 새/구버전 동의 등록, 만료 구분, 유효 동의 확인, 철회 후 차단을 확인했다. 형식이 빈 대화를 사용해 모델 호출 전 종료되는 검사이며 유료 모델 호출은 하지 않았다.


## 2026-09-07 칼럼 동행 안내와 실제 원고 해설·추천

- 승인 범위: 칼럼 목록/본문에서 페이지별 한 번 초대하고 사용자가 클릭·질문할 때 대화한다. 채팅 자동 열기나 자동 유료 호출은 없다. 목록은 주제 탐색, 본문은 이 글 핵심/설명 선택지를 제공한다. 4개 언어 지원. 같은 탭에서 같은 경로의 자동 인사는 sessionStorage로 억제하며 직접 마우스/키보드로 접근한 인사는 유지한다. 다른 페이지의 기존 인사 주기는 유지한다.
- js/dear-ai-widget.js가 현재 경로만 iframe에 전달하고 js/dear-ai.js가 질문에 pagePath를 포함한다. 원고는 브라우저에서 미리 내려받지 않는다. 원문 카피·레이아웃·사진은 유지하고 공통 main.js/위젯 및 채팅 CSS/JS 캐시를 20260907-5로 갱신했다.
- tools/build-dear-ai-columns.mjs가 sitemap에 공개된 26편의 제목·설명·main 본문을 assets/data/dear-ai-columns.json으로 생성한다. Pages 배포마다 다시 생성하므로 후속 발행도 반영된다. 기존 비공개 CMS 입력 형식과 공개 발행기의 입출력 계약은 변경하지 않았다. 공개 발행기 회귀 검사 통과.
- Worker는 실제 질문에만 고정된 공개 자료 주소를 읽는다(2초 제한, 1MB 제한, 정상 응답 60초 캐시). 전체 글 목록과 현재 글 원문을 모델에 제공하고 같은 자료의 반복 질문에는 프롬프트 캐시를 적용한다. 자료 실패 시 원고를 읽었다고 꾸미지 않는다. 100편/원문 3만 자/자료 1MB 상한에 도달하면 생성 단계에서 실패하므로 자료 구조를 검토한 뒤 한도를 조정한다.
- 해설은 원문 핵심과 승인된 저희 디어한의원 진료 관점을 연결한다. 원문에 없는 효과·운영·장점은 만들지 않고 원문과 부연을 구분한다. 개인 진단으로 바꾸거나 예약을 반복 권유하지 않는다. 추천 요청에는 발행 목록에서 1~2개 id와 이유를 골라 실제 주소로 검증한 카드만 반환한다. 클라이언트도 내부 칼럼 경로만 링크로 만들고 세션 복원 시 카드를 유지한다.
- 추가 합의: 원장 소개는 짧게 유지하며 경력을 물어도 타 병원명을 언급하지 않고 역할과 경험만 설명한다. 사이트의 기존 원장 약력 원문은 변경하지 않았다.
- 검증: 공개 회귀 9개 검사와 Worker dry-run 통과. AI/동의를 모의 처리한 tools/test-dear-ai-column-ui.cjs에서 1440/768/390, 한국어·영어·일본어·중국어, 자동 AI/자료 요청 없음, 경로 전달, 안전한 카드, 재방문 자동 초대 억제, 대화 복원, 링크 이동, 가로 넘침 0 확인. 기존 오류 복구 5개 화면/언어 검사도 통과.
- 기능 배포 커밋 a840e79, Pages 실행 34085605841 성공. 운영 위젯/채팅/main/CSS/iframe/칼럼 자료와 로컬 파일 일치를 확인했다. 동일한 5개 화면/언어 UI 검사를 운영 사이트에서도 통과했다(AI/동의는 모의 처리).
- 실제 모델 가상 질문 4종을 3차례, 총 12회 검수했다. 모두 200/continue. 첫 표본의 장황한 해설과 존재하지 않는 이전 기관 비공개 정책 표현을 보정했다. 최종 표본은 원문에 근거한 불면·체중 해설, 실제 칼럼 두 편의 추천과 이유, 타 병원명·제한 정책 설명 없는 원장 경력을 확인했다. 답변 길이는 생성 결과에 따라 달라질 수 있으며 표본 검증을 모든 발화의 품질 보장으로 해석하지 않는다. 시험 동의는 각 회차 종료 후 철회했다.
- 최종 Worker 배포 버전: b6eb0f00-c142-47fe-b5f4-a975ab06098d. 모델/900 출력 토큰/기존 요청 한도와 동의 버전은 유지한다. /health의 publicChat/protections=true 확인.


## 2026-09-07 원장 소개는 짧고 담백하게

- 사용자 피드백: 단순히 김민지가 누구인지 묻는데 과거 근무 이력을 나열할 필요가 없다. 저희 원장님 진료 잘 보신다는 자신감과 어떤 점이 궁금한지 묻는 짧은 소개를 원한다.
- 인물 확인은 1~3개의 짧은 문장으로 답하며, 경력/자격은 명시적으로 물었을 때만 설명한다. 승인된 지식 자체는 유지한다. 치료 결과나 비교 우월성 보장은 하지 않는다.
- 실제 질문 '야 김민지가 누구냐'에 '저희 디어한의원 대표원장님이에요! 진료 잘 보세요 😊 어떤 게 궁금하세요?'와 continue 응답을 확인했다. 별도 경력 질문도 표본 검증했다. 정적/응대 계약/공개 보호 검사 통과. 프런트 화면은 변경하지 않았다.
- Worker 배포 버전: f69d4e59-f87b-4f1e-a9a4-c08e1521938f.

## 2026-09-07 디숭이 병원 소개·예약 판단·오류 복구 개선

- 사용자 승인 범위는 응대 문제 1~5번이다. 비용/사용량 관찰은 사용자가 직접 하며 추가 남용 제한은 도입하지 않았다. 기존 일/월 요청 제한·모델·동의·캐릭터·홈 디자인은 유지한다.
- 디숭이는 외부 평가자가 아니라 저희 디어한의원의 AI 마스코트다. 한국어는 저희 디어한의원/저희 대표원장님, 타 언어도 our clinic 등 자연스러운 소속 표현을 쓴다. 사람·의료진·실제 진료 목격자로 가장하지 않는다.
- Worker 지식에 director.html/philosophy.html/career.html의 승인된 철학·경력·자격·학회 회원 사실을 반영했다. 핵심은 증상과 생활 맥락 확인, 판단 이유 설명, 실행할 계획과 경과 확인이다. 타 기관 비교 우월성, 학회 회원을 활동 성과로 확대, 확인되지 않은 병원 규모·운영 정책 단정을 금지한다.
- non-covered.html의 공개 비급여 시작 금액/단위를 반영했다. 탕약 1팩 단가를 다이어트 한 달 패키지로 환산하지 않는다. 상담료 공개와 무료 상담 운영 여부는 구분하며, 모르는 정보를 미운영/비공개 정책으로 단정하지 않는다. 비용 답변 뒤 문의를 자동 권유하지 않는다. 원본 가격/원장 정보 변경 시 Worker 지식도 같이 갱신한다.
- 정보 탐색은 continue, 명확한 예약/문의 연결 의사는 offer_booking이다. 예약 거절 뒤 새 연결 의사가 나오기 전에는 진단 질문도 한계와 진료 필요성만 설명하고 continue로 답한다. 실제 긴급 징후의 urgent_help는 유지한다.
- 프런트 오류 시 사용자 메시지를 history/savedTurns에서 삭제하지 않는다. pending 상태로 기존 30분 탭 저장에 남기고, 재시도는 같은 사용자 메시지를 중복 추가하지 않는다. 다음 메시지에도 실패했던 말이 포함된다. 새로고침 후 재시도, 동의 철회 시 삭제를 유지한다. 오류 문구와 재시도 버튼은 4개 언어로 제공하며 일괄 전화/예약 유도는 제거했다. 채팅 CSS/JS 캐시는 20260907-4.
- 실제 가상 질문 검수에서 500 출력 토큰 한도로 도구 응답이 잘리는 output_limit을 재현했다. 간결한 답변 지침과 함께 max_tokens를 900으로 조정했다. 오류 분류는 output_limit/invalid_output만 반환하며 원문은 기록하지 않는다. 이는 이번 재현 결과이며 이전 사용자 스크린샷의 오류 원인이 같았다고 단정하지 않는다.
- Worker 최종 배포: 5168b8a7-5056-4f03-a0e6-39fb4963d096. 실제 모델 가상 요청 총 20회(초기 잘림 2회 포함)로 보정하며 확인했다. 최종 4개 표본은 예약 거절/상담료/패키지 비용/영어 소개 모두 200·continue이고 예약 버튼을 제시하지 않았다. 앞선 표본에서는 거절 후 진단 질문 continue, 실제 긴급 상황 urgent_help, 식단 언급 문맥을 확인했다. 생성형 응답 표본 검증이지 모든 발화의 품질 보장은 아니다.
- 검사: test-dear-ai.mjs, test-dear-ai-public.mjs, test-dear-ai-dialogue.mjs, SEO 검사 통과. test-dear-ai-recovery.cjs는 AI/동의를 모의 처리하는 Playwright 검사이며 로컬 서버와 Playwright가 필요하다(DEAR_TEST_URL, 필요 시 DEAR_PLAYWRIGHT_MODULE). 1440/768/390 및 한국어·영어·일본어·중국어에서 실패/새로고침/중복 없는 재시도/다음 말의 문맥/가로 넘침 0을 확인했다.

## 2026-09-07 홈 진입 로딩 최적화

- 디자인·카피·팝업 순서와 영구 닫기·디숭이 크기/프사/인사 주기·원본 히어로 영상/자동재생을 유지했다. AI Worker와 모델은 변경하지 않았다.
- 홈 head에서 현재 화면에 보일 팝업만 먼저 요청한다. PC는 미닫은 안내 전체, 900px 이하에서는 첫 미닫은 안내를 요청한다. 나머지 모바일 안내는 첫 이미지 로드 뒤 낮은 우선순위로 준비하며, 빠른 닫기/화면 확대 시 즉시 요청한다. 기존 닫기 기록·저장소 차단·weather-preview 처리를 유지한다. 안내 추가/변경 시 head의 IDs와 data-popup-id를 함께 수정하고 tools/test-home-loading.cjs를 실행한다.
- Google Fonts CSS를 preload하여 기존 공통 CSS의 import와 병렬로 요청한다. 글꼴 종류·굵기·CSS는 유지한다. 홈의 main.js 캐시만 20260907-3으로 갱신했다. 다른 페이지의 기존 마크업과도 호환된다.
- 하단 세계지도는 무손실 WebP(212,191→88,880바이트)로 제공하고 화면 500px 앞에서 요청한다. 알파와 보이는 RGB 픽셀은 동일하며 완전 투명 픽셀 두 곳의 숨은 RGB만 달라졌다. 원본 PNG는 보존한다. 디숭이 전신은 이미 무손실 WebP여서 재압축 이득이 없으므로 유지했다.
- 비교 측정: 변경 전/후 gzip 로컬 서버, 외부 글꼴 정상 요청, 390×844/DPR1, CPU 4배 제한, 지연 100ms·다운로드 4Mbps, 캐시 비활성, 교대 3회. 중앙값 FCP 1,992→1,528ms, LCP 2,164→1,708ms, CLS 모두 0. 양쪽 LCP 대상은 같은 첫 팝업이다. 이는 통제된 실험 결과이며 실방문자 통계나 이전 라이브 감사 수치와 직접 합산하지 않는다.
- PC 1440·태블릿 768·모바일 390에서 본문 카피/글꼴/팝업 경계 동일, 가로 넘침 0, 닫기 순서/새로고침 후 재노출 없음, 구형 기록 이전, 저장소 차단, 날씨 미리보기, 지도 접근 로드, 디숭이 iframe 열기를 확인했다. 홈 로딩·AI 정적/공개 보호·SEO 검사 통과(사이트맵 79, 칼럼 26).

## 2026-09-07 디숭이 작은 프사 윤곽선 렌더링 보정

- 크기를 키운 뒤에도 일반 1배 화면에서 원본 선이 점처럼 끊겨 보이는 문제를 확인했다. 원본 `disoongi-profile.png`의 얼굴 영역(x121/y318, 719px 정사각형)을 Lanczos3로 정밀 축소한 `disoongi-face-58@1x/2x/3x.png`, `disoongi-face-42@1x/2x/3x.png`를 사용한다. 원본·D 표식·색·질감은 다시 그리지 않았다.
- 상단/첫 인사/추가 답변/로딩 프사에 밀도별 srcset을 연결하고 CSS의 140% 확대와 소수점 translate를 제거했다. 표시 크기는 58/42px로 유지한다. 채팅 CSS·JS 캐시는 `20260907-2`다.
- 실제 사용자 Chrome의 1배 화면에서 선 연결 개선을 비교했고, 1440/768/390px 가로 넘침 0 및 1/2/3배 자산 선택을 확인했다. AI 정적·공개 보호·SEO 검사를 통과했다. 대화 시험은 모의 API만 사용했다.

## 2026-09-07 디숭이 채팅 프로필 가독성

- 채팅 상단 프로필은 46px에서 58px, 답변 옆 프로필은 34px에서 42px로 키웠다. 기존 1006×1440 원본 PNG를 유지하고 CSS의 이미지 배율·위치를 얼굴과 이마 표식 중심으로 조정했다. 바깥 전신 실행 버튼은 변경하지 않았다.
- 채팅 CSS 캐시는 `20260907-1`이다. 1440/768/390px에서 이미지 로드·가로 넘침 0을 확인하고 AI 정적·공개 보호·SEO 검사를 통과했다. 유료 모델 호출은 하지 않았다.

## 2026-09-07 디숭이 상세 세션 인수인계

- 디숭이 작업자는 [전용 상세 인수인계](docs/DISOONGI_HANDOFF.md)를 함께 읽는다. 목적·사용자 판단 배경·응대와 캐릭터 고증·동의 UX·예약 분류·파일 지도·두 배포 경로·검증과 장애 대응을 통합했다. 아래 운영 요약과 과거 이력을 대체 삭제하지 않는다.
- 이번 작업은 문서 정리이며 기능·모델·설정·비밀키 변경이나 유료 모델 시험은 하지 않았다. 후속 수정 시 전용 문서도 실제 상태에 맞춰 갱신한다.

## 현재 운영 요약 — 2026-09-06 / 다른 PC·Codex는 여기부터

이 요약은 아래 날짜별 이력보다 우선하는 **이번 인수인계 시점의 상태**다. 과거의 ‘공개 전’, ‘테스트 전용’, ‘아직 해결되지 않음’은 당시 기록이지 현재 작업 지시가 아니다. 다음 작업에서는 Git 최신 상태·코드·실서버를 다시 대조한다. 과거 기록은 삭제하지 않는다.

### 어디까지 완료했나

- 디숭이는 **일반 공개 중**이다. 홈페이지 공통 페이지 및 독립 외국인 예약 페이지에서 테스트 암호 없이 열리며, 동의 후 실제 Claude API와 대화한다. Worker 이름에 `preview`, 채팅 경로에 `preview/`가 남아 있어도 비공개 상태라는 뜻이 아니다.
- 최근 사이트 배포 기준은 `fec9d94`(개인정보처리방침 정리). Pages 실행 `34006775546` 성공, 네 언어 방침의 운영 응답과 로컬 파일 일치까지 확인했다. 이 인수인계 커밋은 그 뒤의 문서 변경이다.
- 최근 Worker 배포 기록은 `ca1ea95e-8722-49dc-b1bd-29c514076a95`(일반 증상 과잉 응급 안내 보정). 이번 인수인계에서는 재배포하거나 유료 대화를 다시 실행하지 않았다.
- 이번 읽기 전용 `/health` 확인: `ok=true`, `publicChat=true`, `protections=true`, `consentVersion=20260906-public-1`. `revision=20260905-consent-1`은 코드의 표식이며 최신 Worker 버전 번호가 아니다. health 성공만으로 실제 모델 대화까지 검증됐다고 쓰지 않는다.
- 정적 사이트는 HTML/CSS/Vanilla JS + GitHub Pages, AI 서버는 별도 Cloudflare Worker다. **Git push만으로 Worker가 배포되지는 않는다.** DNS·도메인·기존 비밀키를 새 PC라는 이유로 재생성하지 않는다.

### 유지할 디숭이 합의

1. 역할은 진료 분야·방문·예약 안내다. 진단·처방·치료 효과 보장·예약 확정을 하지 않는다. 자연스러운 해요체, 문맥에 맞는 소량의 이모지, 평문을 쓴다. 별표 마크다운이 그대로 보이는 답변으로 되돌리지 않는다.
2. 단순 ‘배 아파요’, ‘열 나요’에 최악의 질환이나 가정형 응급 징후·119 목록을 먼저 붙이지 않는다. 선공감 후 필요한 맥락만 짧게 묻는다. 진단을 요구하면 짧게 한계를 밝히고 김민지 대표원장 진료와 예약 선택지로 연결한다. 실제 구체적인 긴급 징후가 대화에 드러나면 `urgent_help`는 유지한다.
3. 욕설·오타·사투리·성적 화제는 전체 맥락으로 다루고 안내 역할로 자연스럽게 돌아온다. 몇 번 욕했다고 기계적으로 같은 경고를 반복하거나, 모든 대화를 정적 FAQ/키워드 분기로 바꾸는 것은 합의하지 않았다.
4. 예약 경로는 국내 `네이버 예약·톡톡·전화`, 외국인 `인스타그램 DM·외국인 예약 안내 페이지·전화`. 네이버를 쓰기 어려운 한국인은 `인스타그램·전화`로 연결하며 외국인 페이지로 보내지 않는다. 외국인 페이지는 접수 폼이 아니라 진료 일정·전화 안내 페이지다.
5. 캐릭터는 원형 안의 작은 얼굴이 아니라 머리띠를 쓴 투명 전신 실행 버튼이다. 첫 시안의 짧고 통통한 팔다리·원본 질감·이마의 DEAR D 표식을 지킨다. D를 일반 꽃잎이나 임의 글자로 다시 그리지 않는다. 새 파생 시안은 원본 비교와 사용자 확인 뒤 적용한다.
6. 말풍선은 Pretendard, 메인 ‘궁금한 거 있어요? / 제가 도와드릴게요 :)’, 서브에는 접기·이동 안내를 더한다. 최초 약 1.8초 뒤 6초 표시·12초 휴식 반복이다. ‘탭 최초 한 번’으로 되돌리지 않는다. 채팅 열림·드래그·숨긴 탭 등에서는 멈춘다.
7. 열기·닫기 모션, 드래그 이동, − 버튼 접기, 키보드 이동, 움직임 최소화 대응을 유지한다. 위치·접힘은 같은 탭의 페이지 이동 동안 유지한다. 현재는 사용자 이동/접기로 방해를 줄이는 방식이며 본문 자동 회피 기능이 구현된 것은 아니다.
8. 채팅 배경은 기존 한의원 공간 사진에 옅은 덮개를 둔다. 동의창은 뒤의 프로필·첫 인사가 보이는 작은 하단 카드다. 기본 체크는 **해제 상태**, 사용자가 누르는 전체 동의와 개별 해제를 지원한다. 친근하고 짧게 안내하되 연령·개인정보·건강정보·국외 이전 항목과 서버 검증을 임의로 없애지 않는다.

### 개인정보·비용의 현재 경계

- 네 언어 개인정보처리방침은 대화 기록 / 업체·보관기간 표 / 국외 전송 / 동의·철회로 정리했다. 중요한 보관 예외와 공식 링크는 남겼다. 시행일과 이전 방침은 문서 하단에 유지한다. 단순 가독성 변경으로 동의 버전을 올리지 않았다.
- 대화 원문을 자체 서버 DB·로그나 GA4·NAVER·광고 이벤트에 저장/전송하지 않는다. 외부 업체의 처리·보관까지 없다는 뜻은 아니다. 브라우저 최근 14개 메시지는 마지막 저장 후 30분이 지나면 다음 로드에서 삭제한다. 철회는 후속 전송 중단과 브라우저 대화 정리이며 공급자 데이터 즉시 삭제가 아니다.
- 서버 동의 증표는 30분 유효하고 철회·만료 정리를 구현했다. 모델은 `claude-sonnet-4-6`, 설정은 IP당 12회/분, 사이트 전체 200회/일·2000회/월, 최대 출력 500토큰이다. 이는 요청 수 제한이지 확정 월 요금 상한이 아니다. 잔액·자동 충전·실제 비용은 Console에서 별도로 확인하며 ‘무조건 월 커피값’으로 약속하지 않는다.
- 서울 인근 Placement로 과거 Worker 403이 해소됐으나 국내 전용 저장·처리 보장은 아니다. 해당 설정을 근거 없이 지우거나 키·모델·결제를 먼저 바꾸지 않는다.
- 공급자 계약·국외 이전 적용 범위에 남은 확인 문제와 기술적 공개 완료는 구분한다. 법적 적정성을 보증하거나, 반대로 확인 못한 내용만으로 불법·신고·운영 중단을 단정하지 않는다. 공급자 이메일 회신 자체를 필수 공개 관문으로 만들지 않으며 문의 발송·새 계약·자동 비공개 전환은 별도 승인 없이 실행하지 않는다.

### 다음 작업과 검증

- 현재 요청한 공개·증상 응대 보정·방침 정리는 완료했다. 새 기능을 자동으로 시작하지 말고 삥이·부끄님의 실제 사용 피드백을 받아 해당 범위만 수정한다. 이번 인수인계는 운영 기능 변경 승인이 아니다.
- 기능 문제 제보 시 해당 페이지·언어·화면 폭·가상 재현 문장·새 대화 여부를 확인한다. 실환자 개인정보나 API 키를 채팅/스크린샷/공개 문서로 요구하지 않는다.
- 바로 전 검증: AI 정적·공개 보호 검사, SEO 79페이지·26칼럼 검사 통과. 네 언어 방침 1440/768/390px 가로 넘침·내부 링크 검사 통과. 실제 가상 증상 5종은 일반 질문 4종의 과잉 응급 안내 없음과 실제 긴급 문맥 1종의 응급 안내 유지를 확인했다. 표본 검증이지 모든 미래 답변의 보장은 아니다.
- 기본 재검사: `node tools/test-dear-ai.mjs`, `node tools/test-dear-ai-public.mjs`, `node tools/test-seo-surfaces.mjs`. 동의/예산 저장 로직을 바꾸면 `node tools/test-dear-ai-budget-runtime.mjs`도 실행한다. UI 변경 시 PC·태블릿·모바일에서 실제 클릭·줄바꿈·스크롤을 확인한다.
- 실제 모델 시험은 `node tools/smoke-dear-ai-release.mjs --public`, 증상 보정 시험은 `--public --symptoms`다. 유료 API 호출이므로 필요할 때만 가상 문장으로 실행한다. 문서 정리만으로 반복 호출하지 않는다.

### 파일 지도와 새 PC에서의 주의

| 수정 대상 | 파일 |
| --- | --- |
| 공통 노출·위젯 진입 | `js/main.js`, `js/dear-ai-widget.js` |
| 캐릭터·말풍선·이동·접기·패널 모션 | `js/dear-ai-widget.js`, `assets/images/disoongi-launcher-v1.webp` |
| 채팅·동의 UI | `preview/dear-ai.html`, `js/dear-ai.js`, `css/dear-ai.css` |
| AI 응대·예약 분류·API 처리 | `worker/dear-ai/src/index.js` |
| 동의 증표·예산 기록 | `worker/dear-ai/src/budget.js` |
| Worker 설정·배포 | `worker/dear-ai/wrangler.jsonc`, `worker/dear-ai/package.json` |
| 방침 | `privacy.html`, `en/privacy.html`, `ja/privacy.html`, `zh-cn/privacy.html`, `css/privacy-ai.css` |
| 공개 준비의 과거 근거 | `docs/disoongi-release-readiness.md`（현재 운영 지시는 이 요약 우선） |

- `AGENTS.md` → `CLAUDE.md` → `HANDOFF.md` → `COLLABORATOR_BRIEF.md`를 읽고 fetch/status/log로 새 변경을 확인한다. 로컬 변경이 없고 뒤처졌을 때만 `git pull --ff-only origin master`한다. 동시에 같은 파일을 수정 중이면 먼저 조율한다.
- 사이트 변경은 검수 후 관련 파일만 한국어 커밋하고 `master`에 반영한다. 자동 Pages 배포 성공과 실제 URL을 확인한다. Worker 수정은 별도 계정 권한·의존성을 확인한 뒤 `worker/dear-ai`에서 검사·배포하며 실제 API 동작까지 확인한다. 키 값은 출력·커밋하지 않는다.
- 장애 복구는 최신 정상 변경과 영향 범위를 확인하고 진행한다. 과거 전체 사이트 태그로 무조건 되돌리면 이후 칼럼·동의·디숭이 개선이 사라진다. 긴급 차단이 승인된 경우 Worker 공개 모드를 먼저 끄고 두 홈페이지 노출 설정을 함께 조정한다. DO·비밀키를 삭제하지 않는다.
- 이 PC의 미추적 `assets/images/dear-favicon.ico`, `다국어-오역-일괄수정.patch`는 이번 작업물이 아니다. 삭제·적용·커밋하지 않고 보존한다. 다른 PC의 미추적 파일도 사용자 소유로 취급한다.

---

아래는 날짜별 변경 이력이다. 당시 검증값·공개 여부·캐시 버전을 현재 운영값으로 혼동하지 않는다.

## 2026-09-06 개인정보처리방침 가독성 정리

- 한국어·영어·일본어·중국어 AI 안내를 대화 기록, 처리 업체 표, 국외 전송, 동의·철회 및 삭제로 정리했다. 브라우저 기록은 업체 표에서 분리하고 개발 내부 용어·테스트 접근 코드·중복 설명을 덜어냈다.
- 처리 업체·항목·목적·보관기간과 보관 예외, 학습 이용 예외, 동의 기록·철회 한계, 국외 처리 범위 및 공식 안내 링크는 유지한다. 권리행사 문의는 해당 언어의 개인정보 보호책임자 항목으로 연결한다.
- 처리 방식·동의 항목·동의 버전·시행일·이전 방침·일반 진료 및 분석 관련 조항은 변경하지 않았다. 문서 가독성 정리이며 새로운 법적 적정성 판단이나 공급자 확인 완료를 뜻하지 않는다.
- 네 언어 1440·768·390px에서 가로 넘침과 끊긴 내부 링크가 없음을 확인했다. 한국어 PC·모바일 화면을 시각 검수했고 AI 정적·공개 보호 검사 및 SEO 표면 검사(79개 페이지·26개 칼럼)를 통과했다.

## 2026-09-06 일반 증상 응대·진단 질문 예약 연결 보정

- 일반 증상명만으로 최악의 질환·가정형 응급 징후·119 안내를 덧붙이지 않도록 시스템 지침을 수정했다. 짧은 증상 호소는 선공감과 필요한 맥락 질문, 진단 질문은 짧은 진단 한계 설명과 김민지 대표원장 진료 안내 및 `offer_booking`으로 연결한다. 치료 효과 보장은 하지 않는다.
- 실제 대화에 구체적인 긴급 징후가 드러난 경우의 `urgent_help`는 유지한다. 명시적 '응급' 단어 없이도 전체 문맥으로 판단하며, 부정·과거·가정을 현재 증상으로 오인하지 않도록 했다. 증상 키워드 필터나 응급 문구 사후 삭제는 추가하지 않았다.
- Worker 버전 `ca1ea95e-8722-49dc-b1bd-29c514076a95` 배포. 실제 가상 질문 5개에서 열/복통 진단 질문은 예약 연결, 단순 복통/두통은 추가 질문이며 네 답변에 응급 문구가 없었다. 갑작스러운 심한 흉통+호흡곤란은 응급 안내를 유지했다. 이는 표본 검증이지 모든 생성 답변의 보장은 아니다.
- 재검증: `node tools/smoke-dear-ai-release.mjs --public --symptoms` (유료 API 5회), 기존 정적·공개 보호 검사와 Worker dry-run. 모델·동의·비용 제한·예약 URL·화면 디자인은 변경하지 않았다.

## 2026-09-06 동의 카드 하단 소형 패널

- 동의 화면만 불투명·블러 전체 덮개에서 옅은 배경과 하단 카드로 변경했다. 프로필·첫 인사가 뒤에 보이며 배경 채팅은 기존 inert로 조작되지 않는다. 카드 폭 최대 400px, 내부 여백 16px, 항목 간격 6px로 줄이고 작은 화면에서는 카드 내부 스크롤로 모든 항목과 시작 버튼에 접근한다. 문구·동의 항목·서버 로직은 변경하지 않았다. 채팅 CSS 캐시는 `20260906-3`.

## 2026-09-06 말풍선 반복 안내·대화 시작 UX 개선

- 인사 말풍선은 최초 1.8초 뒤 6초 표시, 12초 휴식의 반복형이다. 같은 탭 최초 1회 제한을 제거했다. 메인은 기존 두 줄 인사, 서브는 접기·이동 안내를 추가하며 네 언어에 적용한다. 대화창 열림·직접 접기·드래그·숨긴 브라우저 탭에서는 표시하지 않는다. 포인터/키보드 초점으로도 다시 볼 수 있다.
- 동의 화면은 디숭이 인사와 진료·예약 질문 초대로 시작한다. AI 역할과 입력 주의사항은 별도 보조 문단으로 유지했다. 미리 체크하지 않으며, 사용자가 누르는 전체 동의와 개별 해제/부분 선택 표시를 제공한다. 연령·개인정보·건강정보·국외 이전 항목과 서버 검증·철회는 그대로다. 의미상 처리 범위는 변경하지 않아 동의 버전은 유지한다.
- 모바일 390px에서 반복 표시/숨김, 가로 넘침 0, 기본 미체크·전체 선택·건강정보 개별 해제 시 전송 불가·실제 동의 등록·철회 후 전체 초기화를 확인했다. 메인 1440px의 두 줄 문구도 확인했다. 일반 대화를 정적 메뉴로 바꾸지 않았고 모델·프롬프트·예약 경로는 수정하지 않았다.
- 공통 JS, 위젯, 동의 UI, 채팅 CSS 캐시는 `20260906-2`. 사용자 미추적 파비콘과 번역 패치는 보존한다.

## 2026-09-06 디숭이 일반 공개 전환

- Worker 공개 배포 버전: `8e2aa0e4-a139-47c3-a926-3897bf673663`. `node tools/smoke-dear-ai-release.mjs --public`으로 테스트 암호 없이 동의 등록, 국내/네이버 미사용/영어/일본어/중국어 예약 경로, 응급 안내, 철회 후 428을 실제 확인했다. 임시 QA 비밀키는 생성하지 않았다.
- 사용자의 명시적인 일반 공개 요청으로 홈페이지 두 노출 설정과 Worker 공개 모드를 활성화한다. 테스트 암호 없이 이용하되, 서버가 발급한 유효한 동의 증표 없이는 대화 요청을 처리하지 않는다.
- 개인정보·건강정보·국외 이전의 개별 선택, 연령 확인, 30분 만료·철회, IP 요청 제한, 200회/일·2000회/월 상한은 유지한다. 모델·프롬프트·캐릭터·움직임·예약 경로는 변경하지 않는다.
- 네 언어 개인정보처리방침과 약관의 제한 테스트 표현을 실제 공개 운영에 맞춘다. 공식 자료에서 확인한 저장·처리 범위만 안내하며 전체 경로가 미국 전용이라고 주장하지 않는다. 국외 이전 고지의 정확한 적용 범위에 대한 법적 적정성은 확정하지 못했으며, 사용자 배포 승인이나 기술 시험을 법적 검토 완료로 기록하지 않는다.
- 공개 동의 버전은 `20260906-public-1`, 공통 JS 캐시는 `20260906-1`, 위젯 캐시는 `20260905-5`다. 기존 페이지의 공통 JS 참조만 갱신하며 본문과 CSS는 보존한다.
- 장애 시 Worker `PUBLIC_CHAT_ENABLED=false`를 먼저 배포해 새 요청을 차단하고, 홈페이지 두 `PUBLIC_WIDGET_ENABLED`를 false로 되돌려 노출을 중단한다. 기존 DO와 비밀키는 삭제하지 않는다.

## 2026-09-05 동의 리허설·철회·실제 LLM 검증 완료

- 일반 공개는 여전히 OFF다. `js/main.js`와 `js/dear-ai-widget.js`에 명시적 `PUBLIC_WIDGET_ENABLED=false`를 두었다. Worker 공개 모드도 false다.
- `?dear-ai-test=1&consent-review=1`에서 기존 암호 뒤에 네 언어 동의 리허설을 사용할 수 있다. 개인정보·건강정보·국외 이전은 각각 별도 체크하며 연령 확인을 포함한다. 미동의/14세 미만은 전화·외부 예약 안내를 이용한다. 일반 방문자에게 노출하지 않는다.
- `/consent`는 유효한 항목 확인 뒤 30분짜리 임의 동의 증표를 발급한다. ChatBudget에는 날짜별 요청 합계 외에 동의 번호·버전·서버 접수/만료 시각만 별도 테이블에 저장한다. 대화·IP·이름은 저장하지 않는다. 철회하면 활성 증표를 삭제하며 같은 증표의 후속 대화는 428이다. 만료 기록은 요청 시/예약 알람으로 정리한다. 공급자 복구 백업의 즉시 삭제나 장기 법적 증빙 체계를 보장하는 것이 아니다.
- 클라이언트는 동의 전·만료 후 전송을 막고, 철회 시 브라우저 문맥과 진행 중 응답을 정리한다. 네 언어 방침에도 임시 증표와 철회의 실제 범위를 추가했다. 390/768/1440px 및 네 언어에서 가로 넘침 0, 미체크 상태 전송 없음, 국내/외국인 대체 링크를 확인했다.
- `tools/smoke-dear-ai-release.mjs`로 실제 Claude 응답 6종(국내 예약/네이버 미사용/영·일·중 외국인 예약/응급)을 확인했다. 모두 HTTP 200 및 기대 action/route 통과. 실제 철회 후 428 확인. 기존 암호는 읽지 않았으며 14분 만료 임시 QA 키를 만들어 사용 후 삭제했다. 최초 시험의 401은 비밀키 배포 직후 전파 지연으로, 인증 등록만 최대 6초 재시도하도록 했으며 AI 호출은 자동 재시도하지 않는다.
- 코드 검증 및 비공개 리허설 완성과 일반 공개의 고지 적정성 확정은 구별한다. 남은 국외 이전 고지의 정확한 적용 범위와 공개 순서는 `docs/disoongi-release-readiness.md`를 따른다. 공급자 개별 회신 자체가 법정 필수 조건인 것은 아니다.
- 최종 Worker 버전은 `1cb261d3-f483-4b12-977a-3707e9a0d63e`다. 비밀키 이름 목록에는 기존 `ANTHROPIC_API_KEY`, `PREVIEW_ACCESS_CODE`만 남았음을 확인했다. 동의 접수·만료 시각은 서버가 정하며 방문자 기기의 시각 오류로 등록이 거절되지 않는다.

## 2026-09-05 공개 직전 보호 장치 실제 배포 및 검증

- 사용자의 이번 승인 범위는 **공개 전 준비**다. 일반 공개는 켜지 않는다. 공급자 문의 회신 자체가 공개의 법정 필수 관문이라는 뜻은 아니다. 아래 이전 기록의 공개 보류 표현은 필요한 사실·적용 근거가 미확정이라는 의미로 한정하며, 공식 약관·문서로 확인 가능한 사항은 회신 없이 검토한다.
- Worker `fe24e7be-3be5-4940-84e7-312eb25bfce1` 배포 성공. `/health`에서 `revision=20260905-guards-2`, `publicChat=false`, `protections=true`를 실서버 확인했다. 인증 없는 `/chat`은 401이다. 기존 비밀키를 보존했고 값은 읽거나 기록하지 않았다.
- `CHAT_PROTECTIONS_ENABLED=true`로 비공개 인증 테스트에도 IP 해시 제한과 일/월 요청 합계 제한을 적용한다. 잘못된 한도 설정(0, 음수, 소수, NaN 등)은 차단한다. 공개 모드의 동의 검사는 여전히 UI 연동 전 초안이며, 비공개 인증을 공개 동의로 간주하지 않는다.
- `tools/test-dear-ai-budget-runtime.mjs`는 설치된 Miniflare/workerd의 실제 SQLite·RPC로 동시 20요청 중 설정된 3회만 승인되는 것을 확인했다. 외부 LLM 비용이 발생하지 않는 로컬 시험이다. 실행 전 `worker/dear-ai` 의존성 설치가 필요하다.
- 클라이언트는 인증창이 열려 있을 때 전송하지 않는다. 긴 대화는 최신 14개 이내이면서 총 9,000자 이내로 문맥을 추려 전송하고, 30초 브라우저 대기 제한을 적용한다. 로컬 브라우저에서 1,200자 메시지 12회를 모의 전송해 실제 전송 최대 8,436자/13메시지 및 네이버 미사용 국내 경로(인스타그램·전화)를 확인했다. 실제 LLM 품질 검증과 구별한다.
- 네 언어 방침의 요청 제한 설명을 IP 해시·날짜별 합계 저장 방식과 동기화한다. 모델·브랜드 카피·마스코트·일반 노출 범위는 바꾸지 않았다. Pages 배포 전 두 AI 회귀 검사도 Node 24에서 자동 실행한다.
- **남은 공개 준비:** 공개용 고지/동의 UI와 서버 연동, 만 14세 미만 대체 안내, 동의 증빙·철회 흐름, 실제 적용되는 국외 처리 고지의 확정. 2026-04-24 개인정보위 개정 안내는 AI의 입력·출력/학습 여부/주의사항/신고 창구를 구체적으로 안내하도록 설명하지만 공급자 개별 회신 의무를 뜻하지 않는다. 근거: https://pipc.go.kr/np/cop/bbs/selectBoardArticle.do?bbsId=BS074&mCode=&nttId=12021
- 이번 브라우저 도구 재연결로 이전 인증 테스트 세션이 사라졌다. 따라서 새 배포 이후의 **실제 인증 LLM 한 차례 응답은 아직 재확인하지 못했다.** 비밀번호를 추출하거나 공개 모드를 잠시 켜는 우회는 하지 않는다. 현재 인증된 사용자 화면에서 가상 질문 한 차례로 확인할 수 있다.
- 긴 배포 대기는 반복 폴링하지 말고 현재 상태를 보고한다. 공개 전환은 Worker와 홈페이지 노출 양쪽을 함께 검증해야 하며, 문제 발생 시 우선 `PUBLIC_CHAT_ENABLED=false`를 유지/복구한다. DO 마이그레이션 전 버전으로 무조건 롤백하거나 저장소를 삭제하지 않는다.

## 2026-09-05 법률 문서 순서·약관 정합성 및 공개 준비

- 네 언어 개인정보방침은 디숭이 항목/목차 링크를 먼저 배치하고 시행일과 변경 이력은 하단에 둔다. 이전 방침 원본은 보존한다. 이용약관과 환자의 권리·의무에도 문서형 CSS를 적용했으며 환자 권리·의무 본문은 그대로다.
- 이용약관의 자체 상담 입력 기능이 없다는 모순을 네 언어에서 제거하고 현재 제한 테스트인 AI 안내와 개인정보방침을 연결했다. 책임 범위는 귀책사유와 관계 법령에 따라 판단한다는 문장으로 맞췄다. 약관 시행일은 9월 5일이다.
- 공개 전환용 서버 코드는 준비했지만 Worker에는 아직 배포하지 않았다. `PUBLIC_CHAT_ENABLED=false`를 유지한다. 공개 모드는 CF-Connecting-IP의 해시로 12회/분 요청 제한, 월별 ChatBudget Durable Object로 200회/일·2000회/월 사전 차감 제한, 실제 읽은 요청 본문 32KB 제한, 25초 upstream 제한을 적용한다. 수치는 요금 상한이 아닌 호출 상한이며 실제 비용은 입력 길이에 따라 다르다. 대화나 IP를 budget DB에 저장하지 않고 날짜별 합계만 저장한다. 실패 호출도 차감하며 자동 재시도하지 않는다.
- 공개 모드의 동의 버전/항목 검사는 아직 UI·고지 확정 전의 서버 초안이다. 일반 공개 전 동의의 적용 근거, 건강정보 선택 범위 및 만 14세 미만 처리, 동의 증빙/철회 흐름을 함께 확정해야 한다. 기존 테스트는 변경 없이 사용 가능하다.
- 공개 보류의 구체적 사유: Anthropic Trust Center는 클라우드 하위처리 위치를 Worldwide로 명시한다. US 추론 옵션은 추론만 제한하며 지원·중계까지 모두 미국이라는 근거가 아니다. Cloudflare 역시 개발자 플랫폼 재수탁자와 글로벌망을 사용한다. 현재 계정/서비스에 적용되는 처리 국가·조건을 확인하지 않은 채 미국만 고지하거나 동의로 모든 요건이 충족됐다고 주장하지 않는다. 공급자 확인 또는 적용 근거의 법률 검토 후 공개를 진행한다.
- 테스트: `node tools/test-dear-ai-public.mjs`는 동의 누락, 방문자 세션 변경에 무관한 제한 키, 사용량 한도 및 누락 바인딩 fail-closed, 본문 크기 제한, upstream 호출 차단을 확인한다. 실제 Durable Object 배포 및 공용 LLM 호출은 미검증 상태다.

## 2026-09-05 개인정보처리방침 문서형 정리

- 네 언어 현재 방침만 전용 CSS v20260905-2로 제목 26–28px, 조항 17–18px, 본문 15px/1.7의 문서형으로 조정했다. 접이식 목차와 이전 방침, AI 처리 주체·항목·보관 표를 추가하고 중복 개발 설명 및 내부 미확정 업무 문구를 줄였다. 기존 진료·분석 조항과 이전 방침 원본은 보존했다.
- 공개 전환은 아직 하지 않았다. 현행 서버는 테스트 코드 인증 및 클라이언트 생성 세션 ID 기반 요청 제한을 사용한다. 일반 공개 시 인증만 제거하면 제한을 우회할 수 있으므로 서버에서 신뢰할 수 있는 요청 제한과 비용 상한을 함께 검토해야 한다. 이용 시작 안내/필요한 동의 및 실제 국외 처리 조건도 확정 대상이다. 방침의 문체 개선을 동의 완료로 간주하지 않는다.

## 2026-09-05 디숭이 테스트 개인정보 고지 갱신

- 한국어·영어·일본어·중국어 개인정보처리방침을 9월 5일 시행으로 갱신했다. 입력 기능이 전혀 없다는 기존 문구를 수정하고 `#ai-guide`에 메시지·언어·세션 식별자·접근 코드, Cloudflare 중계와 Anthropic 답변 생성, 브라우저 임시 보관·삭제, 외부 업체 보관 및 국외 처리 범위를 구분했다.
- 최근 14개 메시지/접근 코드의 sessionStorage 복원은 마지막 저장 후 30분까지이며, 만료 삭제는 다음 로드에서 이루어진다는 실제 코드 기준을 명시한다. 새 대화·테스트 종료는 업체 측 데이터까지 즉시 삭제하지 않는다.
- 공급자 공식 문서(2026-09-05 확인)에 따라 Anthropic 표준 30일 삭제와 이용정책 위반/법적 예외, 기본 학습 미사용과 예외를 안내했다. ZDR 계약·학습 옵트인 계정 설정을 확인한 것으로 주장하지 않는다. Workers 운영 로그는 요금제별 3/7일이며 자체 로그 미저장과 구분한다. 서울 인근 Placement는 국내 처리 보장이 아니다.
- 기존 네 언어 방침은 각 언어 폴더의 `privacy-20260903.html`에 검색 제외 상태로 보존하고 현재 방침에서 연결한다. sitemap lastmod는 현재 방침 네 개만 변경했다. 채팅 인증 화면과 하단에 언어별 방침 링크를 추가했다. 대화·예약·노출·인증 로직은 변경하지 않았다.
- **현재는 여전히 제한 테스트다. 방침 게시 = 일반 공개 승인/동의 획득/법적 절차 완료가 아니다.** 일반 공개 전에 민감정보 처리 및 국외 이전의 적용 근거와 별도 동의 UI, 만 14세 미만 처리, 공급자 계약/재수탁자·실제 이전국가·계정 보관 및 학습 선택 설정을 확인·확정해야 한다. 테스트에는 실제 환자정보를 넣지 않는다.
- 근거: https://privacy.claude.com/en/articles/7996866-how-long-do-you-store-my-organization-s-data , https://privacy.claude.com/en/articles/7996868-is-my-data-used-for-model-training , https://privacy.claude.com/en/articles/7996890-where-are-your-servers-located-do-you-host-your-models-on-eu-servers , https://www.cloudflare.com/policies/privacy/ , https://developers.cloudflare.com/workers/observability/logs/workers-logs/ , 개인정보 보호법 제23조·제28조의8. 기존 진료·GA4·NAVER 조항은 AI 관련 모순 문구 외에는 수정하지 않았다.

## 2026-09-04 BE DEER 비가시 검색 관계 보강

- 합의된 `BE DEER`·`Shape` 작명과 화면 디자인·카피·링크 라벨은 변경하지 않고, 검색엔진과 AI가 읽는 내부 관계만 정리했다.
- `be-deer.html`의 `MedicalWebPage` 구조화 데이터에 화면에 이미 연결된 다이어트 핵심 칼럼 6편을 `hasPart`로 동일하게 연결했다. `Service.areaServed`는 포괄적인 `서울` 한 곳 대신 기존 공식 지역 신호인 `서울 서초구`·`서초동`·`강남역`·`교대역`을 명시한다.
- 메인과 기존 칼럼에 남아 있던 `be-deer.html?v=20260728-3` 내부 링크는 canonical과 같은 `be-deer.html`로 통일했다. 사용자가 보는 이동 결과와 링크 문구는 유지하면서 같은 문서가 쿼리 주소로 따로 관측되는 원인을 제거했다.
- `tools/test-medical-editorial-trust.mjs`는 BE DEER의 여섯 칼럼 관계와 네 지역 신호를, `tools/test-seo-surfaces.mjs`는 캐시용 BE DEER 링크의 재유입을 검사한다.

## 2026-09-04 의료정보 작성 책임·근거·검색 신뢰 표면 전면 정비

- 공개 칼럼 26편 모두 첫 화면 작성자 정보 바로 아래에 `김민지 대표원장 직접 집필·의학적 검토·최종 승인`을 정적으로 표시하고 `medical-information-policy.html`의 편집·정정 원칙으로 연결했다. 실제 발행일·수정일은 임의로 오늘 날짜로 갱신하지 않았다.
- 새 의료정보 편집·정정 원칙 페이지는 디어한의원의 진료 관점에서 시작해 저자와 관점, 자료와 출처, 내용과 표현, 콘텐츠 제작, 정정과 업데이트, 의료정보의 범위와 발행 범위를 평상시 편집 기준으로 밝힌다. 내부 업무 분장이나 검색·AI 대응을 설명하는 문구는 두지 않는다. Columns와 원장 소개, 사이트맵에서도 이 문서로 연결한다.
- 전체 칼럼의 `Article`·`BlogPosting`에는 김민지 대표원장을 `author`와 `editor`로, `MedicalWebPage`에는 `author`·`reviewedBy`·`lastReviewed`로 구분해 Schema.org 속성의 적용 범위를 맞췄다. 각 글에 실제로 보이는 외부 참고자료는 `citation`에도 연결하고, 근거가 없는 운영·철학형 세 글에는 출처를 꾸며 넣지 않았다. 모든 편에 `publishingPrinciples`와 병원 발행 주체를 연결했다.
- `tools/normalize-column-editorial-trust.mjs`는 기존 26편의 작성 책임·근거 구조를 동일하게 유지하며 기본 실행은 변경 여부만 검사하고 `--write`에서만 반영한다. `tools/publish-column.mjs`도 앞으로 발행되는 칼럼에 같은 표기와 유효한 구조화 데이터를 자동 생성한다.
- `columns.html`에 공통 작성 원칙 안내를, `director.html`에 전체 칼럼·원칙 연결을 추가했다. `be-deer.html`에는 다이어트 핵심 칼럼 6편을 진료 기준·체중 정체·굶지 않는 감량·비용·월경 전 식욕·지방간 주제로 묶어 내부 주제 클러스터를 만들었다.
- 메인 날씨 카드는 네 장의 대형 장면을 모두 내려받지 않고 현재 상태에 필요한 한 장만 불러오도록 바꿨다. Fast 4G·CPU 4배 감속 모바일 추적에서 LCP는 2.14초에서 1.81초로 줄고 CLS는 0을 유지했다. 태블릿 Columns 분류명이 겹치던 문제는 1100px 이하 가로 탐색으로 수정했다.
- 메인·공통 푸터·팝업의 낮은 대비와 퀵메뉴 표시명/접근성 이름 불일치를 수정했다. 390px 모바일 Lighthouse에서 접근성·권장사항·SEO·AI 탐색성 네 항목 모두 100점, 실패 항목 0건을 확인했다.
- 공통 CSS와 JS 캐시는 `20260904-1`, 홈 날씨 장면 스크립트는 `20260904-1`로 갱신했다. `tools/test-medical-editorial-trust.mjs`가 26편의 정적 표기, 타입별 작성·검토 속성, 실제 검토일, 23편의 근거 연결, 정책·허브 링크와 향후 발행 템플릿을 검사한다. 기존 발행기·Columns SERP·SEO 표면 테스트에도 새 캐시·날씨 지연 로딩·태블릿 필터 회귀 검사를 반영했다.

## 2026-09-04 항암치료 후 통증·후유증 관리 칼럼 발행

- `columns/chemotherapy-pain-clinic.html`을 공개 발행했다. 항암제 유발 말초신경병증, 수술 후 유착과 관절 운동 제한, 방사선치료 후 조직 변화 등 항암치료 뒤 통증의 원인과 진료·침 치료·재활 관리 항목을 설명한다.
- 전체 암을 기준으로 한 항암치료 이용 추정치와 말초신경병증·통증 양상 통계를 출처와 함께 제시하고, 50대 유방암 수술 환자의 양쪽 어깨 통증 진료 사례를 익명화해 담았다.
- 대표 이미지는 흰 티셔츠를 입은 디숭이 캐릭터가 손발 저림으로 바닥에 주저앉은 수채화 일러스트를 사용한다. Columns의 오늘의 글·최신 카드, CollectionPage ItemList, RSS와 sitemap에 연결했다.

## 2026-09-03 네이버 검색광고(파워링크) 전환추적 설치

- 네이버가 발급한 검색광고 공통 인증키를 공통 `js/main.js`와 독립 구조인 `js/international-appointment.js`에 설치했다. 운영 도메인에서 `https://wcs.naver.net/wcslog.js`를 불러 `wcs.inflow()`와 파워링크 광고 페이지뷰를 전송하며, 로컬·GitHub Pages·`preview/` 경로에서는 실행하지 않는다.
- 기존 NAVER Analytics와 검색광고 전환추적이 같은 `wcs` 전역값을 사용하므로 광고 페이지뷰를 먼저 보낸 뒤 NAVER Analytics 페이지뷰를 별도 ID로 보내도록 순서를 고정했다. 각 전환 전송 직전에도 검색광고 인증키를 다시 지정해 두 서비스의 신호가 섞이지 않게 했다.
- 전체 공개 페이지의 네이버 예약 CTA는 `custom001`, 전화 링크는 `custom002`로 클릭 전환을 보낸다. 이는 외부 예약 화면을 열거나 전화 연결을 시도한 클릭이며 실제 예약 완료·통화 성립으로 해석하지 않는다. 외국인 예약 독립 페이지에는 전화 전환 `custom002`를 적용했다.
- `tools/test-naver-tracking.mjs`는 광고 유입→광고 페이지뷰→NAVER Analytics 페이지뷰 순서, 두 스크립트의 단일 로드, 예약·전화 전환의 광고 인증키, 전환 후 분석 ID 복원, 로컬·미리보기 제외를 모의 브라우저 환경에서 검사하며 Pages 배포 전 자동 실행된다.
- 한국어·영어·일본어·중국어 개인정보처리방침에서 홈페이지로 직접 들어오는 네이버 검색광고(파워링크)만 전환추적 대상으로 명시했다. 플레이스 광고는 네이버 플레이스로 이동해 홈페이지 스크립트가 실행되지 않으므로 이 범위에 포함하지 않는다. 파워링크 유입 식별정보(`NaPm` 등), 예약·전화 버튼 클릭 전환, 실제 예약 완료·통화·상담 내용을 전송하지 않는다는 범위를 반영했다. 공통 JS 캐시는 `20260903-2`로 갱신했다.
- 배포 후 네이버 설치 안내 메일의 `데이터 검수요청`에서 설치 검수를 요청한다. 이는 전환 코드와 수집 신호에 대한 기술 검수이며 의료광고 심의와 별개다.

## 2026-09-03 NAVER Analytics 설치

- NAVER Analytics 발급 ID를 공통 `js/main.js`와 독립 구조인 `js/international-appointment.js`에 연결했다. `https://wcs.pstatic.net/wcslog.js`는 디어한의원 운영 도메인에서만 한 번 로드되며, 로컬·GitHub Pages·`preview/` 경로의 확인 트래픽은 수집하지 않는다.
- NAVER Analytics와 네이버 검색광고(파워링크) 전환추적은 위의 전용 초기화 계층에서 함께 관리한다. 두 수집 스크립트의 중복 삽입을 막고 각 호출 직전에 해당 서비스 ID를 명시한다.
- NAVER Analytics 운영 사실을 한국어·영어·일본어·중국어 개인정보처리방침에 반영하고 시행일을 2026년 9월 3일로 갱신했다. 개인정보처리방침에서는 분석 서비스명을 `Google Analytics 4(GA4)`와 `NAVER Analytics`로 표기한다. 이후 검색광고 전환추적 설치와 함께 공통 JS 캐시는 `20260903-2`로 갱신했다.

## 2026-09-02 법률 페이지 문서형 타이포 정리

- 개인정보처리방침·홈페이지 이용약관·비급여 안내·환자의 권리와 의무의 한국어·영어·일본어·중국어 16페이지에 적용되는 타이포를 법률 문서에 맞게 낮췄다. 일반 법률 H1은 최대 72px에서 52px로, 별도 전용 스타일이던 비급여 H1은 최대 약 106px에서 52px로 줄였다. 조 제목은 최대 약 29px에서 24px로 낮추고 본문은 15~16px와 1.8 행간으로 정리했다.
- 첫 화면과 조별 상하 여백, 소제목·목록·정보 상자 및 비급여 표 행의 간격과 둥근 모서리도 함께 줄여 브랜드 랜딩처럼 크게 끊기지 않고 연속해서 읽히도록 했다. 비급여 히어로의 제목·소개문에 박혀 있던 강제 줄바꿈도 제거했다. 법률 문구와 비급여 항목·금액은 변경하지 않았다.
- 모바일에서 `.legal-content` 안의 모든 `<br>`을 숨기던 포괄 규칙을 제거했다. 일반 문단에는 강제 줄바꿈을 사용하지 않되 운영자 정보·국외 처리 정보·NAP처럼 구조상 필요한 줄바꿈은 유지한다. 법률 16페이지의 CSS 캐시는 `20260902-2`로 분리 갱신했다.

## 2026-09-02 광고 집행 전 법률 안내 정비

- 파워링크 전환추적은 추가하지 않았다. 기존 Google Analytics 4의 홈페이지 이용 통계와 주요 버튼 클릭 분석만 실제 운영 내용으로 개인정보처리방침에 공개했다.
- 한국어 개인정보처리방침을 2026년 9월 2일 기준으로 정비했다. 처리 목적·항목·법적 근거, 보유기간, 제3자 제공, Google LLC 처리위탁·국외 처리와 연락처, 쿠키 거부 방법, 정보주체의 권리, 안전성 확보조치, 보호책임자와 피해구제기관을 명시했다. 홈페이지에는 자체 회원가입·상담 입력 기능이 없다는 실제 상태도 반영했다.
- `terms.html`과 영어·일본어·중국어 편의 번역을 새로 추가했다. 정보 제공 범위, 의료정보의 한계, 외부 예약·문의 서비스, 이용자 의무, 저작권, 개인정보, 책임과 분쟁 해결 기준을 안내하며 한국어 원문을 공식 기준으로 둔다.
- 환자의 권리와 의무는 의료법 시행규칙 별표 1에 맞춰 자연스러운 문단으로 정리하고, 기존 안내에서 빠졌던 장기이식 여부를 알 권리 항목에 포함했다. 개인정보처리방침과 환자 권리 문서의 설명용 강제 줄바꿈은 제거했다.
- 메인과 전체 칼럼·다국어 페이지 등 공통 푸터 78개에 홈페이지 이용약관 링크를 추가했다. 칼럼 푸터 동기화 도구와 발행기, 언어 전환, canonical·hreflang, sitemap 및 회귀 테스트도 같은 구조로 갱신했다. 비급여 항목과 금액은 변경하지 않았다.

## 2026-09-02 출산 후 다리 부종 칼럼 발행

- `columns/postpartum-leg-swelling.html`을 공개 발행했다. 출산 후 다리 부종의 원인, 한의진료에서 확인하는 부종의 양상과 산후 회복 조건, 생활관리와 개인별 침·한약 치료의 관점을 설명한다.
- 30대 두 차례 출산 여성과 반복되는 다리 부종으로 내원한 여성의 진료 이야기를 비식별 사례 카드로 구성했다. 각 카드는 3단계 진료 흐름과 결과 영역을 분리했으며 개인별 치료 반응과 기간이 다를 수 있다는 안내를 포함한다.
- 대표 썸네일은 핑크색 상의를 입은 짧고 둥근 비율의 디어 사슴이 침대에 앉아 부은 다리를 눌러보는 부드러운 수채화풍 일러스트다. Columns 카드와 Open Graph·X 공유 이미지에 연결했다.
- `columns.html`, `sitemap.xml`, `rss.xml`에 새 칼럼을 연결하고 김민지 대표원장을 저자·검수자로 명시했다.

최종 갱신: 2026-09-02

## 2026-09-02 불면증 칼럼 하단 간격 정리

- `columns/insomnia-without-sleeping-pills.html`은 전용 본문 래퍼가 `.article`이라 공통 칼럼의 `section { padding: 0; }` 보호 규칙을 받지 못했고, 사이트 전역 `section` 패딩이 FAQ 위·아래와 참고자료 아래에 최대 128px씩 적용되고 있었다.
- FAQ 패딩을 `0`, 참고자료 패딩을 공통 칼럼과 같은 `2rem 0 0`으로 명시하고, 상담 CTA·FAQ·참고자료·NAP의 바깥 간격은 `clamp(2.4rem, 4vw, 3.5rem)`으로 맞췄다. 본문·CTA 카피·푸터 구조는 변경하지 않았다.

## 2026-09-02 전체 칼럼 푸터 메인 기준 통일

- 공개 칼럼 23개의 푸터를 `index.html` 메인 푸터와 같은 구조로 통일했다. 칼럼 폴더의 상대경로만 `../`로 다르고, 로고·슬로건·인스타그램·블로그·네이버 톡톡·대표자·사업자등록번호·주소·전화·법률 링크·저작권 정보는 동일하다.
- 이전 칼럼들에 공통으로 빠져 있던 SNS 링크 3개를 복구했고, `columns/depression-no-hope.html`의 오래된 푸터 구조와 누락된 법률 링크도 함께 정리했다. 각 칼럼의 본문·전용 CTA·NAP 디자인은 변경하지 않았다.
- `node tools/sync-column-footers.mjs`는 메인 푸터를 칼럼용 상대경로로 변환해 전체 칼럼에 동기화한다. `--check`와 `tools/test-seo-surfaces.mjs`는 모든 공개 칼럼의 푸터가 메인과 정확히 일치하는지 검사한다.

## 2026-09-02 불면증 칼럼 하단 공통 UI 복구

- `columns/insomnia-without-sleeping-pills.html`의 본문·이미지·확정 카피는 유지하고, 다른 공개 칼럼과 달랐던 간이 `footer-card`를 제거했다.
- 상담 CTA와 참고자료 뒤에 공통 NAP 카드(주소·전화·네이버 예약·위치 보기)를 추가하고, 페이지 하단에는 로고·대표자·사업자등록번호·법률 문서 링크를 포함한 정식 공통 푸터를 복구했다.
- `tools/test-seo-surfaces.mjs`는 모든 공개 칼럼의 정식 푸터와 불면증 칼럼의 NAP·예약·위치 링크를 검사해 같은 누락의 재발을 막는다.

## 2026-09-02 수면제 없이 불면증 칼럼 발행

- `columns/insomnia-without-sleeping-pills.html`을 공개 발행했다. 가을철 불면, 수면제를 먹지 않는 밤의 예기불안과 반동성 불면, 한의학적 진찰과 개인별 한약 처방을 40대 환자의 익명 진료 경과와 함께 설명한다.
- 검색 핵심어는 `수면제 없이 불면증`이며 제목·첫 답변·FAQ·검색 설명에 자연스럽게 연결했다. canonical 주소는 `/columns/insomnia-without-sleeping-pills.html`이다.
- 대표 이미지는 공룡 무늬 긴 소매로 양쪽 눈을 가린 디어 사슴이 단색 크림 이불을 덮고 누워 있으며, 창밖에는 가을밤 달이 뜨고 탁상시계에는 새벽 3시가 표시된 정사각형 일러스트다. 같은 이미지를 수면제 감량과 예기불안을 설명하는 본문 뒤에 한 번 더 사용한다.
- Columns 오늘의 글·최신 카드·CollectionPage ItemList·RSS·sitemap·`latest-column.json`에 연결하고 `MedicalWebPage`·`Article`·`BreadcrumbList`·`FAQPage` 구조화 정보를 포함했다.
- 사용자가 제외 요청한 `단순히`, `신호`, `흐름` 표현을 수면 칼럼에 사용하지 않는다. 수면제 처방 의료진과 감량 계획을 상의하도록 안내하는 문장은 사용자 요청에 따라 본문과 FAQ에서 제외했다.

## 2026-09-01 다국어 법률 안내 및 푸터 링크 정비

- 영어·일본어·중국어에 `privacy.html`, `non-covered.html`, `patient-rights.html`을 각각 추가해 총 9개의 언어별 법률 안내 페이지를 공개 범위에 포함했다. 외국어 24페이지의 개인정보처리방침·비급여 안내·환자 권리 링크는 더 이상 한국어 페이지나 `#`로 끊기지 않고 같은 언어 폴더의 문서로 이어진다.
- 외국어 법률 문서는 한국어 공식 원문의 의미를 안내하는 편의 번역이며, 각 페이지 상단에서 표현이 다를 때 한국어 원문이 기준임을 명시하고 해당 원문으로 직접 연결한다. 다국어 푸터의 한국어 상호·주소는 현지 길찾기와 법적 식별을 위해 기존 원문 그대로 유지한다.
- 한국어 법률 3페이지와 외국어 법률 9페이지에 상호 대응하는 canonical·hreflang을 추가했다. 공통 언어 선택기도 `privacy`, `non-covered`, `patient-rights`에서 동일 문서의 한국어·영어·일본어·중국어 버전으로 이동한다.
- sitemap은 61개에서 70개 URL로 늘었고 새 언어 전환 로직을 확실히 받도록 한국어·외국어 법률 12페이지의 JS 캐시만 `20260901-6`으로 갱신했다. 칼럼을 포함한 기존 일반 페이지는 `20260901-5`를 유지한다. `tools/test-seo-surfaces.mjs`는 33개 다국어 공통·법률 페이지의 각 캐시 버전, 단일 H1, canonical, 비활성 법률 링크 부재와 세 법률 링크의 언어 폴더 유지를 검사한다.
- 로컬 브라우저에서 새 9페이지를 1440×900, 768×1024, 390×844의 27개 조합으로 확인했다. 문서 가로 넘침, 깨진 이미지, 중복 ID, H1, 푸터 법률 링크, 네 언어 전환 주소 모두 이상이 없었다.

## 2026-09-01 다국어 24페이지 UI 전수 정렬

- 영어·일본어·중국어의 `index`, `about`, `director`, `career`, `philosophy`, `care`, `services`, `columns` 총 24페이지를 한국어판과 1:1로 비교하고 1440×900, 768×1024, 390×844의 총 96개 화면 조합을 검사했다. 단일 H1, 문서 가로 넘침, 메가메뉴 5개, 퀵메뉴 8개, 페이지별 최신 UI 모듈을 DOM 수치와 화면 캡처로 확인했다.
- 공통 `js/main.js`가 URL 경로에서 사이트 언어를 판별해 각 언어의 메가메뉴·링크·퀵메뉴 라벨을 만들도록 바꿨다. 브라우저 자동 번역 기능이 실행 중 `<html lang>`을 한국어로 바꿔도 일본어·중국어 사이트 경로와 UI가 한국어판으로 오인되지 않는다. 번역 페이지 중 퀵메뉴가 없던 Columns에도 같은 8개 메뉴가 자동 생성된다.
- 이전 구조였던 다국어 홈에는 현재 한국어판의 `BE DEER`와 진료 연결 브리지를, 원장 소개에는 번역 칼럼 연결 영역을, Care에는 진료 흐름·임상 장면·관련 칼럼 링크를, Services에는 4개 이미지 CTA·진료 원칙·상세 링크를, Columns에는 검색 중심 저널 헤더·추천어·가로 필터를 공통 UI 계층으로 보강했다. 기존 번역 본문과 NAP·진료시간·확정 정보는 변경하지 않았다.
- 일본어 철학·Care·Services와 중국어 철학·Services의 긴 CJK 문구가 390px 화면을 넓히던 원인을 줄바꿈·grid 최소폭·내부 가로 스크롤 규칙으로 막았다. 다국어 Columns 검색 헤더도 긴 필터의 최소 콘텐츠 폭이 페이지 전체를 넓히지 않도록 수정했다.
- 공통 CSS·JS 캐시 버전을 모든 공개 HTML과 칼럼 발행 템플릿에서 `20260901-5`로 통일했다. `tools/test-seo-surfaces.mjs`가 24개 다국어 파일, 캐시 버전, URL 기반 언어 판별, 공통 UI 보강 클래스와 CJK 모바일 폭 보호 규칙을 검사한다.
- 화면 상단의 작은 `Google` 표시는 사이트 소스가 아니라 사용자의 브라우저 번역 기능이 삽입한 UI다. 저장소에는 Google 번역 위젯 코드가 없으므로 사이트 결함으로 제거하지 않았다.

## 2026-08-31 메인 강한 비·많은 눈 아이콘 겹침 방지

- 메인 날씨 카드의 `강한 비` 상태가 `🌧️🌧️` 두 글자를 58px 아이콘 슬롯에 넣어 Windows 이모지 글꼴에서 두 줄로 접히고 안내문을 가리던 문제를 수정했다. `강한 비`와 같은 잠재 문제가 있던 `많은 눈` 모두 한 슬롯에 단일 아이콘만 표시하며, 상태의 강도는 기존 한글 레이블과 배경 효과로 전달한다.
- 모든 날씨 아이콘의 가상 요소에 줄바꿈 금지를 명시하고 `tools/test-seo-surfaces.mjs`에서 복수 강수 이모지의 재유입과 홈 CSS 캐시 버전을 검사한다. 홈의 공통 CSS 버전은 `20260831-1`로 갱신했다.

## 2026-08-30 전 페이지·칼럼 오류 전수 점검 및 회귀 방지

- 사이트맵 61개 공개 문서와 칼럼 22편을 PC·태블릿·모바일 기준으로 점검했다. 페이지 파일, title·description·canonical·단일 H1, JSON-LD 구문, 파비콘, 이미지·내부 링크, RSS·사이트맵 연결, 가로 넘침과 콘솔 오류를 확인했다.
- 김민지 대표원장이 직접 작성한 `columns/depression-no-hope.html`의 본문·구성·전용 디자인은 변경하지 않았다. 옛 파비콘과 공통 CSS 버전, 홈·Contact 주소, 누락된 구조화 데이터와 이미지 크기 속성만 보완했다. 화면에 사용되는 PNG 두 장은 같은 크기·구도의 WebP로 교체해 약 3.96MB에서 약 0.26MB로 줄였고 원본 PNG는 보존했다.
- `columns/diet-herbal-medicine-price.html`의 3열 비교표가 390px 모바일에서 오른쪽 열이 잘리던 문제는 키보드로도 접근 가능한 가로 스크롤 영역을 추가해 수정했다. 칼럼 공통 CSS 캐시 버전은 공개 칼럼 22편과 발행 템플릿 모두 `20260830-1`로 맞췄다.
- RSS에 `depression-functional-recovery`가 두 번 들어간 중복 항목을 제거했고, `attunement-two-voices.html`의 존재하지 않는 페이지 내부 `#contact` 링크를 실제 홈 Contact 영역인 `/#info`로 연결했다.
- 배포 워크플로에 칼럼 발행기·Columns 검색·SEO 표면 회귀 테스트를 추가했다. `tools/test-seo-surfaces.mjs`는 모든 공개 칼럼의 구조화 데이터와 공통 CSS 버전, RSS 중복·개수, 최신 칼럼 WebP 용량, 모바일 표 스크롤 구조까지 검사한다. 오류가 있으면 GitHub Pages 구성 전에 배포가 중단된다.
- 기상청 API가 일시적으로 실패해도 마지막 정상 날씨로 Pages 배포는 완료되는데 작업 전체가 실패로 표시되던 운영 상태를 바로잡았다. 날씨 갱신 실패는 경고로 남기되, 실제 배포와 사이트 검사가 성공하면 워크플로도 성공으로 기록한다.

## 2026-08-28 공진단 칼럼 하단 UI·전체 칼럼 반응형 보정

- `columns/gongjindan.html` 하단의 참고 자료와 상담 CTA가 전용 클래스만 있고 대응 CSS가 없어 전역 `section` 여백과 기본 링크 스타일로 표시되던 문제를 수정했다. 참고 자료는 본문 폭의 절제된 서지 목록으로, 상담 CTA는 기존 칼럼과 같은 딥그린 카드·반응형 버튼으로 복구했다.
- 넓은 비교표나 가로 카드가 있는 칼럼에서 모바일 단일 열의 grid item이 콘텐츠의 최소 너비만큼 늘어나 본문을 화면 밖으로 밀어낼 수 있던 공통 원인을 `css/style.css`의 900px 이하 `.column-article__layout > * { min-width: 0; }` 규칙으로 막았다. 공진단 조제 단계 카드는 카드 영역 안에서 가로 스크롤되고, 환절기 피부 칼럼의 비교표도 본문 폭 안의 스크롤 영역으로 유지된다. 공통 CSS 캐시 버전은 기존 공개 칼럼 21개와 발행 템플릿 모두 `20260828-1`로 맞췄다.
- 공개 칼럼 21개를 1440×900, 768×1024, 390×844에서 전수 검사했다. 문서 가로 넘침, 본문·목차 잘림, 단일 H1, 중복 ID, 공통 스타일시트, 푸터, 로드된 이미지 오류를 확인했으며 수정 후 전 항목을 통과했다. 21개 칼럼의 로컬 이미지·CSS·JS 경로도 모두 실제 파일과 일치한다.

## 2026-08-26 검사 결과와 증상 진료 철학 칼럼 발행

- `columns/map-is-not-the-territory.html`을 공개하고 Columns 최신 글, 사이트맵, RSS, 최신 칼럼 메가 메뉴 데이터에 연결했다.
- 검색 질문형 제목은 `검사 결과는 정상인데 증상이 계속되는 이유는?`이며, 알프레드 코르지브스키의 `The map is not the territory` 개념을 MRI·혈액검사·통증점수와 실제 증상·기능 변화의 차이를 설명하는 진료 철학으로 연결한다.
- 전문직 시험을 장기간 준비한 만성 목·어깨 통증·피로 사례는 개인정보를 제외한 `DEAR CASE` 3단계 카드로 구성했다. 첫 내원·경과 확인·달라진 일상을 세 점과 이동하는 빛으로 연결하며 움직임 최소화 환경에서는 애니메이션을 정지한다.
- 대표 이미지는 Attunement 칼럼의 녹색 책상·황동 클립·노트 구성을 유지하면서 노트 문구를 `The map is not the territory.`로 바꾼 WebP를 사용한다. 페이지에는 FAQ와 BlogPosting·FAQPage 구조화 데이터를 포함했다.

## 2026-08-26 청담 공진단 전환형 칼럼 랜딩

- `columns/cheongdam-gongjindan.html`을 단순 정보글이 아니라 상담 전환을 목표로 한 칼럼형 랜딩페이지로 공개했다. 첫 화면과 본문 중간, 마지막에 네이버 진료 예약·전화 문의 CTA를 배치했다.
- 기존 김민지 대표원장 작성 칼럼과 공진단 독립 상세페이지는 카피·구조를 수정하지 않고 근거와 사진 소스로만 사용한다. 새 청담 칼럼에는 실물 사진, 상담·구성·조제·전달 사양표, 두 개의 상담 CTA가 결합된 `디어 공진단` 상품 선택 모듈을 추가해 상담형 커머스 상세페이지의 정보 위계를 적용했다.
- 목록 썸네일은 `cover-v6.webp` 혼합매체 시안을 사용한다. 아이보리 석재와 브라스 조명의 청담 프라이빗 감정실, 금박 공진단 한 환과 5개 용기·디어 패키지는 실사 질감으로 두고, 기존 디어 사슴의 둥근 얼굴·넓은 흰 주둥이·짧고 포동한 몸·작은 점눈과 단순한 선은 2D 일러스트 그대로 유지한다. 사슴에게 짙은 초록 정장과 보석 감정용 루페를 더해 `귀한 한 환을 감정한다`는 패러디 훅을 만들었다. 실제 패키지 고증에 맞춰 흰색 케이스의 전면 벽 높이만 소폭 높였으며 5개 용기는 그대로 보이게 했다. 첫 화면 훅은 `중요한 분께 드릴 공진단, 선택은 포장 안쪽에서 갈립니다.`로 압축하고 저널 호수·분류·날짜 머리말과 이슈 표기를 유지한다.
- 와디즈의 고성과 건강식품·선물형 프로젝트와 공식 성공 사례를 비교해, 긴 페이지를 대형 문장으로 나누는 대신 한 화면 안에 제품·차별점·근거·CTA가 함께 보이는 짧은 모듈 구조로 재정비했다. PC 히어로는 최대 680px로 제한하고, 연속되던 대형 선언문·인용문 두 구역은 짧은 `WHY DEAR` 상황 훅 하나로 통합했다. 이후 섹션의 제목·여백·카드 높이도 함께 줄였으며 판매형 페이지에서만 예상 읽기 시간 박스를 숨긴다.
- 커머스형 상세페이지의 미감을 강화하기 위해 첫 화면 팩샷을 실제 손으로 패키지를 여는 장면으로 교체하고, 실제 금박 공진단 사진을 작은 인화지 형태로 겹쳐 `선물하는 순간`과 `실제 한 환`을 한 화면에 함께 제시한다. 제품·원료 사진은 아치형 프레임과 부드러운 그림자를 사용하고, 상단에는 읽기 진행선을 추가했다.
- 스크롤 진입 시 문장·카드·실제 사진이 짧은 간격으로 순차 등장하고 히어로 사진이 최대 18px만 천천히 이동한다. 반복 재생이나 과한 튕김은 사용하지 않으며 `prefers-reduced-motion` 환경에서는 진행선과 이동·등장 효과를 모두 정지한다. 첫 화면 아래의 세 가지 원칙은 전역 칼럼 여백의 영향을 받지 않도록 다시 얇은 증거 띠로 고정했다.
- 정적인 목적 카드 3개는 `부모님·가족 / 중요한 시기 / 나 자신`을 직접 선택하는 접근 가능한 탭형 컨시어지로 교체했다. 선택에 따라 실물 사진, 훅 문장, 상담 전 확인 항목, 예약 CTA가 함께 바뀌며 좌우 방향키·Home·End 키로도 조작할 수 있다.
- 분리되어 있던 원료 사진 갤러리와 4단계 조제 카드는 하나의 `FROM MATERIAL TO ONE PILL` 스토리로 합쳤다. PC에서는 왼쪽 실제 사진이 고정되고 오른쪽 설명을 읽을 때 `약재 확인 → 처방 → 원내 조제 → 전달` 장면과 번호·캡션이 교체된다. 모바일에서는 고정 효과 대신 각 단계에 해당 실물 사진을 직접 배치해 내용과 사진이 떨어지지 않도록 했다.
- 커스텀 랜딩 구조 밖의 `.column-article__content`에만 적용되던 공통 NAP 배경 규칙을 페이지 전용 선택자로 보완했다. 하단 병원 정보는 PC 900px 카드·모바일 좌우 28px 여백으로 고정하고 짙은 초록 배경, 흰 글자와 정상적인 내부 여백을 명시해 흰 배경에 흰 글자가 나타나던 문제를 막았다.
- 가격·할인·특가·금액은 공개하지 않는다. 실제 사향과 구성 약재, 대표원장 상담, 원내 직접 소량 조제, 선물 전 확인사항, 포장·복용·수령 안내를 선택 근거로 제시한다. `tools/test-seo-surfaces.mjs`가 숫자형 금액과 할인 표현의 재유입을 검사한다.
- 기존 실제 공진단 패키지·사향·약재·원내 조제 사진만 사용했으며, 치료후기·효과 보장·치료 전후 비교는 넣지 않았다. 모바일 390px과 PC 1280px에서 가로 넘침, 탭의 사진·카피 전환, 조제 스토리의 장면 전환, 이미지 로드, CTA 링크, 단일 H1과 콘솔 오류 0건을 확인했다.
- 새 글은 `Restore` 분류의 최신 `DEAR JOURNAL · 20`으로 Columns 대표 글·검색 SERP·RSS·sitemap·최신 칼럼 메가 메뉴 이미지에 연결했다. canonical과 `MedicalWebPage`·`Article`·`BreadcrumbList`·`FAQPage` 구조화 데이터도 포함한다.

## 2026-08-26 전 페이지 파비콘 통일

- 일부 최신 칼럼과 칼럼 발행 템플릿에 남아 있던 옛 나뭇가지 `favicon.svg` 참조를 제거하고 메인과 같은 `assets/images/dear-favicon.png`로 통일했다.
- 현재 공개된 사이트맵 59개 문서는 모두 디어 파비콘을 사용하며, 새 칼럼 발행기와 이전 PowerShell 작성기도 같은 파비콘을 생성한다. `tools/test-seo-surfaces.mjs`가 옛 파비콘 참조의 재발을 검사한다.

## 2026-08-26 SEO·GEO·AEO 기반 및 칼럼 발행 안정화

- 화면 디자인·확정 카피·색상·애니메이션은 바꾸지 않고 검색엔진과 발행 운영이 읽는 기반만 보강했다.
- BE DEER 소개와 17개 체성분 기록 페이지에 `MedicalWebPage`·`Service`·`CollectionPage`·`BreadcrumbList` 구조화 데이터를 연결했다. 치료 결과를 평가하는 `Review` 표시는 사용하지 않는다.
- 한국어 루트·칼럼 페이지의 홈/Contact 링크는 중복 주소인 `index.html` 대신 `/`와 `/#info`로 통일했다. 다국어 폴더의 언어별 홈 링크는 기존 상대 경로를 유지한다. 새 칼럼 발행기도 같은 주소를 생성한다.
- 국제 환자 예약 페이지의 언어별 제목은 하나의 H1 안에서 전환되도록 수정해 화면 문구와 동작은 유지하면서 문서당 H1을 하나로 정리했다.
- 2026-08-25 체중 칼럼의 PNG 세 장은 같은 크기의 WebP로 교체했다. 참조 용량은 약 5.5MB에서 약 0.43MB로 감소했으며 원본 PNG는 저장소에 보존한다.
- 로컬 칼럼 작성기는 업로드 사진을 최대 변 2,000px·WebP 품질 90으로 자동 변환한다. CLI 발행기는 1MB를 넘는 JPG·PNG를 차단해 최적화하지 않은 대형 이미지가 다시 공개되지 않게 했다.
- 발행기의 Git 준비 목록에 `rss.xml`과 `assets/data/latest-column.json`을 추가했다. 최신 칼럼 메뉴 이미지의 대체 텍스트가 비어 있으면 제목을 사용하며, 발행된 Article 구조화 데이터에는 카테고리별 Care·Service 연결을 기록한다.
- `node tools/test-seo-surfaces.mjs`는 사이트맵 파일 존재, title·description·canonical·단일 H1, JSON-LD 구문, 최신 칼럼 이미지·링크·alt, Columns 카드의 sitemap/RSS 연결과 한국어 `index.html` 링크 재발을 한 번에 검사한다. 기존 발행기·SERP 테스트도 공개 칼럼 20편 기준으로 갱신했다.

## 2026-08-25 체중과 부주의 맹시 칼럼 발행

- `columns/weight-inattentional-blindness.html`을 공개 발행했다. 보이지 않는 고릴라 실험을 바탕으로 체중 한 가지 수치에 집중할 때 체지방·근육량·부종·허리 사이즈·옷 핏과 생활 상태를 놓칠 수 있다는 점을 설명한다.
- 전신거울 앞에서 팔뚝을 살피는 사슴 그림을 대표 이미지로 사용하고, 본문의 고릴라 삽화 두 장에는 디어한의원 저널 프레임을 적용했다. 원형 건강 지표 다이어그램은 혈압·혈당·체온·심박수·체중·체지방률을 함께 보여주며 데스크톱에서 각 원에 마우스를 올리면 입체적으로 확대된다.
- 첫 화면의 핵심 답변, FAQ 4개, 참고자료와 `MedicalWebPage`·`Article`·`BreadcrumbList`·`FAQPage` 구조화 정보를 포함했다. Columns 오늘의 글·최신 카드, RSS와 sitemap에도 연결했다.
- 마지막 문장 카드는 짙은 초록색 배경과 카드 안에 완전히 보이는 `DEAR` 워터마크를 사용한다. 확정 문구는 `아직 변화하지 않은 것이 아니라, / 우리가 다른 것을 세느라 보지 못했던 것입니다.`이다.

## 2026-08-25 Attunement 진료 철학 칼럼 발행

- `columns/attunement-two-voices.html`을 공개하고 Columns 최신 글 18번, 사이트맵, RSS, 최신 칼럼 메가 메뉴 데이터에 연결했다.
- 심리학의 `attunement`와 Daniel Stern의 `affect attunement`를 설명한 뒤, 환자의 경험과 의료진의 판단이 서로 다른 위치와 역할을 유지하면서 조화를 찾는 진료의 관점으로 확장한다. 별도 Q&A는 두지 않았다.
- 대표 이미지는 짙은 초록색 책상 위 노트·볼펜·디어한의원 명함과 사슴 로고가 음각된 황동 클립으로 구성한 정사각형 WebP를 사용하며, 본문 대표 이미지와 칼럼 썸네일·OG 이미지가 같은 파일을 공유한다.
- 검색 공개 메타, BlogPosting·Breadcrumb 구조화 데이터, 작성자 김민지 대표원장, 발행일과 참고문헌을 포함했다.

## 2026-08-24 최신 칼럼 메가 메뉴 이미지 자동 갱신

- 한국어 상단 `Columns` 메가 메뉴의 왼쪽 이미지는 더 이상 특정 칼럼에 고정되지 않는다. `assets/data/latest-column.json`을 읽어 가장 최근 공개 칼럼의 목록 대표 이미지를 표시한다.
- 칼럼 공개 발행·삭제·`--refresh-index` 실행 시 `tools/publish-column.mjs`가 게시일 기준 최신 글을 다시 계산해 이 데이터 파일도 함께 갱신한다. 미리보기는 공개 메뉴 데이터를 바꾸지 않는다.
- 낮고 긴 메뉴 프레임에서도 인물·사슴 얼굴이 먼저 보이도록 기본 이미지 초점은 가로 중앙·세로 상단 30%(`50% 30%`)로 둔다. 데이터 요청에 실패하면 `js/main.js`에 포함된 배포 시점 최신 이미지가 대체 표시된다.

## 2026-08-24 체질한약 영문 서비스명 정비

- 화면에 표시되던 `HERBAL DECOCTION`을 한의진료 전체가 아니라 한약 서비스를 정확히 가리키는 `KOREAN HERBAL MEDICINE`으로 변경했다.
- 한국어·영어·일본어·중국어 Services와 공통 메가 메뉴 표기를 통일했다. 기존 `#herbal-decoction` 앵커와 CSS 클래스는 과거 링크 호환을 위해 유지한다.

## 2026-08-24 디어 공진단 독립 상세페이지

- 실제 촬영 영상 없이 기존 공진단 제품·약재·원내 조제 사진을 교차 편집하는 스틸 필름 히어로를 `dear-gongjindan.html`에 구현했다. 자동 전환은 일시정지·다음 장면 조작을 제공하고 움직임 최소화 환경에서는 자동재생하지 않는다.
- 약재 선별·보관 및 전처리·원내 조제·복용 안내의 네 단계를 키보드로도 탐색할 수 있는 탭, 제품 갤러리, 진료 판단 3단계, 대표원장 편지, FAQ와 네이버 예약·전화 CTA로 이어지는 독립 서비스 페이지다.
- `services.html` 첫 화면의 공진단 카드와 공진단 소개 사진 아래 CTA는 새 상세페이지로 연결한다. 그 아래 기존 4개 텍스트 바로가기 목록은 같은 페이지의 공진단 요약 섹션 앵커를 유지한다.
- 독립 페이지의 편지 제목은 두 줄을 고정해 낱글자 줄바꿈을 막았고, 제품 갤러리의 영문 캡션에는 환자가 이해할 수 있는 한글 설명을 함께 표시한다.
- 공진단 칼럼의 원내 조제·한 사람을 위한 처방 흐름을 시각적으로 이어 받아, 기준 섹션의 금박 공진단 사진 주위에 사향·녹용·당귀·산수유 네 궤도가 서로 다른 속도로 움직이도록 구성했다. 움직임 최소화 환경에서는 모두 정지한다.
- 기준 섹션의 대형 문장은 약 절반 수준으로 줄이고 세 문장 호흡을 고정해 읽기 쉽게 정리했다.
- 후속 화면 피드백에 따라 첫 화면 사진은 다시 각 프레임을 꽉 채우는 방식으로 전환하고 장면별 초점을 유지했다. 사진 번호 사이의 선에는 4.4초 전환 주기와 일시정지 상태에 맞춰 좌우로 이동하는 흰 진행점을 추가했다.
- 금박 공진단의 중앙 사진을 줄여 네 약재 궤도와 `ONE PERSON` 문구가 겹치지 않게 분리했다. 약재 라벨 가운데를 가리던 궤도용 초록 점은 제거해 사향·녹용·당귀·산수유가 또렷하게 보이며, 네 궤도는 28~52초의 느린 속도로 움직인다. 마지막 편지의 왼쪽 사진은 메인과 동일한 국내·세계 배송 지도 모션으로 교체했다.
- 새 페이지는 기존 공진단 사진만 사용하며 제공되지 않은 영상·사진을 만들지 않았다. canonical·MedicalWebPage 구조화 정보와 sitemap을 함께 연결했다.

## 2026-08-24 DEAR SERVICES 첫 화면 CTA 연결

- `services.html` 첫 화면 오른쪽의 기존 2×2 대표 이미지 네 장을 사진과 그린 CTA가 결합된 전체 클릭 카드로 바꿨다.
- BE DEER 카드는 독립 소개 페이지 `be-deer.html`로 연결하고, 공진단·체질한약·DEER BALANCE 카드는 같은 페이지의 각 상세 섹션으로 내려간다.
- 첫 화면 아래의 두 번째 4개 텍스트 바로가기 목록과 BE DEER 상세 섹션의 `감량 사례 보러가기` CTA는 기존 역할과 링크를 그대로 유지한다.

## 2026-08-24 메인 안내 팝업 영구 숨김

- 메인 첫 진입 안내 팝업은 체크박스 없이 `닫기`를 누르는 즉시 팝업별 영구 숨김 상태를 브라우저 `localStorage`에 저장한다. 같은 브라우저에서는 쿠키·사이트 데이터 등 저장정보를 직접 삭제하기 전까지 해당 팝업을 다시 표시하지 않는다.
- 과거 `오늘 하루 열지 않음`을 선택해 날짜가 저장된 팝업도 새 영구 숨김 키로 자동 승계한다. 저장소 사용이 차단된 브라우저에서는 오류로 페이지 동작을 멈추지 않고 현재 화면에서만 닫는다.
- 화면 하단 안내는 `닫으면 다시 표시되지 않습니다`로 바꿨으며 팝업 세 개의 숨김 상태는 서로 독립적으로 유지한다.

## 2026-08-24 Columns 무검색어 방지 SERP

- Columns 검색은 검색어가 카드의 `data-search`에 직접 포함되지 않아도 환자가 입력한 증상·생활 상황·목적을 동의어와 Care 의미군으로 확장해 관련성 순으로 결과를 구성한다.
- 목 답답함·신물, 야식·식욕, 기운 없음·어지러움처럼 일상 문장으로 입력해도 가까운 칼럼을 먼저 보여 주고, 해당 Care와 DEAR SERVICES·BE DEER 동선을 함께 제안한다. 검색 결과는 진단이나 치료 추천이 아니라 사이트 안의 관련 정보를 찾는 탐색 결과로 안내한다.
- 직접 일치 결과가 부족해도 최소 6편을 제시한다. 의미군을 찾았으면 같은 주제의 칼럼으로 채우고, 전혀 해석할 수 없는 문장은 Focus·Calm·Restore·Relief·Shape가 골고루 포함된 시작점을 제공한다. 따라서 검색 중 `결과 없음` 화면을 환자에게 노출하지 않는다.
- 새 칼럼은 기존 CMS의 `검색 주제 태그`와 제목·요약·분류가 발행기의 `data-search`에 합쳐져 자동으로 SERP 색인에 들어간다. `node tools/test-columns-serp.mjs`는 대표 질문과 무관 문장의 최소 결과를, `node tools/test-column-publisher.mjs`는 새 카드의 분류·검색 태그 생성을 확인한다.
- 사용자가 제공한 2026-08-24 화면에서 네이버 서치어드바이저는 메인 문서의 성공적인 색인과 Meta 정상 상태, Google Search Console은 `https://dearhani.com/`의 등록·페이지 색인 생성·HTTPS 정상 상태를 표시했다.

## 2026-08-24 메가 메뉴 hover 이동 안정화

- PC 상단 메뉴는 링크에서 펼쳐진 메가 메뉴로 마우스를 내릴 때 즉시 닫히지 않도록 명시적인 열림 상태와 360ms 닫힘 여유 시간을 사용한다.
- 메가 메뉴 안으로 포인터가 들어오면 닫힘 예약을 취소하고, 다른 상단 항목으로 이동하면 해당 항목의 메뉴로 전환한다.
- 하위 링크 클릭은 PC에서는 본래 목적지로 그대로 이동하며, 모바일에서는 아코디언 메뉴를 닫은 뒤 목적지로 이동한다.

## 2026-08-24 Contact 메인 NAP 동선 통일

- 모든 상단 메뉴의 `Contact`는 현재 페이지의 푸터나 칼럼 내부 NAP가 아니라 홈페이지 메인의 `#info` 진료문의·진료시간·주소 CTA 영역으로 이동한다.
- 한국어 루트와 칼럼 상세페이지는 canonical 홈 주소인 `/#info`를 사용하고, 다국어 페이지는 각 언어의 `index.html#info`를 사용한다.
- 칼럼 발행기와 로컬 작성기 템플릿도 같은 링크를 사용해 새 글에서 이전 `#contact` 링크가 다시 생기지 않도록 했다.

## 2026-08-24 Columns 대화형 콘텐츠 탐색

- Columns 상단 검색창에 문장형 질문을 입력하면 키워드만 남기는 필터 대신 `디어한의원에는 이런 정보들이 있어요!` 검색 결과 화면을 연다.
- 검색어의 의미를 Care의 `Focus · Calm · Restore · Relief · Shape`에 맞춰 분류하고, 인스타그램 스토리처럼 원형 주제 썸네일과 관련 칼럼 카드, 이어서 볼 Care·DEAR SERVICES·BE DEER 콘텐츠를 함께 제안한다.
- 검색 결과의 주제 썸네일을 누르면 해당 Columns 카테고리 목록으로 전환되고, `전체 칼럼 다시 보기`로 기본 목록을 복원한다. 검색어는 `?q=`에 보존되어 공유·뒤로가기가 가능하다.
- 모바일에서는 검색 결과가 카테고리 막대보다 먼저 나타나며, 주제는 가로 스크롤, 칼럼은 2열 카드로 구성하고 가로 넘침이 생기지 않도록 맞췄다.

## 2026-08-24 전 메뉴 메가 내비게이션

- 한국어 상단의 `About DEAR`, `Columns`, `Care`, `DEAR SERVICES`, `Contact` 모두에 공통 메가 메뉴를 추가했다.
- PC는 hover·키보드 focus·화살표 click으로 같은 규격의 전체 폭 패널이 열리고, 모바일 햄버거에서는 각 메뉴 오른쪽 화살표로 아코디언을 연다.
- Columns 하위 메뉴는 다섯 필터 URL, Care 하위 메뉴는 `?care=Focus|Calm|Restore|Relief|Shape`로 연결하며 Care는 해당 패널을 첫 화면부터 활성화한다.
- Services의 미완성 상세페이지는 현재 각 `services.html` 앵커로 연결하고 BE DEER만 기존 상세페이지로 연결한다.
- 전 공개 HTML의 CSS·공통 JS 캐시 버전을 `20260824-7`·`20260824-4`로 올렸다.
- 각 메가 메뉴의 왼쪽 소개 블록에는 해당 영역의 대표 이미지를 낮은 대비로 배치하고 글자를 약 11pt로 키웠다. About 인물 사진은 얼굴이 보이도록 배경 초점을 별도로 지정한다.

## 2026-08-24 BE DEER 서비스 CTA·Columns H1 보강

- Services 01의 제목과 소개를 BE DEER의 통합 진단 관점 카피로 교체했다.
- 우측 이미지 아래 CTA는 `BE DEER 다이어트 / 감량 사례 보러가기` 문구와 그린 그러데이션·광원·화살표 펄스 효과를 유지하되, 독립 BE DEER 소개 페이지 `be-deer.html`로 연결한다.
- Columns 검색 상단에는 화면 흐름에 맞는 `디어한의원 건강 칼럼` H1을 복원했다.

## 2026-08-24 Columns 검색 중심 상단 개편

- 칼럼 상단의 대형 소개 문구와 설명, `관심 있는 주제부터` 라벨을 제거했다.
- 기존 칼럼 검색을 상단 중앙의 떠 있는 원형 검색창으로 이동했고, 전체와 Care 5개 분류를 그 아래 한 줄로 균등 정렬했다.
- 검색창 아래에는 저녁 식사·다이어트 한약 가격·공진단 질문형 추천 검색어를 떠 있는 해시태그 칩으로 제공하며, 누르면 실제 검색 결과로 연결한다.
- 작은 화면에서는 여섯 분류를 가로 스크롤로 유지하며 검색 기능과 URL 카테고리 필터 동작은 그대로 보존한다.

## 2026-08-24 Care·Columns 다섯 분류 통합

- Columns의 공개 분류를 Care와 같은 `Focus · Calm · Restore · Relief · Shape` 다섯 가지로 통일했다. 별도 `성장·체중·마음·소화·피부·한약` 필터는 사용하지 않는다.
- 기존 17편은 다섯 분류 안에 다시 배치했다. 수험생 집중은 Focus, 우울·불면·마음 회복은 Calm, 공진단·피부 회복은 Restore, 소화·통증·신체 불편은 Relief, 체중·대사·월경·산후 체중·소아성장은 Shape로 연결한다.
- Care의 각 설명 패널에서 `columns.html?category=Focus` 형식으로 해당 Columns 필터를 바로 열 수 있다. Columns는 URL의 category 값이 다섯 분류가 아니면 전체로 안전하게 되돌린다.
- 칼럼 발행기·로컬 작성기·CMS 설치 템플릿도 같은 다섯 이름과 `인지·집중 / 긴장·수면 / 피로·회복 / 통증·불편 / 체중·리듬` 표기를 사용한다.

## 2026-08-24 아이폰 모바일 정렬 보정

- 모바일 하단 퀵메뉴 위로 따로 떠 있던 `다이어트 저널` CTA를 다른 버튼과 같은 행의 7번째 버튼으로 편입했다. 아이폰 폭에서는 일곱 버튼이 동일한 높이와 폭으로 나뉘며 별도 알약형 플로팅 버튼을 사용하지 않는다.
- BE DEER 첫 화면 사진은 모바일에서 4:3 `cover`로 자르지 않고 원본과 같은 3:2 비율로 전체를 표시한다. 공통 `.hero`의 `height: 100svh`가 첫 섹션을 화면 높이로 잘라내지 않도록 모바일에서는 `height: auto`로 해제해 사진 전체가 다음 섹션 위에 등장한다.
- Columns 모바일은 히어로·주제 선택·대표 글·검색·카드 목록의 좌우 기준선을 20px로 통일한다.

## 2026-08-24 상단 메뉴와 BE DEER 진입 동선 정리

- 한국어 공개 페이지의 상단 내비게이션에서 독립 `BE DEER` 항목을 제거해 `About DEAR · Columns · Care · DEAR SERVICES · Contact` 구조로 정리했다.
- `services.html`의 01번 `BE DEER` 서비스 사진 바로 아래에 `BE DEER 다이어트 자세히 보기` CTA를 추가하고 기존 `be-deer.html` 독립 소개 페이지로 연결했다.
- 메인 본문과 칼럼 하단 등 문맥 안에서 제공하던 기존 BE DEER 연결은 유지한다. 상단 메뉴가 아니라 `DEAR SERVICES → BE DEER`의 정보 흐름을 기본 진입 경로로 사용한다.

## 2026-08-22 서초동 현재 강수 교차 확인

- 기상청 초단기실황이 최신이어도 격자 관측에서 강수를 놓칠 수 있으므로, 메인 날씨는 서초동 고정 좌표의 Open-Meteo 현재 강수를 함께 확인한다.
- 기상청이 비·눈을 표시하지 않지만 좌표 기반 현재 관측에서 실제 강수량이 0보다 크고 비·눈 상태가 확인될 때만 좌표 관측을 우선한다. 보조 조회가 실패하거나 강수가 없으면 기존 기상청 최신값을 유지한다.

## 2026-08-21 진료 사례 카드 장기 디자인 기준 확정

- 김민지 대표원장은 환절기 속건조·엑소좀 칼럼의 `CASE` 카드가 읽기 좋다고 확정했다. 이후 진료 사례가 포함된 칼럼은 특별한 요청이 없으면 이 프레임과 정보 위계를 기본값으로 사용한다.
- 기본 구성은 원형 `CASE` 표식, 큰 사례 제목, 익명화된 연령대·성별·주요 고민, 시간 순서형 3열 경과 카드, 하단의 비식별·개인차 안내다. PC에서는 3열, 모바일에서는 세로 흐름으로 유지한다.
- 현재 기준 구현은 `columns/autumn-dry-skin-exosome-booster.html`의 `.clinic-story`와 관련 스타일이다. 새 사례에서는 문구와 단계명만 사례에 맞게 바꾸고, 차분한 배경·얇은 테두리·넉넉한 여백과 정보 위계는 유지한다.
- 환자 이름, 정확한 나이·날짜, 연락처, 차트 번호, 얼굴 등 식별 정보는 기록하지 않으며 치료 결과를 보장하는 표현도 사용하지 않는다.

## 2026-08-21 환절기 속건조·엑소좀 스킨부스터 칼럼 발행

- `columns/autumn-dry-skin-exosome-booster.html`을 공개 발행했다. 가을철 속건조의 원인, 히알루론산·PN·콜라겐 자극·엑소좀 계열의 차이, 피부 고민별 선택 기준과 엑소좀의 장점·근거 한계를 함께 설명한다.
- 대표 이미지는 테라코타색 티셔츠와 헤어 터번을 한 디어 사슴이 거울 속 윤광이 나는 얼굴을 바라보는 정사각형 웹툰이다. 칼럼 본문에서는 테두리 없이 크게 표시하고, Columns의 오늘의 글과 목록 카드에서만 검은 웹툰 프레임을 사용한다.
- 본문 중간에는 중학생 남성의 염증성 여드름과 턱 주변 트러블 진료 경과를 익명 3단계 카드로 구성했다. 개별 진료 경과이며 동일 결과를 보장하지 않는다는 안내와, 피부 상태에 따라 트러블 진정·피부톤 및 색소 고민을 고려한 약침을 병행할 수 있다는 설명을 포함한다.
- `RESTORE · 피부` 분류와 Columns 주제 필터를 추가하고 누적 `DEAR JOURNAL · 16`으로 오늘의 글·최신 카드·CollectionPage ItemList·RSS·sitemap에 연결했다. FAQ 5개와 `MedicalWebPage`·`Article`·`BreadcrumbList`·`FAQPage`, 저자·검수자·참고자료를 포함한다.

## 2026-08-21 역류성 식도염 한약 치료 칼럼 발행

- `columns/reflux-esophagitis-herbal-medicine.html`을 공개 발행했다. 속쓰림·신물·목 이물감과 식후 팽만을 하부식도괄약근, 위 배출 지연, 식도 청소능, 내장 과민성의 관점에서 설명하고 개인별 한약 처방의 목표와 치료 기간을 정리했다.
- 대표 이미지는 노란 티셔츠를 입은 사슴이 목과 배의 불편을 표현하는 웹툰이며, 본문에는 속쓰림·신물과 트림·목 이물감 3칸 픽토그램 카드를 사용한다.
- Columns 목록과 오늘의 글에는 검은 테두리까지 정사각형으로 다시 구성한 `thumbnail-square.png`를 사용하고, 칼럼 본문의 대표 이미지는 기존 가로 그림을 유지한다.
- 데스크톱과 폭 680px 이상의 화면에서는 목차를 본문 왼쪽에 두며, 더 좁은 모바일에서는 본문 위 목차로 전환한다. 치료 목표 3개, 복용 시점 3단계, 생활관리 6개를 서로 구분되는 카드로 구성했다.
- 사용자가 제외 요청한 `단순히`, `흐름`, `균형` 표현은 이 칼럼의 화면 원고에서 사용하지 않는다. 연구 근거 문단은 반복 증상에서 한의학적 치료를 선택지로 제시하되 효과 보장 표현은 사용하지 않는다.
- `columns.html`, `sitemap.xml`, `rss.xml`에 칼럼을 연결하고 `MedicalWebPage`·`Article`·`BreadcrumbList`·`FAQPage` 구조화 정보와 참고자료를 포함했다.

## 2026-08-20 다이어트 한약 가격·비용 칼럼 발행

- `columns/diet-herbal-medicine-price.html`을 공개 발행했다. 환과 개인별 탕약의 제조·조제 과정, 비용 차이를 만드는 항목, 상담 전 확인사항과 FAQ 4개를 담았다.
- 대표 이미지는 작아진 티셔츠와 바지를 입은 사슴이 침대에서 디어한의원으로 걸어가는 모습을 떠올리는 정사각형 웹툰이며, 본문과 Columns 카드에서 테두리가 잘리지 않도록 전체 보기 방식으로 표시한다.
- 검색·AI 검색 이해를 위해 첫 본문에 직접 답변을 두고 `MedicalWebPage`·`Article`·`BreadcrumbList`·`FAQPage`, 저자·검수자·의료기관·참고자료 정보를 연결했다.
- `columns.html`, `sitemap.xml`, `rss.xml`에 새 칼럼을 연결했으며, 사용자가 제외 요청한 표현은 이 칼럼 원고에서 사용하지 않는다.

## 2026-08-20 수험생 보약 칼럼 발행

- `columns/student-herbal-medicine.html`을 공개 발행했다. 시험 때 반복되는 잦은 배뇨와 특정 과목에서 나타나는 두통 사례를 중심으로, 일률적인 체력 보충보다 학생의 증상·체질·시험 상황을 함께 살피는 개별 한약 처방의 의미를 설명한다.
- 대표 이미지는 회색 반팔을 입은 수험생 사슴이 공부하고 엄마 사슴이 열린 방문으로 바라보는 장면이다. 책상 위 디어 한약 파우치는 초록색 로고, 일자형 하단, 갈색 한약 내용물, 뜯은 모서리에 꽂힌 빨대를 반영한 `cover-deer-v4.png`를 최종본으로 사용한다.
- 본문은 기존 칼럼 여백을 따르며 비교표, 두 임상 이야기 카드, 증상 픽토그램, 확인 목록과 FAQ 5개를 소량 배치했다. 비교표는 목차를 침범하지 않도록 본문 폭 안에 두고, 각 소제목 아래 여백과 섹션 사이 구분선을 사용한다.
- Columns의 오늘의 글·최신 카드, CollectionPage ItemList, RSS와 sitemap에 연결하고 김민지 대표원장을 저자·검수자로 명시했다.

## 2026-08-19 대표·협업자 역할과 확정사항 기록 원칙

- 디어한의원의 대표이자 홈페이지 운영의 최종 의사결정권자는 김민지 대표원장이다.
- 박성호(삥이)는 기획·카피 협업자이며 디어한의원의 대표가 아니다. 이후 문서와 대화에서 두 사람의 역할을 혼동하지 않는다.
- 김민지 대표원장이 대화에서 한 번이라도 명확하게 확정한 중요한 카피, 디자인, 의료 표현, 운영 기준은 후속 작업자가 놓치지 않도록 같은 작업에서 이 인수인계문에 기록한다.
- 어린이 성장 한약 칼럼은 `columns/child-growth-herbal-medicine.html`로 2026-08-19 공개 발행했다. Columns의 오늘의 글·최신 카드, RSS와 sitemap에 연결하고 김민지 대표원장을 저자·검수자로 명시했다.
- 이 칼럼의 본문 섹션 여백은 별도 임의 수치가 아니라 기존 `DEAR JOURNAL` 칼럼과 같은 리듬을 사용한다. BEFORE·AFTER 그림은 본문 폭보다 조금 넓은 최대 860px, 2:1 비율로 두되 위아래 여백을 과도하게 벌리지 않는다. 모바일에서는 한 장씩 세로로 배치한다.
- 성장 칼럼의 확정 카피는 `아이의 건강 상태를 확인하고 전문가인 한의사가 처방한 성장 한약이 간에 악영향을 준다는 근거는 전혀 없습니다.`이다. 감기약·항생제도 간수치에 영향을 줄 수 있다는 설명, 성장 한약이 살찌는 체질로 바꾼다는 근거가 없다는 설명, 성장 결과는 치료 시기에 따라 다르다는 Q5 표현을 유지한다.
- BEFORE·AFTER 캐릭터 그림 아래에는 특정 환자의 치료 전후나 결과가 아니라는 별도 안내 문구를 표시하지 않는 것으로 김민지 대표원장이 확정했다. 대표 그림은 초록색 반팔의 아기 사슴과 고봉밥, 디어 한약 파우치 조합이며, BEFORE 그림에는 한약 없이 밥을 거부하며 떼쓰는 모습을 사용한다.

## 2026-08-18 네이버 메인 URL SEO 경고 정리

- 네이버 서치어드바이저가 메인 페이지의 SVG 접근성용 `<title>`까지 문서 제목으로 계산해 `<title> 요소 2개 이상` 경고를 표시하던 문제를 정리했다. SVG의 접근성 이름은 `aria-label`과 `aria-describedby`로 유지하고 문서의 `<title>`은 `<head>` 안의 한 개만 남겼다.
- 날씨 장면과 무한 슬라이드 복제 이미지의 빈 `alt`를 네이버가 누락으로 집계하던 문제를 정리했다. 장식 영역과 복제 세트의 `aria-hidden="true"`는 유지해 스크린리더 중복 노출을 막고, 각 이미지에는 실제 장면을 설명하는 대체 텍스트를 넣었다.
- 배포 후 네이버 서치어드바이저에서 `https://dearhani.com/` 수집 요청을 다시 하고 URL 검사를 갱신해야 새 결과가 반영된다.

## 2026-08-18 오디세이·우울증 일상 회복 칼럼 발행

- 새 칼럼 `columns/depression-functional-recovery.html`을 추가했다. 영화 〈오디세이〉의 이타카 귀환을 우울증 치료 뒤 남는 불면·불안과 일상 기능 회복에 연결하고, 도덕적 상처·잔존 증상·한의진료의 평가와 치료 후 확인 기준을 설명한다.
- 대표 이미지는 종이 질감의 손그림 스타일로 제작한 4:3 가로 오디세우스 일러스트를 WebP로 최적화했다. 이 칼럼에만 PC 대표 이미지 4:3 비율과 축소된 본문 H2를 적용하고 다른 칼럼의 비율과 제목 크기는 유지한다.
- 기능 회복 4영역, 증상에서 회복으로 이어지는 흐름, 평가–치료–재평가, 수면·식사·활동·관계 추적 지표를 HTML/CSS 정보 그래픽으로 구성했다. FAQ 6개와 국내 우울증 한의표준임상진료지침 및 관련 체계적 문헌고찰·추적연구를 출처로 연결한다.
- 검색·AI 검색 이해를 위해 첫 화면에 직접적인 임상 요약을 두고, `MedicalWebPage`·`Article`·`BreadcrumbList`·`FAQPage`, 저자·의학 검토·인용 출처·Open Graph·Twitter Card 정보를 함께 제공한다. 기존 우울증 칼럼과 Care·Services로 이어지는 내부 링크도 추가했다.
- 칼럼 발행기의 허용 HTML에 의미 구조용 `small`·`article`을 추가하고, 생성 페이지 body에 글별 slug 클래스를 부여해 특정 칼럼의 디자인을 안전하게 한정할 수 있도록 했다. 새 발행 템플릿의 CSS 캐시 버전은 `20260818-1`이다.

## 2026-08-17 외국인 예약 퀵메뉴 영문 표기

- 한국어 페이지 오른쪽·모바일 하단 퀵메뉴의 `외국인예약` 레이블을 대상 사용자가 바로 알아볼 수 있는 `Intl. Booking`으로 변경했다. 연결 주소와 영문 예약 화면은 유지한다.
- 기존 퀵메뉴 폭과 높이는 바꾸지 않고 해당 레이블을 14px 두 줄 중앙 정렬로 표시한다. 긴 `International`을 좁은 폭에 맞추기 위해 사용했던 압축 자간은 제거해 다른 버튼과 같은 밀도로 읽히게 했다.
- 메인과 공통 스크립트로 버튼을 생성하는 서브 페이지를 1440px·768px·390px에서 확인했으며, 버튼 내부 넘침과 페이지 가로 스크롤이 없다.

## 2026-08-18 칼럼 최신 글·누적 번호·정식 발행 연결

- 같은 날 공동 작업자가 발행한 `columns/depression-functional-recovery.html`의 원장 작성 본문과 FAQ 문구는 변경하지 않고, 칼럼 목록·검색 노출·향후 발행 자동화만 보완했다.
- `columns.html`의 상단 `오늘의 글`은 더 이상 특정 칼럼에 고정되지 않는다. `tools/publish-column.mjs`가 모든 카드의 게시일을 비교해 가장 최신 칼럼을 대표 글로 갱신하고, 카드·CollectionPage ItemList·RSS도 최신순으로 다시 정렬한다.
- 저널 번호는 화면 순서용 CSS 카운터가 아니라 실제 누적 발행 번호다. 가장 오래된 글이 `01`, 새 글은 전체 칼럼 수에 해당하는 번호를 가지며, 최신순 카드 배치에서는 `11, 10, 09 ... 01`로 보인다. 상단 Cover Story도 최신 글의 동일 번호를 표시한다.
- 새 우울증 칼럼을 Columns 목록, CollectionPage JSON-LD, `sitemap.xml`, `rss.xml`에 연결했다. 칼럼 문서에는 게시·수정일 Article 메타, Twitter 카드 메타, 대표 이미지 크기를 추가했고 전용 반응형 스타일을 공개 저장소에 합쳤다.
- 검증: `node tools/test-column-publisher.mjs`, `node tools/publish-column.mjs --refresh-index`, `git diff --check`, RSS·sitemap XML 파싱, 390/768/1024/1440px Columns와 칼럼 화면 확인. 네 화면 모두 가로 넘침이 없고 FAQ DOM 6개와 FAQPage JSON-LD 6개가 일치했다.

## 2026-08-12 홈페이지 공동 작업·복구 기준

- 삥이와 부끄님이 공개 홈페이지를 함께 개선할 수 있도록 `COLLABORATOR_BRIEF.md`를 추가했다. 새 PC와 Claude·Codex는 `AGENTS.md`의 지시에 따라 `CLAUDE.md`, `HANDOFF.md`, 공동 작업 브리프를 모두 읽고 시작한다.
- 공동 작업 대상은 공개 저장소 `BBINGE/dear-clinic`과 실제 사이트 `https://dearhani.com/`이며 비공개 CMS와 로그인 정보는 제외한다. 작업 브랜치·검증·Pull Request·실제 배포 확인 순서와 공개 저장소 금지 정보를 브리프에 기록했다.
- 2026-08-12 현재 최고 상태는 GitHub 태그 `site-best-2026-08-12`, 커밋 `4d66229`로 고정했다. 복구가 필요하면 해당 태그에서 별도 `restore/` 브랜치를 만들어 확인하며 합의 없이 hard reset이나 강제 push를 실행하지 않는다.

## 2026-08-12 칼럼 CMS 한국형 일반·HTML 에디터 1차 구현

- Cloudflare 관리자 앱의 단순 본문 textarea를 네이버 블로그형 2단 도구막대와 흰 원고지 중심의 일반 모드로 교체했다. 사진·인용구 6종·구분선 8종·파일·표 삽입, 본문/소제목/인용구, 글꼴·글자 크기·기본 글자 서식·색상·정렬·목록·들여쓰기·위아래 첨자·줄 간격·특수문자·링크를 제공한다. 일반 모드와 HTML 모드는 같은 `bodyHtml`을 안전하게 왕복하며 과거 `body` 초안도 계속 연다.
- 글쓰기 중에는 설정 패널을 숨기고 상단 `발행`을 눌렀을 때만 오른쪽 발행 설정이 열린다. 기존 D1 초안 저장, GitHub 비공개 콘텐츠 저장소, 발행 워크플로 연결은 유지한다.
- 본문 이미지와 첨부파일은 대표 이미지를 덮어쓰지 않는 고유 경로에 저장한다. 공개 발행기는 HTML 본문 이미지에 이어 PDF·문서·표·프레젠테이션·한글·TXT·ZIP 첨부파일도 공개 칼럼 자산 폴더로 복사하고 링크를 바꾼다. 허용 형식과 10MB 제한 밖의 파일은 차단한다.
- 네이버 스마트에디터 캡처를 기준으로 관리자 화면의 시각 밀도를 다시 정돈했다. 상단은 54px의 얇은 헤더와 각진 3px 버튼으로 줄이고, 제목·소개·본문을 한 장의 흰 원고처럼 연결했다. 삽입 도구와 글자 도구는 얇은 선과 일정한 클릭 영역의 두 줄 도구막대로 맞췄으며 선택 상태만 옅은 초록색으로 표시한다. Sites 운영 버전 7에서 실제 로그인 세션과 PC 화면을 확인했다.

## 2026-08-11 다이어트 칼럼 하단 배너 2장 정리

- 지방간 칼럼과 '살이 안 빠지는 6가지 이유' 칼럼의 하단 배너를 3장에서 2장으로 줄였다. 사진 비중이 큰 가운데 한방 다이어트 배너를 제거하고 '한약'과 'BE DEER 다이어트 프로그램' 배너만 유지한다. 우울증·BDNF 칼럼의 한약·공진단 배너 2장은 변경하지 않았다.

## 2026-08-11 생리 전 식욕 디툰 1~5컷 원본 복원

- 1~5컷은 이미지 안의 한글이 정상인 원본이므로 HTML 한글 덮어쓰기 레이어와 관련 CSS를 전부 제거했다. 6컷 역시 사용자가 제공한 보정본 자체의 문구를 사용하며, 디툰 6장 모두 이미지 원본 위에 별도 문구 레이어를 두지 않는다.

## 2026-08-11 BE DEER 의료 용어·사례 CTA 및 디툰 6컷 교체

- BE DEER 본편과 체성분 사례 목록에서 헬스장·웰니스 서비스처럼 들리는 `관리` 표현을 제거했다. 문맥에 따라 `비만 진료`, `다이어트 진료`, `치료 시작`, `진료 경과`, `진료 범위와 우선순위`, `대사 위험 평가의 우선순위`로 바꿨으며 `생활 조정`도 사용하지 않는다.
- 본편의 사례 아카이브 연결부에서 크게 보이던 `17 CASES` 숫자를 삭제하고, `다이어트 진료 사례 더 보기`를 큰 초록색 CTA로 강조했다.
- 생리 전 식욕 칼럼의 6번 디툰은 사용자가 다시 제공한 한글 보정본으로 교체했다. 새 이미지의 문구가 정상이라 6번 컷에만 있던 HTML 한글 덮어쓰기 레이어는 제거했다.

## 2026-08-11 디툰 한글 렌더링 보정

- 생리 전 식욕 칼럼의 디툰 6장에 생성형 이미지가 직접 그린 한글 획이 어색하게 남아 있었다. 장면과 캐릭터는 유지하고, 한글 영역을 이미지 톤과 같은 배경으로 정리한 뒤 실제 Pretendard 텍스트 레이어를 동일 위치에 배치했다.
- 1–6컷의 상단 대사와 5컷의 체중 메모, 6컷의 식욕·붓기·수면·컨디션 및 설명 문구를 브라우저 텍스트로 교체했다. 화면 폭에 따라 글자 크기도 함께 줄어들어 모바일에서도 한글 획이 깨지지 않는다.

## 2026-08-11 교대역 다이어트·생리 전 식욕 칼럼 발행

- 새 칼럼 `columns/gyodae-diet-premenstrual-appetite.html`을 추가했다. 생리 전 식욕·음식 갈망과 단기 체중 변화를 월경주기, 체수분, 반복 패턴의 관점에서 설명하며 원고의 임상적 한계 표현을 유지했다.
- 제공받은 디툰 6장을 본문 순서대로 배치하고 WebP로 최적화했다. 목록용 4:3 `cover.webp`와 검색·공유용 1200×630 `og.webp`도 같은 원본에서 분리해 사용한다.
- 에너지 섭취 메타분석, 체중·세포외수분 관찰연구, 증상 기록 지침, 두 사례 비교를 HTML/CSS 정보 그래픽으로 구성했다. 구조화 데이터는 `MedicalWebPage`·`Article`·`BreadcrumbList`·`FAQPage`를 중복 속성 없이 포함한다.
- `columns.html`, `sitemap.xml`, `rss.xml`을 함께 갱신했다. 별도 CMS 마커가 없는 수작업형 칼럼이므로 발행기가 본문을 덮어쓰지 않으며, 공통 목차·예상 완독 시간·활성 독서 시계는 기존 `column-article` 구조를 그대로 사용한다.

## 2026-08-11 산후·다이어트 가로 목차 시계 정렬

- `post-toc`, `diet-toc`처럼 별도 이름을 쓰는 가로 목차에서는 독서 시계가 목차 아래 새 줄의 화면 왼쪽 끝으로 떨어졌다. 특정 클래스 목록 대신 `column-article-body` 안에서 시계를 직접 포함하는 모든 `nav`를 같은 가로 목차 규칙으로 처리하도록 공통 CSS를 수정했다.
- 산후 칼럼과 다이어트 6가지 이유 칼럼 모두 1920×1080에서 1,500px 스크롤 후 시계가 목차 오른쪽 끝 215px 영역에 결합되고, 목차 전체가 상단 70px에 유지되며 가로 넘침이 없음을 확인했다.
- 공통 CSS 캐시 버전은 `20260811-8`이며 기존 공개 칼럼과 새 발행 템플릿을 함께 동기화했다.

## 2026-08-11 칼럼 독서 시계 위치·sticky 전수 보정

- 제목 아래 예상 완독 시간 박스는 최대 520px로 줄이고 제목 영역 가운데에 정렬했다. 아이콘·글자·내부 여백도 함께 축소해 제목보다 시각적으로 앞서지 않도록 했다.
- 공통 임상 칼럼의 왼쪽 목차·시계 레일은 본문 방향으로 22px 이동했다. 목차와 시계는 하나의 sticky 컨테이너이므로 함께 스크롤을 따라간다.
- 성장·지방간·BDNF·다이어트 6가지 이유·산후 칼럼처럼 별도 매거진 템플릿을 쓰는 문서는 메인 컨테이너의 `overflow:hidden` 때문에 계산상 sticky여도 실제로는 화면 밖으로 빠지는 문제가 있었다. 가로 자르기를 `overflow:clip`으로 바꾸고, 가로 목차 오른쪽에 축소 시계를 결합해 목차와 시간이 함께 고정되도록 했다.
- 9개 공개 칼럼을 1440px Chrome에서 1,600px 스크롤한 뒤 실제 목차 좌표로 검사했다. 공통 세로 목차는 상단 120px, 가로 매거진 목차는 70px에 유지되며 전 페이지 가로 넘침은 0이다. 모바일에서는 기존 하단 미니 독서 바를 유지한다.
- 공통 CSS 캐시 버전은 `20260811-7`이며 새 발행 템플릿과 기존 공개 칼럼을 함께 동기화했다.

## 2026-08-11 칼럼 메인 매거진 피드·활성 독서 시계

- `columns.html`의 기존 대표 칼럼·카테고리·검색·칼럼 카드 구조와 모든 URL은 유지하면서, 목록 카드에 `DEAR JOURNAL` 헤더와 `READ COLUMN` 푸터를 더한 3열 에디토리얼 피드 스타일을 적용했다. PC 3열·태블릿 2열·모바일 1열이며 제목·요약은 실제 HTML 텍스트라 검색 접근성을 유지한다.
- 모든 `.column-article-body` 칼럼에 공통 `js/main.js`가 본문 분량을 기준으로 예상 완독 시간을 계산해 제목 영역에 표시한다. 실제 평균 독자 데이터가 아니므로 문구는 `평균`이 아닌 `예상 완독 시간`을 사용한다.
- 독자가 80px 이상 스크롤하면 독서 시계가 시작된다. 페이지가 보이고 브라우저 탭에 포커스가 있을 때만 시간이 증가하며, PC에서는 기존 sticky 목차 아래에, 모바일에서는 화면 하단의 작은 독서 바에 표시한다. 본문·목차·구조화 데이터는 대체하지 않는 점진적 향상 기능이다.
- 새 발행물에도 같은 캐시 버전이 적용되도록 `tools/publish-column.mjs`를 동기화했다. 공통 CSS는 `20260811-6`, 공통 JS는 `20260811-2`이며 기존 공개 칼럼도 같은 버전을 참조한다.
- 로컬 Chrome에서 1440×1000·390×844를 검증했다. 칼럼 메인은 가로 넘침이 없고, 일반 임상 칼럼에서 예상 시간 삽입, sticky 목차 시계, 스크롤 후 시간 증가, 모바일 독서 바와 가로 넘침 0을 확인했다. `node tools/test-column-publisher.mjs`, JS 문법 검사, `git diff --check`를 통과했다.

## 2026-08-11 기능성 소화불량 칼럼 구조화 데이터 구문 수정

- `columns/seocho-functional-dyspepsia.html`의 `MedicalWebPage` JSON-LD 안에 `inLanguage`가 두 번 선언되어 Google Search Console 실시간 테스트에서 `고유 속성 중복` 구문 오류가 발생했다.
- 같은 객체의 뒤쪽 중복 `inLanguage`만 삭제하고 앞쪽 `"inLanguage": "ko-KR"`는 유지했다. 화면 본문·디자인·FAQ·검색 문구·사진은 변경하지 않는다.

## 2026-08-11 기존 우울증 칼럼 임상·안전 전면 개편

- 기존 상위노출 URL `columns/dear-column-20260729-1907.html`과 최초 발행일을 유지하면서 검색 설명·본문·FAQ·구조화 데이터·목록 요약을 전면 개편했다. 글은 슬픔과 우울장애의 구분, 정서·인지·신체·기능 평가, 기분의 시간축·몸의 변화·수면·복용약·생활환경 확인, 한의진료의 판단 구조와 측정 기반 추적 순서로 구성한다.
- 초기 개편에 포함했던 112·119·109 및 타 진료과 안내 패널은 사용자 피드백에 따라 전부 삭제했다. 공통 `section` 여백과 충돌해 왼쪽이 잘리던 해당 패널의 CSS도 함께 제거하고, 그 자리는 디어한의원에서 실제로 확인하는 기준점 설정 → 치료 계획 → 재평가의 진료 우선순위로 교체했다.
- 근거는 WHO 2025 우울증 팩트시트, NICE NG222, VA/DoD 2022 주요우울장애 지침, 2024 우울증 한의표준임상진료지침과 NCCIH 보완건강 자료를 사용했다. 기존 약물은 임의로 감량·중단하지 않으며, 침·한약의 평균 연구 결과를 개인의 치료 효과처럼 약속하지 않는다.
- 기존 스톡사진형 생성 이미지는 실제 원장 외모로 오인될 수 있어 사용하지 않는다. 얼굴·옆얼굴·식별 특징을 모두 프레임 밖으로 제외한 일반 스마트폰 진료 스냅 질감의 `hero-phone-v3.webp`를 새로 생성해 본문·Open Graph 대표 이미지로 적용한다. 기존 이미지와 목록용 인스타툰 썸네일은 삭제하지 않는다.
- 정서·인지·신체·기능 4축, 시간축, 확인 항목 매트릭스, 진료 우선순위, 추적 지표를 HTML/CSS로 구성하고 문서 CSS 캐시 버전을 `20260811-5`로 유지했다. `columns.html`, `rss.xml`, `sitemap.xml`도 함께 동기화한다.

## 2026-08-11 다이어트 한약 상담 칼럼 임상·근거 전면 개편

- 기존 상위노출 URL `columns/seocho-diet-herbal-medicine.html`과 최초 발행일은 유지하면서 제목·검색 설명·본문·FAQ·구조화 데이터·목록 요약을 전면 개편했다. 단순 체중계 안내였던 글을 체중 변화의 시간축, 지방 분포와 대사 위험, 이차 원인 감별, 체중 감량 후 생물학적 적응, 한약 근거의 적용 한계, 안전성·중단 기준, 반응 기반 추적 순서의 임상 칼럼으로 재구성했다.
- 근거는 WHO 비만 팩트시트, AACE 2025 비만 알고리즘, 대한비만학회 2024 약물치료 진료지침, 한국한의약진흥원 2024 비만 한의표준임상진료지침, 체중 감량 후 식욕·호르몬 적응 연구, 마황 함유 경구제제 메타분석과 실제 진료 이상반응 조사, NCCIH 마황 안전성 자료를 사용했다. 단기 평균 효과와 장기·희귀 이상반응 안전성을 구분하고 용량이나 효과 보장 표현은 넣지 않았다.
- 본문 안에 임상 표현형 4축, 체중 변화 시간표, 감별평가 매트릭스, 근거 번역 패널, 복용 전·추적·중단 안전 대시보드, 반응 기반 추적표를 HTML/CSS 정보 그래픽으로 추가했다. 큰 제목 위주의 기존 문서와 달리 H1은 데스크톱 약 37px·모바일 27.2px, H2는 데스크톱 약 24.8px·모바일 20.8px로 제한했다.
- `columns.html`, `rss.xml`, `sitemap.xml`을 함께 갱신하고 문서 CSS 캐시 버전을 `20260811-4`로 올렸다. 로컬 1440×900, 768×1024, 390×844에서 가로 넘침·중복 ID 없이 확인했고 JSON-LD, RSS, sitemap XML 파싱과 칼럼 발행기 테스트를 통과했다.

## 2026-08-11 다이어트 칼럼 임상 체크포인트 패널 개편

- `원인 평가 우선` 안내를 큰 문장 하나만 놓인 초록 배너에서 정보 구조가 있는 `CLINICAL CHECKPOINT` 패널로 교체했다. 급격한 체중 증가, 부종, 피로·근력 저하, 월경 변화, 흉통·호흡곤란을 5개 신호로 분리하고 진료 우선순위를 하단에 짧게 정리했다.
- PC는 5열, 모바일은 2열과 마지막 항목 전체 폭으로 배치했다. 패널 폭은 본문 100%이고 PC·태블릿·모바일에서 가로 넘침이 없다.
- 해당 문서의 CSS 캐시 버전을 `20260811-3`으로 갱신했다.

## 2026-08-11 다이어트 임상 칼럼 타이포·하단 UI 보정

- `columns/seocho-diet-clinic.html`에 전용 `diet-clinical-column` 범위를 추가해 H1을 데스크톱 약 34px·모바일 26.4px, 본문 H2를 데스크톱 약 24.5px·모바일 20.8px로 낮췄다. 다른 칼럼의 공통 타이포는 변경하지 않았다.
- 본문 안에 배치된 `함께 읽으면 좋은 글`과 병원 정보 박스가 바깥 배치용 공통 폭·여백을 중복 적용받던 문제를 바로잡았다. 두 박스는 본문 폭 100%로 정렬되고 PC·태블릿·모바일에서 가로 넘침 없이 같은 간격으로 이어진다.
- 해당 문서의 CSS 캐시 버전을 `20260811-2`로 갱신했다.

## 2026-08-11 기존 다이어트 칼럼 임상 전면 개편

- 기존 상위노출 URL `columns/seocho-diet-clinic.html`은 주소와 최초 발행일을 유지한 채, 제목·검색 설명·본문·FAQ·구조화 데이터·대표 사진을 전면 개편했다. 목록 제목은 `살이 찐 이유는 체중계에 나오지 않습니다`로 바꾸고 검색어 데이터에는 기존 `서초동 다이어트 한의원` 문구를 남겼다.
- 본문은 체중 숫자 중심의 생활수칙 안내에서 체중 변화의 시간표, BMI·허리둘레와 대사 위험, 급격한 증가의 감별, 생활·약물·한의진료 선택, 한약 안전성, 체중 외 추적 지표 순서의 임상 칼럼으로 재구성했다. 효과를 보장하거나 한약을 일률적으로 권하지 않고, 위험 신호와 다른 진료과 평가가 먼저 필요한 경우를 명시했다.
- 근거는 WHO 비만 팩트시트, AACE 2025 비만 알고리즘, 대한비만학회 2022·2024 진료지침, 한국한의약진흥원 2024 비만 한의표준임상진료지침, 변증 기반 한약 임상연구 체계적 문헌고찰을 사용했다.
- 본문 대표 사진은 한 사람의 아침 결식과 야간 섭취가 같은 하루에 공존하는 장면을 담은 1200×900 WebP `assets/images/columns/seocho-diet-clinic/hero-v2.webp`로 교체했다. 기존 사진과 목록용 인스타툰 썸네일은 삭제하지 않았다.
- `columns.html`, `rss.xml`, `sitemap.xml`의 제목·요약·수정일도 함께 갱신했다. 배포 후 Google Search Console에서 해당 칼럼 URL과 `columns.html`의 색인 생성을 다시 요청했으며 두 URL 모두 기존 색인 상태가 정상이었고 우선순위 크롤링 대기열에 추가됐다. `sitemap.xml`과 `rss.xml`도 2026-08-11 재제출에 성공했으며 각각 발견된 페이지 47개와 10개로 표시됐다.

## 2026-08-11 기존 칼럼 4개 목록 썸네일 교체

- 기능성 소화불량, 기존 우울증, 다이어트 한약 상담, 초기 다이어트 진료 칼럼의 `columns.html` 목록 썸네일 4장을 현대적인 디지털 인스타툰 사슴 자산으로 교체했다.
- 소화불량은 적은 식사량에도 먼저 멈추는 위, 우울증은 울지 않지만 일상이 멈춘 장면, 다이어트 상담은 체중계 뒤의 수면·식욕 시간·체성분, 초기 다이어트 진료는 아침과 밤의 다른 생활 리듬으로 구분했다.
- 이번 범위는 목록 썸네일만이며 각 칼럼의 제목·본문·본문 대표 이미지·Open Graph 이미지는 변경하지 않았다. 기존 사진과 대표 이미지는 삭제하거나 덮어쓰지 않고 보존했다.
- 새 썸네일은 모두 1200×900 WebP이며 흰 배경, 검은 디지털 선, 딥그린·코럴 제한색과 같은 사슴 캐릭터 체계를 사용한다.

## 2026-08-11 칼럼 사슴 대표 이미지 화풍 교체

- 통과한 `seocho-diet-6-reasons` 다이어트 툰과 대표 이미지는 유지하고, 소아성장·우울증/BDNF·지방간·산후 한약 칼럼의 기존 빈티지 수채화 사슴 대표 이미지 4장을 현대적인 디지털 인스타툰형 `v2` 자산으로 교체했다.
- 새 공통 캐릭터는 흰 배경, 검은 디지털 선, 딥그린·코럴 중심의 제한색, 작은 점눈과 무표정한 반응을 사용한다. 피터래빗풍 종이 질감·보송한 털·코티지 장식·누런 수채화 배경은 사용하지 않는다.
- 소아성장 이미지는 아이 간 경쟁이나 따라잡기 예측을 피하고 같은 아이를 시간 간격으로 다시 재는 장면만 표시한다. 우울증 이미지는 걷기가 치료를 보장하는 것처럼 보이지 않도록 운동화 한 짝과 현관문을 여는 작은 시작으로 표현한다. 지방간 이미지는 정상 체크표와 별개로 지방간이 존재할 수 있다는 내용을 단순 간 캐릭터로 나타내며 개인 진단처럼 보이지 않게 한다.
- 원본 대표 이미지는 삭제하거나 덮어쓰지 않았으며 새 파일명만 공개 문서와 구조화 데이터에서 참조한다. 모든 공개용 `v2` 이미지는 1200×900 WebP로 저장했다.

## 2026-08-11 외국인 예약 페이지 다국어 문구 교정

- `international-appointment.html`과 `js/international-appointment.js`의 영어·일본어·중국어 예약 안내를 현지 환자가 읽는 전화 예약 문맥에 맞게 다듬었다. 진료시간·전화번호·휴진 규칙과 원장 직접 안내라는 운영 의미는 변경하지 않았다.
- 영어의 `First visits and same-day registration`처럼 부자연스러운 접수 표현을 `Registration for first-time and same-day patients`로 교정하고, 확정된 휴일·임시 변경만 달력에 반영된다는 의미를 명확히 했다.
- 일본어는 외국인 환자 예약, 진료 캘린더, 이메일 예약 미접수 안내를 자연스러운 의료기관 표현으로 정리했다. 중국어는 `补休假日`을 `光复节补休日`로 바로잡고 당일 접수 조기 마감과 달력 갱신 안내를 자연스럽게 교정했다.
- PC·태블릿·모바일에서 세 언어 전환, 월 이동, 2026년 8월 특별 진료일, 전화 CTA와 긴 공지문 표시를 확인한다. 번역 문구 변경 시 `international-appointment.js`의 캐시 버전도 함께 갱신한다.

## 2026-08-10 외국인 전화 예약·월간 진료 달력

- `international-appointment.html`을 Google 비즈니스 프로필의 외국인 예약 링크용 공개 페이지로 추가했다. 이메일·문의 폼 없이 `+82 2-3486-1777` 전화 CTA만 제공하며 영어·일본어·중국어 안내를 한 화면에서 전환한다.
- 월간 달력은 이전·다음 달을 볼 수 있고 기본 진료시간을 요일별로 자동 표시한다. 2026년 8월 15일 광복절은 토요일 정상진료 `10:00–15:00`, 8월 17일 대체공휴일은 월요일 정상진료 `10:00–13:00 / 14:00–20:00`으로 별도 표시한다. 향후 임시 휴진·공휴일 변동은 `js/international-appointment.js`의 `special` 일정에서 관리한다.
- 한국어 메인 오른쪽 퀵메뉴에 `외국인예약` 버튼을 추가해 영문 상태의 예약 달력으로 바로 연결한다. 모바일 하단 퀵메뉴는 기존 6칸 폭을 유지하기 위해 `맨위로` 버튼 대신 외국인 예약 버튼을 표시한다.
- `js/main.js`가 퀵메뉴가 있는 한국어·영어·일본어·중국어 공개 페이지에 외국인 예약 버튼을 공통으로 생성·정규화한다. 아이콘은 둥근 예약 카드와 시간 표시를 결합한 전용 심볼이며, 링크와 레이블은 현재 문서 언어에 맞춘다.

## 2026-08-10 GA4 예약·문의 전환 설정

- GA4 `디어한의원 공식 홈페이지` 속성에서 공통 `dear_cta_click` 이벤트가 정상 수집되는 것을 확인했다. 점검 당시 지난 7일 이벤트 수는 78회였고, 맞춤 설정 전이라 주요 이벤트는 0회였다.
- `dear_cta_click`의 `cta_action` 값에 따라 `booking_click`(`naver_booking`), `phone_click`(`phone`), `naver_talk_click`(`naver_talk`) 맞춤 이벤트를 만들고 세 이벤트를 주요 이벤트로 지정했다. 반복 클릭으로 전환 수가 부풀지 않도록 세션당 한 번 집계하며 기본 금액은 설정하지 않는다.
- 보고서에서 클릭 맥락을 나눌 수 있도록 이벤트 범위 맞춤 측정기준 `CTA 클릭 종류`=`cta_action`, `CTA 클릭 위치`=`cta_location`, `CTA 발생 페이지`=`page_path`를 등록했다. 설정 전 데이터에는 소급 적용되지 않으며 신규 이벤트·측정기준은 처리 후 보고서에 나타날 수 있다.

## 2026-08-10 네이버 검색 설명 길이 정리

- 한국어 메인 `index.html`의 검색 설명을 네이버 URL 검사의 80자 이내 권장선에 맞춰 정리했다. `체중 정체부터 아이 성장, 마음·소화의 불편까지. 김민지 대표원장이 증상의 경과와 생활을 함께 살펴 진료합니다.`로 바꿔 주요 진료 관심사, 원장 직접 진료, 경과와 생활을 함께 보는 진료 방식을 짧게 전달한다.
- 정상 판정을 받은 Open Graph 설명과 화면 카피는 변경하지 않았다. 배포 후 네이버 서치어드바이저에서 메인 URL 수집 요청을 다시 해야 최신 설명 반영 후보가 된다.

## 2026-08-10 네이버 검색결과 주요 콘텐츠 캐러셀 후보 보강

- 한국어 메인 `index.html`의 기존 `WebSite`·`MedicalClinic`·`Person` JSON-LD 그래프에 `ItemList`를 추가했다. 화면의 확정 카피나 배치는 변경하지 않았다.
- 목록은 `BE DEER`와 `BE DEER 체성분 기록`을 1·2순위로 두고 `DEAR CARE`, `DEAR SERVICES`, `디어 임상 저널`, `디어의 진료 원칙`까지 6개 공개 페이지로 구성했다. 각 항목은 서로 다른 실제 대표 이미지, 이름, 설명, 절대 URL을 가진다.
- 이 마크업은 네이버가 캐러셀 또는 확장 검색결과를 구성할 때 참고할 후보 정보이며 실제 노출 여부와 형식은 네이버가 결정한다.

## 2026-08-10 산후 한약·다이어트 한약 임상 칼럼

- `columns/seocho-postpartum-diet-herbal-medicine.html`에 김민지 대표원장 명의의 산후 체중 임상 칼럼을 추가했다. 본문은 툰 없이 HTML/CSS 연구 Figure 7개로 구성하고, 목록·검색 공유용 대표 이미지만 기존 디어 작화에 맞춘 엄마·아기 사슴 툰 `assets/images/columns/seocho-postpartum-diet-herbal-medicine/cover-deer.webp`를 사용한다.
- 글은 출산 후 체중을 단순한 지방량으로 보지 않고 임신 중 증가량, 체액 변화, 수유, 수면, 빈혈·갑상선 등 우선 평가 항목을 시간축으로 해석한다. 산후 에너지 섭취 변화 관찰연구와 분만 방식·합병증에 따른 활동 재개 기준을 보강했으며, 관찰값을 개인 권장 열량으로 오해하지 않도록 한계를 함께 썼다. 산후 한약과 다이어트 한약을 하나의 정해진 처방처럼 설명하지 않으며 수유 중 성분·용량·영아 상태·병용약 검토가 필요하다는 안전선을 명시했다.
- 디어 칼럼 특유의 진료실 상황극과 건조한 드립을 유지하되 각 문장은 연구 해석으로 회수했다. 메타분석과 코호트 연구의 수치에는 연구설계와 일반화 한계를 함께 적었다.
- `columns.html`의 오늘의 글과 첫 카드, CollectionPage ItemList, `sitemap.xml`, `rss.xml`을 새 칼럼으로 갱신했다. 최신 글의 목록용 제목은 모바일에서 낱글자로 깨지지 않도록 `산후 한약과 다이어트 한약, 같은 체중 이야기일까요?`로 짧게 표시하되 문서의 SEO 제목은 유지한다. 개별 칼럼의 전역 `section` 패딩 상속을 차단해 문단과 소제목 사이의 과도한 공백도 제거했다.
- 개별 칼럼 H1의 최대 크기와 본문·메타 열 배분을 조정해 긴 제목이 낱말 단위로 깨지지 않도록 했다. Figure 05·06은 `<figure>` 전체가 아니라 내부 데이터 영역에만 grid를 적용해 캡션·도표·주석이 정상적인 세로 정보 구조를 유지한다.
- 참고문헌 12개를 PubMed 서지정보와 ATA·ACOG·WHO 원문으로 전수 대조했다. 모두 실재하지만 6번의 저자·연도·권호와 9번의 저자가 다른 논문의 서지정보와 섞여 있어 원문 기준으로 교정했다.

## 2026-08-10 검색결과 메인 문구·대표 이미지 개선

- 한국어 메인 `index.html`의 검색 제목을 `서초동 디어한의원 | 증상과 생활을 함께 보는 진료`로 바꾸고, 진료과목을 나열하던 설명을 체중 정체·아이 성장·마음과 소화의 불편에서 출발해 김민지 대표원장이 경과와 생활을 함께 살핀다는 문장으로 개편했다.
- Open Graph·X 카드 문구를 같은 방향으로 맞추고, 빈 대기실 사진 대신 실제 김민지 대표원장이 환자 상태를 설명하는 `assets/images/director.jpg`를 대표 이미지로 지정했다. 이미지 캐시 갱신을 위해 `v=20260810` 쿼리를 사용했다.
- 검색결과에서 큰 이미지 미리보기가 허용되도록 `max-image-preview:large`를 명시하고, 대표 이미지 크기·대체 텍스트와 `WebSite.alternateName`을 보완했다. 검색 제목·설명·썸네일과 하단 사이트링크 노출 여부는 Google·Naver가 검색어와 페이지 구조를 바탕으로 최종 결정하므로 강제할 수 없다.

## 2026-08-10 다국어 SEO·언어 전환 정비

- `js/main.js`의 공통 언어 선택기는 번역판이 있는 `index·about·director·career·philosophy·care·services·columns`만 같은 페이지의 언어판으로 연결한다. 번역판이 없는 BE DEER, 법률 안내, 한국어 개별 칼럼에서는 존재하지 않는 URL을 만들지 않고 영어·일본어·중국어 홈으로 이동한다. 이 변경으로 15개 한국어 전용 페이지에서 생성되던 45개 404 언어 링크를 제거했다.
- 한국어 `about·director·career·philosophy·care·services·columns`에 `ko·en·ja·zh-Hans·x-default` 반환 `hreflang`을 추가해 외국어 페이지와 동일한 양방향 언어 클러스터를 완성했다. 각 언어 페이지의 자기 canonical과 기존 sitemap URL 구조는 유지한다.
- 영어·일본어·중국어 `career.html`에 언어별 H1을 추가했다. 경력 프로필의 상단 여백과 좌측 정렬을 함께 보정해 고정 내비게이션과 제목이 겹치지 않도록 했으며, 관련 4개 경력 페이지의 CSS 버전을 `20260810-2`로 갱신했다.
- 중국어 메인 description·OG description의 오역 `饮食中药·宫筋丹`을 `减重韩药·拱辰丹` 중심의 정확한 진료 설명으로 교정했다. 영어·일본어·중국어 메인의 `MedicalClinic` JSON-LD는 한국어 메인과 같은 `https://dearhani.com/#clinic` ID, 공식 URL, 국제 전화번호, 로고·공간 이미지, 좌표, 공식 채널, 예약 액션을 사용하며 진료 분야만 각 언어로 제공한다.
- 영어·일본어·중국어 Columns의 description·OG description에 김민지 대표원장의 저자성, 연구·진료 기준, 성장·체중과 대사·마음 회복·소화기 증상의 임상 저널 범위를 반영하고 언어별 `CollectionPage` JSON-LD를 추가했다. 한국어 Philosophy에는 누락됐던 절대경로 OG URL·이미지와 `MedicalWebPage` JSON-LD를 보완했다.

## 2026-08-09 Columns 임상 저널 인트로 개편

- `columns.html`의 기존 감성형 상단 문구와 4열 `진료 주제별로 읽기` 영역을 하나의 `DEAR CLINICAL JOURNAL` 인트로로 통합했다. 핵심 문장은 `증상 하나보다, 그 증상이 만들어진 흐름을 읽습니다.`이며, 김민지 대표원장이 연구와 진료 기준을 바탕으로 작성한다는 저자·편집 기준을 상단에서 바로 설명하고 `director.html`에 `rel="author"`로 연결했다.
- 중복되던 주제 박스와 하단 필터를 하나로 합쳤다. 실제 칼럼 카드에 연결된 분류만 `전체·성장·체중·대사·마음·회복·소화·불편`으로 보여 주며, 연결된 카드가 없던 `채움` 분류는 제거했다. 기존 `Focus·Shape·Calm·Relief` 데이터 계약과 URL 쿼리 필터 동작은 유지하고 활성 버튼의 `aria-pressed` 상태를 함께 갱신한다.
- 최신 글 목록 위에는 검색과 `LATEST JOURNAL` 레이블만 남겼다. PC 1440px, 태블릿 768px, 모바일 390px에서 제목 줄바꿈, 주제 선택, 가로 넘침을 확인했으며 모바일 주제 목록은 짧은 가로 스크롤형 목차로 표시한다.

## 2026-08-09 다이어트 칼럼 근거 서술·툰 2차 개편

- `columns/seocho-diet-6-reasons.html`의 여섯 소제목은 유지하면서 각 연구를 연구설계, 실제 수치, 해석 가능한 범위, 개인 진료로의 번역 순서로 보강했다. 월경주기 반복측정, 감량 후 호르몬 추적, 식욕 피드백 모델, 음식단서 메타분석, DIETFITS 무작위시험, 단계적 감량 대사연구의 한계를 각각 본문에 명시했고 clinical obesity 국제위원회 원문을 참고문헌에 추가했다.
- 본문 툰 6장은 광택 있는 AI 일러스트에서 거친 잉크선·종이 질감·제한색의 2컷 웹툰형으로 교체했다. 유행어를 직접 복제하지 않고 무표정 반전과 짧은 한 줄 상황극을 사용했으며, 같은 단발머리·청록 머리핀 주인공을 유지한다. Columns 대표 이미지와 OG 이미지도 새 작화로 교체했다.
- 배포 직후 새 칼럼만 별도 헤더 마크업을 사용해 공통 내비게이션 CSS·JS가 적용되지 않은 문제를 바로잡았다. 기존 칼럼과 같은 `nav__menu`·`nav__link`·`navToggle` 구조로 통일하고 PC·모바일 메뉴를 재검증했다. 화면의 `김민지 대표원장`을 원장 소개 페이지로 연결하고 `rel="author"`를 부여했으며, Article JSON-LD의 저자 ID를 실제 원장 엔티티 `director.html#kim-minji`와 일치시켰다. Contact 앵커도 실제 `#clinic-info`에 연결했다. 향후 발행 글에서도 같은 저자 링크와 엔티티가 나오도록 `tools/publish-column.mjs`와 테스트를 함께 보강했다.
- 새 툰 6장의 말풍선은 각 화자 바로 위로 옮기고 꼬리가 여성·체중계 내부 캐릭터·식욕 캐릭터·체수분·손가락을 향하도록 장면별 좌표를 다시 지정했다. 꼬리는 축소 렌더링에서 뭉개지는 `clip-path`를 쓰지 않고 외곽선·내부색 이중 삼각형을 겹쳐 짧고 선명하게 붙였다. 모바일에서도 대사를 이미지 아래로 분리하지 않고 축소된 오버레이와 꼬리를 유지한다. 하단에는 지방간 칼럼과 동일한 한약·한방 다이어트·운동 지도자 경력 BE DEER 배너 3장, 다이어트 사례 더보기, NAP, 오시는 길 지도를 같은 폭과 연결선 구조로 추가했다.

## 2026-08-09 살이 안 빠지는 6가지 이유 칼럼

- `columns/seocho-diet-6-reasons.html`에 김민지 대표원장 명의의 비만·다이어트 저널형 칼럼을 추가했다. 사용자가 제공한 원고의 6가지 흐름(단기 체중 정체, 감량 후 식욕 적응과 요요, 음식 단서와 craving, 체수분과 부종, 지속 가능한 계획, 체중 너머의 건강)을 유지한다.
- 근거는 월경주기 체중·세포외수분, Sumithran 감량 후 호르몬 적응, Polidori의 추정 식욕 피드백, Boswell 음식 단서 메타분석, DIETFITS, Magkos 5% 감량 연구, 대한비만학회 2024 진료지침으로 구성했다. 숫자는 연구집단 결과와 개인 적용의 차이를 바로 옆 `READ THIS CAREFULLY` 카드에서 설명한다.
- 동일한 30대 여성 주인공이 이어지는 6장의 생성형 에디토리얼 툰을 `assets/images/columns/seocho-diet-6-reasons/`에 WebP로 저장했다. 이미지 자체는 무문자이며 대사·캡션·전체 맥락은 HTML DOM과 alt/transcript로 제공한다. 원본 밈 영상·인물·브랜드 UI는 사용하지 않았다.
- Article·MedicalWebPage·FAQPage·BreadcrumbList·MedicalClinic 구조화 데이터, canonical, OG, 인용 링크를 넣고 Columns 오늘의 글·SHAPE 주제 허브·카드 목록·대표원장 저자 페이지·sitemap·RSS에 연결했다.
- 공개 후 공통 `section` 패딩이 칼럼의 자체 여백과 중복되던 문제를 막아 이유별 간격을 PC 80px·모바일 56px 수준으로 정리했다. 툰 말풍선은 장면별 인물·캐릭터에 맞춰 개별 배치하고 꼬리를 추가했으며, 모바일에서는 이미지 바로 아래 대화띠로 이어진다.

## 2026-08-07 소아성장 Figure 01 근거 표현 정정

- `columns/gangnam-child-growth.html`의 Figure 01에서 두 peak 관찰값 사이를 임의 곡선으로 연결하던 표현을 제거했다.
- 강화 종단 코호트에서 확인된 여아 10세·7.07 cm/yr, 남아 12세·8.62 cm/yr만 카드형 비교 도식으로 표시하며, 전체 연령별 곡선을 재현한 그래프가 아니라는 한계를 본문에 명시했다.

대상: Claude, Codex 및 이후 유지보수 담당자

공개 저장소: https://github.com/BBINGE/dear-clinic

배포 주소: https://dearhani.com/

기준 브랜치: `master`

## 2026-08-07 공식 도메인 연결

- Cloudflare에서 `dearhani.com` 루트에 GitHub Pages A 레코드 4개(`185.199.108.153`, `185.199.109.153`, `185.199.110.153`, `185.199.111.153`)와 `www` CNAME(`bbinge.github.io`)을 모두 `DNS only`로 연결했다.
- GitHub Pages의 custom domain을 `dearhani.com`으로 저장하고 DNS 확인 성공 후 `Enforce HTTPS`를 활성화했다.
- `https://dearhani.com/` 실접속과 `http://www.dearhani.com/`에서 `https://dearhani.com/`으로의 자동 이동을 확인했다.
- 사이트의 canonical, Open Graph, JSON-LD, sitemap, robots, 404 루트 경로와 칼럼 발행기 기본 URL은 커밋 `8024394`에서 새 도메인으로 전환했다.
- Google Search Console에는 `dearhani.com` 도메인 속성을 DNS TXT로 소유권 인증했고 `https://dearhani.com/sitemap.xml`을 제출했다. 제출 직후 상태는 `성공`, 발견된 페이지는 43개였다. Cloudflare의 Google 소유권 인증 TXT 레코드는 삭제하지 않는다.
- 칼럼 RSS는 `https://dearhani.com/rss.xml`이며 현재 공개 칼럼을 최신순으로 제공한다. 칼럼 발행기에서 새 글 발행·삭제 시 RSS 항목도 함께 갱신한다. Google Search Console 제출 직후 유형 `RSS`, 상태 `성공`, 발견된 페이지 7개로 확인했다.
- 네이버 서치어드바이저의 `dearhani.com` 소유권 확인용 `naver-site-verification` 메타태그는 한국어 메인 `index.html`의 `<head>`에 유지한다.
- 네이버 서치어드바이저에서 `https://dearhani.com` 소유권 확인을 완료했고 `sitemap.xml`과 `https://dearhani.com/rss.xml`을 모두 제출했다.

이 파일은 집·회사 PC와 Claude·Codex 사이에서 공유하는 현재 상태 문서다. 이제 Git에 커밋한다. 정적인 커밋 번호를 최신 상태로 믿지 말고 작업 시작 때 반드시 `git fetch`, `git status`, `git log`로 확인한다.

## 2026-08-07 세션 마감 인수인계

- 소아성장 저널형 칼럼을 `columns/gangnam-child-growth.html`에 추가했다. 사용자가 제공한 원고의 문장과 순서를 보존하고, 강화 종단 코호트 성장속도, 2017 소아청소년 성장도표, 서로 다른 성장궤적, 성장판 조절요소, HT042 무작위 위약대조시험, 연구설계 비교, 국제 성장평가 기준과 DEAR GROWTH TIMELINE을 원문 근거와 독립 제작 Figure로 구성했다. HT042의 +0.29cm는 특정 표준화 복합추출물의 연구집단 군 간 차이이며 모든 성장한약이나 개인의 예상 성장량으로 일반화하지 않는다고 반복해 명시했다. 생활 장면 2장은 환자 사례가 아닌 생성형 연출 이미지로 표시하고, 두 사슴이 등을 맞대고 키를 재는 인스타툰형 이미지를 카드와 공유 미리보기에 사용한다. Columns 최신 글·GROW 주제 허브·sitemap·RSS에 반영했다.
- Columns `오늘의 글` Featured 카드가 위 주제 허브와 아래 필터·목록에 붙어 보이지 않도록 카드 위·아래 여백을 늘렸다. 카드 자체의 크기·이미지·카피·좌우 폭은 유지했으며, 1440px에서는 위·아래 약 100px, 390px에서는 위 88px·아래 72px의 간격과 가로 넘침 없음을 확인했다.
- 개인정보처리방침·비급여 진료비용·환자의 권리와 의무 페이지의 하단 SNS CTA와 오른쪽 빠른 메뉴를 공통 페이지 규격으로 통일했다. 빠져 있던 전화·예약·톡톡·블로그·인스타그램·맨위로 SVG를 복구하고, 푸터에 직접 입력된 SNS 이름과 CSS의 접근성 라벨이 중복 표시되던 문제를 제거했다. 세 페이지를 1440px·390px에서 확인했으며 가로 넘침 없이 동일한 아이콘·라벨 구조로 표시된다.
- SEO·GEO·AEO 기반을 보강했다. 홈페이지의 `WebSite`·`MedicalClinic`과 김민지 대표원장의 `Person` 엔티티에 고정 `@id`를 부여하고, 원장 소개·약력·한의원 소개·진료·서비스 페이지의 canonical·Open Graph·페이지 유형 JSON-LD를 정리했다. 공개된 공식 NAP·예약·지도·블로그·인스타그램만 연결했으며 확인되지 않은 개인 프로필이나 우편번호는 넣지 않았다. 원장 페이지에는 직접 작성·검수한 대표 칼럼 4편을 연결했다.
- Columns에는 `비만과 대사`·`마음과 회복`·`소화와 불편`의 주제 허브를 만들고, 기존 칼럼 6편 모두에서 김민지 대표원장 저자 페이지와 다음 읽을 글을 연결했다. 칼럼 Article/MedicalWebPage 스키마의 저자·발행기관 관계도 같은 고정 엔티티를 가리키도록 정리했다. JSON-LD 파싱, H1, 중복 ID, 내부 링크, PC 1440px·모바일 390px 가로 넘침, 칼럼 발행기 회귀 테스트를 통과했다. Search Console의 색인 요청과 성과 확인은 사이트 코드와 분리해 사용자가 계속 관리한다.
- 강남역 비만 진료와 지방간 저널형 칼럼을 `columns/gangnam-obesity-fatty-liver.html`에 추가했다. 사용자가 제공한 원고의 문장과 순서를 보존하고, 정상 AST·ALT와 지방간 소견의 차이, Donnelly 간 지방 출처 59.0/26.1/14.9, Smith DNL 11/19/38, 가상 검진 타임라인, 섬유화 단계와 FIB-4, Vilar-Gómez 체중 감량 연구를 HTML/CSS 기반 학술 Figure로 재구성했다. 원 논문 Figure는 복제하거나 hotlink하지 않았고 각 수치는 연구집단 결과와 개인 적용의 차이를 캡션에 명시했다.
- 새 지방간 칼럼은 우울증·BDNF 칼럼과 같은 흰 배경, 절제된 소제목과 문단 간격, 네이버 예약·전화 CTA, NAP 카드, 오시는 길 지도를 사용한다. 하단 배너는 `한약 → 한방 다이어트 → BE DEER 프로그램` 3장 순서이며 이 글에서는 공진단 배너를 사용하지 않는다. 세 배너의 원본 검은 모서리 선은 가장자리만 소폭 크롭해 제거한 전용 WebP를 사용한다. 배너 묶음과 NAP 사이에는 `다이어트 사례 보러가기` CTA 배너가 있고 `be-deer.html`로 연결된다. 배너 이미지·사례 CTA·NAP 카드·지도는 데스크톱 1040px, 모바일 `calc(100% - 48px)`로 보이는 좌우 끝을 통일하고 각 구역 사이 5.5rem 간격과 중앙 연결선을 유지한다. Columns `오늘의 글`과 카드 목록 최상단에 배치하고 sitemap에 추가했다. 대표 이미지는 시그니처 사슴이 헬스장 러닝머신을 뛰며 `아 왜 안 빠지지 ㅠㅠ`라고 말하는 인스타툰형 `assets/images/columns/gangnam-obesity-fatty-liver/cover-instattoon.webp`이며, 공유용 1200×630 이미지는 `og-20260807.jpg`다.
- BE DEER의 대표 체성분 사례와 후기 사이에 `다이어트 진료 사례 더 보기` 브릿지를 추가하고, 별도 아카이브 `be-deer-cases.html`로 연결했다. 제공받은 17건의 전·후 결과지 34장은 이름·회원번호·측정일이 있는 상단 전체 영역을 비식별 처리하고 EXIF 메타데이터를 제거한 WebP로만 공개한다. 원본 파일명과 폴더명은 저장소에 남기지 않는다. 아카이브는 효과를 단정하거나 임의 변화량을 계산하지 않고 `관리 시작`·`경과 확인` 측정지를 나란히 보여주며, 측정 조건과 개인차에 관한 안내를 함께 둔다.
- 사례 아카이브의 제목은 원본 결과지에서 확인한 연령대·성별만 사용해 `30대 여성 사례` 형식으로 표시한다. 각 사례 아래에는 체중·체지방량·내장지방레벨의 관리 시작값, 경과 확인값, 변화량을 함께 보여준다. `자세히 보기`는 `be-deer-case.html?case=01` 형식의 고정 주소로 연결하며, 현재는 noindex 상태의 상세 칼럼 연결 화면이다. 추후 로그인 여부에 따른 열람 흐름이나 사례별 칼럼을 구현해도 목록 링크는 유지한다.
- 메인 BE DEER의 사례 아카이브 브릿지는 한 공간의 단체사진이 아니라 가상의 성인 20명이 각기 다른 생활 공간에서 웃는 개별 화면을 7·7·6칸으로 재배열한 비디오 월 배경을 사용한다. 텍스트와 `17 CASES`가 먼저 읽히도록 채도와 대비를 낮추고 흰색 오버레이를 유지한다. 실제 환자 사진이나 치료 전후 사진으로 오해될 표현은 사용하지 않는다.
- BE DEER 공유 미리보기는 본문에서 임의로 이미지를 추출하지 않도록 `assets/images/be-deer/be-deer-og-20260807.jpg` 1200×630 전용 카드를 절대주소의 Open Graph·Twitter 메타로 지정한다. 카카오가 이전 로고 이미지를 캐시할 수 있으므로 이미지 교체 시 파일명을 바꾸고 메타 주소도 함께 갱신한다.

## 2026-08-05 세션 마감 인수인계

- 칼럼 작성기는 `7792460`에서 중앙 원고지형 UI, 기본·HTML 왕복, 색상, 일반 표 행·열 편집, 오른쪽 발행 패널, 로컬 자동 저장 구조로 개편됐다. 발행기는 새 `bodyHtml`을 우선 사용하되 과거 `body`·`designBlocks`도 계속 읽는다. `script`·`iframe`·이벤트 속성 등 실행 가능한 HTML은 차단하고, 목차·상담 CTA·FAQ·참고문헌·NAP은 발행 셸에서 유지한다. PC 1440, 태블릿 768, 모바일 390 폭의 브라우저 클릭 검수와 `node tools/test-column-publisher.mjs`를 통과했으며 공개 CSS의 표·핵심문장 스타일까지 Pages 반영을 확인했다.
- 표 편집 2차에서는 기본 표를 원고 폭 100%로 만들고, 셀 사각형 드래그 선택, 병합·분리, 열 너비·행 높이 경계 드래그, 선택 셀 배경색·글자색 독립 적용을 지원한다. 색상 도구는 프리셋 색상표와 채도·명도 영역, 색상 계열 슬라이더로 구성한다. FAQ는 자동 생성하지 않으며 오른쪽 발행 패널에서 작성자가 직접 추가한 경우에만 글 마지막에 표시한다.
- 글자 크기는 11·13·15·16·19·24·28·30·34·38px 목록으로 선택한다. 팔레트의 HEX 값은 읽기 전용 표시가 아니라 직접 타이핑하고 Enter 또는 확인으로 적용할 수 있다. PC 표 도구는 두 줄 안에서 자연스럽게 감기며 불필요한 가로 스크롤을 만들지 않는다.
- 표 병합은 같은 `thead` 또는 같은 `tbody` 안의 빈틈없는 사각형에서만 허용한다. 제목 줄과 본문 줄을 가로지르는 병합, 이미 병합된 셀을 포함한 재병합은 차단한다. `표 작업 취소`는 최근 표 구조 변경을 최대 20회 기억하고, `모든 병합 해제`는 손상되거나 복잡해진 표의 병합을 한 번에 풀어 골격을 복구한다.
- 390×844 모바일, 768×1024 태블릿, 1024×768 노트북, 1440×900 데스크톱에서 한국어 핵심 7개 페이지(`index.html`, `be-deer.html`, `care.html`, `services.html`, `director.html`, `philosophy.html`, `about.html`)를 실제 Chromium 전체 화면 캡처와 DOM 폭 측정으로 재검수했다. BE DEER 모바일은 가로 원본 사진을 화면 높이 기준의 긴 세로 프레임으로 과도하게 자르던 부분을 4:3 비율로 정리하고 섹션 호흡을 줄였다. 철학 페이지는 768px에서 가치 카드 네 장이 한 열로 길게 쌓이던 구간을 2열로 바꾸고 장 사이 여백을 줄였다. 다른 페이지와 화면 폭에는 변경을 주지 않았다.

- 당시 공개 기준 커밋은 `8a1db4f`였고 `master`와 GitHub Pages 배포 성공을 확인했다. 당시 주소는 GitHub Pages 기본 주소였으며, 현재 운영 주소는 문서 상단의 `https://dearhani.com/`이다.
- 메인 히어로 아래에는 넉넉한 아이보리 여백, `오늘, 디어로 오시는 길` 문구와 민트색 세로선으로 구성한 브릿지가 있다. 문구는 PC 1.02~1.15rem, 모바일 0.94rem의 짙은 녹색으로 읽히게 한다.
- 메인 스토리는 히어로의 브랜드 약속 → 오늘의 서초동 날씨 → `오는 길의 날씨부터, 오늘의 몸과 생활까지.` → 대표원장 진료 기준 CTA 순서로 이어진다. 이 연결 카피와 여백을 다음 작업에서 이유 없이 되돌리지 않는다.
- 날씨 렌즈는 생성한 포토리얼 낮·밤·비·눈 배경과 실시간 Canvas 강수 효과를 사용한다. 현재 기온·강수·바람은 기상청 초단기실황의 `T1H`, `RN1`, `WSD`, `PTY`를 사용하고, 실황에 없는 하늘 상태는 초단기예보의 `SKY`, `LGT`로 보완한다. 실황이 일시적으로 없으면 초단기예보값으로 안전하게 대체한다.
- 맑거나 흐린 날은 낮 30°C부터 더위 안내, 33°C부터 매우 높은 기온과 한낮 이동·수분 섭취 주의를 안내한다. 더운 밤과 영하권·강추위도 별도 문구가 있으며, 비·눈·번개·강풍 때는 해당 위험 안내가 우선이다. 공식 특보 데이터 없이 `폭염특보`, `태풍`이라고 단정하지 않는다.
- 날씨 문구는 모든 상태에서 첫 줄에 서초동의 현재 날씨, 둘째 줄에 디어한의원으로 오시는 장면을 둔다. 맑음·흐림은 산뜻한 인사, 비·눈·강풍·번개는 안전 안내, 더위·추위는 디어한의원의 시원하거나 따뜻한 실내로 이어지는 다정한 문장을 사용한다. 날씨 데이터가 만드는 위험도 우선순위와 온도 기준은 바꾸지 않는다.
- 낮 33°C 이상은 일반 `맑음` 대신 `매우 더움 · n°C`로 표시하고 `한낮의 열기가 아주 강해요`와 서두르지 않는 이동 안내를 사용한다. 30~32°C의 `더운 날` 문구와 육안으로 구분한다. 밤 28°C 이상은 `더운 밤 · n°C`로 표시한다. 공식 특보 데이터가 없으므로 `폭염특보`라는 표현은 쓰지 않는다.
- 기상청 `T1H` 원본 온도는 반올림하지 않고 단계 판단에 사용하며, 상단 상태와 본문 문장에는 소수점 첫째 자리까지 표시한다. 따라서 32.6°C를 33°C로 올려 `매우 더움`으로 잘못 분류하지 않고, 실제 33.0°C부터 해당 단계로 전환한다.
- 흐림 장면은 검정 오버레이나 타원형 구름 도형을 사진 위에 얹지 않고, 배경 사진의 채도와 빛 및 상단 하늘 영역의 푸른 회색 톤만 조절한다. 상태 아이콘의 구름 두 개는 정해진 아이콘 영역 안에서만 둥실거리므로 본문을 가리지 않는다.
- 2026-08-05 기능성 소화불량 홈페이지형 칼럼을 `columns/seocho-functional-dyspepsia.html`에 추가했다. 사용자가 제공한 완성 HTML 디자인과 원고를 보존하면서 공통 내비게이션·푸터·목차·작성자와 발행일·네이버 예약 CTA·NAP·FAQ 구조화 데이터·참고문헌을 결합했다. 제공 사진은 웹 최적화한 `assets/images/columns/seocho-functional-dyspepsia/meal.jpg`에 저장해 대표 이미지와 본문 전환 이미지로 사용하며, Columns 카드와 sitemap에도 같은 글을 등록한다.
- 기능성 소화불량 칼럼의 모바일 H1·히어로 여백과 본문 사진 높이를 줄이고, 고정 내비게이션을 불투명하게 만들어 스크롤 중 본문이 비쳐 보이지 않게 했다. 자동 칼럼 콘텐츠 영역 밖에서도 NAP 카드 UI가 적용되도록 전용 스타일을 추가했다. 의료진 검수에 따라 헬리코박터 검사 이력과 화끈거림 표현을 메타 설명·본문·FAQ·구조화 데이터에서 제거하고, 기능성 소화불량을 추정 병태생리로 단정하지 않으며 핵심 증상·구조적 질환 배제·빈도와 기간을 함께 확인하는 표현으로 통일했다.
- 칼럼 목록·공통 칼럼 템플릿·별도 디자인 칼럼의 기본 배경을 흰색으로 통일하고 제목 체급을 PC 최대 약 48px, 모바일 최대 약 34px로 낮췄다. 일반 발행 글은 이 절제된 기본형을 자동 상속하며, 근거와 도표에 힘을 주는 저널형은 추후 별도 프리셋으로 구분한다.
- 우울증·BDNF 저널형 칼럼을 `columns/gangnam-depression-bdnf.html`에 추가했다. 검수 완료 원고의 문장·수치·순서를 보존하고 전체 흰 배경, 절제된 제목, BDNF–TrkB·생활 리듬 순환·SMILES·운동 메타분석·식사/빛/움직임 도식을 HTML/CSS/SVG로 구현했다. 원고에 없는 타과 권유나 교과서식 위기 경고는 추가하지 않았으며, FAQ 5개와 JSON-LD를 동일하게 유지했다. Columns 카드와 sitemap에도 등록했다.
- 우울증·BDNF 저널형 칼럼에 공통 `section` 패딩이 중복 적용되던 문제를 제거하고, 문단 끝과 다음 소제목 사이를 약 72~88px의 3~4줄 호흡으로 맞췄다. 본문 소제목은 기존 대비 약 2/3 체급으로 낮췄다. 글 끝에는 네이버 진료 예약·전화 상담 CTA, 한약·공진단 안내 배너, 주소와 전화번호가 드러나는 NAP 카드, 네이버 지도 링크와 오시는 길 지도를 배치했다.
- 우울증·BDNF 원고의 근거 한계를 재검수해 밝은빛 치료와 일상 햇빛을 구분하고, BMJ 운동 메타분석의 낮음~매우 낮음 근거 확실성과 지중해식 식단 메타분석의 적은 연구 수·높은 이질성·낮은 확실성을 명시했다. SMILES와 운동 효과크기 수치는 원문과 일치해 유지했다.
- Columns 상단 Featured는 고정 추천 글 대신 가장 최근 발행한 우울증·BDNF 칼럼을 `오늘의 글`로 노출하고, 카드 목록은 발행일 내림차순으로 정리했다. `tools/publish-column.mjs`도 새 칼럼 카드를 목록 시작 마커 바로 뒤에 삽입해 이후 발행 글이 자동으로 좌상단에 오도록 변경했다.
- 우울증·BDNF 칼럼 하단의 한약·공진단 배너, NAP 카드, 오시는 길 지도를 모두 같은 1040px 규격으로 통일하고 각 구간 사이에 짧은 1px 세로선을 두어 하나의 흐름으로 연결했다. Columns `오늘의 글` 제목은 의미 단위 3줄로 고정하고 마지막 줄의 체급을 조정해 모바일에서 `진료,`·`이유`가 외톨이로 남지 않게 했다.
- 우울증·BDNF 칼럼의 Columns 카드와 `오늘의 글` 미리보기 이미지는 시그니처 갈색 사슴이 운동화를 바라보며 `우울할 땐… 걸어야 해?`라고 묻는 인스타툰형 `cover-instattoon.webp`로 교체했다. 본문 내부의 저널형 도표와 디자인은 유지한다.
- 날씨 검수 쿼리는 `?weather-preview=sunny&weather-time=day&weather-temp=35#about` 형식이다. 상태는 `sunny`, `mostly-cloudy`, `cloudy`, `rain`, `heavy-rain`, `snow`, `heavy-snow`, `storm`, `strong-wind`, 시간은 `day`·`night`, 기온은 임의 숫자로 조합할 수 있다.
- BE DEER 체성분 사례의 CURRENT 카드에는 START 대비 변화량이 함께 카운트업되고, 네 카드가 좌상단부터 우하단 순서로 붉은 색연필 원 강조를 받는다. 관련 기준 커밋은 `96d71e5`다.
- BE DEER의 `함께 살펴보는 여섯 가지` 심볼은 각 항목이 바로 읽히도록 식사 도구·달과 침대·위장·부종 물방울·대장·24시간 시계로 재구성했다. `생활 리듬`은 추상적인 관찰 문구 대신 늦어진 취침, 건너뛴 끼니, 몰아서 먹는 저녁처럼 실제 하루가 무너지는 순서와 다시 세울 한 지점을 찾는 진료 문장으로 교체했다. 390·768·1440px에서 가로 넘침과 카드 내 잘림이 없음을 확인했다.
- 비급여 진료비용 페이지는 `non-covered.html`에 공개한다. 약침·한약·제증명·상담의 6개 항목을 표로 고지하며, 법적 안내는 현행 「의료법」 제45조제1항·제2항 및 같은 법 시행규칙 제42조의2를 기준으로 한다. 항목이나 비용이 달라지면 기준일, 페이지 표, sitemap과 푸터 연결을 함께 갱신한다.
- 직전 작업 묶음은 `47f86b3` → `79c3167` → `96d71e5` → `b455296` → `57516ba` → `88501ac` → `8a1db4f` 순서다. 새 세션에서는 이 상태를 기준으로 사용자의 다음 지시부터 진행한다.
- 저장소 루트의 미추적 `다국어-오역-일괄수정.patch`는 과거 작업 기록이며 현재 코드에 그대로 적용되지 않는다. 삭제·적용·커밋하지 말고 보존한다.

## 1. 작업 시작 절차

```powershell
git fetch origin
git status --short --branch
git log -10 --oneline
```

로컬 변경이 없고 `master`가 원격보다 뒤처졌다면:

```powershell
git pull --ff-only origin master
```

그다음 `AGENTS.md`, `CLAUDE.md`, 이 파일을 읽는다. 로컬 변경이 있으면 출처를 확인하기 전에는 pull, 덮어쓰기, 삭제를 하지 않는다.

## 2. 저장소 역할

### 공개 홈페이지

- 저장소: `BBINGE/dear-clinic`
- 정적 HTML/CSS/Vanilla JS
- GitHub Pages는 `master`의 공개 사이트를 배포한다.
- 칼럼 발행기가 생성한 HTML, Columns 카드, sitemap, 공개 이미지를 포함한다.

### 비공개 칼럼 콘텐츠

- 저장소: `BBINGE/dear-clinic-content`
- Pages CMS 설정, 비공개 초안 JSON, 원고 이미지를 저장한다.
- Pages CMS: https://app.pagescms.org/bbinge/dear-clinic-content/main
- 별도의 `AGENTS.md`, `CLAUDE.md`, `HANDOFF.md`가 있으며 CMS 작업은 그 문서를 함께 따른다.
- 공개 저장소에 비공개 원고, 인증정보, Deploy key를 복사하지 않는다.

## 3. 현재 공개 페이지

한국어 주요 페이지:

- `index.html` — 홈페이지
- `about.html` — About DEAR 허브
- `director.html` — 김민지 대표원장 소개
- `career.html` — 자격·학회·경력
- `philosophy.html` — 진료 철학
- `care.html` — Focus / Calm / Restore / Relief / Shape
- `services.html` — 비디어 다이어트, 디어 공진단, 원내탕전 체질한약, 디어밸런스
- `be-deer.html` — BE DEER 다이어트 독립 소개, 대표원장 운동 관련 자격, 진료 흐름, 작성자 식별 정보를 가린 네이버 영수증 리뷰와 생성형 인물 이미지
- `columns.html` — 공개 칼럼 목록
- `privacy.html`, `patient-rights.html` — 법률·환자 안내

다국어 초벌 페이지:

- 영어 `en/`
- 일본어 `ja/`
- 중국어 간체 `zh-cn/`

다국어 문구는 기계 번역 초벌이며 의료 용어, 경력, 제품 표현은 검수 전 확정 카피가 아니다. 한국어 원본을 번역 수정과 함께 바꾸지 않는다.

## 4. 칼럼 현재 상태

공개 칼럼:

- `columns/seocho-diet-clinic.html` — 최초 수동 제작 예시 칼럼
- `columns/seocho-diet-herbal-medicine.html` — Pages CMS 발행 칼럼

미리보기:

- `preview/seocho-diet-herbal-medicine.html`
- 미리보기는 `noindex`, `nofollow`, `robots.txt` 차단 대상이다.

발행기:

- `tools/publish-column.mjs`
- 테스트: `tools/test-column-publisher.mjs`
- 테스트 자료: `tools/fixtures/column-publisher-test.json`
- CMS 설치 템플릿: `tools/column-cms-template/`

발행기 변경 후 필수:

```powershell
node tools/test-column-publisher.mjs
git diff --check
```

## 5. Pages CMS 작성·발행 흐름

### 로컬 LIVE 칼럼 에디터

- 저장소 루트의 `칼럼작성기 열기.cmd`를 더블클릭하면 `tools/column-editor-server.mjs`가 로컬 전용 웹 에디터를 `http://127.0.0.1:4174/editor`에서 연다. 실행 중 나타나는 검은 창을 닫으면 에디터도 종료된다.
- 2026-08-05부터 네이버 스마트에디터원에 가까운 중앙 원고지 구조를 사용한다. 위쪽의 넣기·글자 도구, `기본 글쓰기`·`HTML` 왕복, 디어 팔레트와 자유 색상, 본문 사진, 일반 표 삽입과 행·열 추가·삭제·제목 셀 전환을 지원한다. HTML 모드에서 붙인 본문은 기본 모드로 돌아오면 실제 모양으로 확인할 수 있다.
- 발행 설정, FAQ, 참고 자료는 오른쪽 `검토하고 공개` 패널에 모았다. 미리보기는 필요할 때만 큰 화면으로 열며 PC·모바일 폭을 전환한다. 브라우저 로컬 자동 저장은 입력 후 약 0.5초 안에 이루어지고, 대표 사진이 없어도 초안 저장은 가능하지만 실제 미리보기·공개 전에는 대표 사진이 필요하다.
- 첫 샘플에서 확정한 sticky 목차, 상담 CTA, FAQ, 참고문헌, NAP은 작성자가 지우지 못하는 발행 셸로 유지한다. 본문의 `h2`에서 목차를 자동 생성한다. 과거 `body`·`designBlocks` 초안도 새 에디터에서 읽어 한 번에 HTML 본문으로 옮긴다.
- HTML 본문은 허용 태그만 발행하며 `script`, `iframe`, 폼, 이벤트 속성, `javascript:` URL 같은 실행 코드를 차단한다. HTML 본문 안에 올린 사진도 `.column-editor/media`에서 공개 글 전용 자산으로 복사한다.
- 임시 저장 JSON과 업로드 원본은 Git에서 제외되는 `.column-editor/`에만 둔다. 위쪽 `미리보기`는 작성 중인 모양을 즉시 보여 주고, 공개 시에는 `publish-column.mjs`가 본문을 다시 검증한 뒤 완성 HTML·목록·sitemap·해당 글의 공개 이미지만 생성해 커밋·push한다. 작성 중 초안은 공개 저장소에 커밋하지 않는다.
- 사용법은 `tools/칼럼작성기 사용법.txt`에 있다. 기존 `tools/column-editor.ps1`은 과거 WinForms 작성기 기록으로 남겨 두지만 기본 실행 경로에서는 사용하지 않는다.

`Save`, `미리보기 만들기`, `홈페이지에 발행`은 서로 다른 동작이다.

1. Pages CMS에서 작성한다.
2. 오른쪽 위 `Save`는 비공개 저장소에 초안을 저장한다. 홈페이지에 공개되지 않는다.
3. `미리보기 만들기`는 공개 저장소의 `preview/{slug}.html`을 갱신한다. Columns 목록과 sitemap에는 추가하지 않는다.
4. 검토 후 `저장·발행 상태`를 `홈페이지 발행 준비 완료`로 바꾸고 다시 `Save`한다.
5. `홈페이지에 발행`을 실행하면 공개 칼럼, `columns.html`, `sitemap.xml`, 공개 이미지가 갱신된다.
6. GitHub Pages 반영 후 실제 공개 주소를 확인한다.

온라인 칼럼 관리자는 자동 발행 칼럼을 다시 열어 수정 발행하거나 홈페이지에서 비공개로 내리고,
원고까지 삭제할 수 있다. 비공개는 공개 칼럼 HTML·목록 카드·sitemap·공개 이미지만 제거하고
비공개 저장소의 원고와 원본 이미지는 보존한다. 삭제는 공개 파일을 먼저 제거한 뒤 비공개 원고와
해당 이미지 폴더도 Git 커밋으로 제거하므로 필요하면 저장소 기록에서 복구할 수 있다. 최초 수동 제작
예시 칼럼은 생성 마커가 없어 자동 관리 대상에서 제외한다.

현재 Pages CMS 설정 버전은 `.pages.yml`의 `v4.1`이다. 화면 디자인 블록:

- 큰 소제목과 설명 문단
- 진료에서 확인하는 항목표
- 경우의 수 번호 카드
- 진한 배경의 핵심 문장
- 본문 사진과 설명

Pages CMS 자체의 `Save`, `Add an item`, `Choose content block` 같은 시스템 문구는 영어로 남을 수 있지만 입력 항목과 설명은 한글이다.

## 6. 생성 파일과 수정 경계

- `<!-- GENERATED_BY_DEAR_COLUMN_PUBLISHER -->`가 있는 칼럼은 발행기가 다시 생성할 수 있다.
- 수동 제작 칼럼을 발행기로 덮어쓰지 않는다. 발행기가 마커 없는 기존 파일을 발견하면 중단하도록 되어 있다.
- CMS 원고의 확정 카피를 공개 HTML에서만 고치면 다음 발행 때 사라진다. CMS 원본 JSON을 먼저 수정한다.
- 대표 이미지와 본문 이미지는 5MB 이하 JPG/JPEG/PNG/WebP만 허용한다.
- URL slug는 영문 소문자·숫자·하이픈만 사용하며 발행 후 바꾸지 않는다.

## 7. 보안과 개인정보

- 회원가입·환자정보 예약 폼은 없다. 디숭이 제한 테스트는 입력 메시지를 외부 API로 전송하고 임베드 화면에서 sessionStorage에 임시 보관하므로 문서 상단의 AI 개인정보 고지를 따른다.
- 문의·예약은 네이버 예약, 전화, 톡톡 등 외부 서비스로 연결한다.
- 칼럼과 미리보기에 환자 이름, 연락처, 진료 기록, 식별 가능한 사례를 넣지 않는다.
- 비공개 저장소의 Actions secret 이름은 `DEAR_PUBLIC_REPO_SSH_KEY`다. 값은 GitHub Secrets에만 있고 파일이나 문서에 기록하지 않는다.
- Deploy key는 공개 `BBINGE/dear-clinic` 한 저장소에만 쓰기 권한을 갖는다.
- GitHub Actions는 커밋 SHA로 고정한 `actions/checkout`을 사용한다.

## 8. SEO·검색 상태

- `robots.txt`, `sitemap.xml`, canonical, 기본 meta/OG, JSON-LD가 있다.
- 공식 독립 도메인은 `https://dearhani.com/`이며 canonical, Open Graph, hreflang, sitemap, robots와 구조화 데이터가 이 도메인을 기준으로 연결되어 있다.
- Google Search Console의 `dearhani.com` 속성 및 sitemap 제출이 완료됐다. 2026-08-24 메인 URL 검사에서 `URL이 Google에 등록되어 있음`, 페이지 색인 생성 완료, HTTPS 정상으로 확인했다.
- 네이버 서치어드바이저의 `dearhani.com` 소유권 확인과 sitemap·RSS 제출이 완료됐다. 2026-08-24 메인 색인 상태 확인에서 문서가 성공적으로 색인되었고 색인·Meta 항목이 정상으로 표시됐다.
- 새 칼럼 발행 후 사용자가 Google Search Console URL 검사에서 색인 생성을 요청한다.
- 새 칼럼이나 메타 정보가 바뀐 페이지는 네이버에서도 수집 요청 후 최신 상태를 다시 확인한다.

## 9. 현재 남은 주요 작업

### 2026-08-05 BE DEER 체성분 변화량 모션

- `be-deer.html`의 CURRENT 체성분 카드에는 현재 수치와 함께 START 대비 변화량을 청록빛 블루로 표시한다. 변화량은 체중 `−34.8kg`, 골격근량 `−4.2kg`, 체지방량 `−27.4kg`, 내장지방면적 `−143cm²`다.
- 현재 수치와 변화량이 함께 카운트업된 뒤, 체중부터 내장지방면적까지 좌상단에서 우하단 순서로 변화량 주위에 빨간 색연필 질감의 이중 원이 그려진다. 모션 최소화 설정에서는 최종 수치와 원을 즉시 표시한다.

### 2026-08-05 메인 실사형 날씨 렌즈 프로토타입

- 메인 `오늘도 소중한 당신에게` 상단의 작은 한 줄 날씨 배지를 실사형 원형 날씨 렌즈로 확장했다. 디어한의원이 실제 입점한 서초동 건물 사진을 공통 배경으로 사용하고, 그 위에 상태별 명도·채도와 햇빛, 구름, 빗줄기, 유리 표면 물방울, 전경·원경 눈발, 번개 반사광을 CSS 모션으로 겹쳐 영상처럼 움직인다. 생성형 날씨 이미지로 간판과 건물 형태를 다시 그리지 않는다.
- 기상청 초단기예보의 강수 형태뿐 아니라 기온 `T1H`, 시간당 강수량 `RN1`, 풍속 `WSD`를 함께 사용한다. 맑거나 흐린 상태에서 낮 기온이 33°C 이상이면 매우 높은 기온과 이동·수분 섭취 주의를 우선 안내하고, 30°C 이상과 더운 밤, 영하권·강추위도 별도 문구로 나눈다. 비·눈·번개·강풍 상태에서는 해당 기상 위험 안내를 우선한다. 태풍 특보 데이터가 없으므로 풍속만으로 `태풍`이라고 단정하지 않고 `강한 비·바람`으로 안내한다.
- 히어로 영상과 날씨 렌즈 사이에는 넉넉한 아이보리 여백과 `오늘, 디어로 오시는 길` 문구, 민트색 세로선으로 구성한 브릿지를 둔다. 브릿지 문구는 각주처럼 작아지지 않도록 PC 1.02~1.15rem, 모바일 0.94rem과 짙은 녹색을 사용한다. 날씨 렌즈와 다음 소개 사이에도 짧은 민트색 세로선을 두어 장면을 연결했다. 소개 카피는 `오는 길의 날씨부터, 오늘의 몸과 생활까지.`를 축으로, 오늘의 서초동 날씨 → 환자의 생활과 몸을 듣는 진료 → 대표원장의 진료 기준 CTA 순서로 이어진다. 움직임 최소화 설정에서는 모든 렌즈 모션을 정지한다.
- 실제 날씨와 무관하게 검수할 때는 메인 주소에 `?weather-preview=rain`, `heavy-rain`, `snow`, `heavy-snow`, `storm`, `strong-wind`, `sunny`, `cloudy`를 붙인다. `&weather-time=night` 또는 `day`로 시간대를, `&weather-temp=35`처럼 기온을 함께 확인할 수 있다.
- 날씨 UI는 작은 원형 렌즈가 아니라 모든 화면에서 뷰포트 좌우 끝까지 이어지는 와이드 날씨 띠다. 실제 서초동 풍경 위에는 좌측이 더 짙은 검정 유리막을 겹쳐 흰색 날짜·상태·안내 문구의 가독성을 확보한다. 상태별 이모지는 맑음 회전, 구름 왕복, 비·눈 낙하, 바람 횡이동, 천둥번개 순간 발광 모션을 사용하며, 맑음에는 검정막을 더 걷어낸 뒤 따뜻한 렌즈 플레어와 이동하는 햇빛 반사를 추가한다.
- 공개 지도 캡처와 주변 상호가 보이는 원본 사진은 더 이상 렌즈 배경으로 쓰지 않는다. 동일한 서초동풍 유리 건물 구도를 유지한 포토리얼 낮·밤·비 오는 밤·눈 오는 낮 배경을 각각 사용한다. 낮/밤과 강수 상태를 조합해 배경을 선택하되 사진 전체의 느린 확대·이동은 사용하지 않는다. 고정된 장면 위에서 햇빛과 나뭇잎 그림자, 구름 그림자, 건물 유리 반사, 차량 불빛이 짧게 발생한 뒤 쉬는 시네마그래프 리듬을 사용한다. 비는 빠른 빗줄기와 불규칙하게 흘러내리는 렌즈 물방울, 눈은 속도가 다른 원경·전경 눈발로 구분한다. 배경은 날씨 표현용 생성 이미지이며 실제 실시간 카메라 영상으로 안내하지 않는다.
- 기존 CSS 반복무늬 강수는 시각적으로 규칙적인 점과 사선이 드러나므로 실제 화면에서는 Canvas 렌즈 엔진으로 교체했다. 비는 속도가 다른 빗줄기와 렌즈 표면 물방울, 물방울 내부의 배경 확대·굴절, 하이라이트와 흐르는 자국을 실시간으로 합성한다. 눈은 위치·크기·초점·낙하 속도·바람 편차가 모두 다른 원근 입자로 생성한다. 외부 영상이나 유튜브 소스를 사용하지 않으며, 화면 밖에서는 그리기를 멈추고 모바일은 픽셀 비율과 입자 수를 낮춰 성능을 보호한다.

### 2026-08-05 `의료진` 표현과 서비스 카피 정비

- 국문 공개 페이지와 컬럼에서 기관형 표현인 `의료진`을 제거하고, 문맥에 따라 `대표원장 소개`, 주어 생략, `진료 시`, `진찰`로 바꿨다.
- 컬럼 본문·구조화 데이터·면책 문구뿐 아니라 발행기와 내부 작성기 안내도 함께 수정하고, 발행 테스트에 `의료진` 재등장 방지 검사를 추가했다.
- 서비스 페이지의 공진단·원내탕전 문구를 `조제`와 `탕전`의 차이에 맞게 고치고, 약재 관리·원내 조제·개별 복용 안내의 역할이 겹치지 않도록 01–03을 재구성했다.
- 디어밸런스는 불면·불안·우울감·집중력 저하와 동반되는 몸의 불편까지 포괄하도록 확장하고, 하단 상담 CTA를 보다 구체적인 진료 문장으로 바꿨다.

### 2026-08-05 컬럼 목차 현재 구간 표시

- 컬럼의 sticky `CONTENTS` 목차는 클릭한 항목을 즉시 활성화하고, 스크롤 중에는 현재 읽는 구간에 맞춰 자동으로 활성 항목을 바꾼다.
- 활성 항목은 디어 그린 굵은 글씨와 청록·그린·골드가 천천히 흐르는 세로 표시로 구분하며, 모션 감소 설정에서는 애니메이션을 멈춘다.
- `aria-current="location"`을 함께 갱신하고, 발행 템플릿의 CSS·JS 캐시 버전도 맞춰 이후 컬럼에 동일하게 적용한다.

### 2026-08-05 컬럼 NAP 카드 가독성 정비

- 컬럼 하단 NAP 카드의 라벨·한의원명·주소·전화·예약·위치 링크를 모두 흰색으로 통일했다.
- 환자용 NAP 카드에서는 대표자·사업자등록번호를 제거하되, 사이트 공통 푸터의 법적 사업자 정보는 유지한다.
- 기존 공개 컬럼과 미리보기뿐 아니라 `tools/publish-column.mjs`도 수정해 이후 발행되는 컬럼에 같은 기준이 적용된다.

### 2026-08-05 공식 로고 파비콘 적용

- 기존 나뭇가지형 파비콘을 공식 `D·R` 모노그램으로 교체했다.
- `assets/images/dear-favicon.png`는 브랜드 딥그린 `#247860` 바탕과 아이보리 `#F8F8F6` 심볼을 사용하며, 전체 공개 HTML이 이 파일을 참조한다.

### 2026-08-05 메인 진료시간 운영 문구 정비

- 한국어·영어·일본어·중국어 메인 진료시간 카드에서 정확한 접수마감 시각을 제거했다.
- 월·화·수·금은 점심시간 `13:00–14:00`, 목·토는 `점심시간 없이 진료`로 안내한다.
- 예약 후 방문을 권장하고, 초진 및 당일 접수는 진료 상황에 따라 조기 마감될 수 있음을 안내한다.

### 2026-08-05 메인 공간 사진 스트립 상호작용

- 메인 공진단 배송 지도 아래의 한의원 공간 사진 스트립은 평소 정지하고, 정밀 포인터 환경에서 마우스를 올리거나 키보드 초점을 맞췄을 때만 기존의 느린 이동과 미세 확대 모션을 재생한다.
- 스트립 오른쪽 아래에는 기존 팔레트를 따른 반투명 `HOVER TO MOVE` 안내와 가는 화살표를 작게 배치한다. 터치 환경에서는 실행할 수 없는 안내를 숨기고 사진을 정지 상태로 유지하며, 움직임 최소화 설정에서도 모션을 재생하지 않는다.
- 공진단 배송 지도 섹션과 공간 사진 스트립 사이에는 흰색 여백을 추가해 두 시각 요소가 바로 맞붙지 않도록 분리했다.

### 2026-08-04 진료 철학 페이지 임상 언어 개편

- `philosophy.html`은 `신호·흐름·이어짐`을 반복하던 추상적 서사를 걷어내고, 증상의 발생 시점과 경과 확인 → 현재 상태와 임상적 우선순위 설명 → 생활에서 실행 가능한 진료 계획과 재평가의 순서로 전면 개편했다.
- 기존 DEAR ME/YOU/US 장식 심볼은 각각 병력·생활 맥락을 정리하는 진료기록, 확인한 내용을 판단하는 진찰 기록, 우선순위와 재평가 시점을 담은 진료 계획표 심볼로 교체했다.
- 히어로부터 마지막 CTA까지 환자가 실제 진료에서 무엇을 질문받고, 무엇을 설명받으며, 어떻게 다음 단계를 정하는지 이어지도록 구성했다. 마지막 CTA는 대표원장 소개 대신 네이버 진료 예약과 진료 분야 확인으로 연결한다.

### 2026-08-04 메인 공진단 국내·해외 배송 지도 확장

- 메인 `index.html`의 두 번째 공진단 섹션 지도에 기존에 없던 강원 배송 경로와 목적지 표기를 추가했다.
- 국내 지도는 서울 서초에서 강원·충청·대구·부산·호남·제주로 배송되는 장면을 보여준 뒤, 세계 지도 장면으로 전환해 유럽·동남아·북미로 이어지는 배송 모션을 순서대로 반복한다.
- 세계 지도는 제공받은 참고 이미지의 실제 대륙 윤곽만 투명 PNG로 정리해 기존 틸 계열 선으로 표시하며, 장면 전환 주기는 14초에서 26초로 늦추고 택배 이동 시간도 약 두 배로 늘렸다.
- 해외 목적지 점과 배송 경로의 끝점은 북미는 미국 중부, 유럽은 프랑스 부근, 동남아는 베트남 부근에 맞춰 배치한다.
- 기존 섹션 배치와 팔레트는 유지했다. 접근성 설명에는 국내와 해외 목적지를 모두 명시하며, 움직임 최소화 설정에서는 국내 지도를 정지 상태로 표시한다.
- 오른쪽 카피는 `멀리서도 디어를 찾는 까닭`을 헤드카피로 올리고, 자세한 상담·직접 만드는 원칙·개인 맞춤에서 지역과 해외로 이어지는 편지형 본문으로 개편했다. `김민지`는 검은색의 얇은 나눔손글씨 펜체로 서명처럼 표시하고 `올림.`은 한 단계 작게 구분한다.

### 2026-08-04 메인 소개 섹션 날씨 안내 카드

- 메인 `index.html`의 `오늘도 소중한 당신에게` 소개 섹션은 기존 밝은 아이보리 배경을 유지한다. `js/weather-scene.js`는 디어한의원 고정 좌표의 현재 날씨를 화창·흐림·비·눈·천둥 아이콘과 친근한 방문 안내 문구로 표시한다. 날씨 안내는 데스크톱에서 본문을 밀지 않는 섹션 최상단 중앙의 테두리 없는 작은 한 줄형으로 배치하며, 모바일에서는 문서 흐름 안에서 문구만 다음 줄로 흐른다.
- 날씨 데이터는 기상청 `단기예보 조회서비스`의 초단기실황·초단기예보와 서초동 디어한의원 고정 좌표(위도 37.4918829, 경도 127.0252346)를 사용한다. 최신 실황이 아직 제공되지 않은 경우 최근 3개 정시를 차례로 확인하고, 모두 없을 때만 초단기예보값으로 대체한다. GitHub Actions의 `KMA_SERVICE_KEY` 저장소 Secret으로 인증하며, `.github/workflows/deploy-pages.yml`이 매시 17분·47분에 `tools/fetch-kma-weather.mjs`를 실행해 공개 배포물에 키가 빠진 `weather-data.json`만 생성한다. 방문자 위치 권한은 요청하지 않는다.
- 2026-08-10부터 메인 날씨는 관측 시각이 2시간 이내인 기상청 `weather-data.json`을 우선 사용하고 10분마다 갱신한다. 기상청 자료가 없거나 오래됐을 때만 Open-Meteo 현재 날씨를 보조로 사용하며, 두 조회가 모두 실패하면 `날씨 갱신 중`으로 전환한다.
- 날씨 데이터 출처는 실제 적용된 데이터에 따라 카드 안에 `Open-Meteo 현재 날씨` 또는 기상청 출처로 표시한다. Pages 배포 방식은 GitHub Actions이며, `master` push·예약 실행·수동 실행 때 전체 정적 사이트를 다시 배포한다.
- 기상청 서버 연결이 일시적으로 지연되면 공개 중인 마지막 정상 `weather-data.json`을 보존하지만, 브라우저에서는 2시간이 지난 보존값을 현재 날씨로 사용하지 않는다.
- GitHub Actions의 공공데이터포털 연결은 IPv4와 30초 연결 제한을 명시해 GitHub 러너의 연결 시간초과를 줄인다. 기상청 갱신이 실패해도 기존 Pages 배포는 마친 뒤 워크플로를 실패 처리하므로, 날씨 장애가 초록색 성공 상태에 숨지 않는다.
- 하늘 상태는 `맑음`, `구름 많음`, `흐림`으로 구분하며 우산 안내는 실제 비·눈 상태에만 사용한다. 카드 출처에는 실제 관측 시각을 함께 표시한다.
- 모든 날씨 조회가 실패하거나 보조 기상청 데이터까지 오래됐으면 카드 출처를 `날씨 갱신 중`으로 표시한다. 아이콘 모션은 카드 내부에서만 작동하고 `prefers-reduced-motion` 환경에서는 정지한다.
- 제목은 `오늘도 / 소중한 당신에게`이며 따옴표 없이 `소중한`만 Pretendard 700으로 강조한다. 제목·본문·CTA의 기존 녹색 UI를 복구했다.

### 2026-08-03 메인 공진단 사진·전국 안내 개편

- 메인의 첫 공진단 섹션은 기존 상단 2장·하단 3장으로 나뉘었던 사진을 모두 상단 우측에 모아, 공간 사진 필름처럼 5장이 가로로 끊김 없이 흐르는 슬라이드로 통합했다. 왼쪽 확정 카피는 고정하며 변경하지 않았다.
- 두 번째 공진단 섹션의 왼쪽 사진 프레임은 실제 남한 해안선과 제주 윤곽을 반영한 코드 기반 지도 모션으로 교체했다. 서울 서초에서 충청·대구·부산·호남·제주로 이어지는 화살표 경로 위로 디어 택배박스 심볼이 이동한다. 오른쪽 확정 카피는 유지하며, 움직임 최소화 설정에서는 경로와 목적지를 정지 상태로 표시하고 이동 박스는 숨긴다.
- 1440px·768px·390px에서 사진 전환, 지도 배치, 가로 넘침 여부를 확인했다. 데스크톱은 지도 왼쪽·카피 오른쪽, 모바일과 태블릿은 카피 다음에 지도가 이어진다.

### 2026-08-03 메인 진료 체계 섹션 임시 보관

- 메인 `index.html`의 `DEAR CLINICAL PROCESS`와 `DEAR HEALTH SYSTEM` 두 섹션은 사용자 요청으로 화면에서 임시 제외했다.
- 삭제하지 않고 `<template id="archived-main-clinical-sections-20260803">` 안에 HTML 원형을 그대로 보관했다. 관련 CSS와 `js/main.js`의 탭 코드도 유지한다.
- 복원할 때는 두 섹션을 감싼 `<template>` 시작·종료 태그와 임시 보관 주석만 제거하면 된다.
- 기존 두 섹션 대신 BE DEER와 공진단 사이에 짧은 브랜드 브릿지를 추가했다. `ONE PERSON, MANY NEEDS`와 “이름이 다른 진료도, 결국 한 사람의 오늘에서 시작됩니다.” 문장, 두 진료 이야기를 잇는 곡선 심볼로 구성한다.
- 새 브릿지는 진료 체계를 다시 설명하지 않고, 서로 다른 진료도 한 사람의 몸과 생활을 먼저 본다는 디어의 태도만 전달한다.

### 2026-08-03 다국어 번역 전면 재검수

- 영어·일본어·중국어 24개 페이지를 다시 대조해 공진단을 `resonance groups`로 옮긴 영어 오역, 비급여를 급여(월급)로 해석한 일본어 `非給与`, 중국어의 `一妃的恭谨丹`, `不达标`, `补货`처럼 의미가 달라진 기계 번역을 교정했다.
- 영어 원장 소개와 서비스 CTA, 일본어 서비스·Care 문장, 중국어 메인·원장 소개·진료 철학·Care·칼럼·서비스의 깨진 문장을 한국어 원문의 의미에 맞게 정리했다. `DEAR`, `BE DEER`, `DEER BALANCE`, `拱辰丹` 등 확정 브랜드·제품 표기는 유지했다.
- 일본어 `DEARハン医院`은 `DEAR韓医院`으로 통일하고, 비급여 안내는 일본어 `保険適用外項目のご案内`, 중국어 `非医保项目说明`으로 정리했다.
- 목요일을 포함한 진료 요일·시간, 전화·예약 링크·NAP 숫자는 변경하지 않았다. 한국어 상호와 주소 원문은 외국인 환자의 현지 길찾기와 법적 식별을 위해 다국어 푸터에 그대로 유지한다.
- 다국어 24개 페이지의 이미지 로딩·가로 넘침·언어 속성을 확인하고, 영어 메인·일본어 서비스·중국어 원장 소개는 1440px·768px·390px에서 추가 확인했다.
- 저장소 루트의 `다국어-오역-일괄수정.patch`는 과거 작업 기록으로 보존하되, 현재 파일에는 그대로 적용되지 않으므로 새 번역의 기준이나 재적용 소스로 사용하지 않는다.

### 2026-07-28 작업 보류 지점

- 현재 기능·디자인 구현은 부끄님이 전체 사이트를 검토하고 피드백을 줄 때까지 보류한다. 재개 시 새 기능을 임의로 추가하지 말고 부끄님 피드백 반영을 최우선으로 한다.
- 다른 PC나 새 Claude·Codex 작업에서 시작할 때는 이 저장소를 최신화하고 `AGENTS.md` → `CLAUDE.md` → `HANDOFF.md`를 읽은 뒤, 최근 커밋과 공개 Pages 상태를 확인한다. 사용자가 요약·브리프만 요청했다면 파일을 수정하지 않는다.
- 공개 저장소에는 현재 사이트가 실행되는 코드와 배포용 이미지·영상이 모두 있다. 메인 히어로의 웹 최적화 MP4·WebM·포스터는 커밋되어 있으나, 전달받은 편집 전 영상 원본은 저장소 밖 로컬 자산이므로 새 PC에서 원본 재인코딩이 필요하면 다시 제공받아야 한다.
- 환자 개인정보가 남은 리뷰 원본, 비공개 초안, 인증정보는 공개 저장소에 추가하지 않는다. 공개 전 원본 자산 보관이 필요하면 비공개 저장소를 사용한다.

- 다국어 24개 페이지의 title·description·canonical·hreflang을 유지하면서 Open Graph URL·공유 이미지를 보완했다. 영어 원장 에세이·Care·철학·칼럼과 일본어·중국어의 고신뢰 기계 번역 표현을 자연스럽게 다듬었다. 확인되지 않은 중국어 원장 한자명은 `Kim Minji`, 터한의원은 한국어 원문 병기로 두었으며, 공식 표기가 확인되면 교체한다.
- 다국어 메인도 한국어 메인과 같은 WebM 우선·MP4 대체·WebP 포스터 구조를 사용한다. 철학 페이지의 대용량 PNG 7개는 새 WebP 표시 자산으로 교체했으며 원본 PNG는 보존한다.
- `404.html`을 추가했다. 잘못된 주소에서는 한국어 홈·진료 안내·영어·일본어·중국어 홈·전화·네이버 예약으로 이동할 수 있고 검색 색인은 차단한다. `dearhani.com` 연결에 맞춰 자산과 이동 경로는 도메인 루트(`/`) 기준으로 전환했다.

- 메인페이지의 BE DEER CTA와 공진단 소개 사이에 `DEAR CLINICAL PROCESS` 섹션을 추가했다. 병력과 맥락 파악 → 진찰 및 상태 평가 → 감별·위험 신호·한의학적 변증을 포함한 임상적 종합 판단 → 진료 계획과 시행 → 재평가와 조정의 순환 구조이며, 기록·안전·설명은 전 과정을 관통하는 기준으로 분리했다. 기존 메인 섹션은 변경하지 않았다.
- `DEAR CLINICAL PROCESS`와 기존 공진단 소개 사이에 `DEAR HEALTH SYSTEM`을 추가했다. 기존 서비스는 삭제하지 않고 PROGRAM(BE DEER)·CLINICAL CARE(증상과 기능의 회복)·HERBAL MEDICINE(진찰에 근거한 한약 진료)의 서로 다른 세 층위로 분류했으며, 탭을 눌러 각 하위 내용을 확인한다.
- `care.html`은 기존 다섯 가지 Care 탭과 문구를 유지하면서 사진 중심 구조로 개편했다. 실제 원장 사진과 공간 사진을 참고한 문진·상태 평가·치료 준비 이미지 3장과 실제 치료실 사진을 사용한다. 진료 장면과 다섯 Care 사이에는 같은 진료 과정에서 오늘의 상태에 따라 살피는 초점이 달라진다는 연결 문장을 두었다. PC 첫 상담 사진은 글 블록과 비슷한 340–400px 높이의 컨테이너 안에서 크롭하고, 진료 장면은 넓은 대표 사진 1장과 하단 보조 사진 2장의 기존 흐름을 유지하면서 3:1 와이드 비율을 실제 높이에 적용했다. 모바일은 고정 높이를 해제하고 16:9 비율을 사용한다. 밝은 첫 화면의 상단 내비게이션은 초록색으로 보정했다.
- `care.html`의 사진 크롭·배치·초록 갤러리·탭 인터랙션은 유지한다. 카피는 같은 증상도 원인이 다를 수 있다는 문제 제기에서 병력 확인, 상태 평가, 임상적 우선순위 판단, 다섯 Care의 진료 초점, 반복되는 불편을 확인하는 최종 CTA로 이어지도록 개편했다. 감성적·추상적 표현 대신 발생 시점과 경과, 동반 증상, 기능적 제한, 수면·소화·활동 패턴처럼 환자가 이해할 수 있는 임상 언어를 사용한다.
- 한국어 메인 히어로는 외국인 모델 제품 영상을 다시 사용한다. 저장된 후보 중 압축이 가장 적은 약 5MB `assets/video/hero.mp4`를 화질 우선 소스로 먼저 제공하고, WebM과 기존 웹 최적화 MP4를 호환용으로 뒤에 둔다. 6초 지점의 WebP 포스터는 로딩 전에 표시한다. 카피는 굵은 `Always "DEAR" You`와 얇은 `오늘도 소중한 당신의 디어한의원입니다.` 두 줄을 유지하며, 영상 위 네비게이션에는 반투명 딥그린 배경과 블러를 적용해 밝은 장면에서도 읽히게 했다. 남자 얼굴이 상단 네비게이션에 가리지 않도록 영상 초점을 위쪽으로 조정했으며, 본문에는 흰 계열의 반투명 그라데이션과 딥그린 카피를 적용해 영상 원래 색과 글자 가독성을 함께 유지한다.
- `services.html`은 기존 네 서비스와 확정 문구·사진을 유지하면서 `현재 상태와 목적 → 진료 판단 → 방법 선택 → 경과 확인`의 순서가 먼저 보이도록 재구성했다. 네 서비스를 상품처럼 나열하지 않고 PROGRAM·HERBAL CARE·DAILY CARE의 서로 다른 역할로 표시했으며, 히어로·서비스별 사진·제목·FIND YOUR CARE·CTA 크기를 줄여 한 요소가 화면 전체를 독점하지 않도록 했다.
- 메인 첫 진입 시 공진단·진료·패키지 안내 이미지 3종을 서로 독립된 팝업으로 노출한다. 페이지 배경은 흐리게 만들지 않고 각 팝업의 그림자로만 구분한다. PC에서는 3개를 나란히, 모바일에서는 하나씩 차례로 보여주며 개별 닫기와 오늘 하루 열지 않음을 지원한다. 숨김 상태는 브라우저 `localStorage`에 당일 날짜로만 저장한다.
- BE DEER 페이지는 2026-07-28 데스크톱·태블릿·모바일 타이포와 여백을 1차 정리했으며, 실제 공개 반응과 후기·생성형 이미지의 의료광고 심의 필요성을 운영 전 재점검
- BE DEER의 `WHY I MADE BE DEER` 본문은 원장 개인의 말투가 느껴지도록 운동에 대한 공부와 먹는 즐거움, 지속 가능한 다이어트를 잇는 확정 카피로 교체했다. 밝은 배경의 `함께 살펴보는 여섯 가지`는 기존 작은 번호와 픽토그램을 유지한다. 짙은 초록색 `이런 경우라면` 여섯 항목은 환자가 실제로 말하는 문장으로 제목을 개편하고, 픽토그램 대신 낮은 불투명도의 큰 번호를 배경에 배치했다. 설명은 단기 체중 변동과 감량 정체의 구분, 감량 후 생리적 보상 반응, 식욕의 반복 조건, 체액과 체지방의 구분, 비운동성 활동, 대사질환 위험 평가의 임상 논리로 구성한다.
- BE DEER의 여섯 가지 관찰 항목 중 `배변` 심볼은 해부학적 대장 형태가 불명확하게 보인다는 피드백을 반영해, 배변 횟수·양상·리듬 확인을 뜻하는 달력과 체크 표시로 교체했다.
- Google Analytics 4 측정 ID `G-E5M36LQ66P`를 공통 `js/main.js`에 연결했다. 공개 페이지 조회와 전화·네이버 예약·톡톡·블로그·인스타그램·BE DEER 진입 클릭을 `dear_cta_click` 이벤트로 전송한다. `preview/` 페이지는 공개 페이지와 통계가 섞이지 않도록 별도 태그를 추가하지 않았다. 기존 `js/click-tracking.js`는 중복 이벤트가 생기지 않도록 호환 가드를 둔다.
- 실제 글쓰기 경험을 통해 Pages CMS 디자인 블록 사용성을 추가 점검
- 기존 CMS 발행 칼럼에 디자인 블록이 필요하면 카피를 임의 변경하지 말고 사용자와 구성을 먼저 결정
- 영어·일본어·중국어 의료 문구 검수
  - 완료: 고유명사·의료용어 오역 일괄 수정 (2026-07-27). 공진단(共振団·贡金丹 등→拱辰丹), 디어(Deere·鹿→DEAR), 비디어(Video·ビデオ·视频→Be Deer), 한의원(韓議員·東洋医院·东方医院→韓医院·韩医院), 원내탕전(園内湯田·浴室→院内湯煎·煎药室), 대표원장(CEO·首席执行官→Director·代表院長·代表院长), 학회·자격·경력 명칭 3개 언어 전면 재정비. 한국어 원본의 `BE DEER`·`DEER BALANCE` 표기는 확정 카피로 보고 유지함.
  - 완료: 2026-07-28 자동 문맥 검수로 중복 doctype, 언어별 OG locale, 영어 Services 전반의 번역투, 일본어 `ビDEAR`·`湯前`·`湯電`, 중국어 `谐振组`·`元内汤全`·`代表董事` 등 고신뢰 오류를 추가 수정하고 전 다국어 페이지에 클릭 이벤트 스크립트를 연결함.
  - 남음: ① 학회 공식 영문 명칭 확인(AKOM·추나학회 등 잠정 표기), ② 前 근무처 로마자 표기 확인(ター韓医院/Teo韩医院 = 터한의원 잠정), ③ 중국어 대표원장 성함 한자 표기(金敏智는 기계 추정, 본인 확인 필요), ④ director 에세이 등 본문 문체 다듬기
  - 2026-08-05: 한국어 15개·영어 8개·일본어 8개·중국어 8개 공개 페이지를 1440×900, 1024×768, 390×844에서 전수 점검했다. 일본어 Services의 긴 제목이 1024px에서 가로로 넘치는 문제를 언어별 줄바꿈 규칙으로 수정했다. About의 `medical staff` 표현을 대표원장/진료 방식으로 바꾸고, 3개 외국어 Philosophy를 한국어 임상 서사에 맞춰 전면 정비했다. Services의 공진단·원내탕전·DEER BALANCE 문구와 Director의 명백한 기계번역 문장도 교정했다. 고유명사인 전 근무처 표기와 중국어 성명 한자는 본인 또는 원어민 확인 전까지 추가 확정하지 않는다.
- 독립 도메인 구매·연결
- 새 칼럼 발행 후 Google·네이버 수집 요청

## 10. 검증 기준

### 2026-09-05 디숭이 이동·접기 및 패널 모션

- 인사 말풍선은 Pretendard 14px/500을 명시하고 한국어를 두 줄(궁금한 거 있어요? / 제가 도와드릴게요 :))로 고정했다.
- 채팅 패널은 220ms 투명도·확대/축소 전환 후 닫히며, reduced-motion에서는 전환을 생략한다. 캐릭터와 API·대화 로직은 그대로다.
- 마우스/포인터 드래그 및 Alt+방향키로 캐릭터를 이동한다. 6px 미만 움직임은 클릭으로 처리하고 드래그 뒤 채팅창이 열리는 것을 막는다. 화면 밖으로 나가지 않도록 제한한다.
- 캐릭터 위 − 버튼으로 작은 디숭이 탭으로 접고, 탭을 누르면 다시 펼친다. 위치와 접힘 상태는 같은 탭의 페이지 이동 동안 sessionStorage에 유지하며 테스트 종료 시 지운다. 콘텐츠 자동 회피가 아니라 사용자 위치·크기 조절 방식이다.
- 위젯 캐시는 `20260905-3`. 공진단에서 실제 드래그 후 칼럼 이동 시 위치·접힘 유지, 열기/닫기 중간 투명도와 종료 상태를 브라우저에서 확인했다. 테스트 표시·암호 제한은 유지한다.

### 2026-09-05 디숭이 단독 캐릭터 실행 버튼

- 사용자 승인으로 홈페이지 바깥 실행 버튼만 원형 프사에서 머리띠를 쓰고 손을 든 투명 전신 캐릭터로 교체했다. 채팅방 안의 `disoongi-profile.png`와 대화·예약·인증 코드는 유지한다.
- 첫 생성 시안의 짧고 통통한 몸·팔다리를 유지했다. 사용자가 제공한 항암치료 칼럼 그림의 이마 표식 네 조각을 직접 추출·합성하며, 가운데 두 조각의 D 형태를 다시 생성하거나 일반 꽃잎으로 바꾸지 않았다. 배경은 실제 알파로 제거했다. `tools/prepare-disoongi-launcher.py`는 입력 두 장을 받아 이마 이외 몸 픽셀 불변과 투명도를 검사하며 같은 결과를 재현한다.
- 보존 PNG는 `assets/images/disoongi-launcher-v1.png`, 실제 로드 자산은 600px 높이 무손실 WebP `disoongi-launcher-v1.webp`다. PC 버튼 144×214px, 768px 이하 100×149px로 표시하고 원형 배경·테두리·크롭을 제거했다. 퀵메뉴와 간격을 두며 패널을 열면 캐릭터는 숨긴다. 첫 인사와 reduced-motion 대응은 유지한다.
- 위젯 캐시는 `20260905-2`이며 테스트 링크·암호 제한은 그대로다. 일반 공개 활성화나 개인정보 절차 변경은 이번 범위가 아니다.

### 2026-09-05 홈페이지 공통 디숭이 테스트 배포

- 공개 사이트에 코드를 배포하되 일반 방문자는 위젯을 보지 않는 테스트 전용 상태다. `/?dear-ai-test=1`로 들어온 탭에서만 표시하고 API 테스트 암호 검증도 유지한다. `dear-ai-test=off` 또는 패널의 테스트 종료로 해제한다. 이 쿼리는 표시 전환이며 인증 수단이 아니다.
- 공통 `js/main.js`가 테스트 탭에서만 `js/dear-ai-widget.js`를 불러온다. 독립 외국인 예약 페이지에는 별도 연결했다. 사이트맵 79개 페이지 모두 두 진입 경로 중 하나를 가진다. 기존 공통 스크립트가 캐시돼 있으면 첫 진입 때 강력 새로고침이 필요할 수 있다.
- PC 떠 있는 패널/모바일 넓은 패널, 한 번의 인사 말풍선과 짧은 꼬물 모션, 닫기·Escape·테스트 종료를 제공한다. reduced-motion 환경에서는 모션을 끈다. 원본 디숭이는 바꾸지 않았으며 기존 대기 공간 사진 `care-main-space.jpg`에 크림색 덮개를 적용했다.
- 임베드 테스트에서는 최근 14개 메시지와 테스트 접근 코드를 해당 탭의 sessionStorage에 임시 보관해 페이지 간 이어간다. 마지막 저장 후 30분이 지난 기록은 다음 로드에서 폐기한다. 새 대화/테스트 종료로 삭제한다. API 키 및 대화 원문 서버 로그/DB 저장은 없다. 브라우저 복원 기능에 따라 세션이 복원될 수 있으므로 공용 PC에서는 테스트 종료를 사용한다. 공개 전 보관 방식과 개인정보 안내는 별도로 확정한다.
- `booking_route`를 LLM이 domestic/domestic_alternative/international로 분류한다. 네이버 이용이 어려운 한국인은 인스타그램/전화로 안내하며 외국인 페이지로 보내지 않는다. 외국인 페이지는 예약 접수 폼이 아닌 일정 및 전화 안내 페이지다. Instagram 버튼은 공식 계정으로 열어 DM을 보내게 한다.
- 한국어/영어/일본어/중국어 페이지별 기본 UI와 인사를 적용한다. 실제 한국인 네이버 미사용 요청 및 영어 외국인 요청을 확인했다. PC·390px 모바일·768px 태블릿에서 패널, 대화 복원 및 테스트 종료를 확인했다. 일반 방문자 미노출과 사이트맵 진입 연결을 모의 검사한다.
- Worker 배포 버전 `b9c2f5c8-3d54-44cf-be23-4c827b14c2b3`. 개인정보 절차·공개 활성화는 아직 하지 않았다.

### 2026-09-05 디숭이 AI 안내 비공개 테스트판

- 사용자 승인으로 실제 LLM 기반 안내 위젯의 비공개 테스트판을 구현했다. 공개 메뉴나 공통 홈페이지에는 아직 연결하지 않는다.
- 테스트 화면은 `preview/dear-ai.html`, 전용 스타일과 동작은 `css/dear-ai.css`, `js/dear-ai.js`에 있다. 부끄님이 제작한 원본 디숭이 이미지를 파생 생성 없이 `assets/images/disoongi-profile.png` 프로필로 사용한다.
- 백엔드는 `worker/dear-ai/`의 Cloudflare Worker다. 배포 이름은 `dear-ai-preview`, 주소는 `https://dear-ai-preview.dearhani-ai.workers.dev`이며 본사이트 DNS와 GitHub Pages 구조는 변경하지 않았다.
- Claude `claude-sonnet-4-6`이 전체 대화 맥락으로 `계속 대화`, `예약 제안`, `긴급 안내` 중 하나를 구조화해 선택한다. 코드의 키워드·횟수 트리거와 응답 본문의 예약 태그 파싱은 사용하지 않는다.
- 말투는 자연스러운 해요체를 기본으로 하되 오타·비문·반말·욕설·밈·사투리·연령대별 표현을 뜻과 감정 중심으로 해석한다. 유행어를 억지로 먼저 쓰지 않고, 이모지와 이모티콘은 상황에 맞게 제한적으로 바꿔 쓴다.
- 테스트판은 별도 암호로 잠그고, 대화 원문을 서버 DB·브라우저 영구 저장소·GA4·NAVER 로그에 저장하지 않는다. 최근 14개 메시지만 브라우저 메모리에서 요청마다 전달한다.
- Anthropic API 키는 공개 저장소에 두지 않고 Worker Secret `ANTHROPIC_API_KEY`로만 등록한다. 테스트 암호도 Worker Secret `PREVIEW_ACCESS_CODE`로 관리하며 문서에 값을 기록하지 않는다.
- 정적·보안 계약 검사는 `node tools/test-dear-ai.mjs`, Worker 번들 검사는 `worker/dear-ai`에서 `npm run check`로 실행한다.
- 개인정보처리방침과 국외 이전·처리 안내, 공개 노출 위치, 남용 방지, 비식별 전환 측정, 외부 채널 지식 동기화 범위는 삥이·부끄님 실사용 테스트와 승인 뒤 확정한다. 그 전에는 환자에게 공개하지 않는다.

- HTML/CSS/JS 문법
- `git diff --check`
- 내부 링크와 외부 CTA
- Chrome 계열 데스크톱
- 768px 태블릿
- 390px 전후 모바일
- 긴 한국어 제목과 줄바꿈
- GitHub Pages 실제 배포
- 칼럼 변경 시 미리보기와 발행의 분리

### 2026-09-05 디숭이 API 연결 오류 진단

- ANTHROPIC_API_KEY 및 PREVIEW_ACCESS_CODE 비밀변수 이름이 배포 서버에 존재함을 확인했다. 키 값은 읽거나 출력하지 않았다.
- 승인받은 진단 변경으로 인증된 테스트 응답에 고정된 오류 분류만 추가했다. 외부 오류 원문, 키, 대화 내용은 반환하거나 로깅하지 않는다.
- 실제 예약 질문 요청의 Anthropic 응답은 HTTP 403, `forbidden`, `Request not allowed`였다. 방문자 화면에서는 기존 HTTP 502 안내가 유지된다.
- Worker 배포 버전: `0735fa86-6c67-44e4-b78e-e3fe282c1db1`.
- 아직 해결되지 않았다. 계정/키 권한과 서버 발신 경로의 접근 제한은 이 일반 오류만으로 구분할 수 없다. Console 측 호출과 비교 확인이 다음 단계이며 추가 충전이나 모델 교체는 근거 없이 진행하지 않는다.
- 추가 검증: 사용자가 Console Playground에서 동일한 `claude-sonnet-4-6` 모델의 응답 성공을 확인했다. 이는 Worker에 저장된 개별 API 키의 유효성까지 증명하지는 않는다.
- 서버 내부 키 형식 검사에서는 예상 API 키 접두사, 값 존재, 공백/줄바꿈/따옴표 없음이 확인됐다. 키 원문은 반환하지 않았다. 시스템 프롬프트·캐시·도구 없이 고정된 `안녕`과 32 출력 토큰만 요청해도 동일한 403이 발생했다.
- 임시 형식 검사 및 최소 요청 기능은 검사 후 제거하고 재배포했다. 현재 Worker 버전은 `b7ea6211-445a-4a52-b1f8-17fd19c9238b`이며 대화 코드는 진단 전과 동일하다.
- 남은 구분 검증은 동일 API 키를 사용한 서버 밖 호출이다. 저장된 Secret 원문을 추출하거나 채팅에 키를 다시 요구하지 않는다. 아직 원인이 서버 경로인지 키 권한인지 확정하지 않았다.
- 후속 검사: `tools/test-dear-ai-local.ps1`의 숨김 입력으로 같은 키를 PC에서 호출했으며 사용자가 `HTTP: 200 / RESULT: SUCCESS`를 보고했다. 스크립트는 키를 인자/파일/출력에 남기지 않고 고정된 인사만 보낸다.
- Worker의 실패 응답에서 `request.cf.colo` 및 상위 응답 CF-Ray 지역 접미사가 모두 `HKG`였다. Anthropic 공식 API 지원 지역 목록(https://www.anthropic.com/supported-countries)에 한국은 있고 홍콩은 없다. 지역 경유 제한이 유력한 가설이나 일반 403만으로 확정되지 않았다. 데이터센터 코드는 수신/응답 경로의 단서이며 발신 IP의 위치를 독립 검증한 것은 아니다.
- 오류 진단에는 인증 후 데이터센터 코드만 추가했다. 지역 배치 변경이나 프록시 도입은 수행하지 않았다. Worker 버전 `2e9315a4-1cdf-4222-a9e5-d08c85ad120d`.
- 해결 검증: 후속 작업에서 Cloudflare 공식 Placement Hints의 `placement.region: aws:ap-northeast-2`(서울 인근)를 적용했다. 키, 모델, 프롬프트, DNS, 도메인, 결제 플랜은 변경하지 않았다. 배포 버전은 `413174c0-6c1c-4a6f-b850-0a2a566bba6b`.
- 적용 직후 같은 Worker의 실제 API 호출이 HTTP 200으로 성공했다. `예약 어케하노 ㅡㅡ`, `야 예약 어캐해`는 `offer_booking`, `그냥 구경 중이야 아직 예약 생각은 없어`는 `continue`를 반환했다. 위치 설정 변경만으로 실패가 해소돼 이전 서버 경로와의 연관성을 확인했으나 Anthropic 내부 차단 규칙 자체를 확인한 것은 아니다.
- Placement Hints는 지정 클라우드 리전에 지연시간상 가까운 Cloudflare 데이터센터를 선택하는 기능이며 서울 고정이나 국내 데이터 처리 보장이 아니다. 실제 처리 위치를 응답 성공만으로 확정하지 않는다. 참고: https://developers.cloudflare.com/workers/configuration/placement/ .
- 모의 검사에 서울 인근 배치 설정 회귀 검사를 추가했다. 다음 단계는 사용자의 비공개 화면 실사용 및 말투/예약 흐름 조정이다. 실제 모델 답변에 마크다운 강조나 어색한 구어 표현이 간혹 있으므로 연결 성공과 대화 품질 승인은 별개로 본다.
- 사용자 피드백 반영: 별표 마크다운 강조 대신 평문과 문맥에 맞는 이모지를 쓰도록 지침을 보강하고 서버에서 남은 별표 강조를 제거한다. 이모지 및 `:)`, `^~^` 보존 회귀 검사를 추가했다. Worker 버전 `26314f61-b552-4d0e-b51f-f177d3c21898`.
- 상단/메시지의 디숭이 원본 이미지는 변경하지 않고 CSS 확대율만 145%에서 115%로 줄이고 중앙 배치했다. 전용 CSS 캐시는 `20260905-2`. 브라우저 검증 도구가 실행 단계에서 실패해 시각적 최종 검수는 아직 미완료다.

## 11. 작업 종료 절차

1. 변경 파일과 사용자 기존 변경을 구분한다.
2. 테스트와 화면 검증을 수행한다.
3. 운영 상태·절차·남은 작업이 달라졌으면 이 파일을 갱신한다.
4. `HANDOFF.local.md`, 비밀번호, 토큰, 키, 환자정보가 staging에 없는지 확인한다.
5. 한국어 커밋 메시지로 커밋한다.
6. `git push origin master`
7. 원격과 실제 GitHub Pages 결과를 확인한다.

## 12. 다른 PC에서 처음 시작

공개 홈페이지:

```powershell
git clone https://github.com/BBINGE/dear-clinic.git
cd dear-clinic
```

비공개 칼럼 콘텐츠는 GitHub 권한이 있는 계정으로 별도 clone한다.

```powershell
git clone https://github.com/BBINGE/dear-clinic-content.git
cd dear-clinic-content
```

Claude 또는 Codex에 보낼 첫 문장:

```text
저장소 루트의 AGENTS.md, CLAUDE.md, HANDOFF.md를 처음부터 끝까지 읽고,
git status와 최근 커밋, 원격 브랜치 상태를 확인한 뒤 현재 상태에서 이어서 작업해줘.
```


## 2026-09-09 다이어트 중단 칼럼 제목 가독성
- 사용자 요청으로 H1 강제 줄바꿈을 제거했다. 화면 폭에 따라 자연스럽게 줄바꿈한다. 본문 주요 소제목은 23~28px·700 굵기로 강조하며 참고자료·병원 안내는 기존 크기를 유지한다.


## 2026-09-11 자율신경·스트레스 칼럼 발행
- 사용자 승인 본문과 상세페이지 디자인을 columns/autonomic-stress.html로 발행. 증상의 시간표, 침·HRV 근거, 두 사례, FAQ 6개 포함. CASE 01 치료 후 경과는 사용자가 추가 제공한 내용으로 반영.
- 새벽 03:10 디숭이 이미지는 목록·공유 썸네일 전용. 본문에는 시간표 다음 목·어깨 AI 생성 사진과 고지 유지.
- 전용 css/autonomic-stress.css 및 js/autonomic-stress.js, 로컬 Pretendard 사용. 전체 소제목 강제 줄바꿈 없음, Method 문단 간격 22px, 시계는 점만 회전.
- canonical·OG·Article/MedicalWebPage/FAQPage·발행일·저자, Columns·RSS·sitemap·최신 메뉴 연결. 수작업형 페이지로 CMS 생성 마커 없음. 기존 로컬 작업 폴더는 보존.

## 2026-09-11 자율신경·스트레스 칼럼 공통 사이트 셸 복구
- 신규 칼럼의 숨김 `.nav` 자리표시자와 별도 mast를 제거하고, 다른 공개 칼럼과 같은 DEAR 공통 내비게이션·모바일 메뉴·메가메뉴 진입 구조를 복구했다.
- 승인 원고·이미지·도표·사례·FAQ는 변경하지 않았다. 기존 하단 문구를 공통 상담 CTA 구조로 옮기고, 공식 주소·전화·네이버 예약·위치 보기로 구성된 NAP 카드를 추가했다. 공통 푸터의 `contact` ID와 겹치던 CTA ID도 제거했다.
- 전용 비주얼 CSS는 유지하되 고정 공통 내비가 밝은 칼럼 배경에서 보이도록 대비와 첫 화면 상단 여백을 보정했다. 공통 `.hero h1`의 흰색 규칙 때문에 사라지던 제목색을 페이지 범위에서 복구했고, 1024px에서 24px 넘치던 작성·검토 안내 폭도 수정했다.
- `tools/test-autonomic-stress-ui.cjs`로 1440·1024·768·390px의 내비/메가메뉴, 모바일 토글, 빠른 메뉴, CTA, NAP, 푸터, 고유 ID, JS 오류와 가로폭을 검사한다. SEO 표면 검사에도 이 페이지의 내비·CTA·NAP·고유 ID 계약을 추가했다.

## 2026-09-11 자율신경·스트레스 칼럼 하단 전환 UI 개선
- References를 2열 카드형 인덱스로 재구성하고 820px 이하에서는 1열로 전환한다. 논문명·링크·순서·인용 의미는 변경하지 않았다.
- 분리돼 있던 회색 상담 카드와 초록 NAP 카드를 하나의 전환 구역으로 통합했다. `네이버로 예약하기`를 흰색 대형 주 CTA로 올리고, 공식 전화와 위치 보기는 반투명 보조 행동으로 정리했다. 공식 주소와 전화번호는 그대로 유지한다.
- 전환 구역에는 절제된 원형 모션, 스크롤 진입, hover/focus 피드백을 적용하고 `prefers-reduced-motion`에서는 애니메이션과 중간 투명 상태를 제거한다.
- `tools/test-autonomic-stress-ui.cjs`는 References 카드/반응형 열 수, 주 CTA의 64px 이상 높이·300px 이상 폭·클릭 중심 가림 여부, 전화·위치 행동까지 1440·1024·768·390px에서 검사한다.

## 2026-09-11 SEO·AEO·GEO 회귀 전수검사 및 검색 허브 lastmod 배선
- 현재 사이트맵 81개 URL과 공개 칼럼 28편을 다시 기준으로 잡았다. 발행기·Columns SERP·SEO 표면·의료정보 신뢰·디숭이 칼럼 원문 연결·네이버 분석/전환 회귀 검사는 모두 통과했다.
- 운영 사이트맵 81개 URL은 실제 GET 200, canonical 일치, 사이트맵 내 noindex 없음으로 통과했다. 칼럼 외부 근거 120개 중 106개는 직접 정상 응답했고 404·410은 0개였다. 나머지 14개 DOI/CDC/BMJ/NEJM 등은 자동 요청 차단 또는 미확인으로, 끊긴 링크로 단정하지 않는다.
- 전체 사이트맵 페이지에는 title·description·canonical·H1 1개·해석 가능한 JSON-LD가 유지되고, 공개 칼럼 28편 모두 대표원장 작성자 ID·의학적 검토·편집 원칙 연결을 유지한다. 25편은 보이는 외부 근거와 JSON-LD citation이 연결돼 있다.
- 새 칼럼 발행 후 칼럼 URL의 `lastmod`만 갱신되고 홈과 Columns 허브가 2026-09-04에 머물던 배선을 수정했다. 두 허브의 현재 `lastmod`는 2026-09-11이다.
- `tools/publish-column.mjs`는 발행·삭제·`--refresh-index` 때 홈과 Columns 허브의 사이트맵 `lastmod`를 한국 시간 실행일로 함께 갱신한다. `DEAR_PUBLISH_DATE`는 테스트의 결정적 날짜 주입에만 사용한다. 발행기와 SEO 표면 검사는 두 허브가 최신 칼럼보다 오래되면 실패한다.
